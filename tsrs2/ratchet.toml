[t0]
# rate is display-only once matched/total are present; the gate
# compares the exact integer counts (zero-regression).
# 5.9a: catch-up to the observed integers (5.8e's final run already
# produced these; the slice recorded an earlier snapshot — the >= gate
# never tripped, this just removes the slack).
# 5.9b: +10 — keyof/indexed-access relation arms + identity halves,
# getSimplifiedType un-stub, tuple constraint/variadic/rest arms.
# 5.9c: +1629 — the members/signatures band: constructor signatures
# (classType arm) + exports-as-members (enum/namespace/globalThis/
# prototype) + late-bound index reads + member-table merges + the
# type-of-symbol worker completion.
# 5.9d: +900 — the modules/jsx/interop band plus review fixes:
# aliased-JSX wiring,
# synthetic-default interop cloning, super base constructors, import
# types, import.meta ES arms, alias tails, display-presence fixes,
# Node-format/JSON interop, directive ranges, and qualified names;
# final round re-froze the package-scope host boundaries and keeps
# 1543/1544 off (recorded decision, m4-end-sweep-steps.md).
# oracle-correction epoch: goldens regenerated under the pinned
# producer (total 48719 -> 49024; the 5.8d host fix had reached only
# 4 goldens); 47 enumerated lapses; frozen-host boundaries unfrozen,
# 1543/1544 emitters re-added, 2856 CJS-require row implemented.
# m5 6.2: +3702 — flow assignments + evolving arrays + flipped
# initialType ladders (the 6.2 seam holds joins/conditions inert).
# m5 6.3: +6 — branch/loop joins + the loop fixpoint; condition-free
# join topologies only (try/catch, while(true), for-in/of back
# edges) — condition-crossing queries stay seam-reverted until 6.4.
# m5 6.4: +323 — the narrowers a-h: live condition/switch arms +
# narrowType dispatch, truthiness (+ destructuring synthetic
# references), equality/instanceof/in, typeof, switch narrowers (+
# exhaustiveness pulled forward), effects signatures + type
# predicates + getTypeAtFlowCall, optionality, const-inlining. The
# seam flag survives as the narrow M6-deferral channel
# (body-inference predicate candidates); the [FLOW M5] failure-face
# gates stay until 6.6 closes reachability (they still shield its
# dead-code divergence).
# m5 6.4 review fixes: +95 — the 6.4 cross-check against _tsc.js:
# synthetic references never const-inline (isConstantReference is
# false on factory nodes), the falsy empty-string typeof witness,
# getTypeForBindingElementParent's undefined-symbol fallthrough
# (pattern-named declarations un-partial), the effects-signature
# some() sweep probes body-inference candidates per member
# (uncertain selections flag + stay unmemoized), the
# getTypePredicateFromBody probe tightened to tsc's Boolean-only/
# Normal-function gates, Record widening through the real
# getGlobalTypeAliasSymbol (2318/2317), isNonLocalAlias-faithful
# resolveSymbol, containment-orientation fix for synthetic chains.
# m5 6.6: +1131 — reachability (isReachableFlowNode + caches) with
# its consumers: implicit returns real (endFlowNode + the
# exhaustive-switch/never-call refinements; noImplicitReturns lands),
# 7027 error-face + 7029, class-property flow init (this-rooted
# synthetic chains over returnFlowNode), and the [FLOW M5]
# failure-face gate retirement — replaced by the flag-exact
# seam-reverted-answer registry; the FP sweep fixed five latent
# divergences the old syntax gates had hidden (object-literal
# properties table, elaboration name anchors, private-twin heads,
# all-assignment-ops contextual this, structured
# getTypeOfPropertyOfType) and deferred three families with precise
# M6 containments (computed-key destructuring order, array-spread
# elaboration, intersection never-reduction).
# m5 6.6g review fixes: +10 — the 2775/2776 assertion-position port
# (the lapsed M4 residual; getTypeOfDottedName's private arm keyed
# from the class's OWN members table), guardless
# getTypeOfPropertyOfType (apparent-type members), the
# non-augmenting-subtype substitution + tsc-exact private-twin arm in
# reportUnmatchedProperty, elaboration parity (paren recursion,
# array deep-first, satisfies stop, 2418), the SetAccessor
# annotation readers, computed shouldPreserveConstEnums, the
# intersection-only never-2339 containment, and four flag-exact
# containment omissions closed (pattern/merged/2403/empty-pattern
# rows + subtree consults for compound return/declaration operands).
# m5 close: +20 — the checkTypePredicate tail (the last owner=M5
# escape): 2677 assignability (the containingMessageChain wrap makes
# every failure face land the chain head — no display slice needed),
# 1229 rest-reference, 1225 cannot-find-parameter, 1230
# binding-pattern walk; STAGE moves 5.9 -> M5 (whole-M5 due, M6 not).
# m5 post-close review: +100 — the lapsed strictPropertyInitialization
# swap (isPropertyInitializedInConstructor/InStaticBlocks: the 2564
# constructor face, the 2612 fifth disjunct, the 2729 static-block
# leg), static-private assertion targets (exports-side mangled-key
# recovery), and the seam-registry face-coverage fixes (bounded
# composite probe; see m5-flow-steps.md post-close section).
# m4 post-review slice 2 (a-class FN): +105 — checkSignatureDeclaration
# wired for expression forms (2677/2505/1064/2370 bands), obj-literal
# accessor bodies route through the whole checkAccessorDeclaration
# (+ the getSetAccessorValueParameter this-skip the routing exposed),
# the auto-accessor type ladder (annotation/widened-initializer/
# implicit-any + write type), bigint radix literal types, and the
# ambient variable-declaration grammar fall-through (2480/1263).
# m4 post-review slice 3 (M6-start bar): +16 — relation-key
# constraint forcing (A1: `T extends X` keys stop colliding, the `*`
# broadest-key Maybe recheck goes live), the full
# createUnionOrIntersectionProperty port (A5: Contains* modifier
# folding, identical-instantiation clones, writeTypes/nameType),
# lateBindMember first-wins (A7), the post-augmentation
# amalgamated-duplicates flush + immediate mode (A8), the script-file
# `declare global` collector gate (A9), and the B15 no-cache guard
# for context-sensitive-signature parameters.
# m6 7.6: +7 — instantiation-expression TypeQuery face (typeof f<...>
# through the 7.4-live getInstantiationExpressionType; parserTypeQuery8
# 2304 pair + instantiationExpression rows) + getReducedType
# never-consult + getTypePredicateFromBody (both corpus-byte-neutral —
# the predicate family was corpus-blind exactly as 7.5d measured).
# p9 9.3a: +197 — the tuple renderer (typeReferenceToTypeNode's tuple
# arm as the string slice: labeled/optional/rest/variadic elements,
# readonly wrap, empty tuple, the factory parenthesizer rules +
# formatUnionTypes ordering) + the two FP families it unmasked, fixed
# at the source: propertiesRelatedTo's tuple-arm guard on the
# missing-property pre-head (tuple target × array/tuple source keeps
# the generic head) and elaborate-first at the three head-only
# optionally-elaborate sites (binding-pattern initializer, return,
# assignment). Computed-key destructuring containment retired
# (evaluation-order faces probe port==oracle, zero fabrications).
# p9 9.3a review fixes: +1 — return elaboration takes the EFFECTIVE
# check node (satisfies strips, tsc 84585-84587) + the EnumLike
# display arm (E.A / single-member collapse / declared-union E faces;
# enum members no longer print base literals).
# p9 9.3b: +437 — the anonymous-object display arm
# (createTypeNodeFromObjectType structural tail: property/index
# member faces, name machinery, accessor collapse, class-static /
# enum-object typeof faces) + the SEVEN fabrication families it
# unmasked, fixed at the source: hasExcessProperties reporting
# (parent-skipped 2353/2561 at both assignable and comparable head
# sites), the elaborateError entry did-you-mean probe, shorthand
# member elaboration, elaborateElementwise's index-signature member
# lookup, merged-declaration + constructor-return elaborate-first,
# reportUnmatchedProperty pre-walk fidelity (nonPrimitive apparent,
# primitive suppression, type-variable head, faithful isKnownProperty
# index applicability), and the scanner's ECMA number cooking for
# decimal/radix literals (member names like 9.671406556917009e+24).
# p9 9.3b2: +596 with the signature rung (signatureToSignatureDeclaration
# faces incl. fn/ctor shorthands, method/accessor members, tuple-rest
# expansion, the annotation-reuse channel) + the fabrication-audit
# source fixes (shouldReportUnmatchedPropertyError, elaborateArrowFunction,
# TS-expando member suppression).
# p9 9.3b2 review round: +1/+1 (name-precise expando suppression,
# union best-match member rows, IIFE effective-arg optionality, the
# 65185 nullable-candidate report substitution).
# p9 9.3b3: +228 with the symbol/value/module heads (symbolToTypeNode
# error-path Value faces: `typeof Ns` entity heads incl. globalThis
# and merged interface/function+namespace values, `typeof
# import("...")` external-module faces) + the two fabrication
# families the arm unmasked, fixed at source: alias-FOLLOWING
# symbolIsValue in isKnownProperty/getNamedMembers (module export-
# aliases are value properties) and the expando-record consults
# threaded through checker symbol merges (cross-file fn+ns clones).
# p9 9.3b4: +140 with the type-operator display arms (typeToTypeNodeHelper
# Index/TemplateLiteral/StringMapping/IndexedAccess faces incl. the
# printer's template-text re-escape and the factory parenthesizer
# positions) + the fabrication family the arms unmasked, fixed at
# source: the relation-side `${number}` placeholder coercion now
# rides the faithful evaluate.rs ToNumber (the M4-era local slice
# dropped the 0b/0o radix forms).
# p9 9.3b5: +324 with the display-special tail (identically-named /
# 2507-2509 / origin / unique-symbol / empty faces) and its source
# fixes. Review r2's UMD alias / symbol-chain corrections were
# T0-neutral; the implicit-external-module completion added +12 by
# honoring Auto/Force module detection for format, JSX, and package
# scope indicators. p9 9.4b adds +71 by routing call arguments through
# object/array/arrow elaboration and force-tupleizing spread arrays.
# p9 9.4c adds +113 from JSX attributes/children applicability and
# global Object relation-head selection. p9 9.5a adds +7 by admitting
# declaration-preserving mapped-type construction and display. p9
# 9.5b1 adds +3 through finite mapped member materialization. p9
# 9.5b2 adds +87 by instantiating homomorphic mapped arrays/tuples,
# completing their apparent type, and replacing conservative generic
# classification with the constraint/name-type predicate. p9 9.5c1
# adds +55 by substituting generic mapped indexed access and relating
# mapped constraints, templates, name types, and optionality.
# p9 9.5c2a adds +29 through mapped contextual-property substitution,
# mapped validation/readonly writes, constness, and tuple context.
# p9 9.5c2b adds +11 through reverse-mapped member reconstruction and
# homomorphic mapped inference. p9 9.6a adds +13 by constructing and
# rendering the conditional/substitution semantic model. p9 9.6b adds
# +9 through NoInfer production and its inference/relation consumers.
# p9 9.6c adds +62 through conditional resolution, distributivity,
# constraint/simplification consumers, and exact recursive diagnostics.
# p9 9.6d adds +43 through conditional relation/inference, including
# distributive constraints and homomorphic inference under real
# rollback-capable overload candidate trials.
# p9 9.7c adds +1304 by retiring the parser-recovery overload bail.
# p9 9.8b adds +16 through normal expando-member resolution.
# p9 9.8c adds +16 through JS constructor/assignment-declaration
# contextual typing, instance `this`, and constructor property flow.
# p9 9.9a adds +240 by publishing definite checked-JS module
# resolution misses while preserving host-dependent suppression.
# p9 9.9b adds +87 through checkExternalEmitHelpers: decorators,
# CommonJS interop imports/re-exports, disposal helpers, and
# spread-helper arity over provenance-resolved in-program tslib.
# p9 9.9c adds +77 through parameter-initializer self/forward
# reference checks and the emitStandardClassFields scope-change seam.
# p9 9.9d adds +35 recognized-extension and checked-JS require misses.
# p9 9.9e adds +11 through authoritative in-program allowJs resolution.
# p9 9.9f adds +24 through Node per-file CommonJS emit-format collisions.
# p9 9.9g adds +67 through exact mixed-JS name-miss provenance.
# p9 9.9h adds +14 value-as-type alternate diagnostics.
# p9 9.9i adds +12 unchecked side-effect import misses.
# p9 9.9j adds +12 type-as-value/type-as-namespace alternates.
# p9 9.9k adds +5 namespace-as-value alternate diagnostics.
# p9 9.9l adds +9 rest-binding-pattern grammar diagnostics.
# p9 9.9m adds +7 file-less global diagnostics.
# p9 9.9n adds +6 checked-JS typed-declaration arity diagnostics.
# p9 9.9o adds +4 typed missing-namespace tail diagnostics.
# p9 9.9p adds +4 declaration-extension substitution diagnostics.
# p9 9.9q adds +3 rewrite-relative-import safety diagnostics.
# p9 9.9r adds +3 external-module UMD-global diagnostics.
# p9 9.9s adds +2 JSDoc-nullable type-node diagnostics.
# p9 9.9t adds +1 source-text JSDoc function-signature diagnostic.
# p9 9.9u adds +1 checked-JS globalThis collision diagnostic.
# p9 9.9v adds +1 duplicate object-literal method diagnostic.
# p9 9.9w adds +10 through module.exports alias resolution and
# exact checked-JS property-miss publication.
# p9 9.9x adds +7 unchecked-JS name/property spelling suggestions.
# p9 9.9y adds +4 checked-JS strict property-initialization rows.
# p9 9.9z adds +4 checked-JS mixed CommonJS export redeclarations.
# p9 9.9aa adds +2 checked-JS syntactic truthiness rows.
# p9 9.9ab adds +1 ambient const-enum access under verbatim syntax.
# p9 9.9ac adds +1 checked-JS non-JSDoc operator diagnostic.
# p9 9.9ad adds +1 checked-JS non-JSDoc base-constructor diagnostic.
# p9 9.9ae adds +2 export-default use-before-assignment diagnostics.
# p9 9.9af adds +2 checked-JS cross-file redeclaration diagnostics.
# p9 9.9ag adds +1 unresolved internal import-alias reference diagnostic.
# p9 9.9ah adds +2 outer generic-reference display diagnostics.
# p9 9.9ai adds +1 checked-JS class-container merge diagnostic.
# p9 9.9aj adds +8 Object.defineProperty readonly diagnostics.
# p9 9.9ak adds +3 function-expando use-before-assignment diagnostics.
# p9 9.9al adds +2 pure CommonJS export-flow/alias diagnostics.
# p9 9.9am adds +1 duplicated CommonJS export-flow diagnostic.
# p9 9.9an adds +1 checked-JS not-a-module diagnostic.
# p9 9.9ao adds +7 checked-JS CommonJS require-module diagnostics.
# p9 9.9ap adds +2 checked-JS readonly enum-expando diagnostics.
# p9 9.9aq adds +3 complex cross-product union diagnostics.
# p9 9.9ar adds +3 constructor-visibility relation diagnostics.
# p9 9.9as adds +3 JSX excess-property diagnostics against empty props.
# p9 9.9at adds +3 generic-mapped identity diagnostics.
# p9 9.9au adds +4 checked-JS assignment diagnostics hidden by file-wide JSDoc provenance.
# p9 9.9av adds +1 checked-JS empty-object display diagnostic.
# p9 9.9aw adds +2 recursive mapped-keyof constraint diagnostics.
# p9 9.9ax adds +3 bindable computed-accessor return diagnostics.
# p9 9.9ay adds +3 checked-JS require-alias diagnostics.
# p9 9.9az adds +6 checked-JS symbol-free property-miss diagnostics.
# p9 9.9ba adds +2 checked-JS non-JS-declared property-miss diagnostics.
# p9 9.9bb adds +4 checked-JS plain-value-module property-miss diagnostics.
# p9 9.9bc adds +1 checked-JS assignment-class property-miss diagnostic.
# p9 9.9bd adds +2 checked-JS direct-this class property-miss diagnostics.
# p9 9.9be adds +1 checked-JS imported-class-alias expando-miss diagnostic.
# p9 9.9bf adds +1 checked-JS private-name property-miss diagnostic.
# p9 9.9bg adds +2 checked-JS chained-this assignment-miss diagnostics.
# p9 9.9bh adds +1 primitive module.exports assignment-miss diagnostic.
# p9 9.9bi adds +2 CommonJS object-replacement union property misses.
# p9 9.9bj adds +1 checked-JS require-property-alias nested-object miss.
# p9 9.9bk adds +2 checked-JS chained-identifier assignment misses.
# p9 9.9bl adds +2 JSDoc-constructor prototype-object property misses.
# p9 9.9bm adds +3 JSDoc-satisfies object-literal property misses.
# p9 9.9bn adds +3 nested-class this.prototype property misses.
# p9 9.9bo adds +1 JSDoc chained-static-assignment this miss.
# p9 9.9bp adds +1 class-field arrow lexical-this miss.
# m7 8.1a adds +505 through checkGrammarModifiers and its obvious
# modifier/decorator + async helpers, with exact follower suppression.
# m7 8.1b adds +116 through checkGrammarObjectLiteralExpression and
# the object-literal checkGrammarMethod dispatch.
# m7 8.1c.1 adds +10 through checkGrammarParameterList's direct read
# of every routed signature node's parameter array.
# m7 8.1c.2 adds +7 through checkGrammarSourceFile's ambient
# top-level declare/export-modifier producer.
# m7 8.1d.1 adds +69 through checkThisBeforeSuper and the
# isPostSuperFlowNode all-reaching-path flow walk (17009/17011).
# m7 8.1d.2a adds +8 through checkAwaitGrammar's Node16..NodeNext
# CommonJS-format arm (1309; TS and checked-JS publication).
# m7 8.1d.2b adds +8 through checkGrammarForInOrForOfStatement's
# Node16..NodeNext CommonJS-format arm (1309; TS and checked-JS).
# m7 8.1d.3v adds +57 through the complete UTF-16 regular-expression
# validator: the frozen 21-fixture queue contributes 56 and the
# same-owner full-corpus closure contributes parser579071 TS1005.
# m7 8.1e B16 adds +213 through resolveExternalModule's complete
# Node16/Node18 synchronous-import mode-mismatch queue: 1471 x68,
# 1479 x143, 1541 x1, and 1542 x1.
# m7 8.1e A11 adds +9 through checkExportAssignment: 1203 x4 plus
# the exact 1282/1283/1284/1285/1289 type-only export rows.
# m7 8.1e TS1340 adds +72 through getTypeFromImportTypeNode's
# diagnostic-only bare recovered package-module meaning projection.
# m7 8.1e TS1361/TS1362 adds +64 through the exact type-only-alias
# value-use callback, including related origin information.
# m7 8.1f.1 adds +31 through private-name placement in
# checkGrammarObjectLiteralExpression.
# m7 8.1f.2 adds +2 by publishing the checked-JavaScript private
# accessor ES5 rows from checkGrammarAccessor.
# m7 8.1f.3 adds +12 through checkJSDocTypeIsInJsFile's
# nullable/non-nullable TypeScript-syntax pair.
# m7 8.1f.4 adds +12 through checkJSDocAccessibilityModifiers'
# exact checked-JavaScript accessibility-tag projection.
# m7 8.1g.1 adds +8 through checkESModuleMarker's per-file
# Node CommonJS emit-format gate and exact checked-JS publication.
# m7 8.1g.2 adds +6 through checkImportDeclaration's diagnostic-only
# package-exports JSON target projection.
# m7 8.1g.3 adds +4 by publishing checkImportMetaProperty's exact
# CommonJS-format row in checked JavaScript.
# m7 8.1g.4 adds +1 through checkImportDeclaration's JavaScript-
# specific top-level-context message selection.
# m7 8.1g.5 adds +1 through checkExportDeclaration's separate
# JavaScript-specific top-level-context message selection.
# m7 8.1g.6 adds +11 through checkAliasSymbol's isolated/verbatim
# type-only aliases and its CommonJS-format import-alias diagnostics.
# m7 8.1g.7 adds +3 through checkExportAssignment's CommonJS
# export-default diagnostic.
# m7 8.1g.8 adds +1 through checkGrammarImportCallExpression's
# highest-priority CommonJS/verbatim dynamic-import diagnostic.
# m7 8.1g.9 adds +1 through checkModuleDeclarationDiagnostics'
# instantiated-namespace CommonJS/verbatim diagnostic.
# m7 8.1j adds +11 through checkSignatureDeclaration's checked-JS
# async JSDoc return-type producer and checkAsyncFunctionReturnType's
# exact 1055/1064/1065 head, chain, and related-information rows.
# m7 8.1k adds +3 through reportNonDefaultExport's exact checked-CJS
# TS1192 publication in the Node18, Node20, and NodeNext matrix.
# m7 8.1l adds +4 through checkTryStatement's checked-JS JSDoc catch
# type projection and exact TS1196 first-token rows.
# m7 8.1m adds +2 through checkGrammarTypeOperatorNode's checked-JS
# JSDoc unique-symbol property host projection and exact TS1331 rows.
# m7 8.1n adds +2 through checkGrammarTypeArguments' checked-JS
# JSDoc @param type-argument projection and exact TS1099/TS1009 rows.
# m7 8.1o adds +2 by publishing bindNamespaceExportDeclaration's
# exact checked-JS TS1315 row in the ES5/ES2015 matrix.
# m7 8.1p adds +3 through checkGrammarModifiers' checked-JS JSDoc
# template effective-host projection and exact TS1273/TS1274/TS1277 rows.
# m7 8.1q adds +2 through checkJSDocSatisfiesTag's checked-JS
# effective-host duplicate projection and exact TS1223 rows.
# m7 8.1r adds +1 through checkTypePredicate's checked-JS JSDoc
# cast projection and exact TS1228 row.
# m7 8.1s adds +4 through checkSatisfiesExpressionWorker's checked-JS
# JSDoc host/type projections and exact TS1360 rows.
# m7 8.1t adds +5 through checkJSDocVariadicType's checked-JS
# non-final parameter projection and exact TS1014 rows.
# m7 8.1u adds +2 through checkGrammarParameterList's checked-JS
# effective JSDoc optionality projection and exact TS1016 rows.
# m7 8.1v adds +3 through parseTemplateTagTypeParameter's checked-JS
# missing-name projection and exact TS1069 rows.
# m7 8.1w adds +6 through parseJSDocIdentifierName's checked-JS
# missing/invalid entity-name recovery and exact zero-width TS1003 rows.
# m7 8.1x adds +4 through parseJSDocTypeExpression's checked-JS
# satisfies-tag required-brace projection and exact TS1005 rows.
# m7 8.1y adds +3 through tryParseImportClause's checked-JS
# JSDoc default-import `from` projection and exact TS1005 rows.
# m7 8.1z adds +1 through parseImportAttributes' checked-JS
# JSDoc bare-`with` required-brace projection and exact TS1005 row.
# m7 8.1aa adds +3 through parsePrimaryExpression's checked-JS
# JSDoc import module-specifier recovery and exact TS1109 rows.
# m7 8.1ab adds +3 through parseEntityNameOfTypeReference's
# checked-JS JSDoc missing-type recovery and exact TS1110 rows.
# m7 8.1ac adds +5 through parseExpectedJSDoc's checked-JS
# JSDoc close-brace recovery and exact TS1005 rows.
# m7 8.1ad adds +1 through parseExpected's checked-JS JSDoc
# bracketed-template missing-equals recovery and exact TS1005 row.
# m7 8.3c accepted-set catch-up adds +10 from the SourceFile unused
# error-mode producer that landed without updating the stored set.
# m7 8.4a adds +3538 by surfacing the registered SourceFile/Class
# unused producers as suggestions, including checked-JS JSDoc and
# CommonJS reference prerequisites; all-corpus FP remains zero.
# m7 8.4b adds +571 by surfacing Block-owned unused locals after
# checkBlock's deferred registration; T1-T3 have no losses and
# all-corpus FP remains zero.
# m7 8.4c adds +859 by surfacing ModuleDeclaration-owned unused
# locals while excluding global augmentations; T1-T3 gain the same
# exact identities with no losses, and all-corpus FP remains zero.
# m7 8.4d adds +213 from CaseBlock and for/for-in/for-of local
# owners, including destructuring/grouping rows; T1-T3 gain the
# same exact identities with no losses, and all-corpus FP remains zero.
# m7 8.4e adds +4758 from FunctionDeclaration-owned value locals and
# parameters. Local/Parameter category gates remain independent,
# overload declarations and underscore parameters stay exempt, and the
# valueDeclaration-root grouping rule preserves shadowed-binding spans;
# T1-T3 gain the same exact identities with no losses or external gains.
# m7 8.4f adds +1416 from ArrowFunction-owned value locals and
# parameters while leaving type-parameter diagnostics on their separate
# worker boundary. The checked-JS assignment-name prerequisite and
# recursive bindingNameText port also correct existing exact spans/text:
# T1 gains 1416 and T2/T3 gain 1477, all with zero losses, external
# gains, or false positives.
# m7 8.4g adds +1329 from MethodDeclaration-owned value locals and
# parameters across class and object-literal methods. Overload bodies,
# underscore parameters, independent Local/Parameter modes, and
# shadowed-binding anchors remain oracle-exact; T1-T3 gain the same
# identities with no losses, external gains, or false positives.
# m7 8.4h adds +600 from SetAccessor-owned value locals and
# parameters across class, class-expression, and object-literal
# setters. Body presence, underscore exemptions, independent modes,
# and shadowed-binding anchors remain oracle-exact; T1-T3 gain the
# same identities with no losses, external gains, or false positives.
# m7 8.4i adds +452 from Constructor-owned value locals and
# parameters across class declarations and class expressions. Body
# presence, overload and underscore exemptions, parameter-property
# exclusion, independent modes, and shadowed-binding anchors remain
# oracle-exact; T1-T3 gain the same identities with no losses,
# external gains, or false positives.
# m7 8.4j adds +362 from FunctionExpression-owned value locals and
# parameters, including named/anonymous assignments and IIFEs.
# Underscore exemptions, independent modes, destructuring, nested
# locals, and shadowed-binding anchors remain oracle-exact; T1-T3
# gain the same identities with no losses, external gains, or false
# positives.
# m7 8.4k adds +32 from GetAccessor-owned value locals and the
# grammar-invalid parameter face across class, class-expression, and
# object-literal getters. Body/ambient gates, underscore exemptions,
# independent modes, and shadowed-binding anchors remain oracle-exact;
# T1-T3 gain the same identities with no losses, external gains, or
# false positives.
# m7 8.4l adds +373 from checkUnusedTypeParameters across class,
# interface, type-alias, function-like, and signature-only owners:
# TS6133 x353 and every residual TS6205 row (x20). Whole-list versus
# per-parameter spans, underscore exemptions, merged-declaration
# gating, and noUnusedParameters category projection are oracle-exact;
# T1-T3 gain the same identities with no losses, external gains, or
# false positives.
# m7 8.4m adds +23 from checkUnusedInferTypeParameter across conditional
# types and template-literal/this-type inference. Each unused `infer U`
# node keeps its whole-node span (including repeated symbols), underscore
# exemptions, and noUnusedParameters category projection; T1-T3 gain the
# same identities with no losses, external gains, or false positives.
# m7 8.4n adds +68 by matching checkUnusedIdentifiers' recovery boundary:
# valid bound siblings remain eligible when their owner also contains a
# parse error, while malformed declarations, imports, members, and missing
# type-parameter names remain suppressed locally. Source-file registration
# keeps TS global scripts out, and grouped Unicode-escaped names use cooked
# identifier text; T1-T3 gain the same identities with no losses, external
# gains, or false positives.
# m7 8.4o adds +1 by restoring the parser prerequisite for exported
# internal import-equals declarations: `export import x = ns.x` is an
# external-module indicator even though its module reference is not a
# `require()` call. The unexported form remains a global script; T1-T3
# gain the same identity with no losses, external gains, or false positives.
# m7 8.4p adds +1 by using the direct member NodeFlags.Ambient predicate
# at checkUnusedClassMembers. This preserves the invalid `declare`
# accessor recovery pair that tsc still checks without weakening the
# broader effective-ambient rules of other unused owners; T1-T3 gain
# the same identity with no losses, external gains, or false positives.
# m7 8.4q adds +2 by resuming the direct function/arrow RHS of a
# checked-JS property assignment after its LHS hits Rust's display
# containment. This reaches the ordinary checkSignatureDeclaration
# unused-registration tail and preserves parameter-read marking; the
# adjacent used-parameter canary remains clean. T1-T3 gain the same
# two identities with no losses, external gains, or false positives.
# m7 8.4r adds +239 from checkAwaitExpression's no-effect suggestion
# tail. The strict awaited/operand TypeId identity check excludes
# Promise-like changes, error, any, and unknown; checked-JS rows use the
# same provenance-checked publication boundary. T1-T3 gain the same
# identities with no losses, external gains, or false positives.
# m7 8.4s adds +129 from getTypeOfAccessors' errorOrSuggestion tail:
# TS7032 x127 and TS7033 x2 retain the error mode under noImplicitAny
# and switch to suggestion category otherwise. Private and string
# property names recover their complete source-spelled token for exact
# span/message identity; T1-T3 gain the same identities with no losses,
# external gains, or false positives.
# m7 8.4t adds +81 from reportImplicitAny's checked-JS loose parameter
# suggestion face. Exact publication excludes parameters whose effective
# context rides JSDoc @param/@type/@overload semantics (including inline
# and assignment carriers), while unresolved legacy C~A namepaths remain
# implicit-any like tsc. Fourteen tag-owned TS7044/7045 identities move
# to the existing M8 jsdoc-semantics boundary; T1-T3 gain the same 81
# identities with no losses, external gains, or false positives.
# m7 8.4u ports checkGrammarNumericLiteral's two call faces and adds
# all 24 TS80008 large-integer suggestions. Raw-token classification
# preserves tsc's fractional/scientific exclusions without mistaking
# hexadecimal E digits for exponents; T1-T3 gain the same 24 identities
# with no losses, external gains, or false positives.
# m7 8.4w adds all 75 TS7028 unused-label suggestions from
# checkLabeledStatement. The binder-owned Unreachable label flag and
# allowUnusedLabels tri-state preserve referenced-label suppression and
# the suggestion/error/disabled projections, including plain JavaScript;
# T1-T3 gain the same identities with no losses or false positives.
# m7 8.4x adds all 74 M7-owned TS7027 unreachable-code suggestions
# from checkSourceElementUnreachable. The existing binder/flow reachability
# and adjacent-statement range aggregation are unchanged; only the
# allowUnreachableCode suggestion/error/disabled projection and plain-JS
# publication land. T1-T3 gain the same identities with no losses or
# false positives, closing flow-derived-suggestions at 149/149.
# m7 8.5a adds all 142 residual TS6053 identities owned by
# getSourceFileFromReferenceWorker. The program layer now extracts leading
# triple-slash path pragmas, resolves explicit supported extensions against
# the host file set, and reports exact missing-file spans/messages. T1-T3
# gain the same identities with no losses, external gains, or false positives.
# m7 8.5b adds all 4 residual TS6263 identities from
# needAllowArbitraryExtensions. Arbitrary-extension declaration twins now
# resolve deterministically; the importing-file and option gate publishes the
# exact diagnostic while preserving recognized-extension substitution. T1-T3
# gain the same identities with no losses, external gains, or false positives,
# closing program-resolution-options at 180/180 with canaries 3/3.
rate = 0.981315
matched = 48108
total = 49024
allowed_regression = 0.0

[t1]
# M7 close: category-aware shadow identity is now a hard, exact-count
# ratchet. The denominator is the same pinned all-corpus oracle universe
# as T0; future gains may raise matched, while any proportional loss
# fails conformance.
rate = 0.981274
matched = 48106
total = 49024
allowed_regression = 0.0

[t0-2xxx]
# 5.9b: +10 with the relations band.
# 5.9c: +1286 with the members/signatures band.
# 5.9d: +202 with the modules/jsx/interop band and review fixes.
# oracle-correction epoch: total 20916 -> 21051, +28 net matched
# (12 enumerated lapses; 2856 rows and interop recoveries).
# m5 6.2: +3684 with flow assignments + evolving arrays.
# m5 6.3: +4 with the joins/fixpoint (condition-free topologies).
# m5 6.4: +323 with the narrowers a-h.
# m5 6.4 review fixes: +92 (see the T0 comment).
# m5 6.6: +537 with reachability + the gate retirement (see the T0
# comment).
# m5 6.6g review fixes: +10 (see the T0 comment).
# m5 close: +8 with the checkTypePredicate tail (see the T0 comment).
# m5 post-close review: +100 (see the T0 comment).
# m4 post-review slice 2 (a-class FN): +85 (see the T0 comment).
# m4 post-review slice 3 (M6-start bar): +16 (see the T0 comment).
# m6 7.6: +5 with the instantiation-expression TypeQuery face (see
# the T0 comment).
# p9 9.3a: +189 with the tuple renderer + FP-family source fixes (see
# the T0 comment).
# p9 9.3a review fixes: +1 (see the T0 comment).
# p9 9.3b: +407 with the anonymous-object display arm + the seven
# fabrication-family source fixes (see the T0 comment).
# p9 9.3b2: +583 with the signature rung (see the T0 comment).
# p9 9.3b2 review round: +1 (see the T0 comment).
# p9 9.3b3: +213 with the symbol/value/module heads (see the T0
# comment).
# p9 9.3b4: +139 with the type-operator display arms (see the T0
# comment).
# p9 9.3b5: +285 with the display-special tail (see the T0 comment);
# review r2 and its implicit-external-module completion are
# byte-neutral in the 2xxx band. p9 9.4b adds +71 object/array/arrow
# elaboration rows; p9 9.4c adds +113 JSX/report-head rows; p9 9.5a
# adds +7 mapped model/display rows; p9 9.5b1 adds +3 finite mapped
# member rows; p9 9.5b2 adds +84 mapped instantiation/apparent rows.
# p9 9.5c1 adds +55 mapped indexed-access/relation rows.
# p9 9.5c2a adds +28 mapped contextual/check-consumer rows.
# p9 9.5c2b adds +11 reverse-mapped/homomorphic-inference rows.
# p9 9.6a adds +13 conditional/substitution model/display rows.
# p9 9.6b adds +9 NoInfer production/inference/relation rows.
# p9 9.6c adds +60 conditional-resolution/distribution rows.
# p9 9.6d adds +43 conditional relation/inference rows.
# p9 9.7c adds +1097 by retiring the parser-recovery overload bail.
# p9 9.8b adds +12 expando-member rows.
# p9 9.8c adds +16 JS constructor/assignment-declaration rows.
# p9 9.9a adds +240 definite checked-JS module-resolution rows.
# p9 9.9b adds +87 external emit-helper availability/arity rows.
# p9 9.9c adds +77 parameter-initializer ordering rows.
# p9 9.9d adds +35 recognized-extension and checked-JS require misses.
# p9 9.9e adds +11 through authoritative in-program allowJs resolution.
# p9 9.9f adds +24 Node per-file generated-name collision rows.
# p9 9.9g adds +67 exact mixed-JS name-miss rows.
# p9 9.9h adds +14 value-as-type alternate diagnostics.
# p9 9.9i adds +12 unchecked side-effect import misses.
# p9 9.9j adds +12 type-as-value/type-as-namespace alternates.
# p9 9.9k adds +5 namespace-as-value alternate diagnostics.
# p9 9.9l adds +9 rest-binding-pattern grammar diagnostics.
# p9 9.9m adds +7 file-less global diagnostics.
# p9 9.9n adds +6 checked-JS typed-declaration arity diagnostics.
# p9 9.9o adds +4 typed missing-namespace tail diagnostics.
# p9 9.9p adds +4 declaration-extension substitution diagnostics.
# p9 9.9q adds +3 rewrite-relative-import safety diagnostics.
# p9 9.9r adds +3 external-module UMD-global diagnostics.
# p9 9.9s adds +2 JSDoc-nullable type-node diagnostics.
# p9 9.9t adds +1 source-text JSDoc function-signature diagnostic.
# p9 9.9u adds +1 checked-JS globalThis collision row.
# p9 9.9v adds +1 duplicate object-literal method row.
# p9 9.9w adds +10 through module.exports alias resolution and
# exact checked-JS property-miss publication.
# p9 9.9x adds +7 unchecked-JS name/property spelling suggestions.
# p9 9.9y adds +4 checked-JS strict property-initialization rows.
# p9 9.9z adds +4 checked-JS mixed CommonJS export redeclarations.
# p9 9.9aa adds +2 checked-JS syntactic truthiness rows.
# p9 9.9ab adds +1 ambient const-enum access under verbatim syntax.
# p9 9.9ac adds +1 checked-JS non-JSDoc operator diagnostic.
# p9 9.9ad adds +1 checked-JS non-JSDoc base-constructor diagnostic.
# p9 9.9ae adds +2 export-default use-before-assignment diagnostics.
# p9 9.9af adds +2 checked-JS cross-file redeclaration diagnostics.
# p9 9.9ag adds +1 unresolved internal import-alias reference diagnostic.
# p9 9.9ah adds +2 outer generic-reference display diagnostics.
# p9 9.9ai adds +1 checked-JS class-container merge diagnostic.
# p9 9.9aj adds +8 Object.defineProperty readonly diagnostics.
# p9 9.9ak adds +3 function-expando use-before-assignment diagnostics.
# p9 9.9al adds +2 pure CommonJS export-flow/alias diagnostics.
# p9 9.9am adds +1 duplicated CommonJS export-flow diagnostic.
# p9 9.9an adds +1 checked-JS not-a-module diagnostic.
# p9 9.9ao adds +7 checked-JS CommonJS require-module diagnostics.
# p9 9.9ap adds +2 checked-JS readonly enum-expando diagnostics.
# p9 9.9aq adds +3 complex cross-product union diagnostics.
# p9 9.9ar adds +3 constructor-visibility relation diagnostics.
# p9 9.9as adds +3 JSX excess-property diagnostics against empty props.
# p9 9.9at adds +3 generic-mapped identity diagnostics.
# p9 9.9au adds +4 checked-JS assignment diagnostics hidden by file-wide JSDoc provenance.
# p9 9.9av adds +1 checked-JS empty-object display diagnostic.
# p9 9.9aw adds +2 recursive mapped-keyof constraint diagnostics.
# p9 9.9ax adds +3 bindable computed-accessor return diagnostics.
# p9 9.9ay adds +3 checked-JS require-alias diagnostics.
# p9 9.9az adds +6 checked-JS symbol-free property-miss diagnostics.
# p9 9.9ba adds +2 checked-JS non-JS-declared property-miss diagnostics.
# p9 9.9bb adds +4 checked-JS plain-value-module property-miss diagnostics.
# p9 9.9bc adds +1 checked-JS assignment-class property-miss diagnostic.
# p9 9.9bd adds +2 checked-JS direct-this class property-miss diagnostics.
# p9 9.9be adds +1 checked-JS imported-class-alias expando-miss diagnostic.
# p9 9.9bf adds +1 checked-JS private-name property-miss diagnostic.
# p9 9.9bg adds +2 checked-JS chained-this assignment-miss diagnostics.
# p9 9.9bh adds +1 primitive module.exports assignment-miss diagnostic.
# p9 9.9bi adds +2 CommonJS object-replacement union property misses.
# p9 9.9bj adds +1 checked-JS require-property-alias nested-object miss.
# p9 9.9bk adds +2 checked-JS chained-identifier assignment misses.
# p9 9.9bl adds +2 JSDoc-constructor prototype-object property misses.
# p9 9.9bm adds +3 JSDoc-satisfies object-literal property misses.
# p9 9.9bn adds +3 nested-class this.prototype property misses.
# p9 9.9bo adds +1 JSDoc chained-static-assignment this miss.
# p9 9.9bp adds +1 class-field arrow lexical-this miss.
rate = 0.974063
matched = 20505
total = 21051
allowed_regression = 0.0

[t0-syntactic]
rate = 0.998219
matched = 2242
total = 2246
allowed_regression = 0.0

[escapes]
# Untagged Unsupported/M4Dependency reasons (no parseable owner
# stage). Monotone non-increasing; must reach 0 by the M4 close.
# Owner-less reasons with an explicit recovery marker ("parse
# recovery"/"parse-recovery"/"recovery node") classify as milestone-stable
# RECOVERY guards through M7 and ratchet separately below — a new
# marked guard trips max_recovery, so re-labeling real containment
# escapes as "recovery" cannot slip past the gate. Done still requires
# these paths to leave the Unsupported channel (m8-readiness.md).
# 5.9a: 114 -> 93 — the M4-end sweep triage re-owned 19 sites to M8
# (JSDoc/checkJs band, conditional/mapped families, display T2 tails)
# and 2 to M6 (assertion-deferred stash seam, tuple renderer) per the
# m4-end-sweep-steps.md disposition table; the remainder burns down
# via the dead-guard retires (5.9a) and the implement slices (5.9b-d),
# reaching 0 at 5.9e close.
# 5.9a part 2: 93 -> 29 — 66 proven-dead guards converted to
# expect/unreachable per the dead-guard policy (constructibility
# proofs in the m4-end-sweep-steps.md disposition table: L-MISSING /
# L-COUPLING / L-PARENTS / L-BINDER lemmas + per-site caller
# guarantees); the 29 remaining untagged rows are the implement-5.9
# set (burn down in 5.9b-d) plus the 5 tables.rs twin arms retiring
# with the 5.9b demotion.
# 5.9b: 29 -> 20 — the relations band implemented its 4 untagged rows
# (identity halves, getConstraintOfIndexedAccess, variadic tuple
# relations, tuple rest index fallbacks) and the tables twin demotion
# retired its 5 untagged arms (L-TWIN dead-guard conversions); the
# remaining 20 are the 5.9c/5.9d implement rows.
# 5.9c: 20 -> 9 — the members/signatures band implemented its rows
# (const-enum lookup, abstract-2513, instantiated accessor writes,
# late-bound index reads, type-ref unknown-symbol + EWTA errorType,
# enum-literal condition) and adjudicated three recorded amendments:
# deferred synthetic write types re-owned M6 (constituents are M6
# machinery, matching the M6-owned read side); the no-value-decl and
# worker-kind tails reclassed recovery (Debug.assertIsDefined /
# Debug.fail transcriptions). The remaining 9 are the 5.9d rows.
# 5.9d: 9 -> 0 — the modules/jsx/interop band implemented every
# remaining untagged row (import types, eOPT argument head,
# identically-named + origin displays, unique-symbol index head,
# synthetic-access destructuring, Omit/Awaited arity guards); the
# ceiling is now the 5.9e close invariant.
max_untagged = 0
# 5.7c: +16 genuine malformed-tree guards (JSX band tag-name/
# attributes/attribute-symbol reads + the Debug.assert/Debug.fail
# transcriptions of createJsxAttributesTypeFromAttributesProperty and
# getIntrinsicTagSymbol, risk #6).
# 5.8a: +4 statement-band malformed-tree guards (VariableStatement/
# IfStatement/ForInStatement/SwitchStatement recovery nodes, risk #6).
# 5.8b: +8 §5-band malformed-tree guards (ForOfStatement RHS /
# SpreadElement / parameter- and declaration-boundary recovery in the
# overload band + getDeclarationSpaces' failBadSyntaxKind arm).
# 5.8c: +7 decorator/class-band guards (decorator/heritage/class-
# element recovery nodes + the getEffectiveDecoratorArguments and
# getClassElementPropertyKeyType Debug.fail transcriptions, risk #6).
# 5.9a: +1 — createTypeReference-over-non-generic-interface reclassed
# stage/5.8 -> recovery: REACHABLE input (thisless non-generic
# interface as JSX.ElementType) on which the vendored tsc throws
# TypeError (undefined instantiations map), so no golden can exist;
# permanent crash-guard family (Debug.fail-transcription precedent),
# see m4-end-sweep-steps.md.
# 5.9c: +3 — the type-of-symbol worker's crash-guard tails, live now
# that the worker completes: symbol-without-a-value-declaration
# (Debug.assertIsDefined transcription, 56662), worker-declaration-
# kind tail (Debug.fail transcription, 56707-56708), and the
# export-assignment-without-expression parse-recovery guard.
# m6 7.2d: +1 — the tuple middle-slice tsc-crash deviation
# (inferFromObjectTypes passes an undefined slice into inferFromTypes;
# vendored tsc TypeErrors, so no golden can exist — the
# createTypeReference crash-guard precedent). Recorded as
# m8-readiness.md deviation row 4; the differential classifier
# consumes the row when M8 makes the shape corpus-visible.
# m7 8.3b: +1 — checkUnusedClassMembers' default Debug.fail arm.
# Valid class-member kinds are exhaustively handled; a parser-recovery
# shape outside that closed set is contained at the registered class
# boundary instead of panicking the unused drain.
max_recovery = 117
# D2a/D1a initial constructibility census: 23 exact rows. Each row
# names a test canary; once that test exists the annotation is stale
# and its owning model slice must implement, remove, or narrow it.
# Monotone non-increasing after this reviewed transition.
# 9.5a: 23 -> 13 — mapped construction retired all nine mapped
# canary assumptions and activated the shared generic-intersection
# exclusion; downstream mapped member/relation/inference work now
# stops at explicit M8-owned escape sites instead of dormant guards.
max_dormant = 13
