//! h2-7b-w2 lane A controls — the frozen closure rows of the NodeBuilder /
//! declaration-serialization classes (filled by lane W2-A; registered at E1 so
//! the two implementer lanes never touch `contracts.rs`).
