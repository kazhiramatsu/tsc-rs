//! h2-7b-w2 lane B controls — the frozen closure rows of the declaration
//! transformer, printer-comment and option-deprecation classes (filled by lane
//! W2-B; registered at E1 so the two implementer lanes never touch
//! `contracts.rs`).
