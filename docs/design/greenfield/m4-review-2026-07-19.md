# M4包括レビュー最終レポート (2026-07-19)

pre-M6監査。13専門エージェント(バンド別12+横断メタ監査1)+オーケストレータ再検証。
対象: M4時代チェッカー本体 約85k行(flow.rs/narrow.rsはM5レビュー済みのため除外)+escapes.toml/デッドガード/ドキュメント約束の横断監査。
検証水準: 全HIGHをオーケストレータがソース照合、8ルートはポート実行(tsrsdrv)+実tsc(vendored 6.0.3)の両側実行で確定。

## 総括

構造的欠陥(v1クラス)は無し。関係エンジンのコア規律(maybe-stack、Maybe計上、Err非キャッシュ、単純関係表、TypeFacts全定数、checkSourceElementWorker 1:1)は検証でクリーン。
一方で **(1) 実行確定FP 8ルート**(FP=0はコーパス射影上のみ成立)、**(2) 虚偽化した正当化コメント/エスケープ理由 約15件**(M3時代の「まだ存在しない」主張がM4で失効したまま)、**(3) doc約束の失効 4件**、**(4) M6投機トランザクションが必ず被覆すべき状態面の穴** が確定。

---

## S級: 実行確定FP(ポート実行+tsc両側で再現済み)

| # | 所見 | 場所 | 修正方針 |
|---|------|------|---------|
| S1 | `o["__x"]`→7053FP / `O["__x"]`→2339FP+errorType / 2551 args乖離。element/indexed経路がescapeLeadingUnderscoresを欠く(メンバ表はescaped-keyed) | indexed.rs:1749 property_name_from_type(消費 indexed.rs:1022/1471/1515, access.rs:3666) | propName境界でescape付与。表示行(indexed.rs:1346)は生のままが正 |
| S2 | デコレータ内クラス式のprivate名→18013FP。findAncestorの`isClassLike→"quit"`欠落 | access.rs:1185-1209 | quit条件追加 |
| S3 | `keyof typeof E`にnumber混入(FP+FN対)。enumNumberIndexInfoフィルタ欠落+「存在し得ない」虚偽コメント(合成はannotate.rs:5287+に実在) | indexed.rs:263-308(虚偽コメント261-262) | 明示マーカーでフィルタ(IndexInfoはfresh生成なのでID同一性不可) |
| S4 | `if (this.f) { this.f(); }`→2774FP(オーケストレータ実行: port 2774@35 / tsc clean)。条件ウォーカーがThisKeywordでNone | operators.rs:4345-4359, 4525 | ThisKeyword→tryGetThisTypeAtのsymbol |
| S5 | `E.Zero \|\| b ? 1 : 2`→2845FP(実行済み)。walked-leftをtscはcondType再利用、ポートは再チェック(「tsc re-checks」コメント虚偽 — _tsc.js:83647) + `f \|\| g`の2774 FN flavor | operators.rs:4177-4185 | condType(結果型)を渡す |
| S6 | ジェネリック呼び出しアリティ失敗後 `r.toFixed()`→18046FP(実行済み)。スタブ充填シグネチャがresolved-signature stash経由で結果型へ漏れる(ヘッダの可観測チャネル列挙が不完全)。**併せてヘッダの「M6はconstraint stepを除去せよ」はtsc事実誤認 — getInferredType(69271-69313)はconstraint-clampする**(m4-57:25も同罪、m6-inference-calls-steps.md:209が正) | calls.rs:3866-3911(stash 3375-3380) | 失敗パスの結果型を封じ込め(calls.rs:5916のtestが同型の正修正を記録済み) |
| S7 | checkGrammarModifiersスタブのfalseがtscの抑制的trueと非等価: `async class C extends A extends B`→1172FP、`declare m<>(): void {}`→1098FP(両実行済み) | check.rs:272-274(caller class.rs:832-837, functions.rs:3899-3908) | 両callerにmodifiers-would-reportガード(modules.rs:4184/statements.rs:52の既存パターン)。check.rs:654/967の他caller要点検 |
| S8 | `@ts-expect-error`付きobj-literalアクセサbody→2578FP(tsc clean)。deferred封じ込め(check.rs:1767-1771)とmodule-export封じ込め(check.rs:186-190)がpartial range未記録 × A2(obj-accessor body未チェック)の合成 | lib.rs:275-314, check.rs:1767 | 両境界でmark_partially_checked_node |

## A級: コード確定HIGH(誤型/キャッシュ汚染/大FN帯 — M6前に修正)

| # | 所見 | 場所 | 備考 |
|---|------|------|------|
| A1 | **関係キャッシュキー崩れ**: is_unconstrained_type_parameterがインラインconstraintのみ読む(宣言型パラメータはlinksスロット、インラインは恒久None=constraints.rs:20-36)→`T extends X`が常に「無制約」→`=N`backref衝突で誤判定の恒久キャッシュ(FP/FN両方向・順序依存)+`*`マーカー恒久不発でengine.rs:1614-1625(broadest-key Maybe再検査)がデッド→深い循環ジェネリクスがdepth-100 overflow→`Failed\|StackDepthOverflow`恒久誤False。2エージェント独立発見(ENG-1/R1) | relate.rs:88-96 | 修正はtsc同様のforcing(get_relation_keyのfallible化)。非forcing読みは解決順依存の非決定性を生むので不可 |
| A2 | **obj-literalアクセサのdeferredがcheckAccessorDeclaration全体でなくサブセット** → body内文が完全未チェック(2322等すべてFN、markerなし)。m4-55は「checkAccessorDeclarationへroute」と記録 — 静かに縮小版へ差し替わった失効。2独立発見(DR-F1/FN-F2)。S8のFP源 | check.rs:1812-1820, 1929-1939 | checkAccessorDeclaration(functions.rs:2893)へroute |
| A3 | **checkSignatureDeclarationが関数式/アロー/objメソッドで空スタブのまま**(m4-58「2箇所置換」の1箇所失効; 実装はfunctions.rs:1888に実在)→式形式の2677/1064/2370/2730/2505/1055帯すべてFN、manifest/dispositionsに記録なし | functions.rs:219(スタブ:276) | 配線1行+スタブ削除 |
| A4 | **resolved_return_type後勝ち上書き**(tsc 59839は`??=`先勝ち)+生pubフィールドでlinks speculation assert網の外。再入充填で内側の正値を外側が上書き(サイクル時はanyで上書き)。M6ボディ推論の最重要地雷 | annotate.rs:7448(フィールドstate.rs:103) | `??=`化+links API経由化。functions.rs:201-214に正パターン実在 |
| A5 | **union/intersectionプロパティのContainsPrivate/Protected/Static全欠落**(CONTAINS_PUBLIC無条件のみ)→消費側3箇所(union私的bail-out 3446/never縮約3298/modifier reader 3678)恒久デッド。private衝突union→2339FN、A&B衝突→never縮約漏れFP系。同関数でmergedInstantiations/writeTypes/nameType伝播も脱落 | structural.rs:3410(tsc 59149) | ヘッダ「M3 slice」失効 |
| A6 | **auto-accessorが黙ってany**。ヘッダ「that arm escapes」は虚偽 — escapeは存在せずanyでOk返し。binderはACCESSORフラグ付与済み(bind.rs:359-366)、tscのladder(56746+)はgetAnnotatedAccessorType(accessor)+widened-initializer+implicit-any armを持つ。2322/2564/7033帯FN | annotate.rs:4694-4820, 4879-4893 | annotated_accessor_type_nodeにPropertyDeclaration arm追加等 |
| A7 | **lateBindMember重複armがlate表を置換**(tsc 57680はcreateSymbolのみ、表は最初のシンボル維持)→member型last-wins(tscはfirst)+三重複で2733FP shape(probed)。2独立発見(AN-F2/CL-F4) | annotate.rs:3564-3574 | `late.insert`を削除(late_symbolのみ差し替え) |
| A8 | **amalgamated-duplicatesのflushが早すぎ**: initialize_program_globals末尾(merge.rs:644)でflush、merge_module_augmentations(lib.rs:692)はその後 → flush後のクロスファイル衝突(augmentation由来+lazy late-binding由来)の2300/2451がすべて無報告。tscは両passの後にflush+map=undefined化で以後即時報告 | merge.rs:353-402, 644 | flush移動+flush後は即時報告モード |
| A9 | **スクリプトファイルの`declare global`を収集**(tscのisExternalModuleFile gateなし)→同ファイル衝突2300FP(tscは2669のみ)/クロスはA8と相殺FN/無衝突ならglobals汚染(2304/2339抑制FN) | merge.rs:663-701 | collectorにexternal-module条件追加 |
| A10 | **canHaveSyntheticDefaultがbundler/node10でpackage.json参照**(tscはNode16-19+node_modulesのみimplied format計算、それ以外undefined)→bundler+`"type":"module"`+export=+default importで1192FP(tsc probed)。3値化必須(unwrap_or(CommonJs)が根因)。is_esm_cjs_ref(modules.rs:3515)にも波及 | modules.rs:435-450, 3043-3082 | implied formatのOption化 |
| A11 | **export= 1203式にdecisive-extension armなし**: `.cts`+module=esnext→1203FP / ambient `.d.mts`→1203FN(tsc probed)。tsc 86494-86499はimpliedFormatForEmitを全resolution種で拡張子決定的に使う。※oracle-correction epochの「検証」はnode系行列のみだった | modules.rs:5595-5615 | 拡張子armの追加 |
| A12 | **引数エラーのスパンがgetEffectiveCheckNode不経由**(括弧/satisfiesを剥かない)。tsc 76229はerrorNode=elaborationとも effectiveCheckArgumentNode。m4-57:253に正仕様記録済み、rest分岐(calls.rs:3131)は正しい — ループだけ失効。1行修正 | calls.rs:3080 | `("a")`で内側リテラルスパンFP(表示可能な型ペアなら) |
| A13 | **getUndefinedPropertyがundefined使用**(tscはundefinedOrMissingType)。「eOPT未モデル」ヘッダは虚偽 — options.rs:38+tables.rsのundefined_or_missing実在、姉妹サイト(literals.rs:226/facts.rs:323)は正しく消費。eOPT:trueでのwidening FP(tsc probed) | widen.rs:311-327 | intrinsic差し替え1行 |
| A14 | **bigint radix注釈のescape理由虚偽**: 「注釈リテラルは10進のみ/radixはM6」— `type A = 0x2n`は合法(probed)で、完全なparsePseudoBigIntポートがexpr.rs:3680-3773に実在・稼働中。1コール修正。3独立発見(L2/META-3/annotate) | annotate.rs:7541-7557 + escapes.toml M6行 | |

## B級: MED(乖離確定・約束失効・M6/M7準備)

**関係エンジン(M6前推奨)**
- B1 ENG-2: IDENTICAL_BASE_TYPE_CALCULATEDをfallible領域前に書く → Err残渣で恒久「等価base無し」誤メモ (engine.rs:467-543)。Calculated書き込みを後置or revert twin
- B2 ENG-3: is_known_property/hasExcessProperties等にlate-bound-name index armとJSXアーム欠落+「dead in M3」虚偽コメント(jsx.rs:279がJSX_ATTRIBUTES生成、786が関係機構へ) (engine.rs:930-985, 1953-1987)
- B3 ENG-4/R9: get_recursion_identity 2乖離 — `!(Anonymous&&Class)`除外なし/TypeParameterがType(ty)返し(tscはtype.symbol; クローン統一されず深い再帰でoverflow誤False)。「symbol-less synthetics」コメント虚偽(constraints.rs:39がsymbol付与) (engine.rs:2457-2498)
- B4 R3: constructorVisibilitiesAreCompatible完全スキップ(「常にpublic」虚偽)→private ctor代入2322FN(probed) (structural.rs:1874-1878)
- B5 R4: removeSubtypesのTypeParameter-union-constraint armとclass-derivation例外欠落(「M4前は非構築」虚偽)→Subtype縮約union構成が乖離 (unions.rs:369-437)
- B6 R5: isObjectTypeWithInferableIndexにEnum|ValueModule欠落 → namespace→index署名代入2322FP系(tsc probed clean) (structural.rs:3767-3787)
- B7 R6: 型述語関係ゲートが4決定セル中3セルで過剰封じ込め(ignoreReturnTypes union-matching等、tscは述語機構不要)。escape理由は1セルのみ正確。M6 7.5はゲート分割を | structural.rs:4972, 2136/2309/2400
- B8 R7/META-2: ジェネリック署名ゲートがtop-signature/arity判定より上に配置+eraseパラメータ無視(get_erased_signature実在未使用)+same-target-reference pairwise欠落。cite「66727-66730」誤記(正64505-64514) (structural.rs:2181-2188, 1915-1938)
- B9 R8: structuredTypeRelatedTo head 3欠落(intersection-constraint retry全欠[M6で発熱]/`!isGenericObjectType`条件落ち/optionals armの`!isArrayOrTupleType`) (structural.rs:110-187)
- B10 R10/LNK-2: union_property_cache・subtype_reduction_cache・set_symbol_is_discriminant・ty_mut_cached_equivalent_base_typeがlinks規律(depth assert)外 — M6ロールバック危険(特にunion_property_cache: payloadはspeculation-aware書き、cacheは生insert) (structural.rs:3311-3319, links.rs:933/1757)
- B11 R11: requireOptionalPropertiesに`!isEmptyArrayLiteralType`欠落(1条件修正) (structural.rs:1641-1644)

**呼び出し/文脈**
- B12 F2(calls): rest位置の適用性失敗はtscがcontainingMessageChain=void 0(2345)、ポートは2769/headで包む (calls.rs:3408-3479)。M6でジェネリックrest増加時に顕在化
- B13 checkModeドロップ3種: JsxExpression(expr.rs:323+jsx.rs:206ハードコード)/access flow tail(access.rs:1718, 2126)/tagged template逆方向(expr.rs:288はNORMALであるべき)。M6インference再走の前提
- B14 F4(calls): union-of-signatures failureのminArgumentCountがminAndMax(getNumNonRestParameters)でない (calls.rs:4000-4004)
- B15 AN-F7: isParameterOfContextSensitiveSignatureキャッシュガード欠落 — 非文脈クエリが恒久型を書き、M6再計算が汚染読み (annotate.rs:5960-5964)

**モジュール/解決**
- B16 M-3: Node16/18のsync-import 1471/1479行完全欠落+「resolverはその形を作らない」虚偽(probe_module_candidatesは.mts解決する; tsc probed) (modules.rs:1998-2093)
- B17 M-4: 2661がblock-scopeコンテナ判定(tscはgetDeclarationContainer)→`if(true)var q`エクスポートFP/`for(var v;;)`FN(probed) (modules.rs:5470-5484)
- B18 M-5: markAliasReferenced/markExportAsReferenced族が完全未移植・無marker。今日は診断不活性(確認済)だが**M7 unusedバンドの前提**(resolveName(isUse)副作用が6133/6192の将来FP源)。M7-owned dispositionの記録を — 5.8d以来のopen item解消
- B19 L-1: resolve_entity_name_ex raw-hit carve-outが素の2694も抑制(probed FN)。ラベルの範囲主張過小 (resolve.rs:1622-1624)

**annotate/クラス/文**
- B20 AN-F3: static block内`this`が2526でなく解決(has_static_modifier誤用; is_static_element実在未使用) (annotate.rs:1357)
- B21 AN-F4: アクセサimplicit-anyにisPrivateWithinAmbient抑制なし → ambient private setterで7032FP(probed; 既存helper 6152流用可) (annotate.rs:4758-4776)
- B22 AN-F5: get_named_membersがsymbolIsValueのalias armスキップ → `export import`がkeyof typeof/プロパティ列挙から消える(probed; symbol_is_value実在structural.rs:2828) (annotate.rs:5371-5380)
- B23 AN-F6: `extends null`がstrictNullChecks:false下でnull_wideningと比較されず2507エスケープへ(tscは静かに空base) (annotate.rs:4209-4215)
- B24 CL-F3: mergeModuleAugmentationのexport-star pre-mergeがunidirectional=trueでない → merged-symbolリダイレクトの恒久ID汚染(M6顔) (merge.rs:816)
- B25 CL-F5/AN-F8: 2508スパンがexpressionでなくノード全体+「unwinds as Unsupported」ヘッダ虚偽 (annotate.rs:4323-4329)
- B26 FN-F3: grammar_error_at_pos 3箇所がbyteオフセット直渡し(UTF-16要; 姉妹サイトは変換済み)→非ASCII先行でスパンFP (functions.rs:4090/4410/4663)
- B27 FN-F5: 2372/2373(パラメータ既定値の自己/前方参照)未移植+「5.6」stage claim失効(resolve.rs:1364-1373; probed 2372)
- B28 S1: check_grammar_variable_declarationのambient報告後早期return(tscはfall-through)→2480/1263FN(probed) (statements.rs:962-965)
- B29 S2: for-await-over-thenableで**tsc 6.0.3自体がDebug Failureクラッシュ**、ポートは1320正常報告 — 未記録のtsc-crash逸脱。逸脱台帳へ記録(M8 shadow/fuzzerが踏む) (iterate.rs:836-845)。※機能側エージェントも独立にasync-generator yield合成でのtscクラッシュ(N1)を発見 — 同台帳へ
- B30 DR-F3: file-lessグローバル診断の一律drop — tscはempty-snapshot regimeでper-file pullに合流(probed)。ImportMeta carve-outだけでは不足+「pass through」虚偽コメント (lib.rs:712-716, 795-797; globals.rs:586-587)
- B31 DR-F4: @ts-nocheck/checkJs:offファイルもチェック実行し出力段で落とす(tscはチェック自体しない)→file-less 2318のFP面+M6の計算順リスク (check.rs:260-263)

**メタ/記録**
- B32 META-4: m4-end-sweep-steps.md:197の2578 KEEP-OFF決定が失効(同日4b6f87c4でライブ化済み)。doc修正+stale testリネーム
- B33 META-5: **M5 post-close見送り5件(A1/A2/D3/E5/C5)がm5-flow-steps.mdに未記載**(PR #36本文とメモリの主張は虚偽)。今日のライブバグ性は5件とも否定確認済み(E5はnarrow.rs:2860-2883で既にdefused、C5は`let _`で不活性等)。M6着手前にdocへ実記載を
- B34 INV-1: unwind不変条件スイートの盲点 — shared_flow長/reduce_label_overrides/exhaustive_switch_computing/inline_level未検査+Resolved残渣・tables object-flagメモは構造的不可視。M6トランザクション設計はengine系レポートのSTATE-SURFACE INVENTORY(A〜E表)を仕様入力に
- B35 M6-1: clearActiveMapperCaches未移植(推論fix時のper-mapper cache無効化、tsc 73624)/speculation_depth網の穴(Signatureフィールド・診断sink・suggestion_count・partial_check_records)/revert twinのassert非対称(revert_node_enum_values_computedのみdepth assert — M6中unwindでpanic)/suggestion_count予算のロールバック要件

## C級: LOW(修正任意・状況記録)

- calls F5: デコレータarity 1278/1279フレーバー未実装(headが吸収し不可視だが「不忠実表示より無診断」の家訓違反; doc記録済み) / F6: mark_jsx_alias_referencedのResult握り潰し / F7: getResolvedSignature再入フレームのstash漏れ(tsc 77505は無条件復元) / F8: import-callヘッダ「SILENT None stub」虚偽(実装はライブ) / F9: class式コンテナのisConstructorAccessible
- ENG-5/R12: is_related_to fast pathのインラインconstraint(A1と同根、verdict等価) / R13: compare_properties optionality無条件 / R14: tables内template-distribution unionのledger正確・期限失効(「M4で移動」→未移動、M8等へ再所有を)
- OP-3: module.exports構文的early-return欠落(TSでも生きる; probed) / OP-4: shift NaNガード反転→enum内`1<<(0/0)`で6807FP(probed; `!(abs>=32)`修正) / OP-6: comma 2657抑制のbyte/UTF-16混在+end-inclusive
- L3=OP-5: const-assertionのalias未解決受理(「5.8dまで」失効)→import経由1355FN(probed)
- FN-F4: is_arity_smallerのusizeアンダーフロー — コード上実在だが実行再現ならず(リカバリがquestion token落とし)。saturating_sub予防修正
- FN-F6/AN-F9: late-bindable計算名アクセサのthis-parameter借用ゲート(has_bindable_name実在未使用) / FN-F7: super-call帯の再帰ウォーカー2本(反復化discipline違反、露出低) / FN-F8: obj-literal要素contextualのlate-bindable分岐
- CL-F6: check_index_constraintsの!hasBindableName(dedupで現在遮蔽; 1行) / CL-F7: js_grammar.rsヘッダの偽「schema gap」
- AC-F4: 7052 suggestion baseがtryGetPropertyAccessOrIdentifierToStringでない(args乖離probed) / AC-F6: getPropertyNameFromIndexのaccessNode fallback / AC-F7小束(applicable-index lateBound arm、HTML*Element正規表現等)
- AN-F10: is_valid_index_key_typeの!isGenericType(文法1337が遮蔽) / AN-F11: resolve_anonymous_type_membersのgetMergedSymbolホップ / AN-F12小束(空文字computed名表示、file-less 7025、indexed-access nodeキャッシュのwrap位置)
- DR-F5: get_global_symbolのalias hop欠落+「5.1が置換」失効 / DR-F6: partial-range 2578のstart面のみ(b0cd3802で意図的、コード上の記録推奨)
- meta: fn-dispositions.tomlはbacklog(スタブ台帳ではない)確認 / recovery 13サンプル全て正当 / statements iterate.rsのNode16 1432注記は「FN」でなく「代替発行」に修正を

## クリーン確認(深掘りで乖離なし — 信頼してよい面)

- facts.rs 100%(TypeFacts全定数・全アームtsc一致) / 単純関係表5種全一致(comparable非対称含む) / maybe-stack規律・Maybe計上・overflow計上・Err非キャッシュ(engine.rs全数監査で違反なし) / isDeeplyNestedType一致
- checkSourceElementWorkerスイッチ1:1(silent kind gapなし) / addLazyDiagnostic eager-identity成立(87104-87110唯一モード+全32サイト) / deferredノードmid-iteration意味論一致
- 5.9b workerのelse-if形状等価 / 5.9c staged publication一致 / alias解決全hop一致 / getEffectiveCallArguments・arity kit・reorderCandidates一致 / iteration帯(IterationUse定数含む)一致 / spell.rsコア逐語一致 / SCRIPT_TARGET_FEATURE_MEMBERS 278行一致 / js_grammar+plainJSErrors 1:1(92行順序含む) / lib選択は_tsc.js自身のlibEntriesから自己較正(ドリフト構造的不可能) / scanner数値正規化・pseudoBigInt(-0n含む)一致 / trampoline真に反復・深spine安全
- デッドガード: 24敵対入力(パーサリカバリ/JS/.d.ts/bigintキー等)をポート+tsc両実行 — **0パニック、0FP、大半byte一致**
- escapes.toml: 監査対象60行超中、虚偽はA14の1行のみ(他は理由・owner・fire-before-write規律すべて正確)。エスケープ規律違反(書き込み後unwind)ゼロ
- doc約束26件中、失効はB33の1件のみ(他は着地or追跡確認)

## 推奨着手順

1. **fix/m4-review-s級**: S1-S8(実行FP 8ルート)+A12(スパン)。各ルートに単体ピン+可能なら新fixture(oracle-refresh併用)
2. **fix/m4-review-a級FN**: A2/A3(配線系、各1-2行+スタブ削除)、A6、A14(1コール)、B28。虚偽コメント/ヘッダの一括訂正(A1/A5/B2/B3/B4/B5/B6/L2等の失効正当化文)
3. **M6開始バー統合**: A1(キーforcing化)、A4(??=+links化)、A5、A7、A8/A9、B10、B15、B24をM6着手前スライスに。**engine系レポートのSTATE-SURFACE INVENTORY+B34/B35をm6-inference-calls-steps.mdのトランザクション仕様へ転記**
4. **docs/記録**: B32(2578 doc)、B33(M5見送り5件の実記載)、B29(tscクラッシュ逸脱台帳2件)、B18(markAliasReferenced M7 disposition)、R14期限再所有、S6のm4-57:25誤記訂正
5. **M7/M8ノート**: B16(1471/1479はimpliedFormat 3値化後)、B7/B8(ゲート再配置はM6 7.5)、B30/B31(driver帯)、C級随時

## 生成物と出典

- 出典: 2026-07-19 バックグラウンドレビュージョブ(13エージェント+オーケストレータ検証)。エージェント別フルレポート・実行プローブ(ポート側=一時tsrsdrvドライバ、tsc側=vendored typescript.js)・検証ログはジョブtmp配下にあり**ジョブ削除で消える** — 恒久的に必要な部分は本docの付録A/Bに転記済み。
- 修正スライスを切る際は本docの「推奨着手順」を分割単位とし、各所見のfile:lineは着手時点のツリーで再確認すること(レビュー時HEAD=43432368)。

---

## 付録A: M6投機トランザクション用 STATE-SURFACE INVENTORY(engine系エージェント納品・原文)

B34/B35の仕様入力。m6-inference-calls-steps.mdのトランザクション節へ転記する際の原本。凡例: (a)=今日のUnsupported unwind時の復元、(b)=M6ロールバック要件、(c)=残渣リスク。

**A. Transient stacks (must be at entry depth after any unwind — all in UnwindSnapshot unless noted)**

| Field (state.rs) | today | M6 need | risk |
|---|---|---|---|
| `resolution_targets/results/property_names` (659-661) + `resolution_start` (664) | every pusher pops via captured-result pattern; snapshot-checked | save/restore lengths + `resolution_start`; results VALUES below the mark are mutated by cycle flagging — truncate is enough since entries above mark are popped | LOW |
| `contextual_type_nodes/types/is_cache` (502-504) | push/pop paired | truncate to mark | LOW |
| `inference_context_nodes/contexts` (509-510) | exists-but-empty (M6 payload pending) | THE new M6 stack — define rollback with it | — |
| `contextual_binding_patterns` (495) | paired | truncate | LOW |
| `awaited_type_stack` (474) | paired | truncate | LOW |
| `active_type_mappers(+caches)` (306-307) | pushed frames popped on Err (instantiate.rs:1091-1096) | truncate stack; ALSO port `clearActiveMapperCaches` on inference fixing; entries in SURVIVING outer frames computed during a failed candidate stay structurally true — but under a mutable inference mapper they are stale unless cleared | **HIGH** |
| `variance_handler_stack` (303) | popped on Err (engine.rs:1692-1699) | truncate | LOW |
| `class_interface_declared_in_progress` (320), `type_parameter_defaults_in_progress` (324) | popped before `?` | truncate | LOW |
| `flow_loop_stack` (527) + `flow_loop_start` (521) | paired; snapshot-checked | truncate/restore | LOW |
| `shared_flow` (336) | truncate-before-`?` (flow.rs:317) — **NOT snapshot-checked** | truncate to mark; add to UnwindSnapshot | MED |
| `reduce_label_overrides` (343) | restore-before-return (flow.rs:1856-1863) — **NOT snapshot-checked** | snapshot map or forbid across speculation | MED |
| `exhaustive_switch_computing` (551) | remove-before-`?` (narrow.rs:2352) — **NOT snapshot-checked** | must be empty across the boundary | MED |
| RelationChecker maybe stack (engine.rs:707-720) | per-call local struct; Err arm pops its frame's keys (1707-1718); every early return precedes the pushes | nothing — cannot leak by construction | NONE |

**B. Counters / cursors / flags**

`speculation_depth` (203): the transaction's own guard — RAII increment/decrement. `instantiation_depth` (311): restored on Err; snapshot-checked. `instantiation_count` (312): monotone per element, reset at the three entry points (check.rs:344 / check.rs:1764 / expr.rs:102 = tsc 86551/86921/80965) — do NOT restore on rollback (tsc doesn't). `inline_level` (539): NOT snapshot-checked. `in_variance_computation` (295) / `variance_type_parameter` (291): save/restore exists. `flow_analysis_disabled` (331): one-way latch even in tsc — leave. `is_inference_partially_blocked` (247): M6-owned, part of the transaction. `suggestion_count` (454): **must be snapshot/restored or consumption-gated under speculation** — tsc consumes the did-you-mean budget only on reporting paths.

**C. Caches — permanent-truth (keep across failed speculation, keyed structurally)**

`relations` (5 maps + enum_relation) — keep, but fix A1 (key collisions) first; entries referencing speculation-minted types stay internally consistent (types are permanent interned objects; tsc doesn't roll relation caches back either). `subtype_reduction_cache`, `cached_types`, `tables.instantiation_*`, `links.alias_instantiations`, `links.union_property_cache`, `Signature.instantiations`/`erased_signature_cache`/`optional_call_signature_cache`/`isolated_signature_type`, `decorator_context_override_type_cache`, `undefined_properties`, `evolving_array_types`/`final_array_types`, `flow_loop_caches`, `flow_node_reachable`, `last_flow_node(+reachable)`, `switch_types_cache`/`exhaustive_switch_cache`/`effects_signature_cache`/`resolved_type_predicates`, deferred-global memos, `init_global_type_probes`, `jsx_implicit_import_containers`, `js_assignment_containment_indexes`, `unresolved_package_root_cache`, `merged_symbols` — all compute-once truths; no rollback needed PROVIDED no M6 inference-placeholder type can be an input key (in tsc the only inference-sensitive cache is the active-mapper cache). **Verify this property for each new M6 cache.**
Links tables: write-once/protocol slots; the four Resolving-sentinel protocols each have paired Err-reverts (variances / simplified / resolvedSignature call protocol / aliasTarget) — verified. Late-bind revert annotate.rs:3460-3467; members retracts annotate.rs:3117/3263/4360/5354. **Close the discipline bypasses (A4, B10) before enabling speculation, or the "no permanent writes under speculation" guarantee is false.**

**D. Diagnostics sinks (transaction must truncate)**

`diagnostics` (570) — truncate to mark (push-dedupe is order-safe under truncation). `visible_global_diagnostics` (575). `partially_checked_ranges` (580) + `partial_check_records` (584) — a speculative containment permanently marks a range (affects @ts-expect-error exemption) → must roll back. `elaborated_satisfies_expressions` (587), the five `potential_*` per-file accumulators (431-435) — speculative pushes must roll back or double-drain. `deferred_nodes` (420) — tsc DOES still check nodes registered under speculative checkMode; verify against tsc checkNodeDeferred before deciding to roll back. `suggestion_count` — see B.

**E. Interners / arenas (append-only, never rolled back — garbage is safe)**

`tables` types (object-flag memos: COULD_CONTAIN_TYPE_VARIABLES_COMPUTED / IS_UNKNOWN_LIKE_UNION_COMPUTED written only after the fallible region — verified; IDENTICAL_BASE_TYPE_CALCULATED is the exception = B1), `signatures` (196; but see A4 raw field), `members` (197; staged-publication in-place mutation has Err retract twins), `mappers` (262), `widening_contexts` (482).

**Additional M6-start requirements (B35)**: port `clearActiveMapperCaches` (tsc 73624-73629, invoked from getInferredType 69310) with the inference port; decide ONE revert-twin convention (`revert_node_enum_values_computed` links.rs:861-864 asserts speculation_depth while every other revert twin deliberately does not — an unwind DURING speculation panics there via evaluate.rs:112); extend the speculation_depth assert net to Signature fields and the sinks above.

---

## 付録B: S級実行FPの再現入力(修正スライスの回帰ピン用)

すべて strict デフォルト、vendored tsc 6.0.3 と対照済み。

- **S1**: `interface O { __x: number } declare const o: O; o["__x"];` → port 7053 / tsc clean。`type V = O["__x"];` → port 2339+errorType / tsc clean(V=number)。suggestion args: `p["__helo"]` → tsc 2551 arg `'___helo'` / port `'__helo'`
- **S2**: `function dec(x: any): any { return undefined; } @dec(class Inner { #p = 1; m() { return this.#p; } }) class Outer {}` → port 18013 / tsc clean
- **S3**: `enum E { A, B } type K = keyof typeof E; declare const k: K; const s: string = k;` → port 2322 / tsc clean。逆向き `declare const n: number; const k2: K = n;` → tsc 2322 / port clean(FN)
- **S4**: `class C { f() {} m() { if (this.f) { this.f(); } } }` → port 2774@35 / tsc clean(オーケストレータ実行済み)
- **S5**: `enum E { Zero = 0 } declare const b: number; const r = E.Zero || b ? 1 : 2;` → port 2845@55 / tsc clean(実行済み)。FN flavor: `declare const f: (() => void) | undefined; declare const g: () => void; if (f || g) {}` → tsc 2774×2 / port ×1
- **S6**: `declare function f<T>(x: T, y: T): T; const r = f(1); r.toFixed();` → port 2554+18046 / tsc 2554のみ(実行済み)
- **S7**: `async class C extends A extends B {}` → tsc 1042のみ / port 1172(+1042はM7 FN)。`class C { declare m<>(): void {} }` → tsc 1031+1183 / port 1098+1183
- **S8**: オブジェクトリテラルgetter内の `let a: number = "s"` の直上に `// @ts-expect-error` → tsc clean(抑制が directive を使用済みに) / port 2578
