# H2.7c completion and transition

Status: local close update, 2026-09-07. Runtime closure was merged in
[PR #510](https://github.com/kazhiramatsu/tsc-rs/pull/510) at
`218ad0505097a5a2015dd06fd445fd18d99c3d48`; this update adopts its evidence.
[Hosted run 34100436702](https://github.com/kazhiramatsu/tsc-rs/actions/runs/34100436702)
passed on `932c51d4fd57ea49e147e38b9b5e917307b35101`: all historical bands and
H2.7c passed twice (40m28s for acceptance; 40m39s for the job).
The user-authorized lightweight workflow continues; this metadata-only close
introduces no additional hosted call.

The accepted correction projects forced declaration reference paths independently
of ordinary emit eligibility, retaining original declaration-input paths.
Its C-only regression compares 56 complete getter/forced calls covering disabled
declarations, JSON and suppressed JavaScript, without the newer map APIs.

The TypeScript 6.0.3 qualification retains the 42 original candidate cases:
31 exact and 11 later-owner intersections. Rust compares all 31 exact cases
and the rootDir boundary twice through the production entry. The separate
244 focused windows also pass twice: 241 exact and three retained outDir
refusals with no writes. The 37 ordinary, non-forced API references retain
H2.8d ownership and are not counted as Rust successes.

The request canary admits one new bit at all six production constructors.
The actual active mask contains 28 slices; H2.7a remains dormant. H2.7c counts
new declaration-option requests, reached declaration gates, and every explicit
getter or forced request, including cache hits and empty selections. The early
noEmitOnError case already exact in H2.6c retains zero H2.7c activity; the other
30 exact cases each observe one request. H2.7b continues to count transform members.
The new profile/request controls pass, including rejection by the older
profile before printing or writing.

Local validation on the runtime candidate passes:

- Preserved references: all 16 C contracts, including 56 additional complete
  calls (305.65s), and all 21 output-plan contracts. Emitter/compiler all-target
  Clippy passes with warnings denied (12m39s). All 40 policy/schema controls,
  three schemas, profile/close freshness, formatting and frozen-evidence checks
  pass on the corrected candidate.
- Retained activity correction: 31 original exact cases and all 244 focused windows,
  each twice (15 compiler tests, 535.81s), plus seven adjacent compiler and
  two emitter controls. Both historical failure cases pass twice (two xtask
  tests, 38.58s). All tests have zero ignored cases.
- Retained transformer replay: 116 cases and 202 windows, twice, with zero
  decision, byte or production-route differences; the oracle-fault control
  passes (one test, zero ignored, 119.36s).
- Full H2.7b regression: 1,593 candidates, 1,557 exact, zero known divergences,
  36 deferred, two repetitions. The compiler/conformance/project exact
  partitions remain 893 / 468 / 196, with 2,414 planned declaration members,
  six zero-member controls, 28 transform-blocked and five preflight-blocked
  observations.
- Clippy for emitter, compiler and xtask, all targets, with warnings denied;
  the 40 CI policy/schema controls; artifact schemas; profile/close freshness;
  formatting and whitespace checks.

The profile adoption adds `h2_7c_*` counts 42 / 31 / 31 / 0 / 11,
activates H2.7c and advances both next-slice fields to H2.7d
(`bundles-and-outfile`). The recorded completed count advances 26 to 27 and
the inactive count 10 to 9. Following the earlier close packets' additive
adoption accounting, runtime admissions become 10,753 + 31 = 10,784 and
executed candidates 11,272 + 31 = 11,303. The focused windows and boundary
probe do not enlarge these adoption counters, and the historical H2.5g
`admitted_profile` remains 9,196. These counters are not a unique-corpus census. The H2.7c exact band
shares noEmitOnError.ts with the older H2.6c exact band, so its 31 observations
contain 30 new unique IDs relative to that band.

The three live generators and schemas move together. The H2.5h-a
foundation reobserves its six controls twice and updates only its live parent
mirrors. H2.7a's mutable `transition_landing` advances; its complete historical
`runtime_contract` and schema remain unchanged. All four H2.7a foundation
artifacts and both H2.7b/H2.7c qualification artifacts retain their bytes.
The profile retains all 283 runtime input identities, including the new
reference fixture. Generation/freshness,
three artifact schemas, the 40 CI controls and the frozen-content checks pass
for this close update.

The lightweight workflow keeps final hosted acceptance as the runtime landing
boundary. The historical certificate walk and full developer CI remain
omitted. STAGE and the accepted TypeScript version do not change. H2.7d owns
bundles/outFile, H2.7e owns declaration maps, and the existing H2.8/BLD1/API
boundaries remain in place while independent d/e work proceeds in parallel.
