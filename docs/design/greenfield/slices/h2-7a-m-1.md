# H2.7a / m-1 — owner inventory + W-H2.7A witness freeze: rung packet

Status: revision 4 **SIGNED** (2026-08-31) — sol round-4 AGREE after
rounds 1-3 (§9; the round-3 finding resolved by counter-evidence).
First rung of the [h2-7a.md](h2-7a.md) §6 ladder (rev 2, sol-signed);
this packet is the train's design authority.
Support/evidence-only rung: **zero `crates/**/*.rs` production-crate
changes** (the walk's crate-byte definition, chain-walk.sh:274); the
one Rust edit is the mechanically required planner-coverage extension
in `new-ci/src/bin/plan.rs` (support plane). The close record pins
`rust_runs=0`, zero activity/admission deltas, and pre/post hashes
for the corpus/candidate-disposition ratchets and the protected
profile/manifest/refusal/vendor surfaces.

## 1. Identity and deliverables (bound by one case-manifest fingerprint)

`h2-7a-m-1`, kind `evidence`. Four deliverables, the first three as
walk-ordered chain artifacts (`owner-inventory → witnesses →
probe-traces`), all bound to ONE frozen case-manifest fingerprint so
they cannot drift independently:

1. `ratchets/h2-7a-owner-inventory.v1.json` — the seven-surface
   hashed-span inventory: MACHINE ROWS (upstream identity, span,
   hash, reachability, consumers, Rust evidence anchor, disposition,
   target rung) under the full consumer vocabulary
   (H2.7a/7b/7c/7d/7e, H2.8c, H2.8d, BLD1, API1, shared/build-info),
   covering every REACHED helper (not only named §2 functions) AND
   the option-owner closure h2-transition.mjs:448-467. Its `--check`
   recomputes current spans/hashes on every invocation.
2. `ratchets/h2-7a-witnesses.v1.json` — W-H2.7A public-observable
   lanes (exact `.d.ts` bytes, diagnostics, emitSkipped,
   emittedFiles order), fresh-process double-observed. Carries the
   frozen case manifest (exact fixture IDs, option records, input
   hashes, source-universe hash, roles, lane assignments).
3. `ratchets/h2-7a-probe-traces.v1.json` — the instrumented-probe
   trace artifact (§5): internal-decision lanes. Pins the
   public-witness/case-manifest fingerprint; identical case
   IDs/inputs/options/roles/lanes as (2).
4. The printer/factory/parenthesizer coverage audit — machine rows
   in (1) (disposition already-exact / foundation-needed with Rust
   proof anchors per worker/operation); the prose report is
   GENERATED from those rows, never a parallel authority.

Receipts: gate-tax-3 receipts are machine-local `target/` files
minted only by a successful full-recomputation `--check` (never by
`--write`, never "carried" inside tracked artifacts). Receipt-HIT
semantics (round-2 F4): a hit skips ONLY fresh observations — the
check still recomputes manifest selection, quotas and the 14-lane
coverage matrix, schema validation, fingerprints, edit-table
applicability, artifact assembly, and the whole-artifact byte
comparison on every invocation. Two separately keyed receipts:

- **public-witness key**: witness generator, Node, vendored
  bundle/libs hash, shared case-manifest fingerprint, public
  observation-content roll — NO probe inputs (the earlier public
  producer never depends on the later probe's patch or output).
- **probe-trace key**: probe generator, Node, vendored bundle/libs
  hash, shared case-manifest fingerprint, edit-table hash,
  instrumented-output hash, trace-content roll.

Same-slice machine integration: ORDER + `new-ci/src/bin/plan.rs`
extended together (65→68; walk-planner-coverage refuses drift);
the three schemas registered + the fixed registry test updated (the
h2-6b.md §wiring precedent); pin-index classification for the new
`crates/oracle/h2-7a-*.mjs` scripts in this same slice
(pin-index.py scans every oracle script).

## 2. Inventory universe (packet-time static roster; m-1 freezes)

Counting grammar (fixed): a "function" row = one nested
`FunctionDeclaration` in the vendored `_tsc.js` AST; an owner span
row additionally covers its own body. Under that grammar:

- **Declarations module** :114249-115873, 68 rows total:
  `getDeclarationDiagnostics` :114249 + the `transformDeclarations`
  owner :114265-115802 with its 60 nested declarations
  (transformRoot, visitDeclarationSubtree/Statements, the
  ensureType/ensureParameter/ensureModifiers cluster,
  checkEntityNameVisibility, the trackSymbol/report* error cluster,
  transformDeclarationsForJS :114431, late-painted statement
  machinery, binding-pattern recreation, module-specifier rewriting,
  heritage clauses) + 6 module siblings ending `isProcessedComponent`
  :115850.
- **Selection seam**: getDeclarationTransformers :115950,
  noTransformers :115896 (build-info context, sole use :123542).
- **NodeBuilder** :50777-55451 — under the fixed grammar 149 rows
  (148 nested declarations + the owner; the parent packet's "151
  named function forms" was a looser regex-form count, corrected to
  149 in h2-7a.md/README in this same landing) + truncation/error
  contracts :51205-51255 + tracker/specifier behavior :50890-50927;
  `symbolToDeclarations` member :51136.
- **Syntactic builder** :133276-134447 (instantiated :46483).
- **Resolver declaration subset** — measured consumption, 19 members
  / 28 call sites inside the module: isDeclarationVisible ×6;
  createTypeOfDeclaration, isExpandoFunctionDeclaration,
  isImplementationOfOverload, getPropertiesOfContainerFunction ×2
  each; createTypeOfExpression,
  createReturnTypeOfSignatureDeclaration, createLiteralConstValue,
  createLateBoundIndexSignatures (call :115610, resolver impl
  :88624), getDeclarationStatementsForSourceFile, getEnumMemberValue,
  isDefinitelyReferenceToGlobalSymbolObject, isEntityNameVisible,
  isImportRequiredByAugmentation, isLateBound,
  isLiteralConstDeclaration, isOptionalParameter, isSymbolAccessible,
  requiresAddingImplicitUndefined ×1 each.
  - Orchestration-side members with SHARED consumers (not
    H2.7b-only): collectLinkedAliases :116719/:116727 (declaration
    path), markLinkedReferences :116741 AND the JS-side use :116597,
    hasGlobalName :116624 + printer hook :116688 (both printer
    paths). The inventory dispositions each USE SITE, not the member.
  - symbolToDeclarations: zero resolver.* call sites; the resolver
    wrapper :88692 delegates to the NodeBuilder member :51136 —
    dispositioned as NodeBuilder-internal + API surface.
- **Printer subgraph**: the inventory ENUMERATES the selected
  type/declaration-relevant workers of createPrinter :116912-121378
  (370 rows incl. owner) with IDs/spans/hashes — the packet-time
  58-name candidate list (emitTypeParameter … emitTypeAnnotation/
  Arguments/Parameters, onlyPrintJsDocStyle :121145) is the SEED,
  and the frozen numerator is whatever the reachability scan proves
  (TO-VERIFY at freeze; no unfalsifiable counts).
- **Factory/parenthesizer**: the inventory enumerates the factory
  constructors and parenthesizer operations NodeBuilder and the
  declaration printer consume, each with a Rust proof anchor
  (existing `create_node` generic vs missing type-specific surface),
  feeding the G5/m-3.5 split.
- **Orchestration context** :116640-116715 (branch owners frozen in
  h2-7a.md §3).

## 3. The m-2 partition — a LADDER AMENDMENT at the m-2 design gate

`createTypeOfDeclaration` :88359 delegates to
`nodeBuilder.serializeTypeForDeclaration` :88365: the serialization
cluster is NodeBuilder-backed. The signed ladder (h2-7a.md §6.2) has
m-2 implementing the complete frozen subset; splitting it is a rung
completion change, therefore an AMENDMENT ratified at the m-2 design
gate (per h2-7a.md §6's own amendment rule), frozen as an exact
partition. Measured NodeBuilder-dependent set:
createTypeOfDeclaration, createReturnTypeOfSignatureDeclaration,
createTypeOfExpression, createLiteralConstValue (enum/NodeBuilder
path :88491), getDeclarationStatementsForSourceFile,
createLateBoundIndexSignatures, symbolToDeclarations — EXACTLY these seven; any later
addition or removal is itself another ladder amendment. m-1 freezes
this partition in the inventory; m-2's design gate carries the
amendment; the checker-native remainder (visibility, predicates,
alias bookkeeping) implements at m-2.

## 4. W-H2.7A witness families — output-blind selection, frozen pre-observation

Two DISTINCT population predicates, stated separately (round-2 F3):

- **Literal-directive fixture population**: 889 compiler / 526
  conformance unique files carrying `@declaration:true` (compiler:
  893 directive lines — 4 fixtures carry the directive twice, so the
  deduplicated file count is 889, per `grep -rli | wc -l` vs
  `grep -rhi | wc -l`); transpile 21
  declaration-related cases (20 `@declaration:true` controls + 1
  declaration-map control per the transpile suite inventory).
- **Effective-declaration case population**: 1,030 compiler / 861
  conformance expanded cases spanning 897 / 528 unique fixture IDs
  (declaration effective via matrix/config, not only the literal
  directive); project 528 declaration cases over 264 unique
  descriptors × 2 module variants (the h1 project classifier).

The output-blind selector operates over the EFFECTIVE-declaration
case population (the universe the H2.7b runner will face); the
literal-directive population is recorded as a covariate only.

Selection contract — ALL frozen BEFORE any observation runs:

1. Exact family IDs, per-family roles, and case counts (or a
   deterministic, output-blind selection algorithm over the frozen
   population), the 6a precedent (10 families / 36 cases named in
   the packet, frozen in the generator).
2. A family/case → all-fourteen-h2-7a.md-§5-lanes coverage matrix
   (every lane covered by ≥1 family; no lane unobserved).
3. Quotas for positive, adjacent-negative, composition, and fault
   cases.
4. Exact fixture IDs, option records, input hashes, and the
   source-universe hash in the case manifest (§1).
5. The 67-row exact-but-for-declarations set enters as ONE DECLARED
   STRATUM (chosen for js/map-exactness, recorded as such), never an
   undeclared convenience bias.

No hand-authored bytes; totals freeze at the design gate of the
implementing train, not during observation.

## 5. Instrumented probe — full trace discipline

Covers ALL h2-7a.md §5 internal-decision lanes: the transformer
symbol tracker (trackSymbol/report* cluster :114280-114304),
NodeBuilder truncation/error/fallback (:51205-51255), the 19-member
resolver-query results, declBlocked composition (:116664-116671),
PLUS module-specifier override behavior (:50890-50927),
syntactic-builder arm selection/recovery/fallback, hasGlobalName
decisions, and AST identity/original-node provenance/transform-flag
propagation.

Discipline:

- The vendor tree is NEVER modified. Instrumented output is
  generated under `target/` (the measurement-integrity trace rule).
- The edit set is a checked-in exact edit table (or generator-owned
  exact edits): base `_tsc.js` hash + edit-table hash + instrumented
  -output hash all pinned; exact-position edits (no patch fuzz) with
  a generated-to-vendor position map.
- For every case shared with (2): ordinary-run vs instrumented-run
  PUBLIC outputs byte-identical (the probe proves itself inert).
- Traces canonical: no temp paths, PIDs, object addresses, or
  unstable IDs; exact trace-event ordering; fresh-process double
  observation must be trace-identical.

## 6. Prohibitions

All h2-7a.md §8 rows; plus: no vendor-tree modification; no witness
family selected or adjusted after observation output exists; no
inventory disposition left "unknown" (fail-closed); no receipt
minted by `--write`; no artifact carrying its own receipt; ORDER +
planner + schema-registry + pin-index extensions in this same slice.

## 7. Acceptance

Inventory: every reached helper + option-owner closure dispositioned,
zero unresolved, machine rows recomputed on every `--check`; witness
+ probe artifacts double-observed deterministic, bound by one
case-manifest fingerprint, lane-coverage matrix complete (14/14);
probe inertness proven byte-identical on shared cases;
printer/factory/parenthesizer audit rows complete with Rust anchors;
`rust_runs=0` + zero deltas + protected-surface pre/post hashes
recorded; walk ONE-invocation converge (ORDER/planner 68/68); full
local gate green at the train head; hosted pass.

## 8. Evidence trace

Round-1-verified: vendored `_tsc.js` SHA-256 1c59e77a54b1..
(matches the h2-6a-witnesses pin); declarations module 68 rows (60
nested + owner + getDeclarationDiagnostics + 6 siblings); resolver
census 19 members / 28 calls; serializeTypeForDeclaration :88365;
symbolToDeclarations wrapper :88692 → :51136; createPrinter 370
rows; orchestration anchors :116719/:116727/:116741/:116624/:116688
+ the JS-side markLinkedReferences :116597;
createLateBoundIndexSignatures impl :88624 (call :115610).

## 9. Cross-review record

Round 4 (confirmation): sol **AGREE — sign-off** (2026-08-31): the
889/893/4 re-measurement reproduced; revision 4 may land on the m-1
implementation train and authorize m-1 under that train's design
gate (head-time recomputation per the landing note).

Round 3 (verification): sol **REVISE**, 1 finding (2026-08-31):
claimed the compiler literal-directive population is 885 unique
fixtures with 889 being line occurrences. Operator re-measurement
REFUTES the arithmetic while confirming the underlying observation:
`grep -rli '@declaration: *true' compiler/ | wc -l` = 889 (unique
FILES — grep -l deduplicates), `grep -rhi ... | wc -l` = 893
(directive LINES), exactly 4 fixtures duplicated
(es6ImportDefaultBinding, es6ImportDefaultBindingFollowedWith-
NamespaceBinding, contextuallyTypedBooleanLiterals,
es6ImportWithoutFromClause). 889 unique files stands; §4 now
records both figures so the two predicates cannot be conflated
again. Resolution recorded per the operator-experiment precedent
(W5 K13); the landing-train design gate re-verifies at head.

Round 2 (verification): sol **REVISE**, 4 findings (2026-08-31), all
accepted in this revision: the m-2 partition is EXACTLY the seven
named members, further change = another amendment (1 — §3); the
NodeBuilder count corrected to 149 under the fixed
FunctionDeclaration grammar (148 nested + owner; the 151 regex-form
figure in the parent packet/README is corrected at landing) — the
68/370 counts reproduced (2 — §2); the two population predicates
separated with the selector bound to the effective-declaration
universe (1,030/861 over 897/528 unique fixtures; project 528/264
and transpile 21 confirmed) (3 — §4); receipt-hit recomputation
contract written and the two receipt keys enumerated separately so
the public producer carries no probe dependency (4 — §1). Round-2
verified set: anchors :88624/:116597, module 68, resolver 19/28,
printer 370, vendored SHA 1c59e77a54b1... Landing note recorded:
recompute ORDER/planner counts, population values, hashes, and
protected-surface pre-hashes at the actual train head.

Round 1: operator draft (rev 1, scratchpad) → sol **REVISE**, 6
blocking + 1 advisory (2026-08-31), all accepted: the m-2 split is a
ladder AMENDMENT ratified at the m-2 design gate with the frozen
NodeBuilder-dependent partition (1 — §3); witness selection made
output-blind and frozen pre-observation with the lane-coverage
matrix, quotas, exact manifests, the 67-row declared stratum, and
the corrected population counts (project 528 cases / 264
descriptors; transpile 21; effective 1,030/861) (2 — §4); the probe
extended to ALL signed internal-decision lanes with the full trace
discipline (target/-only output, checked-in exact edit table +
triple hashes + position map, probe-inertness byte proof, canonical
traces) (3 — §5); the inventory/audit made machine-first (counting
grammar fixed at 60-nested+owner, the 58-worker numerator demoted to
a seed for the frozen reachability scan, factory/parenthesizer
enumeration added, :88624 impl anchor corrected, per-use-site
dispositions for the shared orchestration members incl. the JS-side
:116597) (4 — §2); the three artifacts bound by one case-manifest
fingerprint with the trace artifact pinning the witness fingerprint
and the audit prose generated from machine rows (5 — §1); the
walk/receipt integration completed (target/-local --check-minted
receipts, separately keyed public/probe receipts, recomputing static
checks, same-slice pin-index + schema registry + ORDER + planner)
(6 — §1/§6); "mjs/artifact-only" corrected to support/evidence-only
with zero `crates/**/*.rs` changes and the explicit zero-delta
close record (advisory — header).
