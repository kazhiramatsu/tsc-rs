# H2.8 — one-shot output, host, API and CLI completion

The requested objective covers all five rows H2.8a–H2.8e in the
[post-H1 schedule](../post-h1-completion-slices.md#45-maps-declarations-outputconfig-and-broad-qualification).
Work starts on `work/h2-8-output-matrix` at trusted base
`10748f6ee` (2026-09-08). H2.8 is open. The first packet below does not
close H2.8a or qualify any complete corpus band.

The implementation follows the current lightweight workflow: fresh complete
TypeScript observations, focused production comparisons and adjacent regression
tests during development, followed by the existing hosted acceptance before
landing. Historical certificate walks and full developer CI are not claimed.

| Owner | Required completion | Current work |
| --- | --- | --- |
| H2.8a | Output/root/common-directory matrix, collisions, text policies, listings and filesystem faults; exact Memory/Fs observations | A1–A5 focused comparisons pass (236 cases). The full A6-34 replay is 736/769 exact twice, 33 failed, no regressions and all 188 projects exact. The separate class replay is 1100/1228 exact twice, 128 failed once, no regressions. Imported helpers and shared comment phases are next; full closure remains open |
| H2.8b | Remaining config, host/System, library replacements and optional capabilities, diagnostic precedence | Pending H2.8a |
| H2.8c | noCheck and transpile API pipelines, diagnostics, built-in transforms and distinct route/performance evidence | Pending dependency inventory; custom transforms remain API1 controls |
| H2.8d | Targeted ordinary emit, emit-only/declaration-only APIs, cancellation and callback precedence | Pending prior output/API rows; builder signatures remain BLD1 controls |
| H2.8e | Remaining CLI commands, trace/diagnostics/profile, English locale, terminal capabilities and exits | Pending host and CLI inventories; external locale catalogs remain REL1 |

## A1: direct output directories

Kind: runtime. This packet removes the source-family/absolute-path restriction
on `outDir` and preserves JavaScript's empty-string option semantics. The
existing whole-Program planner, declaration planner, checker, transformation
pipeline, callback staging and sink implementations remain the owners.
Explicit `rootDir`, config-derived directory semantics, case-insensitive bundle
admission and general API routes are subsequent packets. They are not inferred
from A1's positive direct-option observations. Existing typed guards on those
requests remain until their own packets pass.

Before production edits, `scripts/check-output-directory-readiness.py` validates
the versioned manifest, exact upstream spans, trusted-base Rust hashes, packet
and fixture identities, architecture references and complete step/test coverage.
The before comparison is `target/h2-8a-directories-before.log`; its Cargo process
must finish before edits begin. No unexpected failure is treated as an oracle
success. The base commit is the immutable source of the pre-edit implementation.

| Upstream owner (vendored `_tsc.js`) | State and Rust representation | Gap / concrete edit | Completion check |
| --- | --- | --- | --- |
| `getOwnEmitOutputFilePath`, 16567–16576 | `CompilerOptions::out_dir: Option<String>`; planner borrows options for one request and owns `PathBuf` outputs | A1-1: `plan::get_own_emit_output_file_path` must filter empty strings before relocation; never normalize callback text | `directory/` and `javascript/` empty-option observations |
| `getSourceFilePathInNewDirWorker`, 16638–16643 | Existing normalized absolute source + common-directory prefix, preserved output spelling, canonical collision key | A1-2: remove `execute::validate_emit_request`'s outDir family/absolute restriction and its now-unused counters; preserve all source-extension checks | Nine absolute/relative/dot/parent/root/empty directory witnesses; no path-specific dispatch |
| `getOutputPathsFor`, 116373–116387 | Existing `EmitOutputPaths`, ordered `EmitOutputUnit` and independent declaration-only omission | A1-3: exercise unchanged producer through ordinary `ProgramSession` and direct options | Four JavaScript/declaration/declaration-only combinations and ignored declaration roots |
| `verifyEmitFilePath`, 125018–125045 | Canonical input/output sets and `EmitPreflight` blocked outputs live for one Program request | A1-4: compare existing pre-first-write collision behavior and noEmitOnError gates | Input/multiple/declaration collisions with both blocking settings |
| `emitFiles`, 116530–116858 | Owned artifacts, ordered callbacks, optional lists/results and diagnostic streams | A1-5: preserve existing printer and emitter order; correct only a proven owner gap after a packet amendment | Eight BOM/newline/comment combinations, semantic blocking, JavaScript and zero-source controls |

The manifest pins each upstream span and the prior `execute.rs`/`plan.rs`
source. Allowed production edits are those two files only. No checker, parser,
host, config or printer mutation is authorized by A1. New provenance, transform
flags, receiver/binding scope and comment cursor transitions are unnecessary:
this packet feeds unchanged parsed roots and options into existing passes in
their existing order. A failing text witness owned elsewhere requires an
amendment with its upstream branch before changing that owner.

Architecture concerns are `E-PLAN-SCRIPT` (modified-requalify), `E-DECL-PATH`
(modified-requalify), `E-OUTPUT-SCRIPT` (premise-unchanged but rechecked),
`E-PLAN-FUTURE` and `E-OUTPUT-FUTURE` (future-owned-fail-closed for their remaining
API/build products). Their current rows in
[emitter architecture](../emitter-architecture.md) are research inputs, including
historically qualified rows whose last audit predates later bundles. No stale
row is promoted by inheritance. New H2.8a global profile admission, canaries,
corpus migrations and architecture qualification belong to the final A close.

Frozen witness producer:

```sh
taskpolicy -b nice -n 15 node scripts/observe-output-directories.mjs --write
taskpolicy -b nice -n 15 node scripts/observe-output-directories.mjs --check
python3 scripts/check-output-directory-readiness.py
CARGO_BUILD_JOBS=2 taskpolicy -b nice -n 15 cargo test -p tsc-rs-compiler --test contracts h2_8a_output_directories -- --nocapture
```

The 34 inputs repeat on fresh pinned TS 6.0.3 Programs twice, with only virtual
sources and pinned libraries accessible. Expected data preserves callback text
and materialized bytes, BOM, callback order and metadata, source associations,
ordered diagnostics and related information, emitSkipped, optional emittedFiles
and sourceMaps, command reporting/status and exit code. Rust repeats complete
production calls twice with direct options; the existing exact comparator is
reused without an option floor. Any missing/extra output or diagnostic fails.

Adjacent regressions include the 34 declaration-directory observations (the
three prior outDir refusals now require their existing complete TS tuples),
output-plan contracts, emitter tests and the historical H2.7c full focused band.
Earlier frozen oracle bytes and corpus membership are never rewritten to hide
a changed refusal. Runtime compatibility migrations must be explicit in their
own acceptance readers before H2.8a can close. Filesystem/callback fault matrices
and raw map products remain required A follow-on work.

Resource ceiling: two Cargo build jobs, one oracle process and one test thread
for these Program comparisons. The integrator is the sole writer. Checkpoints
retain inputs, oracle output, design manifest and command logs. A1 readiness
requires five owner rows, five traceable steps, five architecture dispositions,
zero undispositioned rows and zero unresolved rows within this packet.

## A1 amendment: Unicode export names

The first production replay executes 26 of the 34 complete witnesses exactly.
All eight text-policy windows expose the same prior module-transform defect:
the private `is_identifier_export_name` accepts only ASCII, so `export const 名`
prints a quoted preinitializer and an invalid unquoted element-access assignment.
The expected form is `exports.名` in both places. The original fixture is unchanged.

A1-6 extends allowed production files to `crates/emitter/src/builtins.rs`, only
`is_identifier_export_name` and `CommonJsVisitor::create_export_access_from_name`.
The first delegates textual name validity to the existing Unicode-aware
`tsc_syntax::is_identifier_text`; the second selects element versus property
access from the actual name node's StringLiteral versus Identifier kind,
matching `createExportExpression` at `_tsc.js:111805–111851`. Source-map range,
extended-escape normalization and parsed name provenance remain unchanged.
No text replacement, new binding allocation or printer change is involved.
The existing helper's CommonJS preinitializer, named re-export and System
destructuring callers remain on the same factory paths.

The amendment pins `isIdentifierText` at `_tsc.js:8676–8687` and the module owner
above; its before implementation is pinned from the same base. The node-aware
access keeps string-literal export provenance distinct from textual identifier
validity; it does not infer a source node's kind from text.

`scripts/observe-module-export-identifiers.mjs` freezes eight additional direct
Program controls twice: Japanese, Greek, combining-mark and supplementary-plane
identifier exports, ASCII `$`/`_` controls and two Unicode re-exports. The new
production test is `module_export_identifiers_match_complete_typescript_observations`.
Its log before the repair is `target/h2-8a-identifiers-before.log`. The original
eight text-policy witnesses must also pass. `E-NAMES-BASE` is modified-requalify;
the unchanged syntax predicate is a revalidated implementation input, not a
new public API. Run readiness again before editing either amended function.

The existing eight outDir reference sequences in `declaration-reference-paths.json`
also execute through the production declaration session: 48 getters, 48 forced
emits and 16 ordinary commands, preserving original options, caches, complete
maps, callbacks and results. Their former typed outDir refusal is replaced by
their unchanged complete TS observation. All 112 calls pass locally; the eight
preceding in-place sequences pass in the same run.

## Current validation and next packet

All 34 original output-directory windows and eight supplementary identifier
windows now match their complete frozen observations twice through production
Rust, including actual command status and exits (42 cases / 84 emissions;
46.69 seconds after compilation). Both oracle `--check` commands reproduce
their fixtures. The original 34-case comparison failed 33 cases with the outDir
guard; after removing it, only the eight Unicode text-policy windows failed.
The supplementary before comparison failed six Unicode cases and passed its
two ASCII controls. These logs are retained under `target/h2-8a-*` and were
copied outside the worktree to the `tsc-rs-h2-8a-before-jkzo36tz` temporary
directory before further work. Emitter regressions pass: 480 unit tests and
451 contracts (0.21s and 2.32s after compilation). H2.7c's 15 regression tests
pass in 541.06s, including the three migrated outDir observations. The focused
acceptance summary now reports those as exact, keeping its 244-case total.
All-target Clippy for emitter/compiler/xtask passes with warnings denied
(11m09s, including build-lock waiting); subsequent A2 evidence-helper edits
require a fresh check with A2. No H2.8 full-band qualification or hosted
acceptance is claimed.

A2 freezes `scripts/observe-output-roots.mjs` and
`crates/compiler/tests/fixtures/output-roots.json`: the original 27 root/config
windows, 13 inclusion/eligibility controls and ten additional boundary controls,
twice each. The original comparison failed 25 rootDir guards and two missing
TS5011 diagnostics. The 40-case expanded comparison also failed completely
before runtime work. After the ready design gate, A2 removes the rootDir guard,
shares directory calculations through `program::output_directories`, and
generates option diagnostics while loader inclusion/config provenance remains
available. The corrected comparison passes all 50 root windows and all 42 A1
controls twice (five tests, 150.23s). Program contracts pass 467 tests with five
previously ignored tests. Final adjacent regressions remain required.

The observations distinguish these required behaviors:

- Empty rootDir uses inferred common source layout. Relative rootDir preserves
  its original diagnostic argument while output planning uses its absolute form.
- A configured Program defaults its common source directory to the config
  directory in TS6.0.3. A differing inferred source directory creates TS5011
  on the output option, with its migration-information child message.
- Files outside the root create TS6059 with Program inclusion reasons. Direct
  roots have the explicit-compilation reason; config roots have files-list
  reasons and TS1410 related locations. `noEmitOnError` controls all output.
- The upstream root diagnostic uses its normalized canonical string prefix:
  both `/project/sr` and `/project/sr/` accept `/project/src/a.ts`.
  Output relocation separately uses the trailing-separator common directory;
  a source outside that directory retains its absolute output path.
- Declaration-only inputs do not influence the common directory or root
  diagnostics. Empty Programs, rootDir without outDir, and explicit config
  roots retain distinct observations.

The [A2 packet](h2-8a-roots.md) pins 15 upstream owners, six implementation
steps and five architecture dispositions. Both original fixture projections
stay frozen. Follow-up comparisons exposed and fixed reference-argument double
quoting and hidden-directory relative spelling; double-quoted/escaped import
literals now retain their real source text. The retained-position-index
refinement and A3's comparator extraction receive another final regression
replay. H2.7c's original rootDir corpus tuple now executes unchanged, with the
acceptance summary explicitly reporting its migration to 32 exact/10 deferred.
No historical qualification artifact is relabeled.

A3 freezes 24 complete filesystem command observations in
`output-filesystem.json`, produced by `scripts/observe-output-filesystem.mjs`.
The registered production comparison covers relative/dot,
absolute, drive and UNC output paths cross ordinary writes, initial failures,
directory-create failures and permanent write failures. It observes each
filesystem operation and compares successful commands with MemoryOutputSink.
This is a portable callback-worker profile; native System/host capability
completion remains H2.8b. The [A3 packet](h2-8a-filesystem.md) passes its design
gate and corrects retry-directory discovery and TSFILE normalization. Its
24 tuples and six successful Memory/Fs pairs pass twice. The combined A1–A3
run passes all 116 cases (six tests / 244 commands / 212.17s). Final regressions
and the remaining full matrix are still open.

The next original-corpus comparison replays the 23 output-directory intersections
from H2.7d/e. `scripts/observe-output-directory-corpus.mjs` freshly reobserves
both repetitions and asserts equality with the entire original pinned input
and TS tuple; `ratchets/h2-8a-output-directory-corpus.v1.json` records that
unchanged join. The dedicated `h2_8a_original_corpus` test uses the existing
complete command comparator. This is independent of the additional old-input
H2.6c migrations and cannot substitute evidence for those different requests.

The [A4 packet](h2-8a-corpus.md) resolves the one original System bundle
divergence by reusing module-name resolution for dynamic imports. Its 24 new
controls additionally expose literal-template requests missing from Program's
collector; the existing string-literal-like helper now supplies those keys.
All 140 focused cases and all 23 original cases match twice. The current hosted
D/E runner includes the 23-case supplement (314 current IDs, 11 later-owned),
without changing the historical 291-case qualification. Program's 468 contract
tests, 24 units, bundle source facts and native host smoke pass. The earlier
A2/A3 all-target Clippy passed in 10m53s before the A4 production changes; final
Clippy and adjacent regressions remain pending.

The subsequent H2.7c regression runs all 15 tests: 14 pass; the original corpus
test has one residual in its newly executing rootDir case
`nodeAllowJsPackageSelfName2.ts`. Rust loads `/types/src/foo.d.ts` as an input
and reports TS5055 when declaration output would overwrite it. TS instead maps
the package export's output path back to `/src/foo.js`, and emits both original
declarations without diagnostics. The historical 31 exact corpus rows and all
other H2.7c focused/API controls pass (347.86 seconds). The next dependency is
the Program resolver's missing `tryLoadInputFileForPath` owner at
`_tsc.js:41808–41895`; collision preflight is behaving correctly on the wrong
loaded graph and must not be weakened. The A2–A4 checkpoint is not merge-ready.

A read-only preparation probe joins the historical 804 output-directory IDs
with one additional H2.7c rootDir ID and four additional D/E root/bundle IDs:
809 distinct original inputs. All prepare against the pinned sources/configs.
This is a research set, not a current eligible denominator. Its inherited later
owners still need current-code review, notably 20 importHelpers controls that
have no outFile and two ordinary case-insensitive controls. The probe script
and log are `/tmp/h2-8a-candidates-draft.mjs` and
`target/h2-8a-candidate-preparation-probe.log`; no global artifact was minted.

The original H2 candidate inventory has 804 historical H2.8a rows, 940 H2.8b
rows and 164 H2.8c rows, with overlapping ownership and earlier admissions.
H2.8d/e have no rows in that old inventory and require their API/CLI inventories.
Those historical counts are research inputs, not current eligible denominators.
Full output/host/API/CLI inventories and all five close conditions remain open.

The [A5 package-input packet](h2-8a-package-inputs.md) repairs local package
output-to-input resolution, owned ambiguity diagnostics and missing package
format details in root diagnostics. Its 96 new complete windows pass twice.
The adjacent A1–A4 140-case comparison, original H2.7c 32-case band and existing
unsupported-resolution control all pass (nine tests, 179.33 seconds). This
closes the original self-name input gap, without weakening output collisions.
The separate legacy-output checkpoint qualifies 150 unchanged H2.6c inputs:
its complete historical runner now measures 631 exact / 8 known / 4 deferred,
with retained mismatch vectors unchanged. That run precedes A5 and remains
separate from the pending global output qualification.

The [complete original output record](h2-8a-global-output.md) freezes all 809
inputs and complete TypeScript observations, each observed twice. It identifies
769 output-only candidates, 3 H2.8b composite intersections, 8 H2.8c isolated/
verbatim intersections and 29 H2.9 noEmit/syntax/depth intersections, retaining
original owner-list provenance. Its complete native before at 43e1107e8 is 666
exact / 103 failed, each twice; 188 project cases are all exact. Admission of an
option never relabels an unequal output as compatible. The full 769-command
replay after the A6 repairs remains pending. The separate original D/E 23-case
and C 32-case comparisons retain their earlier passing evidence.

## A6: repairs exposed by the complete original matrix

The global before and export-annotation checkpoint is 2793145a6. Its
[annotation repair](h2-8a-export-annotations.md) makes all eight original export
annotation controls exact twice (five repaired, three already exact).
Checkpoint 622850a85 carries three subsequent bounded owners:

- [Require declaration aliases](h2-8a-require-aliases.md): all thirteen runtime
  stops disappear; the first after is 18/22 originals exact, with four complete
  declaration differences explicitly retained for follow-up.
- [Root diagnostic streams](h2-8a-root-diagnostic-streams.md): all six originals
  and 100 adjacent root/config/module-format commands are exact twice.
- [JavaScript import retention](h2-8a-javascript-imports.md): all 42 new commands
  and seven unchanged originals are exact twice, including the ordinary
  ECMAScript require-argument clone amendment.

Checkpoint 7ebdee933 adds [local alias and caller repairs](h2-8a-local-aliases.md).
Its 40 new complete controls and two original ExportForms commands are exact
twice; 17 adjacent checker statement/chain tests pass. Intermediate failures
and two transient regressions remain in immutable before/after evidence. The
[class dependency order repair](h2-8a-class-dependency-order.md) now makes
both ClassExtendsVisibility originals and 28 new class controls exact twice,
with all 40 prior alias controls and 17 checker unit tests passing. A current
original require-alias replay initially reached 21/22 exact: ReexportedCjsAlias
ES2015 exposed a separate missing-import regression after 7ebdee933. The
[source-literal resolution repair](h2-8a-repeated-target-aliases.md) restores
its require lookup context. All 48 new repeated-target commands and all 68
existing alias/class controls now match twice, and the original require band
is 22/22 exact twice. All 25 adjacent checker statement/chain/specifier unit
tests pass. An additional original compound namespace alias also matches twice.
The [class flag repair](h2-8a-class-transform-flags.md) and its
[class statement/name amendment](h2-8a-class-statement-layout.md) make all 114
new complete commands exact twice, with 483 emitter unit tests and
451 emitter contracts passing. All ten selected original JavaScript outputs
match. Six whole commands are exact; DefaultsErr and
ExportAssignedClassInstance2 retain their four unchanged declaration-text
failures for the following checker repairs. The original comparator stays
unconditional. This does not update the global before counts.
The [declaration export-name repair](h2-8a-export-specifier-names.md) now makes
DefaultsErr and ExportSpecifierNonlocal exact twice (four commands). All twelve
selected original declaration outputs match; eight ImportHelpersCollisions3
commands retain only their unchanged JavaScript tslib residue. Among 56 fresh
commands, 48 match twice and eight quoted-name JavaScript failures remain visible
for the following emitter owner. All 88 local/ambient/repeated-target alias
controls and all 22 original require aliases match twice; 25 adjacent checker
unit tests pass. ExportAssignedClassInstance2's two declaration residues remain
open. These outcomes do not change the recorded global before count.
The [module export-name provenance repair](h2-8a-export-name-syntax.md) preserves
name kind and existing-node identity through CommonJS/AMD/UMD/System publication.
All 178 owned/adjacent new complete commands match twice; the remaining 22 are
explicit System publication-order, TS2484 diagnostic-name and H2.9 controls.
All 56 preceding export names, eight module identifier and 24 System dynamic
import commands match twice. Emitter 483 units and 451 contracts pass; the original
ten class/alias commands have eight exact and two unchanged ClassInstance2
declaration residues. The complete global before count remains unchanged.
The [System variable publication repair](h2-8a-system-variable-publication.md)
separates initializer publication from explicit aliases and preserves binding,
default-value and list-comment ownership. Another54 complete commands qualify
the declaration-only JSDoc/pinned-comment policy at token boundaries. All 72 new complete commands match
twice; the preceding 200 have 190 exact with only two TS2484 and eight H2.9
controls remaining. System dynamic import24 and all483 emitter units/451 existing
contracts pass. New standalone printer controls have41 exact and seven recorded
independent comment failures. These are not extra global corpus admissions.
The [execution-count correction](../../../../ratchets/h2-8a-system-publication-execution-correction.v1.json)
clarifies that failed fresh commands using the shared comparison helper execute
once, although their panic payload appears twice. Historical failure repetition
fields based on those duplicate renderings do not prove two executions. Exact
case counts, successful two-run checks and original-corpus tuple runs are
unchanged. The new54-command before separately launched two native jobs.

The [alias-conflict display repair](h2-8a-alias-conflict-display.md) routes
TS2484/TS2440 conflict names through the existing typed symbol display builder.
Its24 fresh commands have22 exact twice with two independent JS getter residues;
all160 preceding ordinary export-name commands now match twice, including the
two TS2484 residues. The40 map commands retain only eight H2.9 refusals and all56
declaration-name controls remain exact. This is270/280 complete commands exact
twice;25 adjacent checker units also pass. No global corpus admission is inferred.

The [delimited list comment repair](h2-8a-array-comment-publication.md) removes
a duplicate element-end phase for compact lists. All52 new complete commands
match twice, covering array/object literals and binding patterns, import
attributes, comment removal and source maps. All483 emitter units/451 existing
contracts/18 source-comment topology controls pass. Standalone printer controls
improve to44/48 exact twice, retaining four parent NoNestedComments residues.
These are focused observations, not extra global corpus admissions.

The [import publication reference repair](h2-8a-import-publication-reference.md)
resolves declaration names through the existing checker projections and preserves
their map/comment ownership. Its printer dependency uses the original identifier
spelling when creating an export-name string. All87 fresh complete commands and
all24 alias-conflict commands now match twice, including the two prior JS getter
residues. The full focused367-command comparison has359 exact twice and only
eight unchanged H2.9 refusals. All483 emitter units,451 contracts,72 new printer
observations and26 checker units pass; the checker count includes an18-case
resolver projection test. The subsequent whole original matrix checkpoint is recorded below.

The [complete A6-14 original replay](../../../../ratchets/h2-8a-global-after-a6-14.v1.json)
measures706/769 exact twice and63 failed twice in1145.77s,exit101.42 previous
failures become exact, while two former positives regress in a default-import
declaration name. All188 project positives remain exact. The archive retains
complete failures and execution-time source receipts; this is an open checkpoint.
The [computed synthetic-default alias repair](h2-8a-synthetic-default-alias.md)
uses the existing effective option in declaration serialization. Both original
regressions and all84 fresh controls are exact twice; all172 preceding complete
alias/class/name controls and25 checker units pass. The frozen706/63 result stays
unchanged; a later whole replay is required to measure the corrected tree.

The [direct JSDoc parenthesis guard](h2-8a-jsdoc-parentheses-guard.md) prevents
an untagged parenthesis from using its variable declaration's inherited type
annotation as an assertion. All40 fresh complete commands and three original
commands match twice, fixing both missing TS2322 diagnostics and inferred-return
literal widening; six adjacent checker tests pass. This is a private checker
guard change and does not alter tag inheritance or parser attachments. The
original whole-matrix counts remain tied to the A6-14 checkpoint.

The [CommonJS class instance clone identity](h2-8a-commonjs-class-instance.md)
repair retains the declared instance identity when CommonJS assignment creates
an anonymous clone and uses the same class-side predicate in declaration
serialization. All ten fresh declaration failures and both original ClassInstance2
failures now match. In the complete focused projection148/158 commands are
exact twice, including all116 prior adjacent commands and all ten originals;
the ten static-field map failures retain their identical first vectors and keep
the combined compiler invocation at exit101. All25 adjacent checker units pass.
Those ten map commands remain in the denominator with later fields unqualified.
This closes the clone-identity owner only and establishes no new global count.

The [static initializer map ranges](h2-8a-static-initializer-map-ranges.md)
repair retains class facts for public-field metadata, preserves literal key
identity, and applies the property range at statement and inline-expression
placement. The final focused projection is293/367 complete commands exact twice,
including all32 prior class-instance commands and their ten formerly failing
maps. The74 remaining fresh map failures stay in the denominator; their decoded
differences are subsets of the original before. Two intermediate candidates are
retained, including a12-case expression-range regression repaired before closure.
All483 emitter units and451 contracts pass. The combined compiler job exits101
for the retained maps. This closes only the property-range owner and does not
establish a new whole-matrix count.

The [hoisted declaration export ranges](h2-8a-hoisted-declaration-export-ranges.md)
repair retains direct-versus-explicit publication origin and declaration-name
identity through CommonJS/AMD materialization, with System's shared consumers
adapted to the same ordered representation. The final projection is528/581
complete commands exact twice:164/168 fresh and63/104 prior static-map commands.
Four earlier ES5 class-name maps,41 other static maps and eight H2.9 recovery
refusals remain in the denominator; all retained map differences stay within
their prior component sets. Emitter483 units/451 contracts and26 checker units
pass. The initial compiler failure is frozen separately with zero commands; the
final combined compiler job exits101 for the53 retained failures. This closes
only the hoisted publication owner and does not establish a new global count.
The planned530 exact results became528 because two static-super endpoint
components had been missed in the earlier adjacent inventory. Their publication
ranges are repaired; the preexisting endpoint differences remain explicitly
failed and are recorded without changing A6-18's frozen evidence.

The [synthetic namespace export modifiers](h2-8a-synthetic-namespace-export-modifiers.md)
repair uses the existing synthetic scope kind when serializing namespace members.
All84 fresh commands and156 adjacent commands are exact twice, and three of four
original corpus commands now complete exactly. Overall243/244 are exact twice;
Implements_class still lacks its five prior implements clauses, with Ns.C5's
export repaired. Its complete failure still executes twice and keeps the combined
compiler job at exit101. All30 related checker units pass. This closes the export
modifier owner; the original before source, complete tuples and remaining clauses
stay recorded without establishing a new global output-matrix count.

The [JSDoc implements serialization](h2-8a-jsdoc-implements-serialization.md)
repair shares the effective clause producer, entity-name tracking, syntactic type
reuse and semantic fallback. All58 fresh declaration failures and eight original
implements commands are now exact twice. Overall342/344 complete commands are
exact twice, including240 adjacent controls; the two ES5 typed class-expression
source-map results retain their existing first vectors and keep the complete
comparison at exit101. All31 related checker units pass, including scope restoration
on successful reuse and a typed factory error. This closes the implements owner;
other A work and B–E remain open without establishing a new global matrix count.

The [transformed class/function assigned-name repair](h2-8a-transformed-class-assigned-names.md)
reads the current parent instead of recovering the original parsed parent after
type erasure. All 26 fresh owned failures and the two A6-21 class-expression map
residues are exact twice. Overall 426/434 complete commands match twice, including
326 adjacent controls; eight independent map failures retain their before first
vectors and keep the comparison at exit 101. All 483 emitter library and 451
contracts tests pass. Other A work and B–E remain open; this focused result does
not establish a new global matrix count.

The [CommonJS esModule marker repair](h2-8a-commonjs-esmodule-marker.md)
projects the existing binder CommonJS indicator and preserves the distinction
between forced module status and real ESM syntax. All 28 fresh and seven original
marker failures are exact twice. Overall 395/397 complete commands match twice,
including all 299 adjacent controls; two independent AMD/UMD mjs transformer
selection failures retain their first vectors and keep exit 101. All 485 emitter
library tests, 451 contracts and 17 checker emit units pass. Other A work and B–E
remain open; this focused result does not establish a new global matrix count.

The [module transformer selection repair](h2-8a-module-transformer-selection.md)
selects the direct module factory for None/AMD/UMD and preserves implied-format
dispatch for CommonJS/ESM/Node modes. All368 complete commands match twice:
184 fresh,91 CommonJS marker, 87 import publication references and six originals.
It repairs 12 fresh and two original failures, plus the two A23 mjs residues.
Eight compiler activity controls, 488 emitter library tests and 451 contracts pass.
Other A owners and B–E remain open; this does not establish a new global count.

The [setter declaration parameter repair](h2-8a-defineproperty-setter-names.md)
selects descriptor method names separately from actual accessor type/range
declarations and omits private write-type queries. Its
[binding-name prerequisite](h2-8a-binding-name-map-ranges.md) recursively clones
names with original identity and detached source positions, and applies the
computed-name type guard before tracking. The focused replay matches 253 of
261 complete commands twice; eight separately owned readonly differences retain
their exact before boundaries. All 65 selected checker tests and 22
declaration-blocking controls pass. Other A owners and B–E remain open; these
focused results do not establish a new global total.

The [semantic readonly descriptor repair](h2-8a-defineproperty-readonly.md)
queries descriptor types and propagates typed checker errors. Its
[diagnostic-cache prerequisite](h2-8a-nonexistent-property-cache.md) suppresses
re-entry inside a candidate and restores newly inserted keys at its boundary.
All 502 focused complete commands match TypeScript twice, repairing 76 readonly
differences and the original function-expando diagnostic. All 1,720 checker
units and 22 declaration-blocking controls pass. The initial crash and failed
test setup remain archived. Other A owners and B–E remain open; this is not a
new global total.

The [MetaProperty names and positions repair](h2-8a-meta-property-token-maps.md)
preserves new.target/import.meta names through module transformation and emits
keyword/name map positions with the upstream ownership. Its
[System range prerequisite](h2-8a-meta-property-system-range.md) keeps the
import.meta replacement synthetic. All82 owned failures
are repaired; 224 of228 focused complete commands match twice and the remaining
4 class-field-alias map cases retain their prior vectors. All490 emitter units
and451 contracts pass. These results leave other A owners and B–E open.

These focused comparisons belong to their recorded checkpoints; they do not
replace the pending whole-band replay, current architecture/profile review,
remaining output controls or any of H2.8b–e. H2.8 remains open.

This A6 progress amendment changes only this packet's progress text. A1's
implementation steps, pinned upstream behavior, witnesses and completion scope
remain unchanged; its document authority hash is refreshed explicitly for this
recorded progress update.


## A6-30: JSDoc declaration container

[The scoped binder packet](h2-8a-jsdoc-block-scope-container.md) corrects the SourceFile branch to
test the current declaration container's identity before source module flags.
Delayed class JSDoc aliases now reach the source locals table. All 60 fresh
complete commands and four original commands match TS twice, including duplicate
diagnostics, noEmitOnError suppression, declaration bytes and maps. All 71
binder units and 108 existing checker JSDoc tests pass with unchanged sources.
The selected owner is complete on the train; other A owners and B–E are open.

The preceding full A6-29 replay has its own immutable result:
[733/769 exact twice](../../../../ratchets/h2-8a-global-after-a6-29.v1.json), 36 failures, no regressions
against either the previous checkpoint or the original before, and all 188
projects retained. Of the 36 failures, 33 have complete unequal tuples and three
retain their previous runtime boundaries. Later focused repairs are not subtracted
from this measured global count. The older A6-14 706/63 record remains historical.


## A6-31: export-star declaration producer

[The scoped declaration packet](h2-8a-export-star-declaration-producer.md) replaces parse-statement
cloning with ordered module resolution and fresh factory declarations. All 56
fresh commands and four originals match TS twice, repairing 24 fresh cases and
exportNamespace_js while retaining 35 preceding positives. All 28 checker
statement, chain and specifier units pass with unchanged sources and IDs.
The full global count remains the measured A6-29 733/769; other A owners and
H2.8b–e remain open.


## A6-32: optional class declaration name

[The optional-name packet](h2-8a-class-optional-name.md) removes the printer's extra
missing-name refusal. 58/64 fresh complete commands match TS twice,
repairing 18 cases and preserving 40 prior positives; all 38
existing printer contracts pass. The original comparison retains3/4 exact
and records its next recovery boundary. The 6 fresh comment cases retain
the class-keyword owner. H2.8a–e and full corpus closure remain open; no new
global count is inferred from this scoped repair.


## A6-33: class keyword comments and header suppression

[The class header packet](h2-8a-class-header-token.md) qualifies the keyword source-comment
phase and existing NoNestedComments context at the name end. 144/152 ordinary
commands match TS twice (80/88 new, 64/64 A32): all 64 repairs and 80 prior
positives. All 32 direct metadata controls match twice, and all 451 unchanged
emitter contracts pass. Original comparison remains 3/4 exact with its unequal
tuple unchanged. Eight modifier/token-leading composition controls and one
original transformer/property-printer case remain failed and explicitly owned.
H2.8a–e and full corpus closure remain open; the global checkpoint stays733/769.


## A6-34: ConstKeyword modifier erasure

[The Const modifier packet](h2-8a-const-modifier-erasure.md) qualifies the existing TypeScript
filter's missing ConstKeyword arm. All 80 new and 64 adjacent commands match
twice, independently repeated; 28 repaired and 116 preserved. Unchanged emitter
tests pass: 494 units and 451 contracts. The original selector remains 3/4 exact;
its complete unequal grammar command loses two extra const prefixes and retains
only the independent PropertyAssignment export prefix. Full corpus closure and
H2.8a–e remain open. The subsequent same-head checkpoint follows.


## A6-34 convergence checkpoint and review

Both complete replays used `b6621f25aa47851b2ee264ebc200639f96a8aa03` with
unchanged production inputs. The [original matrix](../../../../ratchets/h2-8a-global-after-a6-34.v1.json) has
736/769 exact twice, 33 failures, three improvements, no regressions and all
188 projects exact. Its 31 complete unequal commands differ only in writes;
the other two fail at runtime boundaries. All 30 unequal tuples shared with
A29 are unchanged. `plainJSGrammarErrors` now completes, with only its extra
seven-byte PropertyAssignment `export ` prefix remaining.

The separate [class replay](../../../../ratchets/h2-8a-class-convergence-after-a6-34.v1.json) has 1100/1228 exact twice and
128 failures, with no improvements, regressions or changed first failure
vectors. Its failed cases execute once and stop at their first comparison:
120 comparison failures and eight typed boundaries. This run does not qualify
the complete failed tuples. The two populations remain separate; neither
denominator defines all emitter behavior or H2.8 completion.

The [reviewed cause map](../../../../ratchets/h2-8a-convergence-causes.v1.json) covers all 33 original
failures and all 128 class failures. Thirteen original cause groups include
one case with two causes; five class groups retain unresolved subcause
assignments. Group totals are not a proven count of distinct root causes.
The independent review supports continued owner-based implementation and
identifies token-leading/modifier-trailing comment ownership as a shared
repair. The next order is imported helpers (16 original cases), the combined
comment phases, grouped local producer repairs, retained ES2022 class
producers, and unresolved causal probes. Each runtime packet still needs its
own complete before observations and executable readiness check.

Current hosted acceptance ends at H2.7de and does not execute the H2.8a runner.
The original matrix and affected fixture comparisons remain the current
H2.8a regression evidence; hosted acceptance remains required before landing.
Historical full developer CI and certificate walks stay omitted. No profile,
expected output or accepted membership is weakened by this checkpoint.
