# H2.8a A6-20: synthetic namespace export modifiers

Base `e6bebf7b76501984a7ebba9bc57e81e23e7b2429`. The fresh84 complete commands repeat on TS6.0.3 and in
--check. Two independent native before jobs are72 exact/12 failed, in73.60s
and77.30s (exit101). Their complete first vectors and membership agree. Each
success executes twice per job; each failed fresh command executes once per
job, despite duplicate panic rendering. Both jobs and the immutable outside
archive are recorded in ratchets/h2-8a-synthetic-namespace-export-modifiers-before.v1.json.

The12 failures are all script-route declaration callback bytes; maps and earlier
fields compare exactly. Six source shapes at ES5/ES2015 show only missing export
modifiers: function mixed/methods, object mixed, nested function, typedef and
namespace class fallback. Every changed line is exactly its TypeScript line
with the export keyword absent. This is a component classification, not a claim
that later whole-command fields passed. All CommonJS/ESM routes and12 parsed
TypeScript namespace/nested/ambient controls are adjacent positives. Diagnostic
rows5107=42,7012=3,7005=3,7006=6 are retained unconditionally;42 commands include
TS5107 and42 are active semantic controls.

A6-20-1 changes only crates/checker/src/node_builder/statements.rs:add_result's
export-context gate. Include the existing synthetic_scope_kind equal to
ModuleDeclaration alongside the existing parsed exporting-scope/module predicate.
Preserve additional EXPORT and can_have_export_modifier gates, temporary JSDoc
alias source-file projection, old emitted modifiers, ambient/default decisions,
result order and approximate-length accounting. No new context object or query.
The typed synthetic Block kind is not treated as a namespace; its existing parsed
fallback behavior is unchanged. Signature serialization restores its scope before
add_result consumes the completed declaration; this edit changes no install or
restore boundary. If a new context dependency appears, classify it before editing.

The source gap is at one pure consumer. serialize_as_namespace_declaration
installs with_synthetic_module_scope, keeping the parsed parent immutable while
retaining the actual fake ModuleDeclaration kind and locals in NodeBuilderContext.
It visits the property table, restores scope, adding_declare and results, then
propagates visit_result errors. TS serializeAsNamespaceDeclaration instead uses
a fake parsed factory namespace in a copied context; addResult checks that fake
namespace's kind. Native add_result currently checks only the saved parsed parent.
The nearby serializePropertyAsNamespaceElement let/const decision already reads
the same synthetic kind. Reuse that fact without inventing a parsed checker node,
new binder mutation, additional tracker or global scope state.

Keep the later namespace all-export stripping unchanged. The native helper named
effective_modifier_flags reads the actual emitted modifier list; it is not a
checker/JSDoc effective-flags query. Mixed normal and renamed keyword declarations
require exports on normal members while renamed locals and explicit aliases retain
their current flags. Replacing that predicate or blanket-exporting every member
would change the pinned namespace semantics. Existing name allocation, alias
remapping, deferred private draining, child context cloning and tracker order
remain their current owners. This gate introduces no fallible query or host/sink
operation; existing factory and serializer errors still propagate normally.

Architecture: E-CHECKER-FACTS-BASE is modified-requalify at the private declaration
context consumer. E-PROTOCOL, E-RESOLVER-BASE, E-ARENA, E-METADATA-BASE and
E-PRINTER-BASE are premise-unchanged. Parsed trees remain immutable, metadata and
source identities stay separate, the checker/resolver borrow cannot escape, and
the printer receives the same declaration tree protocol. No public API, factory,
printer, Program admission or host edit is proposed. Historical inactive/dormant
rows do not confer compatibility. The current schedule header's lightweight
workflow applies; full developer CI/certificate walks are not claimed.

Four original corpus commands have also been replayed before at this base:
FunctionKeywordPropExhaustive ES5/ES2015, jsFileAlternativeUseOfOverloadTag and
jsdocImplements_class. All four fail in both repetitions in each of two jobs
(20.54s/26.33s, exit101). The first job retained short first-boundary logs; the
second saved all eight complete actual/expected tuples. Those tuples agree
across repetitions. All non-write tuple fields match. The first three cases
have only missing export modifiers; Implements_class has the missing Ns.C5 export
plus five independent missing implements clauses. Their current full before is
ratchets/h2-8a-synthetic-namespace-export-modifiers-original-before.v1.json, not an inferred A6-14 result.

Required after is244 complete commands:84 fresh,84 synthetic-default-alias,
32 CommonJS class-instance,40 JSDoc-parenthesis-guard and four originals. Target243
exact twice; retain Implements_class's complete command failure twice with only
its already observed implements differences. All four original comparisons stay
unconditional and save complete failures using an absolute capture directory.
The original helper repeats failures inside its two-iteration loop; do not apply
the fresh helper's once-per-job failure count to it. Existing assertions and
frozen expectations remain unchanged. Run30 checker units:25 statement/chain/
specifier and five signature/scope/recovery controls. Final counts are targets
until actually measured; compare any remaining original declaration differences
to this base's complete captured tuples, not only their first short boundary.

The change has no new emitter runtime code; A6-19's immediately preceding483
emitter units/451 contracts are immutable prior evidence, not claimed as a new
run. Current complete outputs and checker controls requalify this consumer.
All other A work, B–E, final global769 replay and hosted acceptance remain open.
Do not subtract focused improvements from the global A6-14 checkpoint.

Pinned whole TS6.0.3 functions (AST-verified declaration bounds):
- hasSyntacticModifier: 16931–16933, SHA256 `7dd8709f25333f4dd5745735d9a1e5b97b6b1a5b056f1f34bd9c06517a93112a`.
- getSelectedSyntacticModifierFlags: 16961–16963, SHA256 `03a24a8e333e66a86da83dd24ae5467c721e1dd55d084f01e76db5fbffe44804`.
- getModifierFlagsWorker: 16964–16978, SHA256 `3e5d410640829256c9969dbfc1763fbf3284c0f2830a2f8b500244b2c5e844bb`.
- getEffectiveModifierFlags: 16979–16985, SHA256 `94efcdf1350af5231aa2a3d3d1d8ac176bdfc84611ae3bb4925e385ff65b5473`.
- getSyntacticModifierFlags: 16986–16992, SHA256 `3dc5370fee5262fb6b722011459fd1526ca411f2434c0b24cc03a1d15e55d50c`.
- selectSyntacticModifierFlags: 17007–17009, SHA256 `af764beec355a95bb36a3cf51e723d914a005bb8117ca6a0060170755cae9f0a`.
- selectEffectiveModifierFlags: 17010–17012, SHA256 `6cf7eeb1c2e9ddfe1d26ab1afe12b12fefef6418e4edcc3a5fd5812884cf92ea`.
- getSyntacticModifierFlagsNoCache: 17019–17025, SHA256 `1b7cb8845b02b8a88b15a70b861ff5cfa0e1959d0f056d0316c204da7dcf7644`.
- canHaveExportModifier: 19283–19285, SHA256 `e9f9bfa860bfebc49cefd6207fbb6dde5b4e9b3d7436b0693bb4bd25e373e499`.
- enterNewScope: 52692–52801, SHA256 `2e14fce8894e1878f50325ffceebc3976956fd557cb2637dbd951c73e33322bb`.
- cloneNodeBuilderContext: 53444–53461, SHA256 `18ef4bc35335d2ff447f64ee1bffe8b29c89101b0b41a25825482c6ed2c1c24d`.
- removeExportModifier: 53934–53937, SHA256 `061b1d8d4f6236c6aeed8c7f695a1bf9b9b4f325404dffb63f17f297d2f91bd3`.
- visitSymbolTable: 53938–53975, SHA256 `6c24132a883411f0dd4cce6dfc03ad19ff6f3cdfeaede3e283f075c90e325235`.
- serializeSymbol: 53976–53991, SHA256 `89a62d997aa6147fdb69cb375b6f5a5e4a24830189c28093f5d59dfefacc0647`.
- serializeSymbolWorker: 53992–54179, SHA256 `dc9bf6e639d95e72cefd3e99de392150843321b4b1f785340d52751bbe4bbc38`.
- isExportingScope: 54187–54189, SHA256 `c5e869e20b2780c6401b7722dde00d9f5cc14acde0ac87489d655ea0ff07e416`.
- addResult: 54190–54210, SHA256 `1d5bbaa5bc6714a9dcb56eb0347501e0ebd1c79db08073b2b679e32987b7bee1`.
- serializeAsFunctionNamespaceMerge: 54458–54476, SHA256 `31d4121617baea5146ed7516eaccff1fd2139d7f938bf5008f5b3516dfccd97e`.
- serializeAsNamespaceDeclaration: 54494–54561, SHA256 `1911f6ac2b33198d3a9382ec2faf24b6cbf59aba22c50b2b5a2a41f607ec2cf9`.
- isNamespaceMember: 54562–54564, SHA256 `c67f99f0f91940f9483d5817d5ff9657c73fed0db9ff8ce36994e4ff4e2b5dec`.
- serializeMaybeAliasAssignment: 54966–55082, SHA256 `76bea3c93c2aab13e158de586792f0b9e24d6840feaaf5ff7151b7d97ef249c0`.

Final measured result: all84 fresh commands now match their complete TS observations
twice, including all12 prior script namespace failures and all72 prior positives.
All84 synthetic-default-alias,32 CommonJS class-instance and40 JSDoc-parenthesis
controls also match twice. The four fresh test functions pass in223.75s.

The original FunctionKeywordPropExhaustive cases at both targets and the alternative
overload constructor case are exact twice. Implements_class remains failed in both
repetitions, with the missing Ns.C5 export repaired. Its two captured complete tuples
agree. All non-write fields and all non-byte callback metadata equal TypeScript;
its sole remaining declaration differs only by five previously missing implements
clauses. The remaining declaration line differences are subsets of this base's
captured before. The whole command stays failed; no expected bytes or comparison
boundary were normalized to establish the component result. The original function
retains exit101 in18.56s; the combined compiler invocation also exits101.
Overall243/244 complete commands are exact twice, with one original failed twice.

All30 checker statement/chain/specifier/signature/scope/recovery units pass in
0.02s (exit0). The only production change is the private export-context
predicate in statements.rs. The final record is
ratchets/h2-8a-synthetic-namespace-export-modifiers-after.v1.json. Frozen fresh
before and both original before jobs retain their measured execution counts and
source identities; only the second original before job captured full tuples.
A6-19's emitter tests remain prior evidence, and no fresh emitter test run is
claimed here. This closes the synthetic namespace export-modifier owner only.
The remaining implements clauses, other A work, B–E, whole-matrix final replay
and hosted acceptance remain open. No new global769 count or historical full
CI/certificate walk result is claimed.
