# h2-7b-w5 — the fifth closure wave after the H2.7b activation flip (rev 0 — DRAFT, integrator-authored 2026-09-06 16:45 JST after the w4 merge; sol packet review r1 pending)

Parent: `docs/design/greenfield/slices/h2-7b-m-2.md` (§6: the closure-wave authority). Previous waves: `h2-7b-w1.md` (23), `h2-7b-w2.md` (56; the operating rules and the six criteria), `h2-7b-w3.md` (51; rules 6-8), `h2-7b-w4.md` (37; rules 9-11, the 55-row disposition register, §8 the 18 remaining rows). Trusted base: `main @3f90839e` (PR #508 merged 2d4c8730 + the w4 close values). Train: `h2/7b-w5`.

## 1. Scope

The canonical manifest at the base has 18 rows. Every owner below has a two-path proof: the W5-X analysis by gpt-6-astra (`target/session-notes/7b/w5/astra/w5-owners-astra.md`: 9 of 12 OPEN rows VERIFIED) cross-reviewed by gpt-5.6-sol (`REVIEW-W5X-sol-of-astra.md`: all 9 HOLD, `arrayFlatNoCrashInferenceDeclarations` settled, D2/D5 open; the ticket-shaped file sets W5-P1/P2/P3 and the four guarded checker-core tickets), plus W4-A's evidence for S1 and J10 (`lanes/w4/deliverables-a/STATUS-W4-A.md`).

| lane | classes (rows) | file set |
|---|---|---|
| W5-A (checker core, every class under the conformance guard) | A1 the JSDoc overload cache-property existence (`jsFileMethodOverloads{4,5}`, 2); A2 the genuine-`any` index singleton identity (`declarationEmitClassInherritsAny`, 1 — the complete construction/copy inventory in astra §6); A3 the eager tuple-target length type (`variadicTuples1`, 1); A4 the prefix-unary literal operand allocation (`arrayFlatNoCrashInferenceDeclarations`, 1); A5 J10 the property-alias liveness WITHOUT the TS2304 publication side effect (`defaultDeclarationEmitShadowedNamedCorrectly`, 1 — port only with a two-path proof of a side-effect-free entry point; W4-A's `get_resolved_symbol(left)` attempt added 21 false positives) — 6 rows | `crates/checker/src/{annotate,jsdoc,functions,state,instantiate,inference,literals,mapped,structural,modules}.rs`, `crates/types/src/{ty,tables}.rs`, `h2_7b_w5a_controls.rs` |
| W5-B (NodeBuilder / serializer + emitter) | B1 S1 the JS declaration resolver serializes the source's OWN export table (5 rows: the four empty `export {}` markers + `expandoOnAlias`; W4-A proved the checker half closes four rows and that `jsDeclarationEmitExportAssignedArray` then fails in `serialize_type_for_declaration_seam` — "createTypeOfDeclaration factory operation failed: unknown transform node" — a secondary producer in the syntactic type builder / serializer); B2 W5-P2 the import StringLiteral child reused before its LiteralType parent (`jsDeclarationsUniqueSymbolUsage` write 1, 1); B3 W5-P1 the private-environment name prefix from the current transformed node's provenance (`privateFieldsInClassExpressionDeclaration`, 1); B4 W5-P3 one scoped comment-phase repair for synthesized accessor lists (the three comment rows, indivisible, 3) — 10 rows | `crates/checker/src/declaration_emit.rs`, `crates/checker/src/syntactic_type_node_builder.rs`, `crates/checker/src/node_builder/serialize.rs`, `crates/emitter/src/builtins/class_fields/downlevel.rs`, `crates/emitter/src/printer.rs`, `crates/emitter/tests/unit/{builtins,es2015}/tests.rs`, `crates/checker/tests/unit/syntactic_type_node_builder/tests.rs`, `crates/emitter/tests/source_comment_topology_contract.rs`, `h2_7b_w5b_controls.rs` |

Not targeted: D2 `checkingObjectWithThisInNamePositionNoCrash` and D5 `recursiveConditionalTypes` (no exact file set; a frame/schedule trace is needed first). Partition: 6 (W5-A) + 10 (W5-B) + 2 OPEN = 18 — asserted by the register in §10.

## 2. Staffing cell and the w5 operating rules (the w4 rules plus two additions)

| role | who | ownership | deliverable |
|---|---|---|---|
| integrator + core (single writer) | operator | the w2 single-writer set + the harness | packet, E1 stubs, preflight, train battery, re-mint, walk, gate, PR |
| implementer W5-A | Codex gpt-5.6-sol xhigh | the checker-core set above | rows, `h2_7b_w5a_controls.rs`, STATUS |
| implementer W5-B | Codex gpt-5.6-sol xhigh | the NodeBuilder/serializer + emitter set above | rows, `h2_7b_w5b_controls.rs`, STATUS |
| analysis / review | gpt-6-astra xhigh (docs only) | no production code | done (W5-X); on request: a stuck-row consultation |

Rules: the w4 rules 1-11 unchanged. Additions from the w4 findings: (12) the integrator dry-runs the walk (`WALK_DRY=1 bash scripts/chain-walk.sh`) before every real launch, and every new file under `crates/**` is registered in `crates/oracle/h2-5g-profile.mjs` (runtime or shadow) in the same commit that creates it; unit-test bodies never go inline in `src/`; (13) the terminal app that spawns cargo (iTerm2) stays in macOS Developer Tools — macOS `syspolicyd` otherwise assesses every freshly built binary at exec (the w3/w4 stalls); lane batteries run in the foreground band under `nice 20` when the user is present.

## 3. Mechanisms ported — TO-FILL from STATUS-W5-A / STATUS-W5-B.

## 4. The manifest re-mint and its review — as w4 (`audit.js`, the m-2 §6 componentwise rule; adjacent bands may only shrink).

## 5. Verification — as w4: the lanes' two-stage batteries (whole crates, rule 9) + the launch-relative audit; `preflight.py --verify` per lane from the launch SHA; the train battery; E2 re-mint; E3 pins; the dry walk preflight; ONE walk; `gate.sh <main sha>`; PR at E1.

## 6. Fences — the w2 fences unchanged.

## 7. Success criteria (fixed before launch)

| # | criterion | measured how |
|---|---|---|
| 1 | exactly one walk launch | `target/chain-walk/runs/` between E1 and the merge (a preflight refusal counts) |
| 2 | zero train-battery-first / gate-first regressions | every red in the train battery or the gate the lanes' full batteries did not show |
| 3 | zero STOPs caused by an allowed-path gap | STATUS out-of-scope entries whose owner file the tickets had named for that lane |
| 4 | zero perf-contention gate reruns | `gate.exit` |
| 5 | ≥ 12 rows closed of the 18-row pool (a fixed conservative floor, not a potential-minus-margin calculation) in wall ≤ 8 h 33 m (lane launch → merge) with the fixed tail kept | the re-mint audit; the register timeline |
| 6 | zero lane file conflicts and zero multi-writer pin surfaces | `preflight.py --verify` per lane; clean merges |

## 8. Remaining rows after this wave — TO-FILL at close.

## 9. E-steps
- E1 (integrator, launch-blocking): cut `h2/7b-w5` from main; create and register `h2_7b_w5a_controls.rs` / `h2_7b_w5b_controls.rs` in `contracts.rs` and as H2.5g shadow inputs; the launch scratch (18 live expected); commit = the launch SHA; the tickets' placeholders filled; preflight; PR opened; lanes launched with the w4 kit (`launch.sh`, the canonical battery scripts by absolute path with the W5 control module).
- handoffs → `preflight.py --verify` → `checkpoint-lane.sh` → merges → the train battery → E2 → E3 → `WALK_DRY=1` → ONE walk → E4 → gate → PR body → merge → the register close values on main.

## 10. The 18-row disposition register (one row, one disposition)

| disposition | rows |
|---|---|
| W5-A (6) | `compiler/jsFileMethodOverloads4.ts#default`, `compiler/jsFileMethodOverloads5.ts#default`, `compiler/declarationEmitClassInherritsAny.ts#default`, `conformance/types/tuple/variadicTuples1.ts#default`, `compiler/arrayFlatNoCrashInferenceDeclarations.ts#default`, `compiler/defaultDeclarationEmitShadowedNamedCorrectly.ts#default` |
| W5-B (10) | `compiler/jsDeclarationEmitExportAssignedArray.ts#default`, `conformance/jsdoc/declarations/jsDeclarationsImportNamespacedType.ts#default`, `conformance/jsdoc/declarations/jsDeclarationsReferenceToClassInstanceCrossFile.ts#default`, `conformance/jsdoc/moduleExportsElementAccessAssignment.ts#default`, `conformance/salsa/expandoOnAlias.ts#default`, `conformance/jsdoc/declarations/jsDeclarationsUniqueSymbolUsage.ts#default`, `compiler/privateFieldsInClassExpressionDeclaration.ts#default`, `compiler/accessorInAmbientContextES5.ts#target%3Des5`, `compiler/commentsClassMembers.ts#target%3Des5`, `compiler/declarationEmitClassMemberNameConflict.ts#target%3Des5` |
| OPEN (2) | `compiler/checkingObjectWithThisInNamePositionNoCrash.ts#default`, `compiler/recursiveConditionalTypes.ts#default` |
