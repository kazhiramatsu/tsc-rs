# H2.7e m-5: same-Program command and diagnostic observations

The scoped `DeclarationSession` now exposes ordinary whole-Program emit with
reported diagnostics and a Program diagnostic snapshot. These operations keep
the initialized checker, semantic links and declaration diagnostic cache used
by earlier getter/forced calls. They use ordinary emit's option and prepared
mode boundaries; noEmit and ordinary targeted emit remain later owners.

`EmitCommandOutcome` retains the emit result, reported diagnostic list, status
writes and exit code for embedding callers. The status/exit producer is
extracted from the existing CLI, which continues to call that same producer.
No test constructs replacement TSFILE strings or success/error status values.
`ProgramDiagnostics` exposes the separate config, options, syntactic, global
and semantic streams already used by the ordinary command wrapper.

The checker driver lends the Program diagnostic inputs to `CheckerSession`.
On-demand sessions complete whole-Program semantic diagnostics on their first
request and retain the result. Already checked sessions retain their existing
completed whole-Program result. Assembly reuses the existing Program-aware
semantic producer, including binding, JS/directive filtering, Program rows,
source skip policy, sorting and partial-check evidence. Declaration resolver
preparation does not populate the Program semantic cache or reattach its
previously published file-less diagnostics to a later semantic request.

Ordinary command reporting collects its earlier streams before requesting
semantics, preserving the existing reporting suppression order. A separate
ordinary resolver preparation supplies the checker work required by emit even
when reporting suppressed the semantic getter. The noEmitOnError declaration
step shares the private Program cache worker only after all four earlier
streams are empty. Its optional emitter gate input leaves existing callers'
uncached route intact. Internal requests do not cross the explicit public
getter activity boundary again. The parent-owned H2.7c marker at actual
entry to this declaration step is retained during integration.

The unchanged API artifact still contains 54 H2.7e sequences plus two ordinary
target references. The complete comparator checks each call in order on one
checker, and then compares options, syntactic, global and semantic diagnostics
from that same session. Its exact call set is 77 getters, 92 forced emits and
20 ordinary commands per repetition. The remaining ordinary noEmit call is
kept as the original H2.9 reference and checked as a Rust typed refusal with
zero new activity and no sink writes; it is not counted as equivalent output.
The noEmit reference uses the same `InvalidProgramMode(Emit, NoEmit)` boundary
as `ProgramSession::emit`, before activity or output. Both ordinary target
references remain H2.8d. Every sequence is repeated from
a fresh Program twice, without substituting a fresh checker for any intervening
ordinary call or final diagnostic request.

Complete observations include Program source order and ordered libraries,
callback/map bytes, BOM, source association and metadata before/after feedback,
map and text callback order, partial writes and materialized indices, System
retry attempts, sink onError/skip/throw distinctions, diagnostics, emit/result
presence, sourceMaps/emittedFiles presence, actual command status and exit.
For ordinary throws, the observer's report/status callback buffers are
allocated before entering the command and remain empty arrays after unwinding;
forced calls retain absent buffers. The fixture is unchanged. Resolver request
counts remain a separate comparison; source-less Programs
retain the established no-checker adapter. The raw TypeScript source request
trace is retained in the fixture and is not fabricated from Rust identities.

The TypeScript authority remains pinned 6.0.3: `emitFilesAndReportErrors`
(`_tsc.js:129412-129467`), `handleNoEmitOptions` (`125636-125668`),
`emitWorker` (`123595-123625`), `getEmitResolver` (`47561-47564`) and
`getDiagnosticsWorker` (`87111-87135`). The pinned native
`1f70213d4922b434345f639b441681e470c7cfc1` Program getter/cache interfaces
were also reviewed at `tsc/internal/compiler/program.go:777-782,1426-1498`.
Its checker-pool/concurrent collection is an architectural reference; frozen
TS6 request order and result bytes remain the required observation.

This semantic packet does not add activity/profile/schema/adoption markers or
change original-corpus membership. Bundle maps, transpile references and the
separate source-reference forceDtsPaths investigation remain independent.

Final local runtime comparisons pass on the worktree-exclusive
`target/h2-7e-api-check`, with one Cargo job/test thread at background priority.
The 54 same-session API sequences pass in 80.14 seconds, including 378 exact
calls and two explicit noEmit boundary assertions. The eight dedicated map
tests pass in 138.29 seconds: the prior seven tests are retained, and the new
scoped command comparator repeats all unchanged 24 ordinary, three runtime and
two disabled-declaration fixtures, including their command status and exit.

The existing H2.7c getter 19-sequence comparison passes in 31.39 seconds;
the forced 37-sequence comparison passes in 66.01 seconds. Both filesystem
CLI controls pass in 43.32 seconds, covering config/noEmitOnError before
output and TS5033 continuation/listing after write failure. Clippy passes
with warnings denied for the Program, emitter, checker and compiler libraries
and both dedicated H2.7e comparison tests. Formatting and diff checks pass.
The original API observer and all expected observations remain byte-identical.
