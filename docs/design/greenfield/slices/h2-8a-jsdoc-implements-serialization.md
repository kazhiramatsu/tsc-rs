# H2.8a A6-21: JSDoc implements serialization

Base `2c040c23338f7fc17aaa4dc4d5c83d6638208ec1`. The96 fresh complete commands repeat on TS6.0.3 and in
--check. Two independent native before jobs each have36 exact/60 failed;
all first failure vectors and membership agree. Fresh successes execute twice
per job and failures once per job. Eight original implements corpus commands
also fail twice in one job, with16 complete actual/expected tuples saved at an
absolute path. The immutable before archive and exact run durations are in
ratchets/h2-8a-jsdoc-implements-serialization-before.v1.json. An earlier invalid Cargo package
selection ran no tests and is separately retained; it is not before evidence.

The58 declaration failures all lack implements clauses.26 have only those missing
clauses;32 also lack declarations/imports for the newly referenced dependencies.
Removing the missing implements syntax from TS leaves only additional lines,
with no other removed or changed line. Name tracking must reach the existing
private declaration queues and alias visibility owners. The two ES5 typed class
expression commands first differ in their source-map result and remain outside
this declaration owner. Keep their comparisons unconditional and compare their
first vectors unchanged after. Later fields beyond any first failure are not
claimed qualified. The36 adjacent positives include native TS heritage, empty
JSDoc, no implements, class expressions and selected exports-entity routes.

A6-21-1: crates/checker/src/class.rs preserves effective clause-list presence:
JS returns Some(list), even when empty; TS without implements returns None.
Adapt the sole current diagnostic caller to iterate the optional list in the
same order. Add get_implements_types at the same checker producer: iterate
actual type.symbol declarations, query effective clauses and get_type_from_type_node,
then omit existing TypeTables::is_error_type results. No new cache or scanner.
The shared error predicate also covers Any with aliasSymbol, not just error
identity. Keep all valid declaration/type iteration and query order.

A6-21-2: statements.rs sanitize_jsdoc_implements consumes the effective list.
For every element, install its parsed enclosing declaration; reject an empty
entity identifier, then invoke the existing ProductionSyntacticBuilderResolver
tracking member through a narrow serialize.rs bridge and mod.rs re-export.
An ordinary rejected element is skipped while the remaining elements still run;
any length mismatch rejects the whole list. Each type argument first tries the
existing syntactic reuse bridge, then get_type_from_type_node2 and the shared
type serializer. The existing get_type_from_type_node2 owns mapper instantiation.
Use the existing typed create_expression_with_type_arguments factory so type
argument parenthesizing, child identities and transform flags retain their owner.
No independent name lookup, tracker, resolver implementation or syntax printing.

The per-element parsed scope also disables the private synthetic enclosing
flag/kind/locals overlay while active: those four Rust slots jointly represent
TS context.enclosingDeclaration. Save them in existing SyntheticModuleScopeRestore
and restore with restore_synthetic_module_scope before propagating any Result,
including Factory or checker errors. Move saved locals; do not clone a mutable
checker tree or create a fake parsed declaration. cleanup returns its captured
Result only after restoring context. A focused unit exercises valid reuse under
conflicting synthetic locals and a missing-target Factory failure, each from
ModuleDeclaration and Block contexts, proving all four slots restore. The test
uses an ordinary source handle from a separate arena, with no unsafe raw IDs.

A6-21-3: serialize_as_class obtains original effective implements after its
existing base type query. Some(empty) suppresses fallback; absent clauses or
sanitizer rejection query get_implements_types and the existing
serialize_implemented_type in order. Preserve static type/base queries, length
accounting, heritage ordering, property serialization and surrounding scope.
Unknown names can be reused by upstream and are not uniformly discarded.
Exports-entity rejection goes through semantic fallback; malformed tags and all
associated diagnostics remain in the complete comparison.

Production scope is class.rs, node_builder/statements.rs, serialize.rs and
mod.rs; the unit lives in crates/checker/tests/unit/node_builder_statements/tests.rs.
E-CHECKER-FACTS-BASE is modified-requalify for this private producer/consumer
closure. E-PROTOCOL, E-RESOLVER-BASE, E-ARENA, E-METADATA-BASE and E-PRINTER-BASE
are premise-unchanged: checked parsed syntax stays immutable, the arena and
source IDs own transformed children, resolver errors remain typed, metadata
identities remain separate and no checker dependency enters the printer.
No public API, Program admission, host, sink, emitter or printer edit is proposed.
Existing alias/private-queue, tracker, recovery and factory owners are reused.
Any newly discovered dependency is dispositioned before widening this scope.

Required after:344 complete commands (96 fresh,84 synthetic-default-alias,
84 synthetic-namespace-export-modifiers,32 CommonJS class-instance,40 JSDoc
parenthesis guards and eight originals). Target342 exact twice, retaining the
two pre-existing typed class-expression map failures unchanged. These targets
are predictions until measured; never substitute assertions or regenerate TS
expectations to make the suite pass. Require31 checker statement/chain/specifier/
signature units including the new fault/scope test. Original failures, if any,
retain complete tuples. The two fresh retained failures execute once per job.
The current schedule's lightweight workflow applies; historical full developer
CI and certificate walks are omitted and not claimed. All other A owners,
B–E, final769 replay and hosted acceptance remain open. Do not derive a new
global count by subtracting these focused outcomes from A6-14.

Pinned whole TS6.0.3 functions (AST-verified declaration bounds):
- getJSDocImplementsTags: 11645–11647, SHA256 `1c17545783a1ea9c31347e0b710bd75cb1ac53efae416dc6e48ac9580e2f6c35`.
- getEffectiveImplementsTypeNodes: 15756–15763, SHA256 `7b48d400da24af97592c82568fc79e75d199be120737ae53e40c96d40fe74d3c`.
- getHeritageClause: 15768–15777, SHA256 `9d761ae0d10cae5538f2867cb17ffe2d4005f2b734d7f39a01abf2d6f81f281a`.
- isEntityNameExpression: 17128–17130, SHA256 `2e7694f05260a41567e84db34bfbfd9ec77c27e3c37116b2a9cf88f0ddccfeee`.
- getFirstIdentifier: 17131–17146, SHA256 `90d15bfc484f6bde12d68363ff2927e8c449f9cb7d4f9ea42cdc384e9f62d1c5`.
- createExpressionWithTypeArguments: 22944–22954, SHA256 `9d82e84ce18b62e683344bdba7f4067959b02e3ba13c9303ea37889e54517ddf`.
- getMeaningOfEntityNameReference: 50595–50605, SHA256 `8784944e5eb1e33d5e3df2aa701fc3275e8eb6f0b0608b0ebf355177a2e6f8e2`.
- getTypeFromTypeNodeWithoutContext: 50774–50776, SHA256 `933ff5fd5ea6e20fea30125f2b4d19888b78674e76d2bba1b877692dc6ff26a4`.
- getTypeFromTypeNode2: 51096–51101, SHA256 `f9c9056ae0674444ccf84bfde2d55862444d1927b02a1344a328e44ed99e2e5f`.
- typeToTypeNodeHelper: 51311–51318, SHA256 `69fd0744f9ba5217b598eaa6816f1650532dca59a9044240e3738d2b3e8ff86f`.
- trackExistingEntityName: 53555–53655, SHA256 `209b12123fd836edaefcaef413f04659f4e3b998dac70ab139b159a0125e85ed`.
- sanitizeJSDocImplements: 54565–54599, SHA256 `8ef6127728fc0f5d33791a61d92b478c788836a0a73be6f408bfb2cfe423d30e`.
- serializeAsClass: 54600–54686, SHA256 `ccb4c10cb58d4154e83303d911464ee3af0f91f4b9cc33d3d43e98266e113bff`.
- serializeImplementedType: 55377–55389, SHA256 `8dc5d28d5c09c61f977c319e980df02034d39a52ed02f2d19919b9c9d13987b6`.
- isErrorType: 55821–55823, SHA256 `2c5f5aabe3bf8bf77f0dbb8160922bdde8b776142a5387117432426d7ca501c4`.
- getImplementsTypes: 57190–57209, SHA256 `5c4e8fadb855ac7137714a0347323f8f47f2b9ac5bd2ebe7e5837ae3e0c52dfa`.
- getTypeFromTypeNode: 63196–63198, SHA256 `5d4a798af65bf23738c21df6d7142d44f9ac093ea314f620267fde2a974f3004`.
- checkClassLikeDeclaration: 84994–85111, SHA256 `c6d19b4f07ff5e371ca23c7525da95a00bbd334e745ba9ae039197957eeb7330`.
- tryReuseExistingTypeNode: 133293–133688, SHA256 `73134f6715280a2e5ee712256fea004260db59f5797e30ce14d2d37f21a6d5a4`.

Final measured result:94/96 fresh commands match complete TS observations twice,
including all58 previous declaration failures and all36 positives. All240 adjacent
commands match twice. The eight original implements commands match twice; no
original failure tuple remains. Overall342/344 complete commands are exact twice.
The two ES5 typed class-expression source-map results retain exactly their
before first vectors and execute once each in this after job. Their comparisons
stay unconditional, so the contracts target and combined compiler command exit101.
The five fresh/adjacent functions finish in485.48s (four pass, one fails);
the original function passes in41.66s. All31 checker units pass in
0.02s, including successful reuse under conflicting synthetic locals and
typed Factory failure restoration from ModuleDeclaration and Block scopes.

The final record is ratchets/h2-8a-jsdoc-implements-serialization-after.v1.json. It preserves
the exact prelaunch packet/readiness bytes in a separate outside prelaunch tree
before this measured-result append. Production changes are confined to the four
pinned checker paths; no expected tuple or shared comparator changed. This closes
the JSDoc implements owner only. Other A work, B–E, the final global769 replay
and hosted acceptance remain open. No new global count, fresh emitter unit run,
historical full developer CI or certificate walk is claimed.
