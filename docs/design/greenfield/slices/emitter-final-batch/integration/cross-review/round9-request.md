Round8を独立調査と照合しました。default-library related-info 4行はCodexも同じ2 gatesを特定し、index→property fallback保持、既存file_facts利用で実装。JS optional/getDefaultConstructSignatures/parser先頭(も upstream 原文を照合して修正。JS小修正は3関数ですが genericDefaultsJs と jsExtendsImplicitAny の2行があるので4行、relatedと合わせ8行です。
残るJS2行を「今回は入れない」という判断で終わらせず、ユーザーが可能な修正を全て求めているため、もう一段原因を特定してください。対象 jsExportMemberMergedWithModuleAugmentation2 と jsDeclarationsTypedefAndLatebound のみ。reader調査を続け、必要なら TypeScript oracle を /tmp の scratch script / copied bundle に限定して instrumentation して構いません。repoとvendorは編集禁止、Rustビルド・テスト・cargoは禁止。CPUを大きく使う全fixture再生は禁止。小さな2ケースのNode観測は可。別agent委譲は不可。
元の完全入力は checker-residuals.json。tsc API createProgramとcustom hostを用いれば標準libは元repo絶対pathを読めます。実際のSymbol解決/diagnostic stackを根拠に、最小修正と負の対照、または必要な共有基盤の具体的範囲を返してください。Codex側は他の修正のfocused再生とsource freeze準備を進めます。
なおB2866は新stateを持たせず、既存last_locationを成功callbackへ渡してglobals最終scope判定。helper隣接3probe(ES5 assign/array-binding read/array-assignment read)も旧floor前提の省略を戻しました。これらの再レビューは今回不要。
