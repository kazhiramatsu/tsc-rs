# Follow-up to Claude review round 1

We agree on the priority-1 cause and the minimal visitor ordering fix. Codex has applied it in standard_decorators.rs visit_function_like_body: visit raw NodeData children, end lexical environment, create return/block from the raw body, finally update original once. Both unrelated partial-wrapper refinements in factory.rs/printer.rs have been reverted. Please give a short focused review of this final shape, including original/comment/range/flag risks. No edits/builds/delegation. Avoid re-reading the whole project; round1 context is sufficient.

We also accept the module-request fix, keep parse-recovery/stableTypeOrdering guards unchanged, and will add the authoritative mts/cts contrasts within the existing 40 request-plan tests. All four EF7 rows have been archived/retired after 15 commands were exact twice. Fresh Program flag fix is proven by all 143 old/new helper/control commands exact twice.

For comment guards, baseline now confirms 0 exact/48 refused in 3 targets × removeComments × 8 shapes. This includes four string/template/comment lookalikes (ordinary valid input), actual private-in comments inline/across lines, and optional-as/angle comments. New official TS observations are in import-helpers.json, case IDs comment-boundary. Proposed experiment: in the isolated audit branch, remove these obsolete raw-text guards, test the 48 complete observations and existing full bands. If actual syntax still diverges, fix its proven producer/printer cause or reinstate a typed AST/trivia guard for that real boundary; never keep broad text scanning. No complete claim or merge from simply deleting the guard. Does this A/B experiment address your concern with witnessless retirement? Identify narrow targeted controls still missing. User asks for all feasible repairs and minimum effects.

Numeric target: please verify the two conditions you identified using only the relevant target handling files (i32 total type, no panic/exhaustive restrictive match; unknown default lib falls back to lib.d.ts). Is upper-bound guard removal then preferable to API-only normalization? We will use existing target=1234 H2.8c case plus 99/100/1234 source-transform controls. No target change applied yet.

CLI: parse_module in compiler/src/cli.rs only accepts preserve and parse_target rejects ES5, though config/Program paths support all active modules and ES5. Proposed small separate fix: accept the same named values/aliases as the existing transformer/options support, retain unsupported-flag behavior and diagnostics otherwise. Verify CLI against equivalent config emit plus frozen TS output; do not claim whole CLI parity. Any architectural reason to retain these obsolete named-value restrictions? Keep this review concise; detailed implementation/testing stays with Codex.
