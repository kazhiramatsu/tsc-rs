# Independent Claude review requested by the user

You are a read-only reviewer collaborating with Codex. The user explicitly asked us to compare approaches before high-impact or uncertain repairs. Do not edit files, invoke builds or delegate. Use Read/Grep/Glob on this checkout. Return concrete findings, smallest correct remedies, alternatives and required regression tests. Do not approve a guard removal merely because a finite fixture passes.

Checkout: /Users/hiramatsu/dev/tsc-rs-emitter-final-audit, baseline ed71c45343b2063414d5563c9b5455631d7c5c68, current uncommitted fixes. Producer checkout is separate and must remain untouched. TypeScript 6.0.3 sources are vendor/typescript-6.0.3/lib/_tsc.js and typescript.js. Integration PR #561 remains draft until final expanded CI is green.

Priority 1: unresolved decorator-super arrow map regression. Baseline plus current two attempted corrections still fail 5 primary discard/arrow-expression-body and 5 followup param-default/nested-arrow-in-default variants (all except esnext/define). Source-map has extra CAAA pairs around generated comma body. Full logs: docs/design/greenfield/slices/emitter-final-batch/integration/records/local/audit-decorator-r2.log (uncompressed local exists). Fixtures decorator-super.json.zst and decorator-super-followup.json.zst; Rust tests crates/compiler/tests/integration/h2_8a_decorator_super.rs. JS source generator scripts/observe-decorator-super.mjs plus imported modules. Current hypothesis: parenthesizer ownership or repeated pipeline context. factory.rs parenthesize_concise_body now tests isCommaSequence on body itself, matching upstream 20514; printer context_parentheses ArrowConciseBody now passes original expression, and arrow_concise_body_requires_parentheses avoids unwrapping before comma test. Neither fixes this. Examine printer emission pipeline and standard_decorators producer, compare upstream. Propose minimal true cause fix, not fixture-specific suppression. Codex will independently investigate captures and run tests.

Priority 2: review pending module-request change (not yet tested). Four EF7 KNOWN rows bundlerDirectoryModule node18/node20/nodenext and bundlerOptionsCompat were refused for no authoritative impliedNodeFormat under Node module + Bundler resolution. Upstream getEmitModuleFormatOfFileWorker 125493 falls back to module if implied undefined; getEmitSyntaxForUsageLocationWorker 122290 then returns undefined static mode for Node module number, retaining import/require special modes. Current crates/program/src/module_requests.rs removes refusal in file_emit_module_kind and allows Node range -> Unspecified in static_request_mode. New unit in crates/program/tests/integration/module_request_contract.rs verifies static/dynamic/import-equals modes. A former missing-mode rejection assertion was removed. Are any legitimate invariants lost? Is a distinction between unknown and authoritative-none required in PreparedSourceFile, or should we preserve upstream fallback universally? Recommend smallest justified change.

Priority 3: scope/risks for remaining refusal boundaries. preflight_source in builtins.rs only admits syntax.has_only_literal_recovery; EF7 has 36 H2.9 parse-recovery KNOWN rows, mostly invalid await/default grammar plus one decorator-expression. We have NOT relaxed this guard. Need explain what concrete prerequisites are required to support recovery safely and whether a bounded parser-fact extension is feasible; no broad guard removal. stableTypeOrdering option is refused and impacts checker type ordering, also NOT changed. Fresh Program assumeChangesOnlyAffectDirectDependencies refusal is being removed because upstream reads it only in builder affected-file traversal; 4 fresh-command flag/declaration controls are added. Distinguish emit defects we can close in this train from full parser/checker/builder implementation needing a separate design.

Other current fixes are summarized in integration/residual-audit.md. User said fix everything we can, but minimize impact by comparing designs rather than expanding/refusing silently. Please focus on priorities above. Cite code locations and uncertainty explicitly. Return in Japanese if convenient.
