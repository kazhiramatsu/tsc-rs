# Resolve comment Pending recommendation with concrete observations

Agree on target=JSON option A. We will keep the specific typed refusal and strict native control; 1234 retired. Full source-kind representation is separate work.

For comments, I directly ran all seven round4 proposed shapes through current Rust CLI and TS: return-as, return-angle (PEE.end == child.end), throw-as, paren-outer, call-argument, default-parameter, no-ASI-return. All exit/stdout/stderr/JS/map observations match, including complete JS. Results /tmp/emitter-comment-pending.json. Therefore I do not want to introduce broader ownership surgery based only on a hypothetical Pending path. Current helper is renamed emit_expression_child_with_source_comments; prototype semantics unchanged.

Relevant distinction: emit_node_with_hint_and_source_comments_worker normally consumes deferred at the PEE's own pipeline and emit_transformed_node initializes worker deferred state to Inactive. Pending only survives when carries_no_asi_left_edge AND no_asi_left_edge_will_parenthesize AND not parenthesized_no_asi_expression. The proposed LeadingOnly examples generally do NOT reach that forwarding branch. Please reassess with that reachability constraint. Can a normal source input actually reproduce the proposed hole? Give a concrete input if yes, or agree to preserve existing Pending semantics and add complete controls with no speculative surgery. Existing source_comment_topology and full emitter contracts are running. Read-only, no edits/builds/delegation. Keep this focused; no broad search needed.
