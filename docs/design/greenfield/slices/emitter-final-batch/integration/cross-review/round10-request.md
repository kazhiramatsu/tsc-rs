Round9の実traceありがとうございます。rootは今から2欠落枝をupstreamと独立比較し、負の対照を含めて修正します。別の調査対象: 残る型関連5行を「基盤」とまとめず、局所修正で閉じられるかを最後に精査してください。対象は checker-residuals.json の deeplyNestedMappedTypes / recursiveConditionalCrash4 / relationComplexityError / mappedArrayTupleIntersections / jsxIntrinsicDeclaredUsingTemplateLiteralTypeSignatures の5行のみ。
Codex独立発見: JSX最後の行はType 'string' vs '"smth"'だけの差。calls.rs::elaborate_jsx_named_attributes (6110付近) がupstream elaborateElementwise 64153の specificSource contextual recheck +成功時sourceProperty fallback を省略。elaboration.rsのJSX children (591付近)、object (1282)、array (1466) では既に同じpush_contextual_type→check_expression_for_mutable_location(CONTEXTUAL)→pop→specific/fallbackを実装している。ここを named attributes にも適用すれば小さく閉じる見込み。これを検証し最小案と対照を返してほしい。
他の4行は、既存deferred/escape/gateが原因か具体的な共有基盤不足か調べてください。relationComplexityはTS2859×2に対してRust2322×2 (start224,278)、recursiveはTS2589がstart251だけ欠落・start394は一致、mapped tupleはtoStringがnumberになりspurious2344、deeplyNestedは欠落4診断と{}交差型表示。対象input全て残差JSONにあります。
repo変更・Rust/cargo実行は禁止。小規模のTS scratch instrumentationは可、vendorはコピーのみ。必要な具体的code path/移植欠落、修正リスク、負の対照を示してください。単に全体rewriteと分類せず、局所修正が無理ならその根拠を明記してください。別agentへの委譲は禁止。
