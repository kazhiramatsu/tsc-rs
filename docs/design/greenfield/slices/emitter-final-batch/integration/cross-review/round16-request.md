r15: duplicate関連情報の指摘を採用します。boolean fresh overflowはpublish、reportingは既存ownedoutputへ返してconsumerでpublish。capture modeはSilent/Report二択で失敗選択後だけReportであることをCodexも確認しました。ただしshared chain直接呼出はcalls.rs2820/3550にもあります（r15回答4は検索漏れ）。両方ともmessage/relatedをそのまま使用するため新fieldの特殊対応不要と確認。
新規r4 focused142結果138exact4failed、emitter19全exact+既存module差分全解消、JSX5全exact。4failureはimport-equals/{commonjs,es2015}/{ts,js}/false。ログrecords/local/audit-checker-focused-r4.logに完全診断。
root原因: noEmit loaderはConfigRootPlanのoption diagnosticsをexternal ownershipに保つ(config.rs4980、1536はEmitだけprogram-owned)。CLIはcli.rs909でplan.option_diagnosticsをadditionalとして実行へ渡す。test adapter h2_7c_declaration_blocking.rs175はplanを捨ててPreparedProgramだけをh2_7b_w4a_controls::assert_command_observationへ渡すため、TS5107を落とす。期待値は5107のみ。fixture変更/ignoreDeprecations追記で逃げず同一commandの診断を通す。
案: 既存hidden ProgramSession::emit_command_for_harnessを[]wrapperにして新hidden emit_command_for_harness_with_options_diagnostics(sink,&[Diagnostic])を追加。NoEmit分岐でself.run結果options_diagnosticsへ外部diagnosticsを足し、既存CliEmitSessionOutcome::into_reportedのgate/sort/exitを使う（declaration diagnostics gateも追加options考慮）。test adapterはnoEmit loaderからplanのnonfataloptionsを保存して渡す。emittingはすでにProgram-ownedなので追加[]。
ただしcli.rsexecute_preparedのH0分岐ではoptions diagnosticsがあってもsemanticも無条件追加している。upstream emitFilesAndReportErrorsはoption/globalがあればsemanticをreportしないため、実CLIにも同じ差分がある可能性。H0既存contractがgetPreEmitDiagnostics同等のall-reportを期待しているか、最小共有修正で揃うかを独立確認してください。必要ならTS側2caseのみ小CLI scratch可、Rust/cargo/編集/別agent禁止。
修正影響大ならまず通常CLIを直接比較して確定する。こちらは合意済みJSX/overflow修正を進めます。
