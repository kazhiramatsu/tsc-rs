最終全CIで新しい回帰を確認。現在HEAD91e3b214b（修復source599798019）、コード変更せず原因と最小修復案を独立検討してください。ローカルでは今からconformanceを直列再生するため、ビルド/重いテストは実行しないでください。
1. acceptance conformance set-ratchet: 38 accepted identities regressed。最初は destructuringParameterDeclaration4.ts [] :23:10 TS2322/T4、destructuringVariableDeclaration1ES5.ts :38:15/:38:26 TS2322/T4、同ES5iterable targetes2015。/tmp/emitter-ci-105666074420.log にCI全log。ただし全38詳細はローカル再生でこれから採取する。直近変更は git diff ed71c4534 HEAD -- crates/checker/src。特にrelation owned diagnostics/used_containing_message_chain、JSX source型受渡し、mapped recursion、default-library related-infoをupstreamと照合して、JSX元行を直す一方で通常destructuring診断の観測を壊していないか確認したい。
2. acceptance H2.5h: ES5For-of20.ts#target%3Des5 writes=1 diagnostics=false emit_result=false refused=false NEW divergence。/tmp/emitter-ci-105666074371.log。原文は ts-tests/tests/cases/conformance/statements/for-ofStatements/ES5For-of20.ts。ES2015 pending_assigned_name撤去、commentphase/variable allocationの変更は個別対照で通るが、この影響かproducer由来かまだ未確定。guardやaccepted集合を緩めず原因を修復したい。
3. local最終helper480は468 EXACT×2 /12 typed boundaries /0failed。CLI58はexact。追加JSX28/module8もexact。全CLI契約中filesystem_config_and_source_diagnostic_orderのmacOS case-insensitive outFile旧typed refusal期待だけstale（nativeexit1=TS1、testは2期待）。ここは既存入力を保ち完全TS比較へ移す予定。
4. controls failureはbundle-original-javascript manifestのh2-7de-observations hash同期漏れ（観測自身は差分なし）。これはCodex側で公式writer＋旧行不変検査を行う。
回答は具体的なsource branchと確認すべき観測を。まだ差分が分からないものは仮説として明記してください。最後の提案はローカルの実測と照合します。
