独立照合: round3完全captureをVLQ decodeした。read-assignmentの最終JS列69はactual source61 / TS source51、rest-assignment列48はactual source63 / TS source58。module flatten entryの最後がinlineしたcomma rootへ元BinaryExpression全体のrangeを付けており、leaf export wrapperのendを上書きしている。upstream flattenDestructuringAssignment (_tsc.js:93251–93328) は `inlineExpressions(...)` をそのまま返し、rootへのsetOriginal/rangeを行わない。leaf callbackだけがexpression.originalを持つ。したがってentry末尾のset_original_and_rangeを除くのが原因層での修正。
alias2件はJS末尾にTSが持つ `exports.other = exports.rest` / `exports.b = exports.a` がない。CommonJsVisitor direct_export && binding_pattern armがflatten直後にcontinueし、通常identifier armが行うalias trailing publicationを通らない。既存create_declaration_export_statementsはbinding_name_leavesを列挙しexport_specifiers_by_localで明示aliasのみを作るので、info.appends_declaration_exports()のgate付きでbinding_pattern armにも接続すればupstream appendExportsOfVariableStatementに一致する。実装は元KNOWN測定完了後。
