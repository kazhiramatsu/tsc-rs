最新focused142は51exact/91failed。85件はnoEmitのconfig loaderがsourceMap=trueをH0 scope拒否（前のadapter entry修正だけでは通らなかった）。6件の実差分の完全captureはcross-review/destructuring-residuals-r3.json。元10件は6件exact、残4件はassignment最終map end。追加alias2は末尾exports再発行statementが欠落（JSも差）、flattenされたassignment自体はmap一致。
A. module差分6を独立調査して最小修正を。先行round12 direct+alias順を修正済み、empty2はexact。現在rootは元27KNOWNを測定するのでrepo編集/Rust実行禁止。
B. noEmit+sourceMapは単なるadapter問題ではなく通常config loadの実admission境界と確定。H0_SUPPORTED_CONFIG_OPTIONSはimportHelpers/outDirなど全て通すがsourceMapだけ未許可。`sourceMap`はCompilerOptionsに既にprojectionされるしProgramSession noEmit commandはsourceMapを消費せず空emitを返す。root案: H0許可にsourceMapを単独追加し、他の未対応emit/build/watch flagsを無条件に開かない。noEmit=true absent/false/sourceMap true/falseを普通config/CLIから比較し、zero emitter activity、既存H0 unsupported printer/profile契約を維持する。設定値やnoEmitをtest adapterで消す方法やraw loaderでscope gateを飛ばす方法は採用しない。sourceMapのchecker/config option診断・noEmit時副作用・typedfacts不足がないかを確認し、可否と最小対照を返してください。H0宣言診断やbuildまで広げる必要があるなら別案を。
依頼はこの2点のみ。repo読み取りと必要最小限のTS scratch観測のみ、repo編集/Rust/cargo実行・別agent委譲は禁止。