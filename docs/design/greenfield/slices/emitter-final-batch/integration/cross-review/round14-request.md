元27KNOWNを現在再生中、compiler群24行では22exactx2。残る2行の完全実測はcross-review/checker-residuals-r14.json。
1 JSXは依然quoted literalがstring表示。calls.rs specific/fallbackを追加したがsource checker内のcheck_expression_for_mutable_location自体にupstreamのJSX attribute特例が欠落している可能性がroot仮説。下層を独立追跡して最小修正を。
2 relationComplexityErrorはA診断1/E2。originshortcutのおかげでf1消えてf2診断は2859になったが、identifier長1 vs binary長5の片方が欠落。getNodeLinks/relation cache、conditional report reentry、same diagnostic dedupを追ってください。予算変更なし、診断位置や文面の後処理による穴埋めは不可。
残る3conformance checker行はまだ実行中。repo編集、Rust/cargo、多数TSfixture実行、別agent委譲は禁止。必要なら上記2個のみ小scratchinstrumentation可。
補足: noEmit/sourceMapを1つ追加する点で合意。ただしH0_SUPPORTED_CONFIG_OPTIONSはfrozen h0-qualification.v1.jsonと完全一致している。過去evidenceを再署名しないようbaseline listは維持し、unsupported_config_scopeのno-emit側でsourceMapだけを明示追加し、H0 contract名/説明はfrozen baselineであると正確にし、元allowlistassertは保持する方針。新差分はCLI6cases/既存85noemitcontrolsで検証する。全H1flagのadmitはしない。