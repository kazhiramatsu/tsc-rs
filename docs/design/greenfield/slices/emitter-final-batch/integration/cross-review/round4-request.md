# Final comment design and numeric target JSON boundary

Your round3 analysis agrees with my direct CLI dump: the comment itself is absent. Initial request wrongly inferred JS success before the map assertion; corrected in the work log. No shared writer/map change needed. Current local prototype calls existing emit_access_target_with_source_comments from the PEE arm, so ordinary inactive state creates the full nested phase using current expression_context.comments(). This scope already claims the PEE via active_expression_comment_scope. It preserves pending no-ASI forwarding. Tests running for 66 comments plus 48 CLI cases. Will add satisfies / two block comments / line comment / own-line controls and run existing printer topology. Please check whether pending LeadingOnly forwarding must instead be re-containered without breaking no-ASI contracts. No edits/builds/delegation.

New numeric finding: target=100 is ScriptTarget.JSON, not an ordinary unknown value! _tsc.js createSourceFile at 28870 parses JSON regardless of filename; transpileWorker at typescript.js:146090 uses it. Our checker/lib.rs:1895 only selects JSON by .json suffix. With target100, decorated class + useDefineForClassFields:false, TS gives source exception Output generation failed; Rust emits JS after upper guards were removed. Plain numeric control also needs JSON-specific handling. target1234 old KNOWN is now exact; target99 and target1234 private/async/optional controls exact. Additional upper guard in class_fields initialize was removed; standard_decorators guard now rejects >=ESNext only if define=true, matching list reachability.

Please independently assess minimum-impact choice: (A) retain typed refusal for target==JSON at ordinary emitter admission until parser/source-kind facts correctly represent it, archive newly discovered target100 control as strict native KNOWN while retire1234; (B) small correct shared source-kind route repair, if one exists. Do not fake OutputGenerationFailed just for numeric value or normalize target100 to99. Parse-recovery remains deferred and barebones transpile-declaration lib is explicitly parsed at Latest upstream, which complicates global target-based JSON selection. Focus on proving whether B really is small; otherwise document A's exact remaining boundary. User wants all feasible fixes but cross-review/minimum impact for unknown larger work.
