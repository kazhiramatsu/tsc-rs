最終full468を再生中、CLI58は全exact、コメント対照で旧追加2形状×3targets×remove2=12行がparse拒否になることを確認。cross-review/comment-parse-boundaries-r17.jsonに元input/TS完全観測を保存。inputは `(object?.x //value\n as number)` と `(object?.x\n /*value*/ as number)`。TSも1005/1434/1128の構文エラーでrecoveryemitしている。Rustは3parse diagnostics/4recovery events H2.9 typed拒否。r3の66コメントexact時点にはまだ入っていなかった拡張対照です。
無条件parseguard除去はr12合意に反する。元入力を有効構文へ上書きして失敗を消すことも避ける。案: この12入力/TS観測をそのまま残し、source/boundary原因・正確なtyped拒否・partial writes空を凍結してnegative boundary controlsとして別計上。追加でasを改行前に置く `(object?.x as //value\n number)` / `(object?.x as\n /*value*/ number)` の12有効対照をordinary complete比較する。こうすると全480選択は468exact/12typed boundariesが想定される（まだ他の差分がないとは確定していない）。EF7残36とは別集合なので足して製品残件数にしない。
この境界を安全に閉じる局所案が本当にあるか、またはr12と同じparser missing/skip facts待ちかをsourceで判断してください。retire assertion付きstrict native比較を追加して、新panicやnewwriteを免除しない。既存比較器を流用できる最小案も確認。全corpusを移設/削減する案は採らない。repo/Rust編集・cargo・多数TS再生・別agentは禁止。必要なら新valid2形状だけTS parse直接確認可。
補足: r16のall-report contract0という結論は誤りで、cli_contractのconfig_option_diagnostics_are_rendered_alongside_semantic_diagnosticsが実在した。Codexがdirect TS比較へ置き換え、58CLI全exactを確認済み。sourceのhiddenoptionsdiagnostics入口もinto_reported追加引数へ統一済み。
