新たな実測結果と設計照合です。primary: 追加checker対照から通常emitter 10行の差分を発見しました。`cross-review/destructuring-residuals.json` に完全actual/expectedとdecoded JSがあります。元入力/optionsは`crates/compiler/tests/fixtures/import-helpers.json`でcase_id参照。rest/es2017 variableはJS同じでmap差、rest/es2018 variable/assignmentとadjacent-helper/es2015 read-variable/read-assignmentはJSも差。commonjs module変換がexported destructuringをflattenする際のtarget gate/flatten level/late helper（importHelpers=trueでもTSはinline）を独立調査して、最小修正と周辺対照を返してください。rootはchecker round9/10の小修正を独立照合して実装します。
補足: focused89は28exact、51はh2_7c_declaration_blocking test adapterがnoEmit=trueにemitting loaderを呼んだ入口ミス、残り10は上記実差分。adapterはplan.compiler_options().no_emitでload_config_programに振り分ける予定。既存loader仕様やfixtureのnoEmitは変えません。
secondary: round10 deeplyNested (b) の `is_deeply_nested_type` を正しくfallible &mutへ変更する最小案を確認してください。get_modifiers_type_from_mapped_typeはfallibleなのでcache-only fallbackやunwrapは採りません。呼び出しはengine/structural/inferenceの8箇所。CheckerStateのinfer_reverse_mapped_typeではpush済みstack/flagsがあるのでchecks+workerをclosureに包み、Errでもpop/flags restoreする必要があります。InferenceContext側も関連のcleanup順を調べて、portを置く層と必要なguardを具体化してください。共有relation algorithm全体の書換えは避け、upstream2helperの移植に限定します。
repo編集・Rust/cargo実行禁止。TS scratch実行は小さな対照のみ、全fixtureや多数ケースは禁止。実診断観測と独立source照合だけ。別agent委譲不可。
