## r30 review: printer and registry retirement diff

**Printer (printer.rs 17635-17648, 18317-18358).** Correct and minimal. The unified helper keeps the suppression loop, then for single-line writes the list space between the trailing phase and the close-brace leading phase, matching `emitBlockStatements` order. Because an empty list's `pos == end`, the same comment is scanned by both phases; the `emitted` set prevents a double write, so `{ /*a*/ /*b*/ }` and `{ /*a*/ }` come out right. Function bodies: when every same-line comment is suppressed, `empty_node_array_boundary_has_comments` returns false first and the caller prints the plain `{ }`, so the helper never writes a stray space. `emit_empty_node_array_boundary_comments` is untouched for its list callers. No issue found; the 72-shape native comparison remains the proof.

**Registries.**
- `HOST_CASE_SENSITIVITY` row: pins verify against the frozen artifacts (qualification row `e60e5e…`/`48e558…`, disposition admitted; h2-7de census `required_slices == ["H2.7d","H2.8b"]`, input `a7b13d…`; the three h2-7de artifacts still hash to the pinned `956514…`/`f2e078…`/`ef68d9…`). `validate` pushes `H2.8b` only for that table, so the owner match is exact and not widened for other rows. `promoted_count()` and `all_promotions()` include the new table; `find`/`adjusted_refusals` iterate `all_promotions()`, so the `outFile` bucket drops 171→170 as intended. Counters `0/1/0` and `declaration_members: 0` are only checkable at runtime (`validate_activity`), as you note.
- `output_promotions` isolatedModules row: identity fields match the qualification row (`4249c3…`/`54abf8…`, admitted); the tuple hash `9bbac9…` is computed by `compact_typescript_observation`, which I could not recompute here, so it stays a runtime check. `adjusted_refusals` removes the `isolatedModules` bucket entirely (retain filter), so projected totals lose that key.
- `refusal_migrations`: `CURRENT` empty; removing the `CURRENT.is_empty()` early return from `validate_ordinary_results` is right, since that guard is now needed against vector replacement without any migration. `validate` still early-returns on empty `CURRENT`; that is fine because nothing is registered. `count()==0` makes `legacy_pre_flip` false (promotions exist), so live totals must equal the projected map; with both buckets adjusted the expected live map has no `useCaseSensitiveFileNames` and no `isolatedModules` key.

**Callers.** All eleven consumer sites in h2_2c_acceptance.rs (5482, 5533, 5584-5586, 5672-5740) tolerate zero migrations: `known_diverging = diverging + 0`, the `639/4` totals are unchanged, the summary prints `current_migrated_refusals=0`, and the write path skips the manifest when `retained` is empty. `write_h2_6c_divergence_manifest` already deletes an existing file on empty input, so the test's absent-manifest round-trip is valid without touching the acceptance file. H2.7de acceptance has no reference to the retired rows.

**Contract tests are non-vacuous for the current state.** `Records::load` asserts both promotion registries populated, zero migrations, both retired ids registered as promotions, and an empty live manifest, then runs all three validators. Mutation coverage: identity mutations on both retired rows and one ordinary promotion (`validate` rejects), restored old refusal vectors on each retired row (`validate_ordinary_results` plus the matching `validate_results` reject), missing/duplicate/unknown originals in `partition`, empty totals, absent-manifest round-trip. The one path no longer exercised is `observe`'s fingerprint mismatch, which has no live row; archiving rather than fabricating a migration is the honest choice.

**Necessary corrections: none found. Two things to settle before commit.**
1. `scripts/observe-empty-block-comments.mjs` is untracked and referenced by witnesses.py and the policy hash list; make sure it is added in the same commit, and that `qualification-policy.v2.json` gets its hash if the policy enumerates observer scripts (it lists `scripts/emitter_final_witnesses.py` but I did not see an entry for the new observer).
2. `test_replay.py` raises the `emitter-final` witness count 977→1049 (+72) consistently with the fixture; confirm the count includes only the 72 new ids and nothing double-counted through `emitter_residual_audit.rs` (the new test is added to the residual-audit cargo group with the count bumped 7→8).

No native result is claimed.