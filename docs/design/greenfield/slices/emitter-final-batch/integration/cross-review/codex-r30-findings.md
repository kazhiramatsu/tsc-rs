# r30 integration follow-up

Claude Fable rounds 29 and 30 independently reviewed the empty-block comment phase fix and the final H2.6c refusal retirement. No correctness correction was requested in round 30. Native verification of both changes remains pending at this commit.

The single-line empty-block printer now uses the same trailing-comment, intervening list-space, leading-comment order as TypeScript. Existing function-body suppression and generic empty-list handling are preserved. A frozen 72-case complete-command observer covers twelve shapes, three targets, both comment policies, and source maps; TypeScript reproduced every observation twice.

H2.6c retires its final historical refusal migration into the current exact promotions. The original, corrected host-case-sensitive identity and the isolatedModulesSourceMap identity are both pinned. Five unconditional registry tests check the nonempty promotion registries, reject restored refusal vectors and identity changes, and check the absent divergence manifest. Historical migration bytes remain archived.

Further source audit found stale unsupported-option assertions in config-source commands, seven source-map outDir controls, and AMD/outDir emit-session contracts. These now require their original observations. The four emit-session inputs additionally have twice-repeated TypeScript complete-command observations; the mixed .js/.ts basename case requires TS5056 with no writes, not an outDir refusal. Historical test names referenced by frozen H2.1e profile records remain available.

Hosted emitter-final membership grows from 977 to 1,123: 72 empty-block observations, four original session commands, and 70 historical source-map emit observations. The latter include the existing parse-recovery boundary and sink-failure controls; this is a membership count, not a claim of 70 exact ordinary commands. Thirteen exact contract filters are registered and zero-test execution remains an error. New observers are included in suite input closure; qualification policy pins the runner source, as for the existing observers.

The preceding immutable 3c1dfa51 candidate completed all nine local r23 checks: conformance 49,024 exact; H2.5h 888 exact/44 deferred twice; emitter 508 library/452 contracts/18 comment topology; compiler CLI and H0 32 tests; transpile routes nine tests; universe 216 exact/one known; original PLAN-BASE known set 35 retained/zero unexpected; emitter all-target clippy and compiler library/binary clippy. Receipts do not claim to validate the new changes above.
