r18実測と具体案の照合をお願いします。編集/ビルド/重い実行不要、以下2点へ絞ってsourceレビューしてください。
1. full conformance再現完了: cross-review/conformance-regressions-r18.json。全38内訳はT3診断25＋T4ケース13。全25診断はpass（比較外）を除き、libraryのrelatedInformation欠落だけと機械検証済み。
あなたの2bit案より、既存ProgramFileFacts.default_libraryを正しい意味のまま使い、private check_program_with_prebound_libs_at_observedへProgramFileFacts引数を追加する案を考えています。libs-as-rootsのlegacy check_program_with_libs_at系（prepared/cache on/off）はORDINARY、owned no-emit/実Program/emitはDEFAULT_LIBRARY。lib_count変数は別にあるのでfixtureだけをcheckする既存scheduleは維持できる。ProgramSnapshot.lib_count()はdefault所属数という意味で実際の呼び出し0件（rg確認）。既存owned skipDefaultLibCheck testsはDEFAULTのまま。
ただしauthoritative cached harness/M9経路もlibs-as-rootsか、ProgramSessionからの渡し方を確認し、Some/NoneやLibraryPrefixCompletionを所属factの代用にしない方がよい。必要なら明示のtyped factをcallerからprivatecommonへ渡す。最小で意味が正しいadapter修復案と必要な負の対照を提案してください。filter撤去も二重のdefault意味も避けたい。
2. forof20はcross-review/forof20-cli-r18.jsonでRustがheader/inner-loop v_3、body const v_2へ逆転と再現済み。codex-r18-forof-finding.md参照。convert_for_of_statement_head非pattern宣言（es2015.rs:12415付近）がparsed nameを直接create_variable_declaration_plainへ渡すためloop名がfinalizer eventに出ず、通常constだけがr11のcolliding_declaration_name_substituteでprint_order eventに出る。ここにも既存colliding_declaration_name_substitute(name)?.unwrap_or(name)を適用してbinding共有する案。新状態なし、既存naming-order metadataを使う最小差分。upstreamはprint-timeの一貫したgetGeneratedNameForNode cacheを使う点と照合し、見落としがあれば指摘してください。directiveは原inputからharnessが除去済みなので先頭コメント仮説は該当しません。
