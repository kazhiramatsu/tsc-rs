r14とCodex独立調査が2根因で一致しました。intrinsic JSXは新public関数を増やさず、既存 check_type_assignable_to_and_optionally_elaborate(attr_type,result,Some(tag_name),attributes,head)へ置換済み。現在focused142を再ビルド中です。repo/Rust編集・cargo・別agentは禁止、read-onlyの短い設計確認だけお願いします。
JSX隣接は次のbatchでsource明示版capture wrapperを追加しcomponentにcheck_attributes_typeを渡す。into_sinkもsource_type引数を追加（唯一の利用者）して再チェックを避けます。quoted/expression/nested objectのintrinsic/component、exactOptionalの有無をordinarycommandに追加する予定。
Overflowの報告側でNone返却だけにすると check.rs check_type_assignable_to_worker のgeneric fallbackが偽2322を出します。またupstreamのerrorOutputContainerへoverflow自身を追加する責務も必要です。
案: boolean/reporting共通のprivate finish_relation_overflow(source,target,relation,complexity,error_node)でcacheとerrorAtJsを実行し、同一diagnosticを既存RelationErrorOutputとして返す。reportingはearly returnでerror_info/containing chainを迂回する。capture sinkへも正しいowned診断が届き、programへの重複は既存dedupe。
RelationErrorOutputにused_containing_message_chain:boolを1field追加し、通常分岐は実際にchainを連結したときtrue、overflowはfalse。elaboration.rs206のshared-chain adapterが現在output全件をtrue扱いなのでこのfieldを使う。calls.rsの他shared-chain直接消費も点検する。過剰な設計や不足があれば具体箇所を。比較予算/深さ/ソート/文面加工は触らない。
