# H2.7b / m-2 — the activation flip: rung packet

Status: **rev 10 RATIFIED / design only** (2026-09-04; sol rounds 1-9
REVISE incorporated, round 10 **AGREE** — §11). Second rung of the
[h2-7b.md](h2-7b.md) §6 ladder (rev 9 RATIFIED), kind `runtime`: ONE
design gate, ONE flip, the first post-flip breadth sweep, the manifest
freeze — the [h2-6c-m-2.md](h2-6c-m-2.md) shape. This packet authorizes
the acceptance runner `run_h2_7b`, the production activation of the
non-bundle `.d.ts` member (G1-G12 of h2-7b.md §4), the controls'
retained/lifted re-pin, the H2.7a owner-inventory and close-artifact
amendments, and the hosted-boundary growth by exactly one call. It
executes on its own train (`h2/7b-m2`) cut from `main` after the m-1
train (PR #503) merges.

Authority chain: docs/design/README.md → emitter architecture → post-H1
schedule (§1 house rules; :60-66 exact observables; :169-172
resources; :1692-1705 before-merge) → h2-7b.md (§2 the band; §4 the
gap matrix G1-G12; §5 the W-H2.7B lane table; §6.2 this rung; §7
acceptance; §8 prohibitions; §9 items 2-9) → h2-7b-m-1.md (the frozen
band artifact; §9.6 the guard consequence) → this packet. Structural
precedents: h2-6c-m-2.md (the refusal lattice §2, the implementation-
time amendment §2-A, the manifest contract §4), h2-6b-m-2.md (the
adjacent-facet enumeration), h2-6a-ca-2.md §4-A.1 (the projection
trap), h2-7a-m-4.md (the `M4_ANCHORS` name@line + span-hash pin style).
Evidence base: the m-2 fact sheet (target/session-notes/7b/
h2-7b-m-2-factsheet.md, 2026-09-04) with the round-1 anchor
corrections applied (every anchor below is the 95cf0065 line; the
train re-verifies them at its cut), the m-1 artifact
`ratchets/h2-7b-qualification.v1.json` (first mint, PR #503), and the
vendored `_tsc.js` 6.0.3 spans cited inline.

## 1. Identity, purpose, deliverables, and order

- **Slice ID / kind:** `h2-7b-m-2`, `runtime`; Rust (emitter, checker
  resolver members, harness floor, xtask runner + boundary) + two
  oracle amendments (h2-7a-owner-inventory.mjs, h2-7a-close.mjs) +
  controls + docs.
- **Purpose:** (1) the acceptance runner that turns the frozen band
  into an executable, facet-exact comparison (`run_h2_7b`, §4);
  (2) the flip — the declaration member executed function-complete
  through the production path, independently of the JavaScript member
  (§5); (3) the first full post-flip sweep as the SOLE manifest
  baseline (§6); (4) the cross-slice consequences (§6.2-6.5); (5) the
  dormancy controls re-pinned per the G11 table, the H2.7a
  owner-inventory rows targeted at H2.7b dispositioned with Rust
  anchors, and the H2.7a close artifact's refusal surfaces re-frozen as
  `lifted-at-H2.7b` with span-hash pins (§5.11-5.13); (6) the hosted
  boundary += `run_h2_7b` in THIS train (§4.6; h2-7b.md §6.2/§6.4/§8
  restated in §10.1).
- **Deliverables (files; one writer each — §7):**
  `crates/xtask/src/h2_2c_acceptance.rs` (`run_h2_7b` + consts /
  validator / inputs / comparator / manifest helpers; the 27-slice guard
  + the activity-count assertion), `crates/xtask/src/main.rs` (the
  `h2-7b-acceptance` subcommand; `fn acceptance` += `run_h2_7b`),
  `.github/ci/qualification-policy.v2.json` (the main.rs +
  h2_2c_acceptance.rs pins), `crates/harness/src/upstream_suites/
  execution.rs` (`EmitOptionFloor::DeclarationFamily`),
  `crates/emitter/src/{lib.rs (the public re-exports: `PlanDeclarationPaths`
  + its constructor, `DeclarationPrintHandlers`, `GlobalNameOracle`,
  `JavascriptOmission` — the crate root keeps `declarations` private
  and an explicit re-export list, lib.rs:14 / :39-43), execute.rs,
  plan.rs, printer.rs, transform.rs (the handler carrier), resolver.rs,
  activity.rs, builtins.rs, builtins/target_bindings.rs (the
  reserved-name decision consults the oracle), artifact.rs,
  declarations/{mod.rs, orchestration.rs, paths.rs (new)}}`, `crates/checker/src/
  {declaration_emit.rs, lib.rs (the helper exposure), the
  CheckerSession resolver impl file}`, `crates/compiler/src/lib.rs`
  (the profile constructor), the control tests named in §5.11 and the
  focused tests of §9, `crates/oracle/h2-7a-owner-inventory.mjs` +
  `ratchets/h2-7a-owner-inventory.v1.json` (content re-mint: the
  H2.7b-targeted rows), `crates/oracle/h2-7a-close.mjs` +
  `.github/ci/contracts/h2-7a-close.schema.json` +
  `ratchets/h2-7a-close.v1.json` (the `lifted-at-H2.7b` disposition),
  the ledger rows for every new ported item,
  `ratchets/h2-7b-known-divergences.v1.json` (created only by the
  first post-flip sweep, only if non-empty), the shrunk
  `ratchets/h2-6c-known-divergences.v1.json` (and any 5h/6a/6b manifest
  that shrinks), this packet + the register + the README row + the
  h2-7b.md restatement.
- **Trusted base:** `440e6073` — the m-1 merge (PR #503; the train cut
  2026-09-04; every anchor re-verified at the cut).
- **FROZEN order (h2-7b-m-1.md §1 F13, extended):** repair (PR #502) →
  m-1 (PR #503) → **this rung** → the closure waves `h2-7b-w*` → ca.
  This rung ends with the manifest freeze; it closes NO divergence
  (the waves do); it lands NO local-CI phase (`h2-7b-oracle` landed at
  m-1 P1c) and NO transition landing (ca).

## 2. Frozen opening evidence — the m-1 artifact (measured; asserted at every build)

`ratchets/h2-7b-qualification.v1.json` (generator `f1750148…`,
fingerprint `77350621…`): 1,593 candidate rows = **ADMITTED 1,567**
(compiler 896 / conformance 475 / project 196) + **DEFERRED 26**
(compiler 25 + conformance 1; every first owner H2.7c:
`isolatedDeclarations: true` 21, `noEmitOnError: true` 2,
`stripInternal: true` 2, the standalone `emitDeclarationOnly` 1 =
`declFileEmitDeclarationOnlyError1.ts#default`). The runner executes
exactly the admitted rows twice; the deferred rows stay count-only.

### 2.1 Option shapes (admitted; merged declaration-family options)

| shape | rows | in-band consequence |
| --- | ---: | --- |
| compiler `{declaration}` | 753 | js + d.ts per unit |
| conformance `{declaration}` | 437 | js + d.ts per unit |
| project `{declaration, sourceMap}` | 128 | map + js + d.ts per unit; every one is a current 6c-manifest row (§6.2) |
| compiler `{declaration, emitDeclarationOnly}` | 73 | d.ts ONLY (js/map members absent, :116381-116382; the js path is `None` by plan.rs:313-317) |
| project `{declaration}` | 68 | js + d.ts |
| compiler `{declaration, removeComments:false}` / `:true` | 54 / 4 | the printer honours `removeComments` (:116675) |
| conformance `{declaration, emitDeclarationOnly}` | 38 | d.ts only |
| compiler `{declaration, sourceMap:false}` / `:true` / `{…, removeComments:false, sourceMap:true}` | 5 / 3 / 2 | map facets are H2.6c's |
| compiler `{declaration, isolatedDeclarations:false}` | 2 | explicit-false control |

`emitDeclarationOnly` combined rows = **111** (73 + 38), every one with
`declaration: true`. CORRECTION of h2-7b-m-1.md §2: the 10
`declarationMap: false` rows are all DEFERRED (they carry
`isolatedDeclarations: true`); the admitted band carries NO
`declarationMap` value — the explicit-false control class is
`isolatedDeclarations: false` (2 rows) only.

### 2.2 The pre-flip state of the admitted band (the opening census)

The plan refusal `Unsupported(Declaration)` (plan.rs:208-212) never
surfaces today: the H2.6c flip made execute.rs:629-633 TOLERATE it for
`declaration == Some(true)` units (the js/map members execute, the
declaration member is silently absent), and the six zero-member
controls plan no unit at all. Therefore the frozen opening census the
runner must reproduce BEFORE the flip is:

| pre-flip class | rows | evidence |
| --- | ---: | --- |
| typed option refusal `UnsupportedCompilerOption { option: "emitDeclarationOnly" }` (execute.rs:112-115, reached BEFORE any plan validation) | 111 | the combined rows of §2.1 |
| executed js/map, declaration member absent (the tolerate arm) — exact or divergent on the js/map facets, DIVERGENT on the missing `.d.ts` writes | 1,450 | 1,567 − 111 − 6; the exact/divergent split is MEASURED at the opening commit (a log, never a baseline) |
| zero-member controls (`no-emit-eligible-source`; no unit planned) | 6 | exact today (no write, `emitSkipped` false) |
| H2.7b activity | 0 | on every row and both repetitions (the dormancy proof) |
| deferred | 26 | count-only |

TO-VERIFY(opening): the 111 / 1,450 / 6 partition and the zero
activity are asserted by the runner's `--pre-flip` mode (§4.7).

### 2.3 The 34 `emitSkipped` rows — two classes, not one

| class | rows | facet |
| --- | ---: | --- |
| transform-blocked (`declBlocked` by the declaration transform's diagnostics) | 29 (compiler 19 / conformance 10 / project 0 — frozen, asserted by the runner) | 47 diagnostics in `emit_result.diagnostics` — codes 2883 ×14, 4094 ×14, 2527 ×5, 7056 ×4, 4023 ×3, 4025 ×2, 4118 ×2, 4032 ×1, 4081 ×1, 5088 ×1; the JS member of the unit still writes when it is not itself blocked; the `.d.ts` write is ABSENT |
| preflight-blocked (the output-path collision `verifyEmitFilePath`) | 5 (compiler 3 / conformance 0 / project 2): `declarationFileOverwriteError`, `jsFileCompilationErrorOnDeclarationFile`, `jsFileCompilationWithDeclarationEmitPathSameAsInput`, the two project `jsFileCompilationSameNameDtsNotSpecified` variants | TS5055 in `reported_diagnostics` (compared through the reported-diagnostics facet), `emit_result.diagnostics` EMPTY; writes 1 / 5 / 1 / 0 / 0 — the members block INDEPENDENTLY: a unit can write its `.d.ts` while its js write is blocked (`jsFileCompilationErrorOnDeclarationFile`: `c.d.ts` written, no `c.js`) |

Upstream invokes the two members independently
(`emitSourceFileOrBundle` :116555-116562: `emitJsFileOrBundle` then
`emitDeclarationFileOrBundle`; each computes its own block). Rust today
`continue`s the WHOLE unit when the JS path is blocked (execute.rs:691)
— the flip restructures the loop (G1). Every in-band declaration write
carries `data.diagnostics: []` (a blocked unit never writes), so the
full-diagnostics facet (F11) lives in `emit_result.diagnostics` for the
29 rows and in `reported_diagnostics` for the 5 rows; the comparator
still compares the write callback's `data.diagnostics` field by field.

### 2.4 Adjacency table — machine-asserted at the opening commit (h2-6b-m-2 discipline)

| adjacent predicate / population | admitted value | assertion |
| --- | ---: | --- |
| emit-eligible JSON sources (`!isJsonFile` :116647 ↔ plan.rs:332 `!is_json`) | 0 | none planned; the `d.json.ts` arm (plan.rs:524) unreachable in band |
| JS/JSX source cases / files (`allowJs` 67 rows; `checkJs: true` 42; no `checkJs: false` in band) | 68 / 80 (compiler+conformance `files` rows 66 / 78 + the project `analyzed_files` 2 / 2) | the declaration transformer runs over JS sources |
| live pre-pass rows (G4: files with a `@ts-nocheck` directive — TS files included, :18898-18899; no `checkJs: false` row exists) | 3 admitted TS cases (the reviewer's and the operator's fixture scans agree); REPORT-ONLY — printed by the sweep, never a validator constant | the fourth disjunct fires in band; these rows are the adjacency evidence that H2.7b does not re-own the H2.8c no-check pipeline |
| TSX/JSX cases | 4 | the `jsx` write kind (2 writes) |
| declaration write extensions (`d.ts` / `d.mts` / `d.cts`, plan.rs:519-526) | 2,282 / 66 / 62 | the mjs/cjs path arms are exercised |
| module states / target states | 15 / 8 | as recorded in `summary` |
| output-path collision rows (TS5055) | 5 | §2.3; the G12 frozen evidence |
| `resolveJsonModule` (plan.rs:428-438) | 0 | |
| `noEmitOnError` (execute.rs:643) | 0 admitted (2 deferred) | |
| `declarationMap: false` | 0 admitted (10 deferred) | §2.1 correction |
| `emitBOM: true` / `newLine` set / `removeComments: true` | 1 / 1,567 / 4 | BOM + newline facets |
| every later-owned option (`composite`, `outFile`, `outDir`, `rootDir`, `noEmit`, `noCheck`, `declarationDir`, `stripInternal`, `isolatedDeclarations: true`) | 0 | the floor never drops an executed row's option (§4.3) |
| planned declaration members (emit-eligible, non-declaration, non-JSON sources) | **2,445** = 2,049 over 1,371 compiler/conformance cases (their `files` rows) + 396 over 190 project cases (their `project_input.analyzed_files` — the MOUNTED program, not `root_selection`, which sees only 196; per-case 0 ×6 / 1 ×40 / 2 ×94 / 3 ×56) with the 6 controls at 0 | the H2.7b activity expectation (§4.4) |
| `EmitMode` | every unit `Script` | |

### 2.5 The 6c manifest join

Of the 451 rows in `ratchets/h2-6c-known-divergences.v1.json`, **133
are in the admitted band** (project 128 + compiler 5) — the
`run_h2_6c` re-sweep shrink ceiling (§6.2); the 24 declaration-bearing
rows outside the band (H2.7d 18 / H2.7e 5 / both 1) and the 9
non-declaration `outFile` rows become typed refusals under the §6.4
projection (33 designed transitions); every other row stays.

## 3. The refusal lattice and the unmasking analysis (h2-6c-m-2 §2-A)

| predicate (anchor) | admitted rows it governs after the flip | disposition |
| --- | ---: | --- |
| execute.rs:629-633 "tolerate `Unsupported(Declaration)`" | 1,561 units-bearing rows | **REPLACED** by execution (G1) — the reason the plan refusal never showed |
| printer.rs:1051-1053 `PrintRequest::Declaration(_)` → `PrinterError::Unsupported(Declaration)` | the 2,410 printer-bound declaration members (2,445 planned − 31 transform-blocked members across the 29 transform-blocked rows − 4 declaration-path-blocked members across the 5 collision rows; = the artifact's `declaration_writes`) | **LIFTED** (G3, §5.3) through the declaration-only entry; `PrintRequest::SourceFile`'s declaration-syntax default and `PrintRequest::Bundle` stay refused (RETAINED controls, §5.11) |
| plan.rs:208-212 `paths.declaration.is_some()` → `Unsupported(Declaration)` | 1,561 | **LIFTED** |
| execute.rs:112-115 `emit_declaration_only == Some(true)` → `UnsupportedCompilerOption` | 111 | **LIFTED for `emit_declaration_only && getEmitDeclarations(options)`** (= `declaration \|\| composite`, plan.rs:332 mirrors :18151-18156); the standalone shape KEEPS the typed refusal with the SAME text (0 rows in band; the deferred row + the ca control 3 are its controls; :124946-124950 is H2.7c's) |
| plan.rs:221-225 `paths.javascript.is_none()` → `Contract(ScriptOutputMissingJavaScriptPath)` | 111 (js path `None` by plan.rs:313-317) | **RELAXED through typed provenance**: `validate_bootstrap_shape(&self)` (plan.rs:177) sees no options, so the plan arm :313-317 records the cause on the unit (`EmitOutputUnit.javascript_omitted: Option<JavascriptOmission>` = `EmitDeclarationOnly`) and the validator relaxes :221-225 ONLY when that cause is present; the identical path shape without the cause stays `ScriptOutputMissingJavaScriptPath` (paired controls; the residual arm is a `retained_arms` pin, §5.14) |
| execute.rs:688-691 the JS-path requirement / the whole-unit `continue` on a blocked js path | 111 + the 5 collision rows | **RESTRUCTURED**: the js member and the declaration member each decide their own block (:116555-116562, :116694-116697); a unit without a js path executes only its declaration member |
| the adjacent predicates of §2.4 (JSON filtering, JS/JSX sources, the extension arms, the collision preflight, `resolveJsonModule`, `noEmitOnError`, `EmitMode`) | as tabled | **NAMED + machine-asserted**; none is lifted or widened |
| execute.rs:111 `declaration_map == Some(true)`; :122 `composite`; :120 `strip_internal`; :141 `declaration_dir`; :139-147 `outFile`; plan.rs:213-217 / :218-220 / :190 (`DeclarationOnly`) / :195 (`BuilderSignature`) | 0 in the 7b band; in the 6c runner the 24 later-owned rows now REACH the `outFile` / `declarationMap` refusals (§6.4) | RETAINED |
| the harness floor's silent drops (execution.rs:903-930) | — | `emitdeclarationonly` LEAVES the drop chain (§4.3); the others stay dropped AND the runner asserts no admitted row carries them |

Expected post-flip outcome in the admitted band: **refusals 0**,
TO-VERIFY(flip). After the flip the runner FAILS (before the ratchet
join) on ANY typed refusal and on ANY other error — a residual refusal
is never a manifest row (§4.2); it stops the train until its exact
condition has a frozen band and is named in an implementation-time
amendment of this section.

## 4. The opening commit — `run_h2_7b` and the boundary

The `run_h2_6c` clone (h2_2c_acceptance.rs:4012-4059) with these named
deltas and nothing else structural:

1. **Consts + validator.** `H2_7B_QUALIFICATION_RELATIVE_PATH`,
   `H2_7B_KNOWN_DIVERGENCES_RELATIVE_PATH`
   (`ratchets/h2-7b-known-divergences.v1.json`),
   `H2_7B_WRITE_DIVERGENCES_ENV` (`TSRS_H2_7B_WRITE_DIVERGENCES`,
   path-valued — §4.8), `H2_7B_DIVERGENCE_OWNER =
   "h2-7b-m-2-divergence-closure"`, the generator / contract /
   fingerprint pins; `validate_h2_7b_qualification` = the 6c `!=` chain
   (:3525-3595) over the 7b artifact: header (`schema` 1, `kind`,
   `status`, `phase`), `typescript` 6.0.3, every `selection_contract`
   denominator (2456 / 1593 / 1 / 1567 and the per-suite tables), every
   `summary` const the schema pins (`admitted_cases` 1567,
   `deferred_cases` 26, `typescript_runs` 3134,
   `deterministic_typescript_cases` 1567, `declaration_writes` 2410,
   `emit_refused_cases` 34, `no_emit_eligible_source_cases` 6, the
   negative facts 0, …), `cases.len() == 1593`, PLUS the §2.4 adjacency
   constants derived from the artifact rows (the Rust mirror of the
   7-layer count surface) — EXCEPT the live pre-pass row count, which is
   a report-only figure (§5.4), never a validator constant.
2. **Refusal handling (typed, never text).** The 6c clone classifies
   any error whose text contains `"unsupported emit compiler option"`
   as an `emit_refused` facet (h2_2c_acceptance.rs:3741-3751). The 7b
   runner matches the TYPED error only:
   `DriverError::Emit(EmitFailure::UnsupportedCompilerOption { option:
   "emitDeclarationOnly" })` is accepted ONLY in `--pre-flip` mode for
   the frozen 111 rows; in normal mode every typed refusal and every
   other error fails the run before the ratchet join (`emit_refused` in
   the manifest row shape stays for schema continuity and is asserted
   false on every manifest row; `--pre-flip` BYPASSES the manifest
   writer entirely — it prints a census report and never produces a
   manifest, scratch or canonical).
3. **The option floor.** `EmitOptionFloor::DeclarationFamily` =
   EXACTLY `MapFamily` + `emitdeclarationonly` projected
   (`compiler_options.emit_declaration_only = Some(bool)`): the arm
   moves out of the silent-drop chain (execution.rs:905) into a
   floor-gated arm beside :867-902; `declaration` / `composite` /
   `isolateddeclarations` already project (:855-857); `declarationdir`
   / `stripinternal` / `declarationmap` stay dropped and the runner
   asserts the admitted row's effective options contain none of them.
   Project rows use the recorded plans' projection unchanged
   (project.rs:379-395).
4. **Execution per admitted case + the activity assertion.** Two Rust
   emits (determinism, 6c :3758-3768) through the production entry
   (`emit_with_command_outcome` / `emit_files_with_activity`, never the
   harness seam); the **27-slice guard** (:3775-3800 + `H2_7b`) PLUS the
   per-case **expected-count assertion**: `activity.runtime_slice(H2_7b)
   == expected_declaration_members(case)` on BOTH repetitions, where
   the expectation is derived from the frozen inputs (compiler /
   conformance: the case's `files` rows with `emit_eligible &&
   !declaration_file && !json`; project: the case's
   `project_input.analyzed_files` rows — the mounted program — with the
   same filter; never `root_selection`), the aggregate 2,445 (2,049 +
   396) asserted per repetition (§2.4), and 0 for the six controls and
   for every unadmitted slice — on SUCCESSFUL outcomes only: a refused
   row never returns counters (the driver drops the activity canary on
   error, compiler/src/lib.rs:834-840), so in `--pre-flip` mode the
   runner asserts 0 on the 1,450 executed rows and the 6 controls, and
   the 111 refused rows' zero activity is the STRUCTURAL proof of §6.4b
   (option validation precedes any observation, execute.rs:155-158 /
   :624-625) pinned by one direct canary test. The facets: **writes** through the exact comparator
   (§4.5), **reported diagnostics**, **emit_result** (`emit_skipped`,
   every diagnostic field, source maps), the **emitted-files
   projection** (§4.6), **refusal** (§4.2). The 26 deferred rows return
   count-only and never enter the manifest.
5. **The exact write comparator (F11).** One shared helper modeled on
   `assert_exact_writes` (:359-424) comparing, per write in CALLBACK
   ORDER: path; callback bytes; BOM (`write_byte_order_mark`);
   **artifact kind through the frozen normalization table** — oracle
   `javascript` / `mjs` / `cjs` / `jsx` → `EmitArtifactKind::JavaScript`
   with the path suffix asserted per oracle kind (`.js` / `.mjs` /
   `.cjs` / `.jsx`-target), `source-map` → `JavaScriptMap`,
   `declaration` → `Declaration` (`.d.ts` / `.d.mts` / `.d.cts`
   asserted); **ordered `sourceFiles` with `Option` presence preserved**
   (absent ≠ empty; the 6c helper collapses them at :403 — not
   inherited); **on-error presence**: the oracle records
   `on_error_callback_present: true` on all 5,000 writes and Rust has no
   callback slot, so the expected flag is asserted `true` on every
   oracle write and the Rust analogue is proven STRUCTURALLY by a
   focused test — a failing declaration sink yields the
   `Could_not_write_file_0_1` diagnostic (execute.rs:909-919) and the
   path still enters the emitted-files projection (:872-880 ↔
   upstream's `emittedFilesList` push after `writeFile` :116803);
   data presence; `sourceMapUrlPos`; **every `data.diagnostics` field**
   (code, category, file, start, length, message). Materialized bytes
   as today. Never the count-only 6c facets (:2382-2399, :2775-2804).
   **Divergence identity (F21/F27):** the comparator also emits a
   canonical per-case mismatch vector over EVERY exact facet — missing
   / extra writes and every compared write field; reported-diagnostic
   identity and order; `emitSkipped`; every `emit_result` diagnostic
   field; the emitted-files projection presence and order; source-map
   presence and content; status writes; exit code; the refusal variant
   — each mismatching element as its facet name plus the sha256 of the
   normalized OBSERVED Rust value — STORED on the manifest row as the sorted `mismatch_vector` (each
   element `<profile>:<facet>:<index>:<field>=<sha256 of the normalized
   observed value>`) and hashed into `facet_fingerprint_sha256`. The
   shared vectorizer has TWO frozen profiles: `H2_7b` = the full
   comparator vocabulary above; `H2_6c` = the 6c runner's EXISTING
   legacy facet surface exactly as it compares today (path, callback
   bytes, BOM, data presence, `sourceMapUrlPos`, the count-only
   `data_diagnostics_count` — absent diagnostic CONTENTS are represented
   as the count, never as an empty list —, the oracle kinds present in
   the 6c artifact incl. `other` (the JSON-copy write) mapped to the 6c
   comparator's own kind vocabulary, reported diagnostics, `emit_result`,
   refusal variant); a focused test covers every oracle kind present in
   the 6c artifact and every 7b kind (§6.4c1 uses the `H2_6c` profile); the 7b manifest row = the 6c row
   shape (`case_id`, `owner`, `writes_diverging`, `diagnostics_diverging`,
   `emit_result_diverging`, `emit_refused`) + `refused_option` +
   `mismatch_vector` + `facet_fingerprint_sha256`, and normal mode
   compares the vector EXACTLY: a listed row whose divergence changes
   identity (one mismatch exchanged for another at the same count)
   FAILS the run — only a write-mode regeneration whose new vector is a
   STRICT SUBSET of the old one (§6.2) can move it.
6. **Write order vs the listing projection (F9 / G6).** Two different
   orders, both exact: the CALLBACK stream is `map → js → d.ts` per
   unit (upstream writes the map before the js :116784-116801; the
   frozen `emitBOM` row records map, js, declaration); the
   `emittedFiles` LISTING is `js → js.map → d.ts` per unit
   (:116633-116638, :116803). The runner compares callbacks in stream
   order and derives the listing projection from the oracle write
   stream; it ALWAYS enables the Rust listing (`list_emitted_files`)
   and asserts it equal to the projection (Rust already separates the
   two orders, execute.rs:671-675). A mixed `Written` /
   `SkippedUnchanged` control proves declaration membership and
   ordering in the listing (§9).
7. **`--pre-flip` mode and totals.** `cargo xtask h2-7b-acceptance
   --pre-flip` accepts the frozen opening census of §2.2 (111 typed
   refusals — zero activity by the structural proof; the 1,450 executed
   rows compared on the js/map facets with the missing-`.d.ts`
   divergences counted, never written, activity asserted 0; 6 controls
   exact, activity 0) and prints it; normal mode: `exact + diverging ==
   1567`, `deferred == 26`, `candidates=1593`, `repetitions=2`, the
   per-suite report (the `report_h2_6c_suite_outcomes` shape) incl. the
   declBlocked and declaration-only counts.
8. **Manifest contract (path-valued; F11-6c corrected).** The 6c
   helpers are presence-only and always write the canonical path
   (:4017, :3915-3918) and leave an existing manifest in place on a
   zero-divergence write (:4035-4040). The 7b contract:
   `TSRS_H2_7B_WRITE_DIVERGENCES=<path>` is a validated absolute path;
   exploratory sweeps (dry runs of a wave) write to a scratch path —
   `--pre-flip` never writes (§4.2); the canonical path (`ratchets/h2-7b-known-divergences.v1.json`)
   is written ONLY when the variable names it exactly, and the runner
   requires the canonical target to be ABSENT before the first baseline
   write; write mode with an empty divergence set REMOVES the named
   target if it exists (so a shrink to zero leaves no file); the
   load-side rules are the 6c ones (:3848-3894: schema 1, non-empty,
   owner pinned, ≥ 1 facet per entry, no duplicates) plus the
   fingerprint presence, and the shared `h2_slice_ratchet_join`
   (:2643-2691) extended with the fingerprint equality enforces
   shrink-only. Three
   temp-directory tests cover absent / scratch / canonical-empty.
9. **The boundary (the repair guard; h2-7b-m-1.md §9.6).** The train
   that defines `pub fn run_h2_7b` MUST call it inside `fn acceptance`
   (`hosted_acceptance_and_oracle_phases_cover_h2_2c_rungs`,
   ci_lane_tests.rs:188). This rung lands the `h2-7b-acceptance`
   subcommand (main.rs :96 / :4550 shape) AND `fn acceptance` +=
   `h2_2c_acceptance::run_h2_7b(&workspace)` after `run_h2_6c` (:4416),
   with the policy pins re-recorded. In-train sequencing: the opening
   commit and the flip commit are made BEFORE the first push that opens
   the PR, so the hosted boundary never executes a pre-flip runner; the
   pre-flip census is a local, logged measurement.

## 5. The flip commit — G1-G12 (anchors at 95cf0065; upstream 6.0.3 spans)

Every ported item carries the ledger triple (the declarations/root.rs
:17-19 shape); the ledger check counts them.

1. **G1 — the flip seam and member independence (execute.rs).** The
   tolerate arm :629-633 is REPLACED; the unit loop (:682-861) becomes
   two independent members per unit, in upstream order
   (:116555-116562): the js member (js path present; its own
   `is_emit_blocked(js)` sets `emit_skipped` and skips ONLY itself) then
   the declaration member (`declaration_path` present; its own
   `declBlocked`, §5.4). The declaration artifact:
   `EmitArtifact::declaration(path, callback_text, write_byte_order_mark
   = emit_bom, source_files = the unit's ordered source paths,
   EmitTextMetadata::new(transform_diagnostics, None))`
   (artifact.rs:110-125); the sink order per unit map → js → d.ts;
   `unit_listing` (execute.rs:675) gains the declaration slot and the
   listing is js → map → d.ts (:885-896 extended; `SkippedUnchanged`
   excluded at :871 — `skippedDtsWrite` mapped onto the existing
   disposition, F9; no new metadata field).
2. **G2 — the refusal lifts.** Exactly the §3 rows: plan.rs:208-212
   removed; execute.rs:112-115 narrowed to `emit_declaration_only ==
   Some(true) && !(declaration == Some(true) || composite ==
   Some(true))` with the SAME `UnsupportedCompilerOption { option:
   "emitDeclarationOnly" }` for the standalone shape; plan.rs:221-225
   relaxed only on the typed `javascript_omitted ==
   Some(EmitDeclarationOnly)` provenance (§3); the loop restructured
   (G1). Every other refusal byte-retained (plan.rs:190, :195, :213-220;
   execute.rs:111, :120, :122, :141).
3. **G3 — the printer.** `PrintRequest::Declaration(source)` is
   ACTIVATED at printer.rs:1051-1053 through a NEW declaration-only
   entry, frozen here: `Printer::print_declaration(result:
   &mut TransformationResult, source: TransformSourceId, handlers:
   DeclarationPrintHandlers<'_>)` where `DeclarationPrintHandlers {
   has_global_name: &dyn GlobalNameOracle }` with
   `GlobalNameOracle::has_global_name(&self, name: &str) ->
   Result<bool, EmitResolverError>` — FALLIBLE like every resolver
   member (resolver.rs:675: fail-closed, `Result`-valued), implemented by
   the `EmitResolver` (G5); an `Err` propagates through the generated-
   name / reserved-name decision (transform.rs:881 → builtins/
   target_bindings.rs:741) into `PrinterError` / `EmitFailure` with NO
   default — an unavailable oracle fails before any declaration write
   (a fault-injection test proves it end to end); `create_printer(options)`
   and the JS lane's `print` path are unchanged; the handler lifetime
   is the one declaration file (a printer per declaration member, as
   upstream :116686). The `hasGlobalName` CONSULT SITE is the
   file-level uniqueness decision `isFileLevelUniqueNameInCurrentFile`
   (:120664-120666 → `isFileLevelUniqueName(currentSourceFile, name,
   hasGlobalName)`) reached from `isUniqueName` (:120638-120640) in
   the generated-name machinery; its Rust counterpart consults the
   oracle when printing a declaration file and stays `true`-by-default
   (no oracle) on the JS lane (:116624 is the JS lane's INSTALL site,
   untouched — F16). The transform hooks (`onEmitNode =
   emitNodeWithNotification`, `isEmitNotificationEnabled`,
   `substituteNode`, :116689-116692) are the `TransformationResult`
   hooks the printer already consumes (transform.rs:901-961). Printer
   options (:116674-116685): `removeComments`, `newLine`, `noEmitHelpers:
   true`, `target`, `sourceMap: false` (upstream: `declarationMap`, 0 in
   band), `onlyPrintJsDocStyle: true`, `omitBraceSourceMapPositions:
   true`, plus `with_declaration_syntax(true)` — the m-4 L2 block
   (declaration_transformer_replay_decision_equal.rs:885-892) is the
   model; the four upstream fields `PrinterOptions` cannot represent
   (`module`, `moduleResolution`, `inlineSourceMap`,
   `extendedDiagnostics`) are each DISPOSITIONED at implementation:
   every read site of the field inside `createPrinter` is located in
   the vendored printer; a site reachable from declaration printing is
   ported, an unreachable one is recorded `proven-unobservable` with
   the grep evidence in the register (the L2 lane's 116 byte-exact
   cases are the corroborating evidence, never the proof). The ONE
   production `with_declaration_syntax(true)` site is added to the
   m-3.5 allowlist (h2_7a_m35_controls.rs:31); `PrintRequest::SourceFile`
   keeps its default declaration-syntax refusal
   (declaration_printer_reprint_contract.rs:347-352); `Bundle` stays
   refused.
4. **G4 — orchestration function-complete.** A production entry in
   `crates/emitter/src/declarations/` (`emit_declaration_unit`; the
   harness functions are RETAINED as the L1/L2 bridge and stay absent
   from execute.rs, h2_7a_m4_controls.rs:205-208) porting
   `emitDeclarationFileOrBundle` :116640-116715 end to end: the guard
   (`emitOnly === Js` return; no declaration path → `emitSkipped` iff
   `emitOnly || emitDeclarationOnly`, :116641-116645); `filesForEmit =
   filter(sourceFiles, isSourceFileNotJson)` (:116647); the
   `collectLinkedAliases` pre-pass PER FILE under the EXACT four-disjunct
   predicate `(emitOnly && !getEmitDeclarations(options)) ||
   options.noCheck || emitResolverSkipsTypeChecking(emitOnly,
   forceDtsEmit) || !canIncludeBindAndCheckDiagnostics(sourceFile,
   options)` (:116649-116653) — the first three are false in band
   (`emitOnly` / `forceDtsEmit` absent, `noCheck` 0), the FOURTH is LIVE
   for JS/JSX files with `checkJs: false` or a `@ts-nocheck` directive
   (:18898-18905: TS/TSX/External → true; JS → `isPlainJs || isCheckJs`,
   where `isPlainJsFile` needs `checkJs === undefined`); Rust's exact
   helper exists privately (checker/src/lib.rs:561
   `can_include_bind_and_check_diagnostics(javascript_file, directive,
   options)`) and is EXPOSED to the emitter as an `EmitResolver` member
   `can_include_bind_and_check_diagnostics(&self, source: SourceFileId)
   -> Result<bool>` (the checker owns script kind, the per-file check
   directive, and `checkJs`); the nested walker (:116716-116735:
   export assignments with identifier expressions → `resolver.
   collectLinkedAliases(expression, true)`, export specifiers →
   `(propertyName || name, true)`, else `forEachChild`) ported
   function-complete; focused tests cover TS, plain JS, `checkJs` true
   / false, `@ts-check`, `@ts-nocheck` (true/false branches), and the
   sweep REPORTS the in-band pre-pass file count (report-only; the
   gating evidence is the byte-exact `.d.ts` + diagnostics);
   `get_declaration_transformers` with `DeclarationCustomTransformers::none()`;
   the transform; diagnostics → emitter diagnostics (:116663-116667);
   `declBlocked = diagnostics.len() != 0 || host.isEmitBlocked(dtsPath)
   || options.noEmit` (:116668; the orchestration.rs:99-109 order);
   `emitSkipped ||= declBlocked` (:116669); if `!declBlocked`: the
   `transformed.length === 1` ASSERT (:116671 — orchestration.rs:110-112's
   `roots().first()` becomes a fail-closed contract error,
   `TransformError::UnsupportedCompilerOption { option: "declaration
   transformer contract", detail }` per the m-4 pattern), the printer
   (G3), the artifact + write (G1); `dispose()` (transform.rs:953-961)
   on every path.
5. **G5 — the resolver members.** `EmitResolver` (resolver.rs:675-1322)
   and `EmitResolverMethod` (:418-462) gain `has_global_name(&self, name:
   &str) -> Result<bool, _>` (:88396-88398 `globals.has(
   escapeLeadingUnderscores(name))` — a NEW checker worker),
   `collect_linked_aliases(&self, node: EmitResolverNode, set_visibility:
   bool) -> Result<Option<Vec<EmitResolverNode>>, _>` (the resolver's
   `{ source, node }` convention, resolver.rs:370, validated at
   checker/emit.rs:63 — every returned declaration carries its OWNING
   `SourceFileId`, since :55698 reaches declarations across files;
   forwarding declaration_emit.rs:1493, :55675-55727), and `can_include_bind_and_check_diagnostics(&self,
   source) -> Result<bool, _>` (G4), all unavailable-by-default
   (`UnavailableEmitResolver` refuses) with `CheckerSession`
   implementations; 43 → 46 members; the `GlobalNameOracle` impl for
   the resolver (G3).
6. **G6 —** §4.6 / G1 (the two orders, `SkippedUnchanged`,
   `emit_skipped ||= declBlocked` per unit).
7. **G7 — the ledger triple** on `declaration_output_path`
   (plan.rs:512-531; `getDeclarationEmitOutputFilePath` :16580-16591)
   and on every new ported item.
8. **G8 — admission.** `h2_7b_profile()` (activity.rs after :483) ORs
   the `H2_7b` bit onto `h2_6c_profile()`; ALL FIVE production
   constructor sites move (compiler/src/lib.rs:834; execute.rs:264 and
   :309; builtins.rs:108 and :123 — they must agree, else the admitted
   bitmask differs per entry); `h2_6c_profile()` stays `#[doc(hidden)]`
   as the chain's history; the 6c runner's guard and floor are amended
   in the same train (§6.4); a control asserts `H2RuntimeSlice::H2_7b.
   index()` is admitted in exactly one function (`h2_7b_profile`) and
   that no production `h2_6c_profile()` call remains outside the
   profile chain; `observe_runtime_slice(H2_7b)` once per executed
   declaration member (the §4.4 expectation), beside the js-lane
   observations (:752-761).
9. **G9 —** §4 (the runner, the floor, the comparator, the projection).
10. **G10 — the production path resolver (ownership decided).** ONE
    emitter-owned implementation `crates/emitter/src/declarations/
    paths.rs::PlanDeclarationPaths` (lifted from
    declaration_transformer_replay_decision_equal.rs:1919-1966:
    `declaration_file_path` → `EmitOutputPaths::declaration_path`;
    `reference_target_path` → declaration → javascript → source),
    constructed once per emit from the preflight plan and PUBLICLY
    re-exported from the crate root (`tsc_emitter::PlanDeclarationPaths`
    with its constructor; lib.rs:39-43 lists the re-exports explicitly);
    the replay test IMPORTS it and its private copy is deleted (no
    second behavioural copy); a focused test pins the fallback order.
11. **G11 — the controls table (assertion-level).** LIFTED (edited and
    re-pinned, exactly these): output_plan_contract.rs:149-152 (the
    `with_declaration` tuple inside the :148-166 loop; the other tuples
    stay); h2_7a_m4_controls.rs:179-189 (the `Unsupported(Declaration)`
    plan assertion) and :191-197 (the printer refusal literal);
    printer_foundation_contract.rs:697-702; declaration_printer_
    reprint_contract.rs:347-352 ("Declaration stays unsupported");
    h2_7a_ca_controls.rs:184-189 (control 3 gains the COMBINED-shape
    admission; its standalone assertion STAYS); the m-3.5 allowlist
    (h2_7a_m35_controls.rs:31) gains the ONE production site; the H2.7a
    inventory rows of §5.12. RETAINED (a red one is a defect, never an
    amendment): h2_7a_m4_controls.rs:166-177 (the bundle-root plan
    refusal) and :198-208 (the execute literals incl. the ABSENCE of
    `transform_declaration_unit_for_harness` in execute.rs);
    output_plan_contract.rs:123-146 (the API / builder / build-info mode
    loop) and :153-165 (`declarationMap` + `buildInfo`);
    printer_foundation_contract.rs:690-696 (`Bundle`); the `SourceFile`
    declaration-syntax default; the ca controls 1-2 (H2_7a never
    admitted); every non-goal option refusal.
12. **G12 — the declaration-output collision preflight
    (`modified-requalify`, the declaration arm ONLY; frozen evidence =
    the 5 TS5055 rows of §2.3).** plan.rs:401-464 already scans
    `[javascript_path, declaration_path]` (:439-443); the
    requalification ports `verifyEmitFilePath`'s arm behaviour exactly
    (:125015-125045): the `!options.noEmit && !options.suppressOutputPathCheck`
    gate (`suppressOutputPathCheck` is absent from the option surface →
    a typed absent control); the JS check SUPPRESSED under
    `emitDeclarationOnly` (:125022-125024) while the declaration check
    is always on when `getEmitDeclarations`; the canonical-case key
    (`toFileNameLowerCase` unless `useCaseSensitiveFileNames`,
    :125043) made explicit at this seam; the tsconfig-hint chain
    (:125033-125038) and the overwrite chain (:125040); the diagnostic
    ORDER; the propagation `isEmitBlocked(declarationFilePath)` →
    `declBlocked` (:125418-125420 → :123592 → :116668). The base
    collision matrix (js/map, outDir/rootDir) stays H2.8a.
13. **The H2.7a owner inventory — the eight H2.7b-targeted rows.**
    `ratchets/h2-7a-owner-inventory.v1.json` carries 8 rows with
    `target_rung: H2.7b` and `rust_anchor: null` (the generator's
    `orchestrationUseSpecs`, h2-7a-owner-inventory.mjs:1209-1215, and
    the option-owner rows): `getOutputPathsFor` (shared/path),
    `product:declaration`, `rejected-option:declaration`,
    `rejected-option:emitDeclarationOnly` (option-owner),
    `hasGlobalName` :116688 (shared/printer-hook), `collectLinkedAliases`
    :116719 and :116727 (declaration-orchestration), and
    `markLinkedReferences` :116741 (declaration-orchestration). The
    generator amendment (a machine amendment, the M4_ANCHORS name@line
    precedent): every H2.7b-targeted row that this rung ACTIVATES gains
    a header-verified `rust_anchor` (`getOutputPathsFor` →
    plan.rs `get_output_paths_for`; `product:declaration` → the
    declaration member; the two `rejected-option` rows → the lifted
    execute/plan sites; `hasGlobalName` :116688 → the G3 consult site;
    the two `collectLinkedAliases` sites → the G4 walker), keeping
    `target_rung: H2.7b` as the landing rung and the inventory's
    existing `disposition` / `reachability` / `partition` vocabulary
    (no new enum value — "landed" is expressed by a non-null anchor);
    the `markLinkedReferences` rows are DECIDED here: both :116597 (the
    `emitJsFileOrBundle` call) and :116741 (the helper body's
    `resolver.markLinkedReferences(n)`) become `script-orchestration`
    with `target_rung: H2.8c` and consumers `["H2.8c"]` — the traversal
    is the no-check pipeline's (Rust says so at
    checker/src/modules.rs:9216; the schedule :1576 assigns the smaller
    linked-reference / no-check pipelines to H2.8c); :116741 leaves the
    H2.7b anchor denominator, and the generator asserts the remaining
    SEVEN H2.7b-targeted rows are anchored. The admitted `@ts-nocheck`
    rows (§2.4) are the current-band adjacency evidence that H2.7b does
    not silently re-own that pipeline. The inventory re-mint is
    CONTENT-bearing (not pin-only); the close artifact follows (§5.14).
14. **The H2.7a close artifact — `lifted-at-H2.7b` with span-hash
    pins.** h2-7a-close.mjs:639-662 pins plan.rs / execute.rs /
    activity.rs by whole-file sha256 and the printer by the refusal
    span; the first m-2 byte fails its `--check`. The machine
    amendment: each `refusal_surfaces` entry gains `disposition` ∈
    {`retained`, `lifted-at-H2.7b`}; a lifted entry replaces the
    whole-file baseline by `retained_arms[]` — name@line +
    function-span sha256 pins (the M4_ANCHORS header-verified style)
    of the exact RETAINED arms: `validate_bootstrap_emit_options`
    (execute.rs) with its standalone `emitDeclarationOnly` /
    `declarationMap` / `stripInternal` / `composite` / `declarationDir`
    arms, `validate_bootstrap_shape` (plan.rs) with `DeclarationMap` /
    `BuildInfo` / `DeclarationOnly` / `BuilderSignature` and the
    residual `ScriptOutputMissingJavaScriptPath` arm (the js-less shape
    without the typed cause), the
    `H2RuntimeSlice` enum + `h2_7b_profile` as the ONLY admission of
    `H2_7b` (activity.rs), the printer's `Bundle` refusal arm — each
    asserted as one live occurrence inside the named function (never a
    comment or a dead duplicate); `summary.refusal_surfaces_retained`
    4 → 0, `refusal_surfaces_lifted` 4 (schema amended; 22 contracts
    unchanged); the behaviour tests of §5.11 remain the semantic pins;
    the `runtime_contract` block stays frozen history; the walk
    re-mints the close artifact after the inventory.

## 6. The first sweep, the manifest, and the cross-slice consequences

1. **The first full post-flip sweep** (`TSRS_H2_7B_WRITE_DIVERGENCES=
   <canonical path> cargo xtask h2-7b-acceptance`) is the SOLE manifest
   baseline; `ratchets/h2-7b-known-divergences.v1.json` is created only
   if ≥ 1 admitted row diverges; exact / diverging TO-VERIFY(flip),
   recorded in the register. Expected divergence classes (h2-7b.md §5):
   `.d.ts` bytes, callback metadata, listing order, `emitSkipped` /
   `declBlocked` + diagnostics, declaration-only routing, paths /
   collisions, resolver / NodeBuilder results; each row is later closed
   by a wave proving the facet exact from the frozen observation.
2. **The `run_h2_6c` re-sweep (exact commands).** Post-flip, an
   in-band 6c-manifest row whose js/map facets are now exact would FAIL
   the normal run as a stale entry (:2657-2663). The shrink is a
   write-mode regeneration followed by a diff review and a normal run:
   `TSRS_H2_6C_WRITE_DIVERGENCES=1 cargo xtask h2-6c-acceptance`
   (regenerates the canonical 6c manifest from the sweep) → `git diff
   ratchets/h2-6c-known-divergences.v1.json` is reviewed COMPONENTWISE:
   allowed = a row deletion, a boolean facet `true → false`, a numeric
   `writes_diverging` DECREASE (the 133 joined rows carry 1 ×21 / 6 ×64
   / 11 ×48 — a partial fix legitimately lowers the integer), and — EXACTLY ONCE,
   for the 33 rows listed in the register — the designed `emit_refused:
   false → true` transition with the write / diagnostic / result facets
   cleared and the vector replaced by its refusal element (§6.4a/c2);
   rejected = any other row addition, `false → true`, numeric increase,
   owner change, or change. The three new fields are populated BEFORE
   the flip (§6.4c1: coarse facets byte-identical), never at this
   post-flip step. After the population, on BOTH
   manifests the review is on the VECTOR, never on the opaque hash:
   every row is exactly equal, or deleted, or — if it survives changed —
   its new `mismatch_vector` is a proper subset of the old one (elements
   removed, none added or replaced — a partial fix removes mismatches by
   construction) with the coarse facets decreasing accordingly and the
   fingerprint recomputed; any element added or replaced is a changed
   divergence identity and is rejected (§4.5) — with exactly two
   registered exceptions at the flip regeneration: the 33 refusal
   replacements and the 133-row activation refinement (§6.4c2), each
   bound to its row list in the register and to the 7b manifest's
   projected vector → `cargo xtask
   h2-6c-acceptance` (normal mode) green with the totals 639 / 4 / 643
   unchanged; the shrink is bounded by the 133 in-band rows (§2.5).
   The same command pair applies to any 5h / 6a / 6b manifest that a
   normal run reports stale (`TSRS_H2_5H_WRITE_DIVERGENCES` etc. — the
   lane records the exact names).
3. **The corpus ratchet** byte-identical for js/map (T0 100% FP=0).
4. **The 6c runner under the flip (F25/F26; the same-train amendment).**
   The production profile becomes `h2_7b_profile` for every entry, so
   `run_h2_6c` executes the declaration member on every 6c row whose
   effective options carry `declaration`. Machine-derived from the
   frozen artifacts (h2-6c-qualification, h2-candidate-dispositions,
   the 6c manifest): 434 of the 643 6c cases carry `declaration` /
   `composite`; 134 are in the 7b band (133 of them 6c-manifest rows,
   293 planned members); of the 300 declaration-bearing 6c-manifest
   rows OUTSIDE the band, 276 are already typed refusals (the h2-6c-m-2
   §2-A residue: `outDir` 132, `outFile` 128, both 16 — project rows
   refused by their descriptors) and exactly **24 execute today only
   because the 6c `MapFamily` floor DROPS `outFile` and
   `declarationMap`** (`outFile` 16 → H2.7d; `emitDeclarationOnly` +
   `outFile` 2 → H2.7d; `declarationMap` 5 → H2.7e; `declarationMap` +
   `outFile` 1 → H2.7d/H2.7e). The projection change is option-wide,
   not declaration-conditional (an option-dependent drop would be the
   prohibited silent-drop rule): the `MapFamily` route carries 33
   `outFile` / `declarationMap` rows in total (28 recorded-compiler-plan
   + 5 qualified-vfs; the 144 project carriers are refused by their
   descriptors and never reach the floor) — the 24 above plus **9
   non-declaration compiler `outFile` rows** (`inlineSourceMap2` ×2,
   `inlineSources` ×2, `inlineSources2` ×2,
   `sourceMapWithCaseSensitiveFileNames`,
   `sourceMapWithNonCaseSensitiveFileNames`,
   `sourceMapWithMultipleFilesWithFileEndingWithInterface` — all
   current manifest rows, none refused, all owned by H2.7d through
   `outFile`; none in the 7b band). Decision: (a) the `MapFamily` floor
   stops dropping `outfile` and `declarationmap` — they PROJECT (the
   typed refusals fire: `declarationMap` execute.rs:111, `outFile`
   :139-147), so those **33** rows move from diverging to
   `emit_refused` with their owners named (24 → H2.7d / H2.7e as above;
   9 → H2.7d) — the §2-A pattern, every `case_id` listed in the
   register; no exact 6c row carries either option (asserted at the
   re-sweep) and the 7b band carries neither (§2.4); this also retires
   two silent drops (h2-7b.md §8); (b) the 6c guard
   (h2_2c_acceptance.rs:3770-3808) admits `H2_7b` with the EXACT
   expectation on every EXECUTED row — the planned members on the 133
   in-band rows (293 total: 5 compiler ×1 + project 16 ×1 / 64 ×2 /
   48 ×3, per-case derived as in §4.4) and ZERO on every other executed
   6c row — the non-declaration rows plus ONE named declaration-bearing
   control, `compiler/noEmitOnError.ts#default` (in the 6c band and in
   the 7b DEFERRED set), whose `noEmitOnError` early return
   (execute.rs:643) precedes every member; on REFUSED rows (the 276 + the
   33) the runner cannot observe counters — the refusal is raised by
   `validate_bootstrap_emit_options` BEFORE any plan or member
   (execute.rs:71-149; the 6c runner returns at :3741-3751 before its
   guard, and `emit_with_command_outcome` drops the canary on error,
   compiler/src/lib.rs:834-840) — so zero activity on a refused row is
   a STRUCTURAL fact proven once by a focused test (`emit_files_with_
   activity` with `outFile` / `declarationMap` / standalone
   `emitDeclarationOnly` set → the typed error and every counter 0),
   never a runner assertion; (b') the 6c runner's refusal
   classification becomes TYPED like the 7b runner's (§4.2): the
   substring match at :3741 is replaced by the exact
   `DriverError::Emit(EmitFailure::UnsupportedCompilerOption { option })`
   match, the option name is recorded on the manifest row
   (`refused_option`, a new field populated at the regeneration for ALL
   refused rows — the pre-existing 279 (h2-6c-m-2.md §2-A: `outFile`
   144, `outDir` 130, `rootDir` 4, `isolatedModules` 1; within the 276
   declaration-bearing ones `outFile` 144 / `outDir` 130 / `rootDir` 2)
   plus the 33 transitions (`outFile` 27, `declarationMap` 6 — the
   `declarationMap` + `outFile` row refuses at `declarationMap` first,
   execute.rs:111 before :139-147 before the dynamic `outDir`
   :195-205) = **312** refusals: `outFile` 171 / `declarationMap` 6 /
   `outDir` 130 / `rootDir` 4 / `isolatedModules` 1, frozen as a
   per-case option map asserted against the frozen blockers and the
   validation order), both attempts are run
   and must agree, and the refusal variant enters the fingerprint; an
   unexpected refusal variant fails the run; (c) the 6c manifest gains `refused_option`, `mismatch_vector`, and
   `facet_fingerprint_sha256` computed by the §4.5 shared helper over
   the 6c observation — populated in TWO steps so the flip's own
   transition is identity-checked: (c1) in the OPENING commit, BEFORE
   the flip, a write-mode regeneration against the pre-flip execution
   adds the three fields to every one of the 451 current rows with the
   coarse facets asserted byte-identical (a coarse change at this step
   rejects the population) — the pre-flip vectors are the baseline;
   (c2) after the flip, the regeneration is reviewed by the §6.2 rule:
   every ordinary row is either EXACTLY EQUAL to its pre-flip row
   (vector, coarse facets, `refused_option` — the 279 pre-existing
   refusals and every unaffected divergence) or DELETED (now exact) or,
   if it survives changed, a PROPER SUBSET of its pre-flip vector with
   the coarse facets decreased accordingly; EXACTLY the 33 registered
   transition rows REPLACE their vector by the single refusal element
   (`refused_option` `null → the expected option` from the frozen map,
   former facets cleared); and EXACTLY the 133 joined rows may take the
   registered **H2.7b activation refinement**: their pre-flip vector is
   partitioned into the js/map component (must be equal or a proper
   subset) and the missing-declaration sentinel elements (the length
   delta the legacy comparator counts as missing writes), and the
   sentinel may be replaced ONLY by declaration-owned write / result /
   diagnostic / listing elements that are EXACTLY EQUAL to the same
   row's post-flip `H2_7b` mismatch vector projected through the `H2_6c`
   profile (the two manifests agree on the row's declaration
   divergence; a row that became exact loses the sentinel with nothing
   in its place), with the coarse facets recomputed from the new vector
   (a legitimate increase when one missing write refines into several
   mismatching fields); every other addition or replacement is rejected
   before the regenerated manifest is accepted; the shared join compares the vector exactly from (c1)
   on (so an identity change on an in-band row — a missing `.d.ts`
   becoming a content mismatch at the same count — cannot hide, not
   even across the flip). The alternative (a band-conditional drop of
   `declaration` for non-7b rows) is rejected as a cross-slice
   projection rule; the alternative of tolerating H2.7b activity on the
   24 rows is rejected as later-owned declaration work attributed to
   H2.7b.
5. **The W-H2.7A L2 lane through production:**
   `declaration_transformer_replay_decision_equal` gains a
   production-path mode driving `emit_files_with_activity` with the
   activated member (importing `PlanDeclarationPaths`, §5.10) and
   compares the frozen L2 bytes (199 unblocked + 3 blocked windows,
   116 cases) — 0 / 0 (TO-VERIFY); the harness mode is retained; no
   H2.7a artifact re-observation.

## 7. Resource contract (F15; schedule :169-172) — frozen here

| item | value |
| --- | --- |
| workers | `TSRS_H2_5G_WORKERS` ceiling 2 (the runner's `bounded_pipeline::ordered_map`); CPU ceiling 4 cores (2 workers × ≤ 2 threads); no oracle worker (the observations are frozen) |
| memory | per-worker RSS ceiling 4 GB; aggregate 8 GB at 2 workers (measured at the first sweep and recorded in the register; exceeding a ceiling STOPS) |
| wall | the local sweep ≤ 10 min demoted, **STOP 15 min** (abort, record, re-enter the design gate); the hosted `run_h2_7b` step ≤ 10 min (6c: 643 cases in 36 s; declaration emit is checker/NodeBuilder-backed) — the `gates` wall stays inside the historical band + the measured step; the first hosted execution is recorded in the PR body |
| checkpoint / resume | NONE by decision: the sweep is bounded (STOP 15 min), idempotent, and re-runnable; the resume command is the re-run. `ordered_map` (bounded_pipeline.rs:59) has no checkpoint surface and adding one for a ≤ 10 min sweep is machinery without a gating lane (operator calibration; recorded) |
| single write owners | the runner is the sole writer of the 7b manifest (and only under the path-valued variable); the 6c re-sweep is the sole writer of the 6c manifest (write mode); the chain walk is the sole writer of every other `ratchets/*.json` incl. the inventory and the close artifact; the operator writes the packet / register / README; the lane writes crates/**, .github/ci/**, crates/oracle/** |
| the pre-flip sweep | cheap (the option refusal and the tolerate arm short-circuit the declaration work); its wall is the floor |
| walk budget | the h1 ladder + 15 profiles re-mint on crate bytes (pin-only); the 5g/6a/6b/6c/7b receipts HIT (their keys project the inventory); the H2.7a inventory re-mints with CONTENT (the anchors), then the close; zero TypeScript re-observation |

## 8. Prohibitions

- No bundle/`outFile` arm, no `.d.ts.map`, no `declarationDir` /
  `stripInternal` / `isolatedDeclarations` lift, no
  `getDeclarationDiagnostics` API, no `noCheck`/transpile route, no
  `emitOnly`/`forceDtsEmit`/BuilderSignature axis, no custom
  `afterDeclarations` transformer, no `outDir`/`rootDir` lift
  (h2-7b.md §8).
- No refusal lift outside the §3 table; every predicate a lift unmasks
  is named with its band BEFORE the flip; a residual refusal fails the
  run and is named in an amendment, never manifested.
- No manifest baseline from the pre-flip sweep; the manifest shrinks
  only; the deferred rows never enter it; the canonical manifest path
  is written only when the path-valued variable names it.
- No hand-authored `.d.ts` expectations; no fixture-specific branch; no
  output substitution; no unknown-as-success fallback; no text-matched
  error classification.
- No option floor that silently drops a declaration-family setting on
  an EXECUTED row (asserted).
- No activation through the harness seam; the harness entries stay
  absent from execute.rs; no second `PlanDeclarationPaths`.
- No change to the four H2.7a artifacts' observation content (the
  inventory's anchor rows and the close's dispositions are the only
  content changes; pins otherwise); the close artifact's
  `runtime_contract` stays frozen history.
- No `active_runtime_slices` / transition change (ca); no local-CI
  phase change (landed at m-1 P1c).
- No wave closure in this train.

## 9. Acceptance (train-internal) — exact commands

1. Opening commit: `cargo xtask h2-7b-acceptance --pre-flip` prints the
   §2.2 census (111 / 1,450 / 6; activity 0; deferred 26) and writes no
   manifest; `TSRS_H2_6C_WRITE_DIVERGENCES=1 cargo xtask h2-6c-acceptance`
   at the PRE-flip tree populates `refused_option` / `mismatch_vector` /
   `facet_fingerprint_sha256` on the 451 6c rows with the coarse facets
   byte-identical (§6.4c1; the diff shows only the three new fields),
   then `cargo xtask h2-6c-acceptance` green with 639 / 4 / 643; `cargo test -p tsc-rs-xtask
   hosted_acceptance_and_oracle_phases_cover_h2_2c_rungs` green;
   `node .github/ci/qualification.mjs check` valid (policy pins
   current); `validate_h2_7b_qualification` green against the m-1
   artifact (a unit test).
2. Flip commit: every LIFTED control re-pinned, every RETAINED control
   untouched and green (`cargo test -p tsc-rs-compiler --test contracts` AND `cargo test -p
   tsc-rs-emitter --test contracts` — the two control aggregators — for
   the control files of §5.11); the focused tests:
   the three resolver members (refusal + CheckerSession values), the
   pre-pass predicate (TS / plain JS / `checkJs` true / false /
   `@ts-check` / `@ts-nocheck`), the `transformed.len() == 1` contract,
   the collision preflight (input overwrite / duplicate / canonical
   case / JS suppression under `emitDeclarationOnly` / `declBlocked`
   propagation / the five frozen TS5055 rows), the printer activation
   (`Declaration` prints; `SourceFile` still refuses declaration
   syntax; `Bundle` refused; the oracle consulted at the uniqueness
   decision), the mixed `Written` / `SkippedUnchanged` listing control,
   the failing-declaration-sink control, the `PlanDeclarationPaths`
   fallback order, the profile admission control; the L2 production
   replay `cargo test -p tsc-rs-compiler --test contracts
   declaration_transformer_replay_decision_equal` 0 / 0; `cargo xtask
   ledger check` 0 stale / 0 undispositioned.
3. The first full sweep: `TSRS_H2_7B_WRITE_DIVERGENCES=<canonical>
   cargo xtask h2-7b-acceptance` — refusals 0 (else §3 amendment);
   the manifest written from that sweep only; the six controls exact;
   the 29 transform-blocked rows exact on `emit_skipped`, every
   `emit_result` diagnostic field, and the absent `.d.ts` write; the 5
   collision rows exact on TS5055 and their write sets; the activity
   assertion (§4.4) on every row and both repetitions; determinism;
   then `cargo xtask h2-7b-acceptance` (normal mode) green; exact /
   diverging recorded in the register.
4. `TSRS_H2_6C_WRITE_DIVERGENCES=1 cargo xtask h2-6c-acceptance` → the
   componentwise diff review (§6.2 / §6.4c2: in-band deletions, the
   133-row activation refinement checked against the 7b manifest's
   projected vectors, proper-subset decreases, every other row exactly
   equal, the 33 designed refusal transitions with `refused_option`
   `null → expected`)
   → `cargo xtask h2-6c-acceptance` green with 639 / 4 / 643 and the
   exact H2.7b activity (293 on the 133 rows, 0 on every other executed
   row; refused rows structurally 0); the 5h / 6a / 6b lanes
   green (shrink-only if stale).
5. `cargo xtask conformance` band all T0 100% FP=0 (js/map
   byte-identical).
6. `node crates/oracle/h2-7a-owner-inventory.mjs --check` and
   `node crates/oracle/h2-7a-close.mjs --check` green after their
   re-mints (the walk);
   the 7b qualification receipt HIT (the m-1 artifact untouched).
7. `bash scripts/chain-walk.sh` ONE-invocation converge (ORDER 71/71,
   zero TypeScript observation); `cargo xtask ci --baseline <trusted
   base>` green; hosted `gates` green with the first `run_h2_7b`
   execution and its wall recorded; merge.

## 10. Trace table, unresolved items, and record

### 10.1 Parent restatement (this design gate)

h2-7b.md §6.2 / §6.4 / §8: the hosted boundary grows by exactly one
call (`run_h2_7b`) at m-2, not at ca, because the repair guard binds a
defined runner to `fn acceptance` in the same train; the
`h2-7b-oracle` local-CI phase landed at m-1 (P1c). The ca rung keeps:
the policy/close pins, the close record, the transition landing, the
empty admitted-row manifest. h2-7b-m-1.md §1's "the ca rung later adds
exactly ONE hosted call" is superseded by its own §9.6 and this item.

### 10.2 Owner → Rust → test → evidence trace (the schedule :169-172 row)

| upstream owner / invariant | Rust | focused test | ratchet / ledger evidence |
| --- | --- | --- | --- |
| `emitSourceFileOrBundle` member independence :116555-116562 | execute.rs unit loop (G1) | the 5 TS5055 rows; the js-blocked/dts-written control | `run_h2_7b` sweep; ledger triple |
| `emitDeclarationFileOrBundle` :116640-116715 | `declarations::emit_declaration_unit` (G4) | the pre-pass branches; the length-1 contract; the L2 production replay | sweep; ledger triple; h2-7a inventory anchors |
| `collectLinkedAliases` walker :116716-116735; the resolver worker :55675-55727 | the walker + `EmitResolver::collect_linked_aliases` (G5) | true/false-branch tests; resolver refusal/value tests | inventory rows :116719/:116727 anchored |
| `canIncludeBindAndCheckDiagnostics` :18898-18905 | `EmitResolver::can_include_bind_and_check_diagnostics` (checker lib.rs:561) | the six directive/`checkJs` cases | ledger triple |
| `hasGlobalName` :88396-88398 / the uniqueness consult :120638-120666 | the checker worker + the fallible `GlobalNameOracle` carried through transform.rs:881 into the reserved-name decision builtins/target_bindings.rs:741 (G3/G5) | the collision control; the default-`true` JS-lane control; the unavailable-oracle fault-injection control (fails before any declaration write) | inventory row :116688 anchored |
| the declaration printer options + handlers :116674-116693 | `Printer::print_declaration` (G3) | activation + refusal-retention tests; the four field dispositions | L2 replay; the m-3.5 allowlist control |
| `declBlocked` / `emitSkipped` :116668-116669; `skippedDtsWrite` :116803 | G4 / G6 (`SkippedUnchanged`) | the 29 transform-blocked rows; the mixed listing control | sweep |
| `getOutputPathsFor` :116373-116387; `getDeclarationEmitOutputFilePath` :16580-16591 | plan.rs:304-340 / :512-531 (G7 triple) + `PlanDeclarationPaths` (G10) | the fallback-order test; the extension arms (66 / 62) | inventory row anchored; ledger |
| `verifyEmitFilePath` :125015-125045 → `isEmitBlocked` :125418-125420 | plan.rs:401-464 requalified (G12) | the collision matrix tests; the 5 frozen rows | sweep |
| the option surface: `emitDeclarationOnly` :124946-124950 (standalone diagnostic H2.7c) | execute.rs:112-115 narrowed (G2); the floor (§4.3) | the ca control 3; the standalone refusal | the 111 rows; inventory `rejected-option` rows anchored |
| admission `H2_7b` | `h2_7b_profile` + the five sites (G8) | the single-admission control | close artifact `lifted-at-H2.7b`; the 27-slice guard + count assertion |
| `markLinkedReferences` :116597 / :116741 (the JS-lane / no-check pipeline helper) | NO H2.7b Rust activation — re-owned to H2.8c (§5.13) | the `@ts-nocheck` adjacency evidence (3 admitted TS rows; report-only) | the inventory re-mint (`script-orchestration` / H2.8c; seven anchored H2.7b rows) |

### 10.3 Items resolved at this gate (design decisions)

1. The five `h2_6c_profile()` constructor sites all move (§5.8).
2. `suppressOutputPathCheck`: a typed absent control (§5.12).
3. The emitted-files facet = the listing projection derived from the
   oracle write stream; callbacks compared in stream order (§4.6).
4. `PlanDeclarationPaths` ownership: emitter-owned, one copy (§5.10).
5. The printer handler API and consult site frozen (§5.3).
6. No checkpoint/resume for the sweep (§7).
7. Refusal handling typed, never text (§4.2); a residual refusal stops
   the train (§3).
8. The `markLinkedReferences` rows (:116597, :116741) are
   `script-orchestration` / H2.8c (§5.13).
9. The JS-absence provenance on the unit (§3, §5.2).
10. `EmitResolverNode` at the `collect_linked_aliases` boundary (§5.5).
11. The manifest `facet_fingerprint_sha256` over every facet, on both
    the 7b and the 6c manifests, + the componentwise shrink rule (§4.5,
    §6.2, §6.4).
12. The 6c runner under the flip: `outFile` / `declarationMap` project
    in `MapFamily` (33 rows become typed refusals: 24 owned by H2.7d /
    H2.7e, 9 by H2.7d), the 6c guard expects exactly 293 on the 133
    in-band rows and 0 on every other executed row, refused-row zero
    activity is a structural proof, the 6c refusal classification is
    typed with `refused_option` recorded (§6.4).
13. The mismatch vector is stored on every manifest row and the shrink
    review is a strict-subset test on the vector, on both manifests;
    the 6c vectors are populated PRE-flip in the opening commit so the
    flip's regeneration is itself subset-checked except for the 33
    registered transitions (§4.5, §6.2, §6.4c).
14. The `PrintRequest::Declaration` refusal is a §3 lattice row (2,410
    printer-bound members) with `SourceFile` / `Bundle` retained.
15. Pre-flip and refused-row zero activity are structural proofs pinned
    by one canary test; runner assertions apply to successful outcomes.
16. The 312-refusal `refused_option` map with its totals (§6.4b').
17. The post-flip 6c review vocabulary: equal / deleted / proper subset
    / the 33 replacements / the 133-row activation refinement bound to
    the 7b manifest's projected vectors (§6.4c2); the vectorizer's two
    frozen profiles (§4.5).
18. `GlobalNameOracle` is fallible with typed propagation and no
    default; transform.rs and builtins/target_bindings.rs are
    deliverables (§5.3, §1, §10.2).
19. The crate root re-exports `PlanDeclarationPaths`, the handler /
    oracle types, and `JavascriptOmission` (§1, §5.10).

### 10.4 TO-VERIFY at implementation (measurements, not decisions)

1. The 111 / 1,450 / 6 opening census and zero activity (§2.2); the
   adjacency constants derived by the runner (§2.4: 68 / 80, 2,445 =
   2,049 + 396); the report-only `@ts-nocheck` row count.
2. The exact / diverging split of the first sweep; the 6c shrink count
   (≤ 133); any 5h/6a/6b shrink.
3. The four printer-option field dispositions (§5.3) with grep evidence.
4. The resource measurements of §7 (RSS, walls, the hosted step).

## 11. Cross-review record

Round 1 (2026-09-04): operator draft rev 1 → sol **REVISE** (15
blocking + 1 advisory). Verified without finding: 1,593 = 1,567 + 26;
the option-shape table; the 111 combined rows; the write kinds; the
451 / 133 join; `on_error_callback_present` on all 5,000 writes;
`emitted_files` null on every admitted row; the diagnostic-code
multiset. Every finding INCORPORATED in rev 2 with the operator's
calibration: F1 the opening census corrected (the tolerate arm; 111
typed refusals / 1,450 executed / 6 controls; §2.2, §3); F2 typed
refusal matching, residual refusals fail the run (§4.2, §3); F3 the
29 / 5 split, the reported-diagnostics facet for TS5055, member
independence (§2.3, §5.1); F4 the adjacency table machine-asserted +
the `declarationMap: false` correction (§2.4, §2.1); F5 the four-
disjunct pre-pass predicate with the resolver-exposed helper and the
branch tests (§5.4, §5.5); F6 the two orders (§4.6, §5.1); F7 the
comparator normalization table, `Option` presence, the on-error
structural proof (§4.5); F8 the per-case activity-count assertion
(§4.4); F9 the frozen handler API, the true consult site, the four
field dispositions (§5.3); F10 one emitter-owned `PlanDeclarationPaths`
(§5.10); F11 the path-valued manifest contract with the three states
(§4.8); F12 the exact shrink commands + diff rule (§6.2); F13 the eight
inventory rows with anchors and the `markLinkedReferences` correction
(§5.13); F14 span-hash `retained_arms` pins + the single-admission
control (§5.14); F15 the frozen resource contract (workers / CPU / RSS
/ wall / single writers) with the checkpoint dispositioned as
unnecessary for a bounded ≤ 10 min sweep (operator calibration), the
trace table (§10.2), exact commands (§9), decisions vs measurements
split (§10.3-10.4); F16 the anchors corrected (plan.rs:190/:195/:401,
execute.rs:675, h2_7a_m35_controls.rs:31, plan.rs:512-531).

Round 2 (2026-09-04): rev 2 → sol **REVISE** — F1, F2, F5, F6, F7, F9,
F10, F11, F15, F16 RESOLVED; F3, F4, F8, F12, F13, F14 PARTIAL (each
closed by a round-2 finding below); the expected intentional reds
(the close generator's whole-file checks, the declaration-syntax
allowlist, the LIFTED controls, the hosted-runner guard) confirmed as
same-train amendments; no unlisted option predicate found beyond F19.
Every round-2 finding INCORPORATED in rev 3: F17 the frozen per-suite
split 19 / 10 / 0 and 3 / 0 / 2 (§2.3); F18 the project members from
`project_input.analyzed_files` (396; 0 ×6 / 1 ×40 / 2 ×94 / 3 ×56) and
the 2,445 aggregate, JS/JSX 68 / 80 (§2.4, §4.4); F19 the typed
JS-absence provenance on the unit with paired controls and the
residual arm pinned (§3, §5.2, §5.14); F20 `EmitResolverNode` for the
argument and every returned node (§5.5); F21 the per-case mismatch
vector → `facet_fingerprint_sha256` compared exactly in normal mode
(§4.5, §4.8); F22 the componentwise shrink rule incl. numeric decrease
(§6.2); F23 both `markLinkedReferences` rows → `script-orchestration` /
H2.8c, seven anchored H2.7b rows, the `@ts-nocheck` adjacency evidence
(§5.13, §2.4); F24 `--pre-flip` bypasses the manifest writer (§4.2).

Round 3 (2026-09-04): rev 3 → sol **REVISE** — F17, F18, F19, F20,
F22, F23, F3, F8, F12, F13, F14 RESOLVED (the census, the 2,445
derivation, the provenance, `EmitResolverNode`, the shrink rule, the
seven anchored rows, the retained-arm pins all re-verified); F21, F24,
F4 PARTIAL. New: F25 (the 6c guard rejects H2.7b activity under the
global profile), F26 (the 6c projection lets 24 later-owned rows
execute the declaration member; the coarse 6c manifest could hide an
identity change), F27 (the fingerprint must cover every facet), F28
(two acceptance commands wrong), F29/F30 (advisory contradictions).
Every finding INCORPORATED in rev 4: F25/F26 → §6.4 (the `MapFamily`
floor projects `outFile` / `declarationMap`, the 24 rows become typed
refusals owned by H2.7d / H2.7e — machine-derived: 434 / 134 / 300 /
276 / 24 —, the 6c guard expects exactly 293 on the 133 in-band rows
and 0 elsewhere, the 6c manifest gains the fingerprint) with the two
rejected alternatives recorded; F27 → the mismatch vector over every
facet (§4.5); F28 → the two `--test contracts` aggregators and the
`node crates/oracle/` prefix (§9); F29 → `--pre-flip` writes nothing
anywhere (§4.2, §4.8, §9); F30 → the `@ts-nocheck` count report-only
and excluded from the validator constants (§2.4, §4.1, §10.4).

Round 4 (2026-09-04): rev 4 → sol **REVISE** — F28, F29, F30, F21,
F24, F4 RESOLVED; F25, F26, F27 PARTIAL; the reviewer independently
re-derived the whole census (434 / 134 / 300 / 276 / 24; 293 = 5 + 16
+ 128 + 144; 2,445 = 2,049 + 396; 111 / 1,450 / 6; 29 / 5; 68 / 80;
the two orders; the four-disjunct predicate) and found the printer
API, the inventory vocabulary, and the close-artifact span-hash
strategy implementation-ready. New (numbered F31-F33 here; the review
text reuses F17-F19): F31 the projection change affects 33 executing
`MapFamily` rows, not 24 (nine non-declaration `outFile` compiler rows);
F32 refused rows return before the 6c guard with no counters on the
error path, so refused-row zero activity is not runner-observable and
the substring refusal classification loses the typed variant; F33 the
fingerprint diff coupling excluded the 6c manifest. Every finding
INCORPORATED in rev 5: F31 → 33 designed transitions (24 → H2.7d /
H2.7e, 9 → H2.7d), the option-wide projection rationale, every
`case_id` to the register (§2.5, §6.2, §6.4, §9.4, §10.3); F32 → the
structural zero-activity proof for refused rows (the refusal precedes
planning; one focused counter test) replacing the runner claim, the
6c runner's typed refusal match with `refused_option` recorded, two
attempts, the variant in the fingerprint (§6.4b/b'); F33 → the
fingerprint coupling on both manifests after population (§6.2).

Round 5 (2026-09-04): rev 5 → sol **REVISE** — F31, F25, F27 RESOLVED
(the 33-row set, the 293 members, the all-facet fingerprint all
re-derived; the JS/JSX split compiler 38 / 41, conformance 28 / 37,
project 2 / 2; the live pre-pass set = the three `tsNoCheckForTypescript*`
files; the path-valued environment contract; the inventory and close
amendments confirmed sound); F32, F33, F26 PARTIAL. New (numbered
F34-F38 here; the review text reuses F17-F21): F34 the
`PrintRequest::Declaration` refusal missing from the §3 lattice; F35
pre-flip zero activity not runner-observable for the 111 refusals;
F36 the fingerprint coupling permits identity substitution under a
coarse decrease; F37 the `refused_option` population incomplete (the
pre-existing `rootDir` / `isolatedModules` refusals; owner-group counts
vs first-refusal counts); F38 the zero-activity wording too strong
(the `noEmitOnError` early-return control). Every finding INCORPORATED
in rev 6: F34 → the §3 row (2,410 printer-bound members; `SourceFile` /
`Bundle` retained); F35 → the structural proof + one canary test,
runner assertions on successful outcomes only (§4.4, §4.7); F36 → the
`mismatch_vector` stored on every row and the strict-subset shrink
review on both manifests (§4.5, §6.2); F37 → the 312-refusal map with
the totals `outFile` 171 / `declarationMap` 6 / `outDir` 130 / `rootDir`
4 / `isolatedModules` 1 in validation order (§6.4b'); F38 → the named
`noEmitOnError` control (§6.4b).

Round 6 (2026-09-04): rev 6 → sol **REVISE** — F34, F35, F37, F38, F32
RESOLVED (2,410 = 2,445 − 31 − 4 re-derived; the 312-refusal map
re-classified in validation order; the structural proofs accepted);
F36, F33, F26 PARTIAL on one remaining gap: the first 6c vector
population happened at the post-flip regeneration, leaving the flip's
own transition identity-unchecked (F39 here; the review text says
F17) + an advisory span cite (F40; printer_foundation_contract.rs
697-702). Both INCORPORATED in rev 7: F39 → the 6c vectors populated
PRE-flip in the opening commit with the coarse facets byte-identical
(§6.4c1), the post-flip regeneration a strict subset per row except
the 33 registered transitions, which may replace their vector by the
single refusal element (§6.4c2, §6.2, §9.1); F40 → the span cite.

Round 7 (2026-09-04): rev 7 → sol **REVISE** — F40 RESOLVED; F39, F36,
F33, F26 PARTIAL on wording: F41 (the review text says F17) the
"strict subset for every ordinary row" rule is impossible for the 279
unchanged refusals and §9.4 still spoke of post-flip population; F42
(F18) the shared vectorizer lacked a 6c profile (`kind: "other"`, the
count-only callback diagnostics); F43 (F19, advisory) the 2,410
derivation wording; F44 (F20, advisory) the `markLinkedReferences`
trace row. Every finding INCORPORATED in rev 8: F41 → equal / deleted
/ proper-subset / the 33 replacements (§6.4c2, §6.2, §9.4); F42 → the
two frozen vectorizer profiles `H2_7b` / `H2_6c` with the kind test
(§4.5); F43 → "31 transform-blocked members + 4 declaration-path-
blocked members" (§3); F44 → the trace row (§10.2).

Round 8 (2026-09-04): rev 8 → sol **REVISE** — F42, F43, F44, F33
RESOLVED; F41, F39, F36, F26 PARTIAL on one relation: the post-flip
6c rule rejected the legitimate activation refinement of the 133
joined rows (a missing `.d.ts` sentinel becoming a present-but-
divergent `.d.ts`, which §6.1 permits the first sweep to freeze) —
F45 here (the review text says F17); and F46 (F18) the
`GlobalNameOracle` boundary was infallible while its backing resolver
member is `Result`-valued (a fail-open risk), with transform.rs and
builtins/target_bindings.rs missing from the deliverables. Both
INCORPORATED in rev 9: F45 → the registered 133-row activation
refinement (js/map component equal or proper subset; the sentinel
replaced only by declaration-owned elements exactly equal to the 7b
manifest's vector projected through the `H2_6c` profile; coarse facets
recomputed) as the second registered exception at the flip
regeneration (§6.4c2, §6.2, §9.4); F46 → the fallible
`GlobalNameOracle::has_global_name -> Result<bool, EmitResolverError>`
propagated through the reserved-name decision into `PrinterError` /
`EmitFailure` with no default, the fault-injection control, the two
files added to the deliverables and the trace row (§5.3, §1, §10.2).

Round 9 (2026-09-04): rev 9 → sol **REVISE** — F45, F46, F41, F39,
F36, F26 all RESOLVED (the activation refinement, the fallible
oracle, the post-flip vocabulary, the pre-flip population, the
vectors, the 6c amendment re-verified); one roster item remained —
F47 here (the review text says F17): `crates/emitter/src/lib.rs`
missing from the deliverables although the replay test must import
`PlanDeclarationPaths` from `tsc_emitter` (the crate root keeps
`declarations` private with an explicit re-export list). The reviewer
found no further trust-path defect and named the four printer-field
dispositions and the no-checkpoint decision as explicit standing
decisions, not blockers. INCORPORATED in rev 10: lib.rs added with the
public re-exports of `PlanDeclarationPaths` + constructor,
`DeclarationPrintHandlers`, `GlobalNameOracle`, `JavascriptOmission`
(§1, §5.10, §10.3).

Round 10 (2026-09-04): rev 10 → sol **AGREE** — F47 RESOLVED; the
round-9 resolutions (F45 the 133-row activation refinement, F46 the
fallible oracle, F41 the post-flip vocabulary, F39 the pre-flip
population, F36 the stored vectors, F26 the 6c amendment) re-verified;
"no predicate unmasked by the flip is omitted from §3, and no evidence
path permits a mismatch to hide"; the intentional same-train reds
(inventory / close re-mints, the LIFTED controls, the declaration-
syntax allowlist, the hosted runner guard) confirmed rostered and
ordered; no new findings. This revision is the h2-7b-m-2 design
authority; it lands at the m-2 train's Phase E after the m-1 train
(PR #503) merges.
