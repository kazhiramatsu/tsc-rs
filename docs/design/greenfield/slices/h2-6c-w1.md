# H2.6c / W1 — compiler source-map closure wave

Status: revision 2 (2026-08-29), operator-authored, incorporating the
gpt-5.6-sol cross-review verbatim (round-1 verdict REVISE; every
objection accepted). Parent: `h2-6c-m-2.md` §3 wave table + §2-A.
Baseline: `ratchets/h2-6c-known-divergences.v1.json` (500 rows, owner
`h2-6c-m-2-divergence-closure`).

## 1. Wave identity and band (revised per objection 1)

W1 is the parent's explicit **39-row** compiler source-map-only band;
the `case_id` join to the m-2 table is authoritative. The wave closes
against a FROZEN case list (this packet's appendix at
implementation time), never a mutable facet predicate — the naive
predicate selects 44 rows (39 + five post-flip F-C rows such as
`out-flag.ts#target=es5`, which carries `declaration` and belongs to
Wave F). The manifest join measured 40 writes+emit_result rows, 29
writes+diagnostics+emit_result rows (W2), and 4 writes-only rows —
the draft's "73 = 40 + writes-only 33" was wrong and is withdrawn.
A wider probe sweep may run as a separately named, non-binding
**cross-wave triage batch** whose findings are tagged to their owning
waves; it never defines W1 closure.

## 2. Probe fidelity (confirmed + tightened per objections 2, Q3-Q5)

- The production CLI is DISQUALIFIED as a probe: the compiler-suite
  harness pins absent `newLine` to CRLF (execution.rs:334, the
  CompilerBaselineRunner rationale) while production emission maps
  `None => LF` (execute.rs:656 — not "host default"; it does not
  query the host). Mapping coordinates are newline-width independent
  (writer.rs:200), so mapping evidence gathered via the CLI stands.
- The probe generalization must reproduce the H2.6c EXECUTION ROUTE,
  not just the artifact path: all W1 candidates run through
  `recorded-compiler-plan` with `EmitOptionFloor::MapFamily`
  (h2_2c_acceptance.rs:3637); the existing band probe instead uses
  the direct qualification loader + `SourceMap` floor and hardcodes
  case-sensitive hosting (execution.rs:165) that some recorded
  fixtures contradict. The W1 probe reuses the recorded plan +
  MapFamily route.
- Probe output per write: callback bytes AND materialized bytes,
  path, write index, BOM flag, source provenance, callback metadata —
  acceptance compares callback hashes and BOM separately
  (h2_2c_acceptance.rs:2382); materialized bytes alone hide the
  diverging facet (`emitBOM.ts` is a live two-write example).
- Selector: exact case ID or exactly-one-match required (the current
  substring `.find` silently takes the first match); setting both the
  6a and 6c selectors is an error; dump names carry write index +
  full path (basename collisions exist in multi-output cases).

## 3. Defect classes (round-1 observations CONFIRMED, causes per sol)

- **K1 — parameter-property store misses its statement-end mapping.**
  `sourceMapValidationClass.ts#target=es2015`: the oracle's final
  `,CAAQ` segment is the generated statement-end mapping (generated
  col 33 after `;` → parameter end, source col 39). Later mappings
  re-align via delta state (the draft's "every later segment
  disagrees" was wrong). CAUSE: TypeScript-visitor projects the
  parameter into a synthetic property (builtins.rs:10071);
  class-fields lowering materializes the store via
  `materialize_moved_instance_initializers` →
  `create_property_initializer_statement`, which copies its range
  from the synthetic property (class_fields.rs:1204) and never sets
  the outer trailing range. Upstream sets
  `setSourceMapRange(statement, propertyOriginalNode)` when the
  property's original is a parameter (_tsc.js:97454). FIX SHAPE: the
  materialized statement takes the original parameter as its explicit
  source-map range. Verify on BOTH the ES2015 class-fields path and
  ES5 downleveling variants before mechanical rollout (Q7).
- **K2 — namespace lowering emits unpositioned synthesized names.**
  `sourceMap-Comments.ts#target=es2015`: the oracle maps
  name-start (0,10), name-end (0,13), declaration-end (18,1) —
  `IAAU,GAAG,CAkBZ`. CAUSE (two sites):
  `create_declaration_identifier_with_original` sets original
  provenance but no text range (builtins.rs:11869) where upstream
  `getName` clones + `setTextRange` (_tsc.js:24788); and
  `namespace_parameter_name` → `create_parameter` produces an
  unpositioned IIFE parameter (builtins.rs:12871, 12146) where
  upstream sets the generated parameter name's source-map range to
  `node.name` (_tsc.js:95717). FIX SHAPE: name text ranges on
  declaration/local references + explicit source-map range on the
  IIFE parameter.
- **Shared-cause ruling (Q2, agreed):** K1 and K2 are SEPARATE
  caller-level range-propagation defects. The generic
  `set_original_node` rule is NOT touched: the printer already honors
  explicit node ranges and trailing ends (printer.rs:12485, 12517);
  making original links inherit ranges would diverge from upstream's
  deliberate identity/range separation.
- **K4a — parameter update misses upstream range/flag metadata
  (4 destructuring-parameter rows; sol-pinned).** The fixtures are
  ES2015-target with retained parameter syntax; the extra segment is
  the binding pattern's trailing boundary before the erased type.
  CAUSE: TypeScriptVisitor::visit_with_typescript_gate's Parameter
  arm returns update_generic without upstream visitParameter's
  metadata (builtins.rs:9689 vs _tsc.js:95029). FIX: on update, set
  commentRange=original, textRange=sourceMapRange=
  moveRangePastModifiers(original), and NO_TRAILING_SOURCE_MAP on the
  transformed binding name (printer honors it at printer.rs:12478).
  RISK: touches every updated parameter form (typed/optional/rest/
  decorated/parameter-property).
- **K4b — case/default clause trailing newline shifts the clause
  boundary (sourceMapValidationSwitch; sol-pinned).** CAUSE:
  Printer::emit_case_clause_statements writes a newline after the
  FINAL multiline statement (printer.rs:6334, 6364), recording the
  clause's After boundary on the next line; upstream's
  CaseOrDefaultClauseStatements carries NoTrailingNewLine
  (_tsc.js:119486, 120330) — the outer case-block list owns the
  newline. FIX: keep initial/inter-statement newlines, drop the
  final one; emitted JS must stay byte-identical. RISK: all
  multiline case/default clauses; verify comments between clauses.
- **K4c — object-literal method function-expression range start
  (derivedClassConstructor…01#es5; sol-pinned).** CAUSE:
  Es2015Visitor::visit_method_declaration passes the COMPLETE method
  as the function-expression location (es2015.rs:4828); upstream
  passes moveRangePos(node, -1) — synthesized start, original end
  (_tsc.js:107600). FIX: range [-1, method.end], preserving
  original-node link and NoLeadingComments; do NOT touch
  transform_object_literal_method_declaration_to_expression
  (computed/split path, es2015.rs:6388).
- **K5 — for-of head declaration-list range (4 es5 siblings;
  sol-pinned, AGREE).** CAUSE: Es2015Visitor::
  convert_for_of_statement_head assigns range_union(&flattened)
  (es2015.rs:12068; range_union at :5491 takes max end = the
  pattern-close endpoint); upstream uses
  createRange(declarations[0].pos, last(declarations).end)
  (_tsc.js:106583). FIX: at THIS SITE ONLY, explicit source-map
  range = first flattened declaration's pos .. last's end; keep text
  range/original attachment; do NOT modify range_union (ordinary
  destructuring lists intentionally mirror upstream getRangeUnion,
  _tsc.js:106440). RISK: both callers (es2015.rs:12287, 12402) —
  test array/object, nested, rest, default patterns with
  downlevelIteration off and on.
- **SPECIAL-5 disposition (sol-reviewed, signed):**
  (a) the two …AndOutDir#default rows + requireOfJsonFileWithSourceMap
  #default are **H2.8a carry-overs excluded from W1 closure**: the
  shared projection silently drops `outdir` (execution.rs:894 region)
  and the option-owner map assigns outDir to H2.8a
  (h2-transition.mjs:466, transition row :697). Extending MapFamily
  NOW would only convert the fake write divergences into
  emit_refused — production pins TS-source outDir fail-closed with
  zero writes until H2.8a (emit_session_contract.rs:1512). Their
  manifest rows stay and shrink when H2.8a admits + proves the
  relocated outputs.
  (b) sourceMapValidationVarInDownLevelGenerator (es2015 target,
  downlevelIteration, noEmitHelpers:true) is an **H2.8b carry-over**:
  the rejected-option map assigns noEmitHelpers to H2.8b
  (h2-transition.mjs:465). The runtime already observes the option
  (execute.rs:662 -> printer.rs:1222 suppresses helper bodies), so a
  floor extension would heal it technically, but that crosses the
  option-owner boundary — scheduled under H2.8b unless ownership is
  explicitly amended.
  (c) **K7 — erased parameter annotation strands a name-trailing
  comment (sol-pinned, wave-3 fix).** Upstream visitParameter keeps
  the ordinary post-name comment phase (emitCommentsAfterNode,
  _tsc.js:117855/120978) so the comment at name.end survives type
  erasure. CAUSE: TypeScriptVisitor removes Parameter.type
  (builtins.rs:9689) without transporting it onto the updated name
  (metadata records only ranges + NO_TRAILING_SOURCE_MAP,
  builtins.rs:11902); the parameter printer has no post-name comment
  phase (printer.rs:3876) and the list-end phase reads the parameter
  end past the erased type (printer.rs:7682). FIX: capture the
  original type before erasure and attach via EmitMetadata::type_node
  (the variable-declaration precedent, builtins.rs:9731); in the
  Parameter printer arm, when the marker exists, call the existing
  emit_trailing_comments_at_node_position(name) after the name,
  before initializer/list close; keep the erased-type end cursor
  (printer.rs:3885); no generic identifier/list-end comment
  broadening. RISK: every transformTypeScript-updated typed
  parameter — verify rest/non-rest, ES5 downlevel rest, K4
  intersections, comments on both sides of the type, and an untyped
  negative control. The comment writer already brackets positions for
  maps (printer.rs:13411), so JS bytes AND mapping segments repair
  together.
- **Wave-4 investigation (operator, 2026-08-30; TO-VERIFY(review)):**
  the post-W3 residue decomposes into three candidate classes:
  (i) **K2-siblings** — the enum lowering's IIFE shows the exact K2
  shape (probe: `(function (E)` line — oracle maps name cols 11,12;
  rust maps col 0 only), and the Classes/Comments namespace-CLOSING
  residues (L32/33 5->9/5->7) plus the import/module family look like
  the same synthesized-identifier-range gap in SIBLING transform
  paths (enum transform, export/import bindings, closing references).
  Hypothesis: extend the signed K2 fix pattern (name text ranges +
  explicit source-map ranges) to those sites.
  (ii) **K8 — decorator transform range discipline** (largest family,
  decorators es2015+es5): three sub-shapes from the probe — the
  decorated member maps at the DECORATOR line where the oracle maps
  past the decorators (L20 rust src-line 41 vs oracle 43 — the
  moveRangePastDecorators analog of the signed K4a); the __decorate
  argument back-reference duplicate-position segment is missing
  (L27 oracle has (15,col34)+(15,col21), rust only the first); a
  close-line over-map (L26 rust 2 segments vs oracle none).
  (iii) **K11 — unicode-escaped name maps** (sol's W3 census:
  IdentifierName/PrivateIdentifier extended maps L12/L20 0->2) —
  escaped identifier names lack name segments in the extended map
  families.
  Review asks: pin the exact sites/upstream parities per class (the
  K1-K7 discipline) and rule whether (i) is one mechanical rollout
  or per-site fixes.
- **K3 — remaining families TO-VERIFY(probe)** (jsxFactory*,
  string-literal newline, contextualTyping, decorators, …): one
  representative founds a hypothesis; CLASS MEMBERSHIP is established
  per case (frozen membership table added here before any mechanical
  implementation), each class confirmed on its target variants.

## 4. Wave execution (revised per objections 4, Q6)

1. Train commit 1: the probe rebuilt on the recorded-plan + MapFamily
   route with the §2 dump/selector contract (tests-only bytes).
2. Operator probe passes → freeze the W1 39-row class-membership
   table into §3 (byte evidence per class, variants covered).
3. Implementation split: sol implements K1/K2 (emitter-internal range
   propagation); luna takes mechanical per-family rollouts only after
   a class fix pattern exists and its membership is frozen.
4. Manifest mechanics per fix: regenerate the FULL proposal in a
   temporary workspace via the runner, verify shrink-only, then apply
   exact row/facet changes — a row leaves only when ALL four facets
   are exact; partial fixes shrink the recorded facets/count within
   the row (the ratchet rejects stale exact rows AND facet mismatches,
   h2_2c_acceptance.rs:2656; the join demands exact manifest equality,
   :2677). Diagnostic-intersection rows normally REMAIN after a pure
   source-map fix (they are W2's).
5. W1 closes when every frozen-list row is exact or reassigned (with
   the reassignment recorded here).
6. **Adjacent-band manifest sweep (hosted-discovered, 2026-08-29):**
   a mapping fix heals cases in ADJACENT bands' manifests too — the
   first landing healed five H2.6a rows (StringLiteralWithNewLine
   es2015+es5, FunctionInInternalModule…01, sourceMapValidationClass
   es2015+es5) and the hosted run_h2_6a ratchet correctly refused the
   stale-exact rows. Every W-wave landing therefore runs the
   h2-5h/6a/6b acceptance sweeps BEFORE pushing. CAUTION: the
   regenerate-manifest env writes DEFAULT owner fields — apply
   targeted row removals preserving the curated owners of the
   surviving burn-down rows (the owned-queue amendment), never adopt
   a wholesale regeneration.

## 5. Prohibitions (wording per objection 5)

Manifest shrink-only; no hand-authored expected bytes; no change to
the production `None => LF` newline behavior; the 279 slice-owned
refusal rows untouched; the probe stays `#[ignore]`-gated; no
`set_original_node` semantics change.

## 6. Cross-review record

Round 1: operator draft → sol REVISE with 11 numbered findings (all
accepted; §§1-5 above incorporate them). Round 2: sol AGREE —
sign-off (2026-08-29): "The revision faithfully incorporates all
Round 1 findings … No residual objections." Round 3 (K4/K5): operator characterization -> sol REVISE decomposing K4 into K4a/b/c with pinned causes + K5 AGREE with the convert_for_of_statement_head pin; the revision incorporates the review text verbatim (agreement by construction). The §3 class table is signed
only when K1/K2 fixes land probe-verified and K3 membership is frozen.
