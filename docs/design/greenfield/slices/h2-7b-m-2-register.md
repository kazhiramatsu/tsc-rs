# h2-7b-m-2 — Phase-E/P register (values recorded as minted; `__X__` = TO-FILL at the named step)

## Design gate
- Packet rev 10 RATIFIED 2026-09-04 (sol r1 REVISE 15+1 → r2 REVISE 7+1 → r3 REVISE 4+2 → r4 REVISE 3 → r5 REVISE 4+1 → r6 REVISE 1+1 → r7 REVISE 2+2 → r8 REVISE 2 → r9 REVISE 1 → r10 AGREE). Authority file: `docs/design/greenfield/slices/h2-7b-m-2.md`.
- Frozen order: repair (PR #502 @a67fdd37) → m-1 (PR #503 @`440e6073`) → this rung → the closure waves → ca. Trusted base for the train: `440e6073`.
- Parent restatement landed with the packet (h2-7b.md §6.2 / §6.4 / §8: the hosted boundary grows by exactly one call at m-2).

## Opening commit (pre-flip) — FILLED (lane A, sol xhigh, SPEC-M2-A with operator resolutions 1-3; STATUS archived as target/session-notes/7b/lanes/m2/STATUS-m2-a.md; commits 6e236a9b (opening) + 810f16ab (the §6.4 a+b hunk) on lane/m2-a; train merges E5 cdc30de5 / E7 045f4143)
- `--pre-flip` census (normal priority, `TSRS_H2_5G_WORKERS=2`; lane 107.55 s, operator re-runs 103-105 s): `typed_emitDeclarationOnly_refusals=111 executed=1446 executed_non_controls=1440 zero_member_controls_emit_exact=6 deferred=36 full_vector_exact=0 full_vector_diverging=1446 h2_7b_activity=0 manifest_writes=0`; the six controls diverge only on `exit_code` / `reported_diagnostic` (fence amendment #3; the 22-row class printed with owner h2-7b-w1: `states={"discovered-config": 4, "project-config": 18}`); the report-only `@ts-nocheck` count 3; the structural canary: the refused-option sets (`declarationMap` / `outFile` / standalone `emitDeclarationOnly`) return the typed error with every counter 0 and no sink write (lane B's `refused_option_sets_leave_every_activity_counter_and_sink_write_at_zero`).
- the 6c pre-flip population (§6.4c1): `TSRS_H2_6C_WRITE_DIVERGENCES=1 cargo xtask h2-6c-acceptance` → 451 rows written (19.61 s), machine review `coarse_facets_byte_identical=true added_fields=refused_option,mismatch_vector,facet_fingerprint_sha256`; the pre-(a) `refused_option` totals outFile 144 / outDir 130 / rootDir 4 / isolatedModules 1 (the four runtime `rootDir` first refusals joined to the dispositions' `outDir` blocker — emitter validation order); normal run 639 / 4 / 643 (operator re-run 25 s). After the (a) hunk the one-off normal run reports EXACTLY the 33 registered transitions (totals declarationMap 6 / outFile 171 / outDir 130 / rootDir 4 / isolatedModules 1; the 33 case_ids in STATUS-m2-a.md and the 810f16ab message; operator reproduction: 33 lines, outFile 27 / declarationMap 6).
- guard test `hosted_acceptance_and_oracle_phases_cover_h2_2c_rungs` green with `run_h2_7b` inside `fn acceptance` (`cargo test -p tsc-rs-xtask` 269 / 0 / 2 ignored at both commits); `.github/ci/qualification.mjs` `HOSTED_ACCEPTANCE_QUALIFIED_CALLS` += `h2_2c_acceptance::run_h2_7b` (operator edit; qualification check 22 contracts, test 38 / 38); policy pins re-recorded — main.rs `393b2443…`; h2_2c_acceptance.rs `1f4df636…` at the opening commit, `b46778a1…` at the hunk commit.
- lane A stops on the way (all archived): STOP 1 (project loader roster), STOP 2 (the A1 census gap), STOP 3 (the §7 wall: workers=1 default + demotion), STOP 4 (packet/SPEC conflict: E4 not yet merged into the worktree — correct), STOP 5 (`.github/ci/qualification.mjs` outside the file list — the operator edit above).

## Flip commit — FILLED (lane B, sol max, SPEC-M2-B; STATUS archived as target/session-notes/7b/lanes/m2/STATUS-m2-b.md; commit fb550f8d on lane/m2-b; train merge E6 123d5ab7; operator battery re-run demoted: fmt / clippy / emitter 459-449-18 / checker 1700 / emitter contracts 449 / compiler contracts 175-0 (510 s) / ledger 3785-0-0)
- LIFTED controls re-pinned (the §5.11 list + the h1 addendum): output_plan_contract.rs 149-152 → 150-155 (a declaration-bearing script unit validates `Ok(())`); h2_7a_m4_controls.rs 179-189 → 181-185 (`Ok(())`), 191-197 → 186-190 (routing to `print_declaration`); printer_foundation_contract.rs 697-702 → 697-703 (exact text); declaration_printer_reprint_contract.rs 347-352 → 347-354 (`type Alias = string;\n`); h2_7a_ca_controls.rs control 3 keeps the standalone refusal and admits the combined shape (190-198); h2_7a_m35_controls.rs :31 → 31-34 (+ `crates/emitter/src/declarations/orchestration.rs`); h1_emit_qualification_contract.rs 449-465 → 522-553 (both frozen writes exact) + 569-576 (the H2.7b member observation beside H2.6c). RETAINED controls untouched: h2_7a_m4_controls.rs 166-177 / 198-208, output_plan_contract.rs 123-146 / 153-165, printer_foundation_contract.rs 690-696 (Bundle), the SourceFile declaration-syntax default (reprint contract 355-361), the ca controls 1-2, every non-goal option refusal — verified in the zero-context diff and green in both aggregators.
- focused tests (§9.2): the three resolver members (refusal + CheckerSession values), the pre-pass branches, the length-1 contract, the collision matrix (input overwrite / duplicate / canonical case / JS suppression under `emitDeclarationOnly` / `declBlocked` propagation; the TS5055 control: js written, d.ts blocked, `emitSkipped` true, listing js only, H2.7b activity exactly 1), the printer activation (`Declaration` prints; `SourceFile` refuses; `Bundle` refused; the oracle consulted at the uniqueness decision), the unavailable-oracle fault injection (the nested typed `EmitFailure::Printer → PrinterError::Transform → TransformError::Resolver → UnavailableForName(HasGlobalName)`, reached once, no declaration write), the mixed `Written` / `SkippedUnchanged` listing control, the failing-declaration-sink control, the `PlanDeclarationPaths` fallback order, the single-admission profile control (`H2RuntimeSlice::H2_7b.index()` in exactly one function; five production `h2_7b_profile()` sites; no production `h2_6c_profile()` outside the profile chain), the structural zero-activity canary (declarationMap / outFile / standalone emitDeclarationOnly → the typed error, every counter 0, no sink write): all green in `cargo test -p tsc-rs-compiler --test contracts` (175 / 0 / 13 ignored) and `cargo test -p tsc-rs-emitter --test contracts` (449).
- L2 production replay 0 / 0: `declaration_transformer_replay_decision_equal` — harness mode cases 116 / windows 202 / unblocked 199 / blocked 3, divergences 0, byte mismatches 0; production mode writes 199 / diagnostics 3 / global_name_queries 0 / oracle_fault_proofs 1 / mismatches 0 (168.96 s lane; operator re-run in the full aggregator). ledger check: entries 3785 / stale 0 / undispositioned 0 (todo_port 0).
- the four printer-field dispositions (grep evidence, `createPrinter` = _tsc.js:116912-122340): `module` proven-unobservable on the non-bundle route (effective read :116928 `moduleKind`; sole downstream use :117722 guarded by `bundle && moduleKind === None`); `moduleResolution` proven-unobservable (zero reads inside createPrinter; copied into the options object at :116678 only); `inlineSourceMap` proven output-unobservable (sole read :117775 selects `writeLiteral` vs `writeStringLiteral`, both aliases of `writeText` :12683-12684; no declaration-map generator in band); `extendedDiagnostics` proven output-unobservable (sole read :116925 → the comment timer :116966). No Rust field added for any of the four.
- Residuals (out of band, recorded for the closure waves / the named owners): (1) upstream :116642-116645 sets `emitSkipped = true` when a unit has NO declaration path under `emitOnly || emitDeclarationOnly` (JSON sources under `emitDeclarationOnly`; 0 emit-eligible JSON sources in the band, §2.4) — the Rust loop runs the declaration member only when the plan has a declaration path and does not set the flag → H2.3d/H2.8a adjacency, or the first closure wave if a band row ever reaches it; (2) upstream adds a sink-failure diagnostic per unit as it writes, Rust writes after the unit loop, so a failing write of unit N would follow later units' declaration diagnostics in `emit_result.diagnostics` (no sink faults in the band; H2.8a owns filesystem faults).

## First sweep / cross-slice — TO-FILL
- exact 1,372 / diverging 185 (manifest rows 185 — compiler 92 / conformance 71 / project 22; owner `h2-7b-m-2-divergence-closure`); refusals 0: ✓ (no typed refusal in normal mode; the pre-flip census 111 / 1,446 / 6); the 28 (A1; 29 at rev 10) transform-blocked + 5 collision rows exact: PARTIAL — 17 / 33 exact (transform-blocked 14 / 28, collision 3 / 5); the 16 residual rows are GENUINE divergences carried as manifest rows for w1: 11 rows where Rust emits and TypeScript blocked (`nodeModulesExportsBlocksSpecifierResolution` ×4 and `nodeModulesExportsSourceTs` ×4 at module=node16/18/20/nodenext, `legacyNodeModulesExportsSpecifierGenerationConditions`, `declarationEmitObjectAssignedDefaultExport`, `declarationEmitUsingTypeAlias1` — the TS2883 / TS2307 class), 2 diagnostic text / position rows (`declarationEmitMappedTypeTemplateTypeofSymbol` TS4118, `declarationEmitMixinPrivateProtected` TS4094), 1 write-bytes row (`privateFieldsInClassExpressionDeclaration`), and the 2 `jsFileCompilationSameNameDtsNotSpecifiedWithAllowJs` project rows (the TS5107 config-driven class); activity 2,414 (A1; 2,445 at rev 10) per repetition: ✓ (repetitions 2; the canonical manifest is sweep attempt 9, 2026-09-04 16:38-16:50 — attempts 1-6 are the amendments #4-#4e, attempt 7 the #4f facet correction (1,363 / 194), attempt 8 the revised #4e (1,371 / 186: nine cross-file rows exact, one TS4023→TS2527 regression), attempt 9 the recovery-boundary deferral (1,372 / 185))
- 6c regeneration: in-band shrink 133 (451 → 318; ≤ 133 ✓), the 133-row activation refinement ✓ (every deleted row is a joined row, `writes_diverging` 1 ×21 / 6 ×64 / 11 ×48 as §6 predicts; list in the appendix), the 33 registered refusal transitions (`case_id` list): declarationMap 6 + outFile 27 (`emit_refused` false → true, write / diagnostic / result facets cleared, vector = the single refusal element; list in the appendix); componentwise review of the manifest diff: 285 rows byte-equal, 133 deleted, 33 transitions, 0 added, 0 rejected; totals 639 / 4 / 643: ✓ (exact 321 + diverging 318 = 639, deferred 4, candidates 643); 293 / 0 activity: ✓ (admitted_join_rows 133, declaration_members 293, other_executed_rows 0)
- 5h / 6a / 6b shrink-only: no stale row — the normal runs pass unchanged after the flip (5h 824 exact / 64 diverging / 44 deferred; 6a 171 / 4 / 2; 6b 6 / 0 / 0; 2026-09-04 17:02, workers=2), no regeneration; corpus ratchet T0 100% FP=0: ✓ (`cargo xtask conformance` band=all fixtures 5,908 / cases 7,691, matched 49,024 / 49,024, FP=0 FN=0)
- inventory re-mint (seven anchored H2.7b rows; the two `markLinkedReferences` rows → H2.8c): `__`; close artifact `lifted-at-H2.7b` ×4 with `retained_arms`: `__`
- resources: local sweep wall 136 s normal mode at 2 workers, normal priority, uncontended (the post-E12 RSS run, 2026-09-04 16:59; attempt 9's contended write mode 348 s / normal 136 s; STOP 15 min), per-worker RSS: the runner is ONE process with 2 worker threads — max RSS 4,036 MiB (3.94 GiB; `ps` sampled every 3 s over 45 samples) so the per-worker share is under the 4 GiB ceiling, aggregate 4,036 MiB (8 GiB ceiling); the first hosted `run_h2_7b` step wall `__`

## Walk / gate / PR — recorded in the PR body at the final head

## Implementation-time amendments
- 2026-09-04 lane B STOP 1: `h1_emit_qualification_contract.rs` (:449-465 the declaration-control write truncation; :488 the H2.6c activity mapping) is a LIFTED control missed by the G11 survey — packet §5.11 addendum; the lift = both frozen writes exact incl. the `.d.ts` bytes + the H2.7b member observation.
- 2026-09-04 lane A STOP 1: the harness project loader `crates/harness/src/upstream_suites/execution/project.rs` joins the opening-commit file roster (the emitted-files listing must be enabled for project rows through `load_project_emit`; packet §4.6 unchanged in substance).
- 2026-09-04 lane A STOP 2 → amendment A1 (packet §2.0; h2-7b-m-1.md §2 A1 / §9.7 + its register): the m-1 census was option-only and admitted 10 parse-diagnostic rows the emitter refuses to H2.9 before any outcome (`declarationEmitInvalidExport.ts#default`, ordered case 239). Lane D repaired the machine + schema and re-minted the artifact on this train (admitted 1,557 / deferred 36; generator `c9cf469d…`, fingerprint `6ff65e52…`); every frozen count of this rung reads through the §2.0 table (opening census 111 / 1,440 / 6 / 36; planned members 2,414; `declaration_writes` 2,380; transform-blocked 28). Lane A resumes from the re-minted artifact (its validator/consts re-derived); lanes B and C are unaffected (the 7a witness/probe and the inventory/close pins do not read the 7b counts).
- 2026-09-04 lane A STOP 3 → operator resolution (no design change): the pre-flip census had run with the runner's LOCAL default of one worker (`TSRS_H2_5G_WORKERS` unset; the §7 ceiling is 2 and the hosted workflow sets 2) under an operator-side background-QoS launch of the lane (efficiency cores) — 992 s unfinished. Rule recorded: every §7-bounded command runs with `TSRS_H2_5G_WORKERS=2` at NORMAL priority (a wall-bounded measurement is never demoted, the perf-ceiling rule); the operator's census then took 103-105 s (`user` 191 s) from lane A's worktree.
- 2026-09-04 **mid-train fence amendment #3** (the operator's census exposed two facts; packet §2.2 note): **(a)** the six zero-member controls are EXACT on the emit facets (`writes_diverging=0`, every `emit_result` field, the listing) and diverge only on `exit_code` (oracle 2 / Rust 0) and `reported_diagnostic` presence — the oracle reports TS5107 deprecations for the project runner's SYNTHESIZED options (`module=AMD`, `moduleResolution=classic`) while Rust's `programmatic_option_diagnostics` (compiler/src/lib.rs) yields to the config parse whenever a config file exists and `deprecation_option_diagnostics` (program/src/config.rs) sees only options present in the config TEXT (the controls' `tsconfig.json` is empty; upstream `verifyDeprecatedCompilerOptions` runs on the merged options). The class = the 22 config-driven project rows of the band (`root_selection.state` ≠ `explicit-inputs`; 391 admitted rows carry 5107 in total, but only the config-driven ones lose it in Rust); none is a 6c-manifest row (6c's 30 `diagnostics_diverging` rows are compiler rows) — the class is NEW to this band, not emit-owned, and is assigned to the first closure wave h2-7b-w1. Consequence for the runner (§2.2 / §4.7 / §9.1): the zero-member control assertion applies to the EMIT facets (+ H2.7b activity 0), the non-emit facets are reported per control and become ordinary manifest rows in normal mode; the census expectation is refused 111 / executed 1,446 (1,440 + 6) / controls 6 emit-exact / deferred 36 with the full-vector exact / diverging split printed as measured (0 / 1,446 today). **(b)** `conformance/moduleResolution/resolutionModeTripleSlash2.ts#default` executed instead of refusing: its declaration options come only from the fixture's virtual `/tsconfig.json`, which the qualified-vfs loader pushed into the VFS without parsing (directive `settings` only; the recorded-compiler-plan route parses configs; 7 band rows carry `input.virtual_config`, this is the only one whose declaration options depend on it). Lane A fixes the loader (config `compilerOptions` applied under the floor BEFORE the directives, the upstream harness precedence) and adds the structural guard: the Rust program's declaration-family options must equal the census `effective_declaration_options` on every admitted row. The census then reproduces 111 typed refusals.
- 2026-09-04 integration (E5-E10): lane C merged (E8 2f9805ee) and its TO-FILL anchors filled from the landed flip (E9 daffb43b: `product:declaration` → `emit_declaration_unit`, `hasGlobalName@116688` → `file_level_unique_name`, the two `collectLinkedAliases` rows → `collect_linked_aliases_for_declaration`, `h2_7b_profile` → activity.rs 488); 72 curated printer.rs audit anchors re-pointed (+30 lines after the flip); the close generator's `composite` marker became the tuple text (the flip's `emitDeclarationOnly` narrowing added a second `options.composite == Some(true)` inside `validate_bootstrap_emit_options`; the generator requires one live occurrence per arm) and its `h2_7b_profile` arm is anchored at the function header (the lane's marker-on-header check could not hold for a multi-line function); E10 2229d8f2 re-recorded the fuzz-preflight source pin for the flipped `crates/checker/src/lib.rs`. The two H2.7a generators refuse as expected until the walk re-mints their artifacts (owner inventory `stale`; close: the stored artifact predates the lifted schema).
- 2026-09-04 **mid-train fence amendment #4** (the first post-flip sweep, packet §3 "a residual … stops the train until its exact condition is named"): `cargo xtask h2-7b-acceptance` (write mode, normal priority, workers=2) PANICKED after 278 executed rows on `compiler/declarationEmitOptionalMappedTypePropertyNoStrictNullChecks1.ts#default` — `crates/checker/src/access.rs:143 debug_assert!(strictNullChecks)` inside `get_optional_type` (upstream `getOptionalType` carries the same `Debug.assert(strictNullChecks)`), reached from the NodeBuilder's annotation-reuse decision `ProductionSyntacticBuilderResolver::can_reuse_type_node_annotation` (node_builder/serialize.rs) via `typeFromProperty → serializeTypeAnnotationOfDeclaration` for a mapped-type optional property whose `requiresAddingImplicitUndefined` is true WITHOUT strictNullChecks (the property branch of the host's `requiresAddingImplicitUndefined` :50790-50800 has no strictNullChecks gate upstream either). Upstream's `canReuseTypeNodeAnnotation` (:50932-50955) applies `addOptionality(annotationType, !isParameter(node))` = `strictNullChecks && isOptional ? getOptionalType(type, isProperty) : type` (:56029-56031) — the gate lives INSIDE addOptionality; the two Rust reuse-decision sites called `get_optional_type` directly (node_builder/serialize.rs `can_reuse_type_node_annotation`; check.rs `reusable_annotation_…` text-slice twin, which had noted "the strictNullChecks gate held upstream"). Fix (E12): both sites gate on `tables.strict_null_checks`, mirroring addOptionality; no design change, no schema/ratchet change beyond the sweep artifacts. Control: `crates/compiler/tests/integration/h2_7b_m2_controls.rs::optional_mapped_type_property_without_strict_null_checks_emits_the_frozen_declarations` replays the exact band row through the runner's production entry (`load_qualified_compiler_emit_with_option_floor` + `emit_with_reported_diagnostics_for_harness`) and pins the two frozen `.d.ts` writes byte-exact from the m-1 artifact. The 6c regeneration that ran in the same sweep before the panic is unaffected (no 6c row reaches the branch): activity 293 / 133 rows, refused_option totals declarationMap 6 / outFile 171 / outDir 130 / rootDir 4 / isolatedModules 1, manifest 451 → 318 rows (exact 188 → 321: the 133 in-band rows now exact — the §6.4(c2) shrink at its ceiling).
- Runner diagnostic (operator, E12): `TSRS_H2_7B_TRACE=1` prints `H2.7b trace: executing <case_id>` before each row (the panic left no case id; the bounded pipeline aborts on the first worker panic).
- 2026-09-04 **fence amendment #4b** (sweep attempt 2, after the #4 fix): a second checker panic at `crates/checker/src/node_builder/specifier.rs:2216` — `byte index 28 is out of bounds of "/.src/node_modules/umd.d.ts"` in `try_get_module_name_as_node_module`'s package-root walk: upstream `path.indexOf(directorySeparator, packageRootIndex + 1)` (:46064-46179) returns -1 when the start index sits at or beyond the end of the path (a module file DIRECTLY under `node_modules`); the Rust slice `path[package_root_index + 1..]` panicked instead. Fix (E12): `path.get(package_root_index + 1..).and_then(|rest| rest.find('/'))`. Control: `h2_7b_m2_controls.rs::node_modules_file_specifier_emits_the_frozen_declarations` (`compiler/importShouldNotBeElidedInDeclarationEmit.ts#default`, the only band row carrying `/.src/node_modules/umd.d.ts`; js + d.ts writes byte-exact). Both amendment-#4 controls pass at the fixed tree; the sweep re-ran with `TSRS_H2_7B_TRACE=1` (attempt 3).
- 2026-09-04 **fence amendment #4c** (sweep attempt 3): `compiler/declarationEmitPrivatePromiseLikeInterface.ts#default` (a band row whose frozen outcome is TS7056 ×3 + `emitSkipped`: the inferred type of `abc1..3` expands a non-exported recursive alias `TPromise` until the 1,000,000-char truncation cap) took > 240 s in Rust against 0.54 s upstream — not a loop: the recursion is bounded (`symbolDepth` cap 10, fan-out 2 → ~2k anonymous-type visits, ~450k approximate length in 100 s) but every syntactic-reuse seam cloned the WHOLE `TransformArena` (7 `let snapshot = arena.clone()` sites in node_builder/serialize.rs: the `ProductionSyntacticBuilderResolver` needs a parse-tree view + a scratch arena while the builder holds `&mut arena`) → thousands of deep clones of a growing arena (the hot frames: `NodeArena::clone`, `EmitMetadata::clone`, BTreeMap `clone_subtree`). Two repairs (E12), both upstream-invisible: (1) the seams share ONE snapshot per top-level NodeBuilder call (`NodeBuilderContext::arena_snapshot: Option<Rc<RefCell<TransformArena>>>`; parse nodes are immutable, the snapshot's own scratch additions persist; nested seams reuse it); (2) the `links.serializedTypes` reuse of `visitAndTransformType` (:51823-51853) is ported — `LinksTables::serialized_type_nodes` keyed by (enclosing declaration, type, flags, internal flags) with the TransformArena identity (new `TransformArena::id()`), replaying tracked symbols under their recorded enclosing declaration (`track_symbol_in_context_at`), the truncation state and the added length, cloning the node into the arena (`deep_clone_or_reuse_node`, previously dead code). Control: `h2_7b_m2_controls.rs::recursive_inaccessible_alias_truncates_with_ts7056_and_blocks_the_declaration` (the frozen writes js / d.ts / js byte-exact, `emitSkipped` true, reported [7056, 7056, 7056]); the three m-2 controls now run in 14.5 s total (the row ≈ 10 s; still ~20× upstream — the residual per-visit cost is a w1 performance item). No design change; no oracle / schema / ratchet change beyond the sweep artifacts.
- 2026-09-04 **fence amendment #4d** (sweep attempt 4 — the first sweep without a panic; the runner's ordered join reported the first row error): `compiler/computedEnumTypeWidening.ts#default` — `createLiteralConstValue … literal-const type is not a literal`. `emit_literal_type_to_node` (declaration_emit.rs, the `literalTypeToNode` port :88491-88505) selected the symbol-expression arm with `flags.contains(TypeFlags::ENUM_LIKE)`, which demands BOTH `Enum` and `EnumLiteral` bits; upstream's `type.flags & EnumLike` takes either. The arm was dead: a computed enum member's fresh `Enum` type (createComputedEnumType :57475) fell through to the value arms and aborted, and constant members' `EnumLiteral | NumberLiteral` types printed their VALUE where upstream prints the member expression (`declare const c1 = E.B`). Fix (E12): `intersects`. Control: `h2_7b_m2_controls.rs::computed_enum_member_literal_const_prints_the_member_expression` (frozen `.d.ts` byte-exact). Runner: `run_h2_7b_mode` now prints EVERY row error (`H2.7b row error: …`) before failing, so one sweep names the whole class list instead of the first row.
- 2026-09-04 **fence amendment #4e** (sweep attempt 5 — the first sweep that listed EVERY row error: 6 rows, two classes; the (a) resolution REVISED at the E12 battery, see below): **(a)** five rows (`declarationEmitMappedTypeDistributivityPreservesConstraints`, `declarationEmitPartialReuseComputedProperty`, `declarationEmitResolveTypesIfNotReusable`, `mappedTypeWithAsClauseAndLateBoundProperty2`, `conformance/…/noInferRedeclaration`) failed with `factory operation failed: unknown transform node <source>:<id>`: the syntactic NodeBuilder reused or inferred from syntax that lives in ANOTHER source file (a re-export serializing an imported declaration's constraint / initializer) and its rebuilt parent received children created in the emitted target — TypeScript's single node pool allows it, the Rust arena keys child handles by source. The first resolution (yield to type-based serialization for foreign-source syntax) was WRONG: the E12 battery's m-3/m-4 replay contracts (`declaration_resolver_replay_decision_equal` 3 printed-form mismatches, `declaration_transformer_replay_decision_equal` 19 divergences / 6 byte mismatches; `F1/alias-inlining`, `F1/alias-from-indirect-file`, `S3/literalconst-4`, `S3/typeofexpr-2`, `F12/static-block-substitution`) showed TypeScript reusing the foreign annotation (`(v: import("./aExp").O["prop"], …)` where the fallback printed `(v: string, …)`). Resolution (the upstream semantics, no design change): (1) `tryReuseExistingTypeNodeHelper` / `visitExistingNodeTreeSymbols` CLONE every reused node and `setTextRange(context, …)` copies no positions from another file — the three walk entries (`try_reuse_existing_type_node`, `type_from_expression`, `type_from_function_like_expression`) first clone foreign-source syntax into the emitted target (`clone_node_to_source`: synthesized, position-free, original chain kept; `approximateLength += end - pos` follows the original chain back to the positioned parse node — the second battery's only residue was this 74-character bookkeeping gap on `S3/literalconst-4`) and rebuild the clone; (2) the resolver reads the BUILDER'S arena — every `SyntacticBuilderResolver` member now receives `arena` (15 members threaded; `parse_node` / `project_parse_node` / the accessibility error-name display node run on it), which retires the m-3 per-seam arena clone and the #4c shared snapshot (`NodeBuilderContext::arena_snapshot`, `shared_arena_snapshot`) entirely — a clone in the builder's arena is visible to the resolver, and the `EmitTrackerAccess::is_symbol_accessible` path (no builder arena in scope) keeps a lazily mounted per-resolver scratch arena; (3) belt-and-braces: the generic child visitor places every visited child in its parent's source, `track_existing_entity_name` hands the tracked name back in the requested source, and all seven routing seams clone a finished subtree into the target (`into_target`); (4) sweep attempt 8 exposed the last gap the fallback had masked — a reused foreign type literal carrying a computed property name with an inaccessible symbol (`declarationEmitComputedPropertyNameSymbol2`: upstream TS4023, Rust TS2527) — because upstream's `createRecoveryBoundary` wraps the six tracker error reports (`reportCyclicStructureError`, `reportInaccessibleThisError`, `reportInaccessibleUniqueSymbolError`, `reportLikelyUnsafeImportRequiredError`, `reportNonSerializableProperty`, `reportPrivateInBaseOfClassExpression`) with `markError` — deferred, truncated by a recovery scope, replayed at `finalizeBoundary` — while the Rust boundary let them reach the inner tracker at once; `NodeBuilderTracker` now keeps recovery frames (`push_recovery_frame` / `recovery_scope_top` / `recover_recovery_scope` / `pop_recovery_frame`) that the boundary drives (verified against an instrumented copy of the vendored `_tsc.js`: the same arm, `introducesError=true`, no TS2527). Controls: `cross_file_annotation_reuse_rows_complete_the_production_emit` (emit completes, emitSkipped and the write count exact) and `foreign_computed_property_name_reuse_defers_the_tracker_reports_like_upstream` (TS4023, byte-exact); the replay contracts are the fidelity gate. **(b)** `mappedTypeNoTypeNoCrash`: the printer refused a mapped type without a type clause (`MissingTransformedChild`); upstream `emit(node.type)` of `undefined` prints nothing → `{ [K in keyof T]: ; }` — the type clause is optional in `emit_mapped_type`; control `mapped_type_without_a_type_clause_prints_the_frozen_declaration` (byte-exact). Also in E12: `TransformArena` clones take a FRESH identity (manual `Clone`; `PartialEq` ignores the identity) so `serializedTypes` entries are arena-bound; the runner prints every row error before failing; the 6c `refused_option` totals test asserts the post-flip map (declarationMap 6 / outFile 171 / outDir 130 / rootDir 4 / isolatedModules 1) and derives the c1 map by subtracting the 33 transitions.
- 2026-09-04 **runner facet correction #4f (sweep attempt 7)**: lane A's H2_7b vectorizer compared the oracle observation's `emit_refused` — the m-1 machine's `emitSkipped` alias (`const emitRefused = emitResult.emitSkipped`, h2-7b-qualification.mjs) — with a constant `false` (the Rust typed refusal, which never reaches a successful-outcome vector) and derived a `refusal:variant` element from the same value, so every emitSkipped row diverged by construction (attempt 6: 1,346 / 211 with the 33 rows diverging ONLY on those two elements). Correction in `vectorize_successful_observation` (crates/xtask/src/h2_2c_acceptance.rs): the `emit_result:emit_refused` element compares the oracle alias with the Rust `emit_skipped()`, and successful outcomes carry no `refusal:variant` element (typed refusals fail the run before the join, packet §4.2; the manifest row field `emit_refused` stays false on every row). The canonical manifest was re-minted from scratch (attempt 7: 1,363 / 194 / 36); xtask h2_7b unit tests 5 / 5; the 6c manifest bytes are unchanged by the correction (the H2_6c profile keeps its legacy facet).

## Appendix — the flip-regeneration row lists (packet §6.4c2; minted 2026-09-04, sweep attempt 7)

### The 33 refusal transitions (`emit_refused` false → true; declarationMap 6 / outFile 27)

`compiler/declarationMapsWithSourceMap.ts#default`, `compiler/inlineSourceMap2.ts#target%3Des2015`, `compiler/inlineSourceMap2.ts#target%3Des5`, `compiler/inlineSources.ts#target%3Des2015`, `compiler/inlineSources.ts#target%3Des5`, `compiler/inlineSources2.ts#target%3Des2015`, `compiler/inlineSources2.ts#target%3Des5`, `compiler/out-flag2.ts#target%3Des2015`, `compiler/out-flag2.ts#target%3Des5`, `compiler/out-flag3.ts#target%3Des2015`, `compiler/out-flag3.ts#target%3Des5`, `compiler/outModuleConcatAmd.ts#target%3Des2015`, `compiler/outModuleConcatAmd.ts#target%3Des5`, `compiler/outModuleConcatCommonjs.ts#target%3Des2015`, `compiler/outModuleConcatCommonjs.ts#target%3Des5`, `compiler/outModuleConcatCommonjsDeclarationOnly.ts#target%3Des2015`, `compiler/outModuleConcatCommonjsDeclarationOnly.ts#target%3Des5`, `compiler/outModuleConcatES6.ts#default`, `compiler/outModuleConcatSystem.ts#target%3Des2015`, `compiler/outModuleConcatSystem.ts#target%3Des5`, `compiler/outModuleConcatUmd.ts#target%3Des2015`, `compiler/outModuleConcatUmd.ts#target%3Des5`, `compiler/outModuleTripleSlashRefs.ts#target%3Des2015`, `compiler/outModuleTripleSlashRefs.ts#target%3Des5`, `compiler/sourceMapWithCaseSensitiveFileNames.ts#default`, `compiler/sourceMapWithMultipleFilesWithCopyright.ts#default`, `compiler/sourceMapWithMultipleFilesWithFileEndingWithInterface.ts#default`, `compiler/sourceMapWithNonCaseSensitiveFileNames.ts#default`, `conformance/classes/classStaticBlock/classStaticBlock25.ts#target%3Des2022`, `conformance/classes/classStaticBlock/classStaticBlock25.ts#target%3Desnext`, `conformance/esDecorators/classDeclaration/esDecorators-classDeclaration-sourceMap.ts#target%3Des2015`, `conformance/esDecorators/classDeclaration/esDecorators-classDeclaration-sourceMap.ts#target%3Des2022`, `conformance/esDecorators/classDeclaration/esDecorators-classDeclaration-sourceMap.ts#target%3Desnext`

### The 133-row activation refinement (rows deleted from the 6c manifest — now exact; `writes_diverging` 1 ×21 / 6 ×64 / 11 ×48)

`compiler/emitBOM.ts#default`, `compiler/out-flag.ts#target%3Des2015`, `compiler/out-flag.ts#target%3Des5`, `compiler/properties.ts#target%3Des2015`, `compiler/properties.ts#target%3Des5`, `project/mapRootAbsolutePathMixedSubfolderNoOutdir.json#module%3Damd`, `project/mapRootAbsolutePathMixedSubfolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootAbsolutePathModuleMultifolderNoOutdir.json#module%3Damd`, `project/mapRootAbsolutePathModuleMultifolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootAbsolutePathModuleSimpleNoOutdir.json#module%3Damd`, `project/mapRootAbsolutePathModuleSimpleNoOutdir.json#module%3Dcommonjs`, `project/mapRootAbsolutePathModuleSubfolderNoOutdir.json#module%3Damd`, `project/mapRootAbsolutePathModuleSubfolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootAbsolutePathMultifolderNoOutdir.json#module%3Damd`, `project/mapRootAbsolutePathMultifolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootAbsolutePathSimpleNoOutdir.json#module%3Damd`, `project/mapRootAbsolutePathSimpleNoOutdir.json#module%3Dcommonjs`, `project/mapRootAbsolutePathSingleFileNoOutdir.json#module%3Damd`, `project/mapRootAbsolutePathSingleFileNoOutdir.json#module%3Dcommonjs`, `project/mapRootAbsolutePathSubfolderNoOutdir.json#module%3Damd`, `project/mapRootAbsolutePathSubfolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootRelativePathMixedSubfolderNoOutdir.json#module%3Damd`, `project/mapRootRelativePathMixedSubfolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootRelativePathModuleMultifolderNoOutdir.json#module%3Damd`, `project/mapRootRelativePathModuleMultifolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootRelativePathModuleSimpleNoOutdir.json#module%3Damd`, `project/mapRootRelativePathModuleSimpleNoOutdir.json#module%3Dcommonjs`, `project/mapRootRelativePathModuleSubfolderNoOutdir.json#module%3Damd`, `project/mapRootRelativePathModuleSubfolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootRelativePathMultifolderNoOutdir.json#module%3Damd`, `project/mapRootRelativePathMultifolderNoOutdir.json#module%3Dcommonjs`, `project/mapRootRelativePathSimpleNoOutdir.json#module%3Damd`, `project/mapRootRelativePathSimpleNoOutdir.json#module%3Dcommonjs`, `project/mapRootRelativePathSingleFileNoOutdir.json#module%3Damd`, `project/mapRootRelativePathSingleFileNoOutdir.json#module%3Dcommonjs`, `project/mapRootRelativePathSubfolderNoOutdir.json#module%3Damd`, `project/mapRootRelativePathSubfolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlMixedSubfolderNoOutdir.json#module%3Damd`, `project/maprootUrlMixedSubfolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlModuleMultifolderNoOutdir.json#module%3Damd`, `project/maprootUrlModuleMultifolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlModuleSimpleNoOutdir.json#module%3Damd`, `project/maprootUrlModuleSimpleNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlModuleSubfolderNoOutdir.json#module%3Damd`, `project/maprootUrlModuleSubfolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlMultifolderNoOutdir.json#module%3Damd`, `project/maprootUrlMultifolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlSimpleNoOutdir.json#module%3Damd`, `project/maprootUrlSimpleNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlSingleFileNoOutdir.json#module%3Damd`, `project/maprootUrlSingleFileNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlSubfolderNoOutdir.json#module%3Damd`, `project/maprootUrlSubfolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlsourcerootUrlMixedSubfolderNoOutdir.json#module%3Damd`, `project/maprootUrlsourcerootUrlMixedSubfolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlsourcerootUrlModuleMultifolderNoOutdir.json#module%3Damd`, `project/maprootUrlsourcerootUrlModuleMultifolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlsourcerootUrlModuleSimpleNoOutdir.json#module%3Damd`, `project/maprootUrlsourcerootUrlModuleSimpleNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlsourcerootUrlModuleSubfolderNoOutdir.json#module%3Damd`, `project/maprootUrlsourcerootUrlModuleSubfolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlsourcerootUrlMultifolderNoOutdir.json#module%3Damd`, `project/maprootUrlsourcerootUrlMultifolderNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlsourcerootUrlSimpleNoOutdir.json#module%3Damd`, `project/maprootUrlsourcerootUrlSimpleNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlsourcerootUrlSingleFileNoOutdir.json#module%3Damd`, `project/maprootUrlsourcerootUrlSingleFileNoOutdir.json#module%3Dcommonjs`, `project/maprootUrlsourcerootUrlSubfolderNoOutdir.json#module%3Damd`, `project/maprootUrlsourcerootUrlSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootAbsolutePathMixedSubfolderNoOutdir.json#module%3Damd`, `project/sourceRootAbsolutePathMixedSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootAbsolutePathModuleMultifolderNoOutdir.json#module%3Damd`, `project/sourceRootAbsolutePathModuleMultifolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootAbsolutePathModuleSimpleNoOutdir.json#module%3Damd`, `project/sourceRootAbsolutePathModuleSimpleNoOutdir.json#module%3Dcommonjs`, `project/sourceRootAbsolutePathModuleSubfolderNoOutdir.json#module%3Damd`, `project/sourceRootAbsolutePathModuleSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootAbsolutePathMultifolderNoOutdir.json#module%3Damd`, `project/sourceRootAbsolutePathMultifolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootAbsolutePathSimpleNoOutdir.json#module%3Damd`, `project/sourceRootAbsolutePathSimpleNoOutdir.json#module%3Dcommonjs`, `project/sourceRootAbsolutePathSingleFileNoOutdir.json#module%3Damd`, `project/sourceRootAbsolutePathSingleFileNoOutdir.json#module%3Dcommonjs`, `project/sourceRootAbsolutePathSubfolderNoOutdir.json#module%3Damd`, `project/sourceRootAbsolutePathSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootRelativePathMixedSubfolderNoOutdir.json#module%3Damd`, `project/sourceRootRelativePathMixedSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootRelativePathModuleMultifolderNoOutdir.json#module%3Damd`, `project/sourceRootRelativePathModuleMultifolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootRelativePathModuleSimpleNoOutdir.json#module%3Damd`, `project/sourceRootRelativePathModuleSimpleNoOutdir.json#module%3Dcommonjs`, `project/sourceRootRelativePathModuleSubfolderNoOutdir.json#module%3Damd`, `project/sourceRootRelativePathModuleSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootRelativePathMultifolderNoOutdir.json#module%3Damd`, `project/sourceRootRelativePathMultifolderNoOutdir.json#module%3Dcommonjs`, `project/sourceRootRelativePathSimpleNoOutdir.json#module%3Damd`, `project/sourceRootRelativePathSimpleNoOutdir.json#module%3Dcommonjs`, `project/sourceRootRelativePathSingleFileNoOutdir.json#module%3Damd`, `project/sourceRootRelativePathSingleFileNoOutdir.json#module%3Dcommonjs`, `project/sourceRootRelativePathSubfolderNoOutdir.json#module%3Damd`, `project/sourceRootRelativePathSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourcemapMixedSubfolderNoOutdir.json#module%3Damd`, `project/sourcemapMixedSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourcemapModuleMultifolderNoOutdir.json#module%3Damd`, `project/sourcemapModuleMultifolderNoOutdir.json#module%3Dcommonjs`, `project/sourcemapModuleSimpleNoOutdir.json#module%3Damd`, `project/sourcemapModuleSimpleNoOutdir.json#module%3Dcommonjs`, `project/sourcemapModuleSubfolderNoOutdir.json#module%3Damd`, `project/sourcemapModuleSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourcemapMultifolderNoOutdir.json#module%3Damd`, `project/sourcemapMultifolderNoOutdir.json#module%3Dcommonjs`, `project/sourcemapSimpleNoOutdir.json#module%3Damd`, `project/sourcemapSimpleNoOutdir.json#module%3Dcommonjs`, `project/sourcemapSingleFileNoOutdir.json#module%3Damd`, `project/sourcemapSingleFileNoOutdir.json#module%3Dcommonjs`, `project/sourcemapSubfolderNoOutdir.json#module%3Damd`, `project/sourcemapSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourcerootUrlMixedSubfolderNoOutdir.json#module%3Damd`, `project/sourcerootUrlMixedSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourcerootUrlModuleMultifolderNoOutdir.json#module%3Damd`, `project/sourcerootUrlModuleMultifolderNoOutdir.json#module%3Dcommonjs`, `project/sourcerootUrlModuleSimpleNoOutdir.json#module%3Damd`, `project/sourcerootUrlModuleSimpleNoOutdir.json#module%3Dcommonjs`, `project/sourcerootUrlModuleSubfolderNoOutdir.json#module%3Damd`, `project/sourcerootUrlModuleSubfolderNoOutdir.json#module%3Dcommonjs`, `project/sourcerootUrlMultifolderNoOutdir.json#module%3Damd`, `project/sourcerootUrlMultifolderNoOutdir.json#module%3Dcommonjs`, `project/sourcerootUrlSimpleNoOutdir.json#module%3Damd`, `project/sourcerootUrlSimpleNoOutdir.json#module%3Dcommonjs`, `project/sourcerootUrlSingleFileNoOutdir.json#module%3Damd`, `project/sourcerootUrlSingleFileNoOutdir.json#module%3Dcommonjs`, `project/sourcerootUrlSubfolderNoOutdir.json#module%3Damd`, `project/sourcerootUrlSubfolderNoOutdir.json#module%3Dcommonjs`
- 2026-09-04 **fence amendment #3 — scope correction (E14)**: the hosted `gates` check on PR #504 (head e343410f) went red in H2.5g at `conformance/references/library-reference-13` (frozen TS2592 `Cannot find name '$'` expected, none reported): lane A's #3 virtual-tsconfig parse lived in the SHARED loader `load_qualified_compiler_emit_with_option_floor` and applied a case's `tsconfig.json` (`types: ["jquery"]`, `typeRoots: ["/a/types"]`) on every floor, so `$` resolved. The H2.5g / 5h / 6a / 6b / 6c census machines splice the virtual config unit out of the program and record it for identity only (`h2-5g-qualification.mjs` virtualConfig); only the H2.7b machine parses it (`parseJsonText` + `parseJsonSourceFileConfigFileContent` from the config's directory, the #3 config-driven rows). Correction: the parse is gated to `EmitOptionFloor::DeclarationFamily` — each band's loader mirrors its own oracle. Verified: `cargo xtask h2-5g-probe --indices 8158` exact (writes 1 / diagnostics 1); the E14 battery re-runs 7b (must stay 1,372 / 185 / 36), 5h / 6a / 6b / 6c, the harness unit tests, and the FULL H2.5g acceptance (9,027 cases) before the walk. Lesson (playbook): a shared-loader change needs the full 5g run in the operator battery — the hosted acceptance is the only pre-gate consumer of 5g and it stops at the first red case.
- 2026-09-04 **walk (P7) — the H2.5g profile runtime-input closure** (walk attempt 5, `h2-5g-profile` WRITE FAILED: "runtime input closure is missing crates/compiler/tests/integration/h2_7b_m2_controls.rs, crates/emitter/src/declarations/paths.rs, crates/xtask/tests/unit/h2_7b_acceptance/tests.rs" — the m-4 P7 class): `crates/oracle/h2-5g-profile.mjs` `NEW_RUNTIME_INPUTS` += `crates/emitter/src/declarations/paths.rs` (the flip's declaration output-path planner), `NON_RUNTIME_SHADOW_INPUTS` += the two test files, the runtime-input count const and the schema `minItems`/`maxItems` (`.github/ci/contracts/h2-5g-profile.schema.json`) 241 → 242 (MEASURED: a bounded `--write` dry run minted exact=9196 next=H2.7b). Walk attempts: 1 refused (stale source pins), 2 killed by the operator (cascade pin), 3 self-killed (the operator's pkill hit its pre-suite), 4 killed (the E14 harness fix re-stales the ladder), 5 the closure failure above, 6 failed at `h2-7a-owner-inventory` ("curated Rust anchor for factory.createComputedPropertyName is not at a tsc-port header": the E12 factory.rs / printer.rs edits moved 117 curated `path:line` anchors — re-pointed with the E9 ±3-line `tsc-port:` window rule, `repoint-window.py`; owner inventory dry write decl 68 / node_builder 149 / printer 370 / audit 308 / 0 / 0 / h2_7b_anchors 7 / 7, close dry write first_blocker_rows 0), 7 launched 19:15.
- 2026-09-04 **fence amendment #5 — the m-3 W5 stratum pool is frozen provenance** (walk attempt 7, `h2-7a-witnesses` WRITE FAILED: "invalid H2.6c divergence pool"): `crates/oracle/h2-7a-witnesses.mjs` drew the signed W5 stratum roster (67 cases) from the LIVE `ratchets/h2-6c-known-divergences.v1.json` (pinned 451 rows / 172 non-refused); the §6.4(c2) regeneration leaves 318 rows / 6 non-refused, so a live read would have re-derived the m-3 witnesses from a 6-row pool and rewritten the m-3/m-4 replay evidence. Resolution (evidence-preserving, no design change): the 172 non-refused case ids at the m-3 lineage (main @440e6073) are checked in as `ratchets/h2-7a-w5-stratum-pool.v1.json` (schema 1, `kind` h2-7a-w5-stratum-pool, `status` frozen, provenance recorded); the generator reads the pool from it (validates 172 unique ids), its `inputs.h2_6c_divergences` pin becomes `inputs.w5_stratum_pool` (schema `.github/ci/contracts/h2-7a-witnesses.schema.json` follows), and the live 6c manifest is no longer a witness input. Identity REPRODUCED: the re-minted artifact's `case_manifest`, `observations`, `stratum`, `population`, `coverage_matrix`, `quotas`, `summary` and `observation_content_roll_sha256` are byte-identical to HEAD; only `generator.sha256`, `inputs` and the fingerprint move (cases 120 / stratum 67 / lanes 14 / oracle_runs 240). Walk attempt 8 (20:03) then refused at its PRE_SUITE: the probe artifact pins the exact witness bytes it observed against (`probes.witnesses.sha256`, the m-4 E3 cascade), so the moved witness fingerprint reds the replay pair until `h2-7a-probe-traces` is re-minted — done ahead of the walk (`node crates/oracle/h2-7a-probe-traces.mjs --write`, identity rolls expected unchanged), the replay pair re-proven, then walk attempt 9.
- 2026-09-04 **gate-tax 9 landed with the train (E16), right before the gate — user directive 「その処理を追加してください。ただし次回のローカルCIの直前に実装してください」** (`docs/design/greenfield/gate-tax-9.md`): the walk driver runs the static checks first (ORDER / planner coverage, topology, `walk-preflight.py`, and the new `scripts/walk-static-checks.py` — the generators' own `--check` bounded in time, refusing only on the runtime-input-closure and curated-anchor error families), then fmt / layout scan / clippy / PRE_SUITE, and records a preflight receipt keyed by the Rust tree + PRE_SUITE that a relaunch with unchanged crate bytes honours (RECORDED in the run summary; `WALK_PREFLIGHT_RECEIPT=0` forces the full preflight; `WALK_DRY=1` never writes one). Validated by `WALK_DRY=1 bash scripts/chain-walk.sh` on the converged tree (static checks clean in seconds, fmt / clippy green, converged-walk record matches, exit 0). No generator, artifact or Rust change — the ladder stays converged (cert 20260904-201533-52187).
- 2026-09-04 **P7 pin cascade (E17)** — the hosted `gates` check on E16 (e320d8c9) went red at the 7b step: "H2.7b qualification artifact contract differs from the m-2 wiring" — the walk's round-1 re-mint of `ratchets/h2-7b-qualification.v1.json` (its `inputs.owner_inventory` / `inputs.global_candidate_dispositions` follow the re-minted 7a inventory and the h2-candidate-dispositions artifact) moved two values the runner pins as Rust literals: `H2_7B_QUALIFICATION_FINGERPRINT_SHA256` 6ff65e52… → d8a5c6ed… and `origin.global_dispositions.sha256` ee8dfc2f… → bf0ec41c… (the dispositions' `cases_sha256` ed0036eb… and the generator / contract shas are unchanged; the summary consts hold). Re-pinned in `crates/xtask/src/h2_2c_acceptance.rs` (xtask h2_7b 5 / 5; `h2-7b-acceptance` 1,372 / 185 / 36; `h2-6c-acceptance` 639 / 4 / 643) + the hosted policy `rust_source_sha256`; the runner bytes are pinned by the profile ladder, so walk attempt 10 re-mints it (the m-1 P7 walk-2 precedent). The local gate run of 21:52 was stopped in its workspace-tests phase once the hosted red named the cause. Follow-up for gate-tax 9-D: these two runner literals are pure functions of the artifact and belong in the §3 recovery family.
