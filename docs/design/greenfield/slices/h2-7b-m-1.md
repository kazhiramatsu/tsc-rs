# H2.7b / m-1 — band census + qualification machine: rung packet

Status: **rev 3 RATIFIED / design only** (2026-09-03; sol round 1 REVISE
13+1, round 2 REVISE 5 partial, round 3 **AGREE** — §10). First rung of the [h2-7b.md](h2-7b.md) §6
ladder (rev 9 RATIFIED), kind `evidence`, **mjs-only**: zero
`crates/**/*.rs` bytes. This packet authorizes the oracle machinery and
its frozen artifact, the owner-graph amendment of h2-7b.md §1 (F1), and
their registrations; it authorizes no production edit. It executes on
its own train (`h2/7b-m1`) cut from `main` after the H2.7a close (PR
#501) merges — the era begins only then (`next_slice` = H2.7b).

Authority chain: docs/design/README.md → emitter architecture → post-H1
schedule (§1 house rules; :169-172 resources) → h2-7b.md (§2 the
candidate band and the census contract; §5.1 the machine's named
deltas; §6.1 this rung; §9 items 1, 7, 9-10) → this packet. Structural
precedents: [h2-6a-ca-1.md](h2-6a-ca-1.md) (the band-observation rung:
"the clone with N named deltas and nothing else structural") and the
h2-6c m-1 landing (`406c8893` → `8a8a2e0c` → `303006f8` → `c49d1ef6`,
PR #487: schema TODO placeholders expanded to per-field consts from
the first mint).

## 1. Identity, purpose, and deliverables

- **Slice ID / kind:** `h2-7b-m-1`, `evidence`; mjs + schema + ratchet
  + registrations only.
- **Purpose:** (1) freeze the H2.7b CANDIDATE band and split it by an
  effective-option census into the ADMITTED band and the count-only
  DEFERRED set (h2-7b.md §2, F2/F3); (2) observe every admitted row
  twice through pinned TypeScript 6.0.3 with full declaration-write and
  callback-metadata capture (h2-7b.md §5.1, F11); (3) land the machine
  `owner_arms` amendment of the H2.0a owner graph (h2-7b.md §1, F1); (4)
  freeze the resource contract (F15) before observation begins.
- **Deliverables:** `crates/oracle/h2-7b-qualification.mjs`;
  `.github/ci/contracts/h2-7b-qualification.schema.json`;
  `ratchets/h2-7b-qualification.v1.json`; the `ARM_SPECS` /
  `owner_arms` amendment in `crates/oracle/h2-transition.mjs` +
  `.github/ci/contracts/h2-owner-inventory.schema.json` (`$defs.owner_arm`)
  with the three h2-transition outputs regenerated; registrations
  (chain-walk ORDER 70 → 71 after `h2-7a-close`, LADDER_ORDER,
  qualification.mjs + qualification.test.mjs 21 → 22 (after the 6c
  repair), pin-index consumer entry). No Rust file, no local-CI phase, no hosted call (those are
  the m-2 / ca rungs, h2-7b.md §6).
- **Trusted base:** `__REPAIR_MERGE__` — the H2.6c acceptance-wiring
  repair merge (PR #502), the train cut per F13 (landed 2026-09-03).
- **Prerequisite and FROZEN train order (F13; user directive
  2026-09-03):** the H2.6c acceptance-wiring repair train
  (`fix/h2-6c-acceptance-wiring`: `run_h2_6c` onto `fn acceptance`, the
  `h2-6c-oracle` local-CI phase, the two 6c schema registrations, the 6c
  receipt-miss diagnosis of §6, the boundary guard test) lands FIRST —
  immediately after PR #501 — then this rung, then m-2. Consequently the
  registration base this rung starts from is **21** contracts (19 + the
  two 6c pairs) and this rung makes **21 → 22**; the H2.7b ca rung later
  adds exactly ONE hosted call (`run_h2_7b`), preserving h2-7b.md §6.4.

## 2. The band — measured at authoring, asserted at every build

The SELECTOR (h2-7b.md §2; the 6a shape `h2-6a-qualification.mjs:1605-1633`
over `ratchets/h2-candidate-dispositions.v1.json`, `cases` roll
`ed0036eb…`): rows whose `required_slices` contain `H2.7b` and are
otherwise ⊆ the 26 active slices (h2-5g-profile.mjs:637-643). Machine
constants asserted at preflight and every build:

    GLOBAL_H2_7B_ROWS            = 2456   (compiler 1034 / conformance 861 / project 528 / transpile 33)
    CANDIDATE_BAND_H2_7B_ROWS    = 1593   (compiler 921 / conformance 476 / project 196 / transpile 0)
    FROZEN_NEXT_SLICE_H2_7B_ROWS = 1
    TRANSPILE_BAND_ROWS          = 0      (fail-closed guard: a re-census that adds one FAILS the build — the h2-6a-ca-1 §2 CA-3 shape)

**The effective-option census** turns the selector into an executable
band. Per route the machine parses the EFFECTIVE options exactly as the
6c machine does, then classifies:

| route | rows | effective-option source (reused 6c resolvers) |
| --- | ---: | --- |
| `recorded-compiler-plan` (compiler) | 921 | `test-suite-expansion.v1.json` `compiler_fixtures[case.source].settings` + `configurations[case.configuration.configuration].settings` (`mergedSettings` :612-616, `effectiveCompilerOptions` :631-649) PLUS the fixture's virtual `/tsconfig.json` unit through the recorded plan (`compiler-config-plans.v1.json`, `loadDirectiveContext` :523-611 / `parseConfigContext` :433-490) — a settings-only census UNDERCOUNTS: 3 band rows get `declaration` only from their virtual config (`declarationEmitJsReExportDefault.ts`, `dtsEmitTripleSlashAvoidUnnecessaryResolutionMode.ts`, and the conformance `resolutionModeTripleSlash2.ts`) |
| `qualified-vfs` (conformance) | 476 | `conformance-suite-expansion.v1.json` — a SEPARATE artifact: `cases[]` carry no `suite` field and a bare integer `configuration`; the fixture list is `fixtures[]` looked up by VALUE (`fixtures.find(f => f.source === case.source)`, 6c :532-537); virtual configs parsed live (`createParseConfigHost` :355-421) |
| `project-mount` | 196 | `project-profile-classification.v1.json` (the 632 recorded plans; identity asserted :1169-1174) + the descriptor + `parseProjectConfig` :1014-1069 for the config-driven rows (`root_selection`: explicit-inputs 174 / project-config 18 / discovered-config 4 over 98 distinct descriptors, module variants amd 98 / commonjs 98) → `projectEffectiveOptions` :1070-1089 (note its `delete options.noEmit` :1087) |
| `transpile-api` | 0 | the route stays dormant and excised; `TRANSPILE_BAND_ROWS === 0` |

Classification — a TOTAL decision function over the fully merged
effective options (F2): every effective option is either an ADMITTED
facet, a DEFERRAL predicate with a named owner, or an UNCLASSIFIED
field that FAILS the build. `required_slices` cannot see most of these
(h2-transition.mjs:422-489 names no `stripInternal`, `isolatedDeclarations`,
`noEmitOnError`, `noEmit`-option, `out`, `rootDir`, `sourceRoot`,
`mapRoot`, `emitBOM`, `newLine`, `removeComments` blocker; verified: the
ratchet's non-`required-option:` blocker vocabulary is exactly the
:446-471 map). The census runs on the options BEFORE any route-specific
erasure — the project resolver's `delete options.noEmit` (6c :1086)
happens only after classification, on admitted rows (F3). Deferral
predicates are evaluated in `SLICE_RANK` order; every matched later
owner is collected into `required_slices` and `first_owner` is the
lowest-ranked one:

| effective option (merged) | class | owner / rule |
| --- | --- | --- |
| `getEmitDeclarations(options)` false (neither `declaration: true` nor `composite: true`) | DEFERRED | H2.7c option-diagnostic lane (the standalone-`emitDeclarationOnly` row `declFileEmitDeclarationOnlyError1.ts`, h2-7b.md F3) |
| `composite: true` | DEFERRED | **H2.8b** (h2-transition.mjs:453; schedule :1575) — `getEmitDeclarations` is only the output floor (F1); asserted 0 in the band |
| `emitDeclarationOnly: true` with `declaration: true` | ADMITTED facet | H2.7b declaration-only routing |
| `isolatedDeclarations: true`, `stripInternal: true`, `declarationDir` (any value), `noEmitOnError: true` | DEFERRED | H2.7c |
| `declarationMap: true` | DEFERRED | H2.7e; `declarationMap: false` = ADMITTED explicit-false control (10 compiler rows) |
| `outFile`/`out`, `outDir`, `rootDir` | DEFERRED | H2.7d / H2.8a / H2.8a (asserted 0 in the band) |
| `noEmit: true` (any route, incl. a project tsconfig — classified BEFORE the resolver's erasure), `noCheck: true` | DEFERRED | H2.9 / H2.8c (asserted 0) |
| `isolatedDeclarations: false` (2 compiler rows) | ADMITTED facet | explicit-false control |
| `sourceMap` / `inlineSourceMap` / `inlineSources` / `sourceRoot` / `mapRoot` / `emitBOM` / `newLine` | ADMITTED facets | active owners (H2.6a-H2.6c / H2.8a facets already exact); recorded (134 rows `sourceMap: true`; project `sourceRoot`/`mapRoot` 64/64) |
| `removeComments` (60 compiler-band rows: true 4 / false 56) | ADMITTED byte-affecting facet | H2.7b (the declaration printer honors it, :116674); recorded per row |
| `target`, `module`, `moduleResolution`, `lib`, `strict*`, `allowJs`, `checkJs`, `jsx`, `esModuleInterop`, `skipLibCheck`, `types`, `paths`, `baseUrl`, `noResolve`, `experimentalDecorators`, `useDefineForClassFields`, `verbatimModuleSyntax: false`, and every other option already admitted by the H2.1a-H2.6c floors | ADMITTED facets | the active bands' semantics; recorded in `option_facets` |
| any other effective field that reaches emit (an option not in the table) | UNCLASSIFIED | **FAIL the build** with the option name — never unknown-as-success |

The "no emit-eligible source" class (F4): 6 admitted project rows have
ONLY `.d.ts` roots (`jsFileCompilationSameNameDTsSpecified` ×2,
`…WithAllowJs` ×2, `…DtsNotSpecified` ×2, both module variants) — upstream
performs no write with `emitSkipped === false` (`sourceFileMayBeEmitted`
:16617-16638 excludes declaration files). They keep `disposition:
"admitted-for-execution"` and are OBSERVED twice like every admitted row
(h2-7b.md §7.1 — a spurious Rust write or a wrong `emitSkipped` must not
hide), with a separate facet `emit_eligibility: "no-emit-eligible-source"`
(every other admitted row: `"emit-eligible"`); only their
missing-declaration-write assertion is exempted — the guard applies
when at least one reached source is emit-eligible.

First-cut census at authoring (settings + matrix only; the virtual-config
and project-config merges are TO-VERIFY(m-1) at `--preflight`):
compiler `isolatedDeclarations: true` 21, `noEmitOnError: true` 2,
`stripInternal: true` 1, standalone `emitDeclarationOnly` 1 →
**≥ 25 compiler rows DEFERRED**; conformance `stripInternal: true` 1 →
**≥ 1 conformance row DEFERRED**; project: 22 config-driven rows to
re-derive → TO-VERIFY. Forecast: ADMITTED ≈ 1,567 (921 − 25, 476 − 1,
196 − TO-VERIFY). The machine PRINTS the census at `--preflight` and
the packet's frozen constants `ADMITTED_H2_7B_ROWS` /
`DEFERRED_H2_7B_ROWS` (per suite) are pinned from that print in the
schema (the c49d1ef6 TODO-placeholder → per-field-const pattern), never
hand-derived.

Guards that FAIL the build: `TRANSPILE_BAND_ROWS !== 0`; an ADMITTED row
whose effective options fail `getEmitDeclarations` or carry a
later-owned option; an UNCLASSIFIED effective field; a DEFERRED row
without `first_owner`, `required_slices`, `facets`, `reason`; an
ADMITTED row with an emit-eligible source whose observation produced NO
`kind: "declaration"` write while `emitSkipped === false` (a census
defect, never a deferral); non-determinism across the two observations.

## 3. The machine — `crates/oracle/h2-7b-qualification.mjs`

The h2-6c qualification machine (`crates/oracle/h2-6c-qualification.mjs`,
2,647 lines) cloned with TEN named deltas and nothing else structural:

1. **Selection + the per-suite adapter** — the 6a dispositions selector
   (`h2-6a-qualification.mjs:1605-1633`) over
   `ratchets/h2-candidate-dispositions.v1.json` replaces the 6c
   census-driven `positive === true` filter (6c :1674-1755 / :1984-2021):
   `GLOBAL_DISPOSITIONS_RELATIVE_PATH` pinned; the §2 constants
   asserted; `CLOSED_SLICES = SLICE_ORDER.slice(0, indexOf("H2.7b"))`
   (6c :158-160 re-anchored). Because a dispositions row carries only
   `suite / id / upstream_case / source{path,sha256} / profile_blockers /
   required_slices / next_slice` (F6), a FAIL-CLOSED adapter
   materializes the complete resolver row per suite before the census:
   compiler → `test-suite-expansion.cases[upstream_case]` (assert
   `id`, `source` index → `sources[].path`/`sha256` equal to the
   dispositions `source`), its `compiler_fixtures[source]` and
   `configurations[configuration.configuration]`; conformance →
   `conformance-suite-expansion.cases[upstream_case]` (assert `id`;
   `fixtures.find(f => f.source === case.source)`; the bare integer
   `configuration`); project → `project-profile-classification.cases`
   by `id` (assert `expansion_case` / `descriptor_path` /
   `module_variant`) + the descriptor; one-to-one, any mismatch FAILS.
2. **The effective-option census + admitted/deferred split** — the §2
   table implemented over the 6c per-route resolvers (`resolveDirectiveRow`
   :1828-1878, `resolveProjectRow` :1166-1268 incl. `parseProjectConfig`
   :1014-1069 for the 22 config-driven project rows) PLUS a NEW
   conformance virtual-config parser (F5): the 6c `loadDirectiveContext`
   parses virtual configs only for compiler rows (:582-606 via the
   recorded plans); for conformance rows the clone parses the fixture's
   `/tsconfig.json` unit from `loaded.allUnits` with `parseConfigContext`
   :433-490, asserts ZERO config diagnostics and fixture identity, and
   feeds BOTH the parsed options and the parsed file/root selection into
   program construction (the 6c conformance path today merges neither).
   The census runs on the fully merged options before any erasure (F3).
   Every row records `route`, `effective_declaration_options` (the
   classified facet vector), `disposition` (`admitted-for-execution` /
   `deferred-to-slices`), `emit_eligibility` (`emit-eligible` /
   `no-emit-eligible-source`, admitted rows only), `first_owner`,
   `required_slices`, `facets`, `reason`. Deferred rows are NOT observed
   (count-only), the h2-6c.md :174-197 rule; EVERY admitted row —
   including the six `no-emit-eligible-source` controls — is observed
   twice.
3. **The declaration option floor** — asserted per admitted row at
   `--preflight` AND every build (the 6a shape :1484-1490):
   `getEmitDeclarations(options) === true` ∧ no later-owned option; the
   admission contract string names it.
4. **Write capture** — `serializeWrite` (6c :1408-1430) gains
   `data_diagnostics: data?.diagnostics === undefined ? null :
   data.diagnostics.map(serializeDiagnostic)` (the existing normalizer
   :1373-1382 — keys `code`, `category`, `file`, `start`, `length`,
   `message`), keeping `data_diagnostics_count` for continuity; every
   other field retained (`kind` via `outputKind` :1384-1393 —
   `.d.ts/.d.mts/.d.cts → "declaration"`, `callback_*`,
   `write_byte_order_mark`, `materialized_*`, `on_error_callback_present`,
   `source_files`, `data_present`, `data_source_map_url_pos`,
   `inline_source_map_payload`).
5. **Owner closure** — the H2.7b rows of `ratchets/h2-owner-inventory.v1.json`:
   `declaration-output-path` (:143) and `declaration-output-name` (:178)
   PLUS the four `owner_arms` rows landed by §4 (`declaration-source-
   output-paths`, `declaration-emit`, `declaration-write`,
   `declaration-collision-preflight`); `OWNER_KEYS` (6c :161-164)
   re-pointed; the arm rows verified by key + `enclosure` + every
   segment hash.
6. **Receipts, shards, and the two denominators (F7)** — the artifact
   denominator is `CANDIDATE_BAND_H2_7B_ROWS` (1,593 census records) and
   the OBSERVATION denominator is `ADMITTED_H2_7B_ROWS`: shards partition,
   observe, adopt, and reuse exactly the admitted rows (the 6c
   `buildCaseRecords` :2308 / shard denominator :2419 / adoption guard
   :2470 / run-count guard :2158 all re-based on the admitted count);
   the DEFERRED census records are assembled only in the parent (the six
   `no-emit-eligible-source` rows are admitted rows and therefore
   sharded, observed, adopted, and reused like the rest); the final
   `cases` array equals the band and is asserted per disposition. The receipt key
   (`target/h2-7b/check-receipt.v1.json`, kind
   `h2-7b-qualification-check-receipt`) hashes BOTH the admitted
   observations roll and the complete census-disposition roll, over
   typescript / `observationInputs(inputs)` (the 5g/6a idiom that DROPS
   the pin-carrying `owner_inventory` and `global_candidate_dispositions`
   whole-file hashes and compares the `owner_closure` rows canonically —
   h2-5g-qualification.mjs:1008-1021, h2-6a-qualification.mjs:1024-1034)
   / execution_contract / project-mount fingerprint / library inventory;
   the 4-shard `--check` (`TSRS_H2_7B_CHECK_SHARDS`, default 4, range
   1..8; `--internal-check-shard`); the schema conditional on
   `disposition` (observation fields required only for admitted rows).
   NOT inherited: the 6c receipt's miss-on-every-check behaviour (§6).
7. **Artifact identity** — `phase: "H2.7b-declaration-observation"`,
   `kind: "h2-7b-qualification"`, `status: "qualified-typescript-oracle"`;
   `selection_contract.candidate_definition` = "the dispositions rows
   selected by the frozen H2.7b selector, split by the effective-option
   census; no applicability is re-derived by the acceptance runner";
   `execution_contract` names the floor and the `rust_expectation =
   "two-deterministic-exact-runs"` for admitted rows; `summary` mirrors
   the 6c fields (:2107-2162) plus `admitted_cases`, `deferred_cases`,
   per-suite admitted/deferred, `declaration_writes`,
   `emit_declaration_only_cases`, `explicit_false_declaration_map_cases`,
   `first_deferred_slices`.
8. **Counting/consts** — every 6c count const re-frozen for the 7b band
   (the 7-layer surface: the census guard, corpus denominators, band
   prep, summary, artifact/shard/receipt, the schema consts, and — at
   m-2 — the Rust `validate_h2_7b_qualification` mirror).
9. **The `--write` checkpoint (F11)** — the serial `--write` loop writes
   `target/h2-7b/write-checkpoint.v1.json` atomically every 64 admitted
   rows (per-case records keyed by `case_id` + `observation_input_sha256`
   + facets + `case_fingerprint_sha256`); on start, `--write` adopts the
   checkpoint's records under the same per-case guards as the
   previous-artifact reuse (6c :2243-2295) BEFORE that reuse, and deletes
   the checkpoint after the artifact is written; the parent is the sole
   writer of the artifact, the checkpoint, and the receipt (§6).
10. **The probe selectors (F12)** — `--probe <suite>:<N>` (suite ∈
    compiler / conformance / project; the first N admitted rows of that
    suite in band order) and `--probe-case <id>` (one admitted row by
    `case_id`; refused for a deferred or unknown id), both never writing
    (the 6c `--probe N` :2571-2600 selects compiler rows only and is
    retained as `--probe compiler:N`); `--preflight` prints the RSS of
    every probed row.

Everything else retained unchanged: fixture loading, virtual-config
splicing, program construction (`makeProgram`), the in-process double
observation with the determinism assertion (6c :1621-1626), the
`emit_result` capture (:1447-1486), the previous-artifact `--write`
observation reuse (:2254-2295, now preceded by the delta-9 checkpoint
adoption), the `--preflight` / `--check` / `--write` CLI (:2602-2647,
with the delta-10 probe selectors replacing `--probe N`),
`validateRuntime` (:247-259).

Registrations (same commit): `scripts/chain-walk.sh` ORDER + LADDER_ORDER
(`h2-7b-qualification` after `h2-7a-close`; the coverage self-check
refuses drift), `.github/ci/qualification.mjs` ARTIFACT_SCHEMA_CONTRACTS
+ `.github/ci/qualification.test.mjs` (21 → 22 once the 6c repair has
registered the two 6c pairs — the label list today jumps from "H2.6b
qualification" to "H2.7a owner inventory", §9.1), the
pin-index consumer entry (the dispositions hash as a grammar-D pin;
no frozen literal). Walk topology: the rung consumes `h2-transition`
outputs and the H2.7a artifacts → placed last.

## 4. The owner-graph amendment (h2-7b.md §1, F1) — landed by this rung

`crates/oracle/h2-transition.mjs` gains `ARM_SPECS` rendered as the
`owner_arms` block of `ratchets/h2-owner-inventory.v1.json`. Line-pin
rule (F8): the H2.7a inventory pins the option-owner span by PHYSICAL
line numbers :448-467 and text hash (h2-7a-owner-inventory.mjs:37 /
:1639-1647), so EVERY edit to h2-transition.mjs before line 448 —
including the `summary.owner_arms` field, the second assertion next to
`owner_roots === 50` (:382), and any serialization change in
`buildOwnerInventory` (:271-…) — must be line-count-neutral (same line
count as the text it replaces), or the span pin is re-fingerprinted in
the same commit with both the bytes and the line numbers verified
before the H2.7a inventory re-mints; the `ARM_SPECS` table itself and
its rendering helper are defined AFTER :467. Four rows
with `key`, `enclosure` (name, start line, whole-function range +
sha256), `ranges[]` (each segment start/end + sha256), `shared_lines[]`
(each with every co-owner), `owner_slice: "H2.7b"`, `disposition`,
`role`, `activation`, `consumers[]`, `dependencies[]` — the line-exact
partition of h2-7b.md §1 (`declaration-source-output-paths` [:116383];
`declaration-emit` [:116640] [:116642] [:116644-116645] [:116649]
[:116651-116668] [:116670] [:116672-116679] [:116681-116695]
[:116697-116699] [:116703-116708] [:116712-116715] [:116716-116735];
`declaration-write` [:116803]; `declaration-collision-preflight`
[:125025]; the shared and excluded lines as recorded there). Never fed
through `selectedByDeclaration`; `owner_roots === 50` unchanged,
`summary.owner_arms === 4` added (`:382` gains the second assertion);
`.github/ci/contracts/h2-owner-inventory.schema.json` gains
`$defs.owner_arm`; `transition_rows === 39` untouched. `h2-transition.mjs
--write` regenerates all three outputs (the owner inventory content +
`owner_arms`; the candidate dispositions content UNCHANGED — the
`cases` roll `ed0036eb…` the H2.7a close artifact asserts stays valid;
the profile pins); the walk then re-pins the h2 ladder, the H2.7a owner
inventory (span pin intact), and `h2-7a-close`. Observation budget
(F9, measured on the ca walk 20260903-141336-72734): the 5g / 5h / 6a /
6b receipts key on `observationInputs(inputs)` (the owner-inventory
whole-file hash DROPPED, `owner_closure` rows compared canonically —
5g :1008-1021; 6a :1024-1034) so a NEW `owner_arms` block leaves them
receipt HITS (walk evidence: 8 s / 3 s / 2 s / 2 s); the 6c
qualification pins the owner inventory whole-file hash into its receipt
key (6c :1809 / :2201-2211 / :2271) and re-observes — 96 s per 4-shard
pass — which is ALSO its behaviour on every check today (both rounds of
the ca walk re-observed: the 6c receipt never hits; a 6c defect for the
repair train, §6). Budget: one to two 6c passes (≤ 192 s), zero 5g
observation (`TSRS_H2_5G_FRESH` unset; the check-outcome record is the
proof).

## 5. Frozen evidence minted by this rung

`ratchets/h2-7b-qualification.v1.json`: the candidate band (1,593
rows) with every row's census record; the ADMITTED rows each observed
twice (fingerprint-equal) with declaration writes captured in full; the
DEFERRED rows count-only with named first owners; the artifact's
`summary` constants and the schema consts pinned from the first mint
(TO-FILL: `admitted_cases`, `deferred_cases`, per-suite splits,
`typescript_runs` (= 2 × admitted), `declaration_writes`,
`typescript_writes`, `typescript_reported_diagnostics`,
`typescript_emit_diagnostics`, `emit_refused_cases`, `first_deferred_slices`,
`target_states`, `module_states`). Negative facts frozen alongside:
`transpile_band_rows 0`, `out_file_rows 0`, `out_dir_rows 0`,
`root_dir_rows 0`, `no_emit_rows 0`, `no_check_rows 0`, `composite_rows 0`
— so the m-2 flip's manifest starts free of those classes.

## 6. Resource contract (F15; schedule :169-172) — frozen BEFORE observation

| item | value |
| --- | --- |
| measured base (F10) | the ca walk 20260903-141336-72734: one h2-6c `--check` pass = 1,286 in-process runs (643 cases × 2) in **96 s wall at 4 shards** (round 1 AND round 2 each re-observed — the 6c receipt-miss defect); no CPU-time measurement exists — wall × shards is NOT quoted as CPU |
| `--write` execution model (F11) | SERIAL in the parent (the clone shards only `--check`; `--write` observes every admitted row 2× in one process). Expected wall: 1,593 × 2 runs at the 6c per-run wall (≈ 0.30 s/run/shard ⇒ ≈ 0.075 s of wall per run per shard-equivalent… stated conservatively as ≤ 20 min serial); **STOP threshold 40 min wall** (abort, record, re-enter the design gate) |
| checkpoint / resume (F11) | a target-local write checkpoint `target/h2-7b/write-checkpoint.v1.json` written atomically every 64 admitted rows by the parent (per-case records keyed by `case_id` + `observation_input_sha256` + facets + fingerprint — the same guards as the previous-artifact reuse, 6c :2243-2295), adopted on restart by `--write` BEFORE the previous-artifact reuse; deleted after the artifact is written. The parent is the SOLE writer of the artifact, the checkpoint, and the receipt; shards write only their stdout adoption records. Resume command = re-run `--write` |
| `--check` | the 4-shard model (`TSRS_H2_7B_CHECK_SHARDS` default 4, max 8); receipt hit = seconds; receipt miss = one full pass (≤ 5 min at 4 shards is the expectation, 15 min the STOP) |
| memory (F12) | per-process RSS ceiling **4 GB**, aggregate **12 GB** at 4 shards; measured at `--preflight` on the heaviest rows via a NEW selector `--probe <suite>:<N>` / `--probe-case <id>` (the 6c `--probe N` selects only the first N compiler rows, :2571-2580) over the three heaviest project rows and the three largest compiler fixtures; exceeding a ceiling STOPS before the full mint |
| workers | mjs shards only (above); the walk runs every node command under `taskpolicy -b nice -n 15`; no Rust worker (m-2 introduces `run_h2_7b` under `TSRS_H2_5G_WORKERS`, ceiling 2) |
| stop thresholds | any non-deterministic case; any admitted emit-eligible row with no declaration write while `emitSkipped === false`; any UNCLASSIFIED effective field; the wall/RSS ceilings above; a receipt miss inside one walk run is one full pass, never two (gate-tax-5 :185-188) |

## 7. Prohibitions

- No `crates/**/*.rs` byte; no local-CI phase; no hosted call (m-2 /
  ca own those); no Rust `EmitOptionFloor` change here.
- No observation of a DEFERRED row; no re-derivation of applicability
  by any later consumer (the artifact IS the band).
- No hand-derived count const: every schema/summary const is pinned
  from the first mint's print (the c49d1ef6 pattern).
- No change to the four H2.7a artifacts, the H2.7a close artifact's
  content (pins only), or the candidate dispositions' `cases`.
- No `ROOT_SPECS` root added or edited; the amendment is the
  machine-distinct `owner_arms` block only; the :448-467 span byte-
  identical.
- No adoption of the 5g resume-journal library in this rung; the §6
  write checkpoint is the rung's own, target-local mechanism.

## 8. Acceptance (train-internal)

1. `node crates/oracle/h2-7b-qualification.mjs --preflight` green: the
   §2 selector constants, the census table (per suite: admitted /
   deferred by first owner), the floor over every admitted row, the
   negative facts; `--probe project:3` / `--probe-case` on the heaviest rows within
   the §6 RSS ceilings (measured and recorded).
2. `--write` deterministic (every admitted row 2×, fingerprint-equal)
   within the §6 wall ceiling; the schema consts expanded from the
   print; `--check` sharded green + receipt minted; a second `--check`
   = receipt hit, green.
3. `node crates/oracle/h2-transition.mjs --check` green after the
   `owner_arms` amendment with `owner_roots 50 / owner_arms 4`, the
   dispositions `cases` roll unchanged; `node
   crates/oracle/h2-7a-owner-inventory.mjs --check` and
   `h2-7a-close.mjs --check` green after their pin-only re-mints.
4. `node .github/ci/qualification.mjs check` valid with 22 registered
   contracts (21 after the 6c repair + this rung); `node --test
   .github/ci/qualification.test.mjs` green.
5. `bash scripts/chain-walk.sh` ONE-invocation converge (ORDER 71/71,
   topology clean, all pin surfaces clean, zero 5g observations);
   `cargo xtask ci --baseline <trusted base>` green; hosted `gates`
   green; merge.

## 9. Unresolved items and trace

1. **Latent 6c gaps (recorded, not repaired here):** (a) the
   `h2-6c-census` / `h2-6c-qualification` schema pairs are NOT in
   `ARTIFACT_SCHEMA_CONTRACTS` (`.github/ci/qualification.mjs:92-188`
   jumps from "H2.6b qualification" to "H2.7a owner inventory");
   (b) `run_h2_6c` is NOT on the hosted `fn acceptance` boundary
   (`crates/xtask/src/main.rs:4384-4415` ends at `run_h2_6b`; `run_h2_6c`
   is reachable only via `cargo xtask h2-6c-acceptance`) and no
   `h2-6c-oracle` local-CI phase exists — contrary to h2-6c-ca.md:12.
   Also (found on the ca walk): the 6c check receipt never hits (both
   rounds re-observed, 96 s each). Owner: the repair train, a
   PREREQUISITE to m-2 (user directive 2026-09-03: it starts after PR
   #501 merges) — it adds `run_h2_6c` to `fn acceptance`, the
   `h2-6c-oracle` phase, the two schema registrations, and the receipt
   diagnosis; the H2.7b ca rung then adds exactly ONE call (`run_h2_7b`),
   preserving h2-7b.md §6.4 (F13).
   **Landing note (Phase E, 2026-09-03):** the repair train merged as PR
   #502 @`__REPAIR_MERGE__` — `fn acceptance` ends at `run_h2_6c`, the
   `h2-6c-oracle` phase exists, 21 contracts are registered, and the 6c
   receipt now hits (decisive cause: census and qualification shared one
   receipt path; plus the projected key). One residual is recorded in
   [h2-6c-ca-repair-register.md](h2-6c-ca-repair-register.md): the 6c
   check compares the stored artifact BEFORE minting its receipt, so a
   re-mint walk re-observes once more in round 2 — this clone MUST mint
   the receipt before the byte comparison (SPEC-M1 §A.6).
2. The virtual-config undercount and the 22 config-driven project rows:
   TO-VERIFY(m-1) at `--preflight`; the census must parse them
   (delta 2), never approximate from settings.
3. The exact admitted/deferred constants and every summary const:
   TO-FILL from the first mint.
4. The `owner_arms` schema shape and the h2-transition assertion edit:
   design-gate-approved here; implementation verifies the H2.7a
   inventory's span pin is byte-identical after the edit.
5. The 7b conformance/project census reproduction by the reviewer:
   TO-VERIFY at the design gate (the compiler column is reproduced
   twice already: h2-7b.md §2 and the m-1 fact sheet).

Evidence read: h2-7b.md (rev 9), the m-1 fact sheet
(target/session-notes/7b/h2-7b-m-1-factsheet.md, 2026-09-03: the 6c
machine anatomy :17-2647, the 6a selector :1605-1633, the two expansion
artifacts' shapes, the project classification, the option-owner map
:446-471, the 7-layer count surface, the 7a-ca walk timings, the
registration surfaces of 0489a6b9), h2-6a-ca-1.md §2-§7, h2-6c.md
§5-§7, the schedule :169-172; no oracle machine ran.

## 10. Cross-review record

Round 1 (2026-09-03): operator draft rev 1 → sol **REVISE** (13 blocking
+ 1 advisory); verified without finding: the band counts, the first-cut
option counts, the three virtual-config rows, the project split
174/18/4, the registration counts, the owner-closure membership, the
full-diagnostics serialization; the option-only projection 1,567
admitted / 26 deferred. Every finding INCORPORATED in rev 2: F1
`composite` → H2.8b (§2); F2 the total decision function incl.
`removeComments` (60 rows), multi-owner ordering, fail on unclassified
(§2); F3 census before the project `noEmit` erasure (§2, §3.2); F4 the
six `.d.ts`-only project rows as the `no-emit-eligible-source` class
(operator-reproduced) (§2); F5 the conformance virtual-config parser
(§3.2); F6 the fail-closed per-suite adapter (§3.1); F7 the two
denominators + the receipt over both rolls (§3.6); F8 the line-count-
neutral rule for pre-:448 edits (§4); F9 the budget re-measured: 5g/5h/
6a/6b receipts drop the owner-inventory hash (hits), 6c re-observes 96 s
per pass and never hits today (§4, §9); F10 96 s per pass, no CPU claim
(§6); F11 serial `--write` + the target-local write checkpoint (§6);
F12 the probe selector + RSS ceilings (§6, §8); F13 the 6c repair as
the m-2 prerequisite (§1, §9); F14 anchors :532-537 / :1621-1626.

Round 2 (2026-09-03; the first round-2 run was discarded — it replayed
the round-1 transcript it could read under target/session-notes/, so
prior outputs are now archived outside the repo before every round):
rev 2 → sol **REVISE** — F1, F2, F3, F5, F6, F8, F9, F10, F14 RESOLVED
(counts, the three virtual-config rows, the adapter join over all 1,593
rows with zero identity mismatches, the span hash `25632b51…`, the
receipt projections, the 96 s passes all re-verified); PARTIAL: F4/F7
(the six `.d.ts`-only rows must stay admitted AND observed twice, only
their declaration-write assertion exempted), F11 (the write checkpoint
must be a named delta), F12 (the probe selectors must be a named delta
and the "CLI unchanged" claim qualified), F13 (the train order and the
registration count must be frozen). Rev 3 incorporates all five: the
`emit_eligibility` facet with the six rows in the observation
denominator (§2, §3.2, §3.6), deltas 9 and 10 (§3), and the frozen
order repair → m-1 → m-2 with 21 → 22 (§1, §3, §8).

Round 3 (2026-09-03): rev 3 → sol **AGREE** — F4, F7, F11, F12, F13
RESOLVED with fresh verification (the six `.d.ts`-only rows, the four
denominator sites, the clone's serial `--write` :2639 / atomic write
:222, the compiler-only probe :2571, the 19 current registrations and
the boundary ending at `run_h2_6b`); no new findings. This revision is
the h2-7b-m-1 design authority; it lands at the m-1 train's Phase E
after the 6c repair train.
