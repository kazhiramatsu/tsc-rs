# h2-7b-w4 — register (values recorded as minted; `__X__` = TO-FILL at the named step)

Packet: `h2-7b-w4.md` (rev 1 draft at E1; ratification after the sol review r2). Trusted base `main @399c62ba`. Train `h2/7b-w4`.

## E1 (2026-09-06 JST)
- 06:53 `h2/7b-w4` cut from main 399c62ba; 06:54 the ahead lane W4-A0 merged (2b293d59; `lane/w4-a0` 9f8f6ff5, verified against its base c2075797; 11 rows incl. `exportDefaultNamespace`); 06:56 the integrator's S2 + P1 merged (9cd8f79c; `lane/w4-i` 203816a7 on d0cd9a0b; 10 rows; program 467+24 / harness 83+32 tests, clippy, fmt clean in the worktree); stubs `h2_7b_w4a_controls.rs` / `h2_7b_w4b_controls.rs` registered; launch scratch `target/session-notes/7b/lanes/w4/launch-scratch.json` (expected 34 live divergences of 55; closed 21) → verdict OK: scratch 34 live / canonical 55, closed 21, equal 33, strict subsets 1 (the inherited jsDeclarationsUniqueSymbolUsage reduction), problems 0 (07:04).
- Launch SHA c16f2d2e (E1c: the packet at rev 2 after the sol reviews r1/r2; E1b bc175dcd = stubs + packet + register; E1 2b293d59 / 9cd8f79c = the A0 and integrator merges); lanes launched 07:05 (W4-A gpt-5.6-sol xhigh `tsc-rs-w4-a`; W4-B gpt-5.6-sol xhigh `tsc-rs-w4-b`); preflight OK (SPEC-W4-A 11 allowed paths, SPEC-W4-B 9, disjoint; every file present at the launch SHA); the E1 train battery started 07:05 on the canonical checkout (jobs 2, 1 sweep worker).

## Lanes
- **W4-B** (gpt-5.6-sol xhigh, `tsc-rs-w4-b`): launched 07:05 from c16f2d2e; STATUS at 10:40 (3 h 35 m; the time box's implementation stop 09:55 held, the full battery ran 09:56-10:3x). Rows: **8 of 8 exact** (B1 3, B2, B3, B4 = T1, B5, B6) — launch-relative audit OK (34 → 26 live, closed 8, problems 0); whole emitter crate green (459 lib / 449 contracts / 18 topology). Verify OK (9 allowed paths: 5 emitter, 3 checker, the controls file). Checkpoint 5235af29 (10:41), merged 9cab3818 (10:42). Full battery 2 (09:56): xtask 270/0, 6c 321/318/4, 5h 830/58/44, 6a 171/4/2, 6b 6/0/0, 5g full 8511 exact; compiler contracts 305/3 — the three reds: two INHERITED w2 TS5055-hint boundary controls made stale by the integrator's P1 port (fixed E1e 66abd797), and one lane-caused `declaration_transformer_replay_decision_equal` failure (B6's `moduleName` route asked `getExternalModuleFileFromDeclaration` of the historical no-moduleName replay resolvers → `Unavailable`; the lane then treats exactly that method/error pair as upstream's no-target fallback and demonstrated the exact replay test green on the final tree; no third full battery within the time box — the train battery covers it). Out of scope (not claimed): the printer-owned comment tail, the genuine-`any` singleton. Deviations: one accidental `cargo fmt --all` reordered the forbidden `contracts.rs` (restored byte-for-byte) — which revealed that the E1b registration order was not rustfmt-clean (every lane battery's fmt red; fixed E1e); light 5's sweep terminated by the integrator's 07:39 load intervention.
- **W4-A** (gpt-5.6-sol xhigh, `tsc-rs-w4-a`): launched 07:05 from c16f2d2e; STATUS at 11:29 (4 h 24 m; the implementation stop 09:55 held, the full battery ran 10:4x-11:2x). Rows: **8 of 14 exact** — T7, C1, S3, S4, E2, E3, E4, D1; the 2xxx and syntactic conformance guards 100 % / FP 0 after the guarded edits (annotate, calls, contextual, elaboration, engine, checker lib, binder). OUT-OF-SCOPE with evidence: **S1 (5 rows)** — the own-export-table port closes four rows but `jsDeclarationEmitExportAssignedArray` then fails in `serialize_type_for_declaration_seam` (`createTypeOfDeclaration factory operation failed: unknown transform node`), a secondary producer in W4-B's `syntactic_type_node_builder.rs` / `node_builder/serialize.rs` (both lanes are merged now → a train/W5 item); **J10 (1 row)** — `get_resolved_symbol(left)` as upstream closes the row but adds 21 TS2304 false positives in the 2xxx guard (typeofThis ×15, classStaticBlock6 ×2, initializerReferencingConstructor{Locals ×2, Parameters ×1}, typeofThisWithImplicitThis) → reverted; needs an entry point with the liveness effect without the publication side effect. Verify OK (9 allowed paths). Checkpoint 432b40cd (11:29), merged e9b758dd. Full battery: every band green (6c 321/318/4, 5h, 6a, 6b, 5g 8511), rc=1 only from four launch-head assertions in two integrator-owned test files — the two w2 TS5055 boundary controls (fixed E1e) and a checker node-builder unit control expecting an export modifier on synthesized import-equals property aliases (stale after T7; integrator repair on the train). Deviations: `cargo xtask` alias supplied via `CARGO_ALIAS_XTASK` (the lane-local `.cargo/config.toml` overrode the repo alias — an integrator kit defect); a rustfmt `reorder_modules=false` wrapper to keep the forbidden launch-head `contracts.rs` untouched; the battery lock was re-entered with a 2-second wait behind lane B's retries.

## First w4 sweep / re-mint
- merged-train scratch sweep (11:30, before the canonical re-mint; head e9b758dd + E1f): exact 1,539 / diverging **18** (owner stays `h2-7b-m-2-divergence-closure`); componentwise audit vs the canonical 55: closed **37** (A0 11 + integrator 10 + W4-B 8 + W4-A 8; `lanes/w4/w4-closed-rows.txt`), equal 17, strict subsets 1 (the inherited `jsDeclarationsUniqueSymbolUsage` reduction), added or replaced elements 0, new rows 0. Adjacent bands (merged train, 1 worker): 6c 321 / 318 / 4, 5h 830 / 58 / 44, 6a 171 / 4 / 2, 6b 6 / 0 / 0 (all unchanged); 5g full 9,027 / 8,511 (unchanged); whole suites: compiler contracts 314 / 0 (18 ignored: the lanes' out-of-scope controls), checker 1,700 / 0 (after E1f), emitter contracts 449, emitter lib 459, compiler lib 11, xtask 270 / 0; clippy clean; fmt clean; ledger 3,815 / 0 / 0 (base 3,801). Battery 11:30 → 12:28 (58 min in the foreground band at nice 15, one sweep worker).
- the canonical re-mint (E2, 12:29-12:37, write + normal run + `audit.js` vs the trusted base 399c62ba): exact 1,539 / diverging 18 (55 → 18); closed rows **37** (`lanes/w4/w4-closed-rows-final.txt`); byte-identical 17; strict subsets 1 (the inherited `jsDeclarationsUniqueSymbolUsage` reduction); added or replaced elements 0; new rows 0; owner unchanged — verdict OK.

## Timeline (JST)
- 06:52 PR #507 merged (w3) → 06:53 `h2/7b-w4` cut → 06:54 A0 merged (2b293d59) → 06:56 the integrator's S2/P1 merged (9cd8f79c; implemented 06:20-06:50 during the w3 gate) → 06:56 E1b stubs + packet (bc175dcd) → 06:59 E1c packet rev 2 (c16f2d2e = the launch SHA) → 07:04 launch scratch OK (34 live) → **07:05 lanes launched** (A and B) → 07:30 the E1 train battery's bands all matched w3 (stopped 07:39 on the user's load report) → 07:39-09:39 the lanes on E-cores (background band), then the foreground band at `nice 20` on the user's request → **10:40 B handoff** (8/8) → 10:41 checkpoint 5235af29 → 10:42 merged 9cab3818 → 10:41 E1e (contracts.rs order + the w2 boundary controls) → **11:29 A handoff** (8/14; S1/J10 out of scope) → 11:29 checkpoint 432b40cd → 11:29 merged e9b758dd → 11:30-12:28 the train battery (all green) → 11:36 E1f (checker unit controls after T7) → 12:29-12:37 E2 re-mint (8bef1a5c) → 12:31 E3 pins (b5ccfd73) → 12:38 walk launch 1 refused in preflight (the inline test module) → 12:5x E3b/E3c → **13:00-15:25 walk** (cert 20260906-130025-98260) → 15:26 E4 (879fba6d) → 15:26 gate run 1 (killed 16:12: syspolicyd) → 16:13-16:30 gate run 2 exit 0 → **16:32 merge 2d4c8730** (wall lane launch → merge **9 h 27 m**; w3 10 h 37 m; the target ≤ 8 h 33 m).

## Walk / gate / PR — TO-FILL
- walk cert 20260906-130025-98260 (13:00-15:25; round 1 re-minted, round 2 CLEAN, 5g receipt hit; harness pins 59 rows) — launches 2: 20260906-123810-96812 refused in the preflight (the integrator's inline test module), 20260906-130025-98260 converged; gate exit 0 at 879fba6d (run 2, 16:13-16:30, foreground band `nice 15`; run 1 killed after 46 min in workspace-tests: every freshly built test binary blocked at exec on macOS `syspolicyd`'s provenance assessment — 0 of 29 targets recorded — until the user granted iTerm2 Developer Tools access, after which run 2 finished in 17 min with the journal reusing structural-preflight / clippy / oracle): ratchet 49024/49024 (2xxx 21051, syntactic 2246); conformance all / 2xxx / syntactic T0 100 % FP 0; invariants 5908/7691 ok; families frozen 15/433; ledger 3815/0/0; escapes 0; relpin 415/415; 7b qualification fresh 1593/1557/36 (receipt hit); performance 15.592 s / 25 s; readme ok; hosted pass 41m53s @879fba6d (run 34016496939; the E1-head runs red by design until E2); PR #508 merged 2026-09-06 16:32 JST as 2d4c8730 (merge commit).

## Success criteria (packet §7) — recorded at close
| # | criterion | value | pass |
|---|---|---|---|
| 1 | one walk launch | 2 run directories — 20260906-123810-96812 refused in the driver's preflight within 25 s (the integrator's inline test module in `crates/program/src/symlinks.rs`; no rung minted) and 20260906-130025-98260 converged; the kit now dry-runs `WALK_DRY=1` first | FAIL |
| 2 | zero train-battery-first / gate-first regressions | 0 — the merged-train battery green on its first run, the gate green on its first completed run; the reds in the lanes' full batteries were integrator-stale expectations (the w2 TS5055 boundary controls after P1, the checker unit controls after T7, the E1b `contracts.rs` order) repaired on the train before the walk (E1e, E1f) | PASS |
| 3 | zero allowed-path STOPs | 0 — W4-A's S1 (5) and J10 (1) and W4-B's two OPEN rows were out-of-scope notes with proven owners; no lane waited | PASS |
| 4 | zero perf-contention reruns | 0 — the ceiling passed on the first measurement (15.592 s / 25 s, foreground band `nice 15`); gate run 1's kill was the OS policy stall, not contention | PASS |
| 5 | ≥ 35 rows in ≤ 8 h 33 m | 37 rows (55 → 18) in 9 h 27 m — the 07:39-09:39 E-core window, the refused walk launch, the integrator's own repair commits and the 46-min syspolicyd-blocked gate run are the excess | rows PASS / wall FAIL |
| 6 | zero lane conflicts / multi-writer pins | 0 — both lane merges clean; `preflight.py --verify` OK for A, B and the ahead lane; lane A's accidental `contracts.rs` reorder was restored by the lane before handoff; the pin surfaces were written by the integrator and the walk only | PASS |

## Implementation-time amendments
- 15:45 ROOT CAUSE of every 0-CPU stall of this and the previous train: macOS `syspolicyd` assessed each freshly built executable (`com.apple.provenance`) at exec — test binaries sat at `_dyld_start` for 2-15 min each while the daemon burned 60-110 % CPU (400+ CPU-minutes over two days): the w3 gate's 15-min contracts stall, the lanes' 20-25-min sweeps, the walk's harness-contracts stall, gate run 1's 0 of 29 targets in 46 min, and much of the user-visible CPU load. Fixed by the user at 16:11 (System Settings → Privacy & Security → Developer Tools → iTerm2); gate run 2's test binaries ran at 98 % immediately. The earlier `taskpolicy -B -p` "un-sticking" was coincidence.
- 12:38 the first walk launch (run 20260906-123810-96812) was REFUSED in the driver's preflight within 25 s: `crates/program/src/symlinks.rs` carried an inline `#[cfg(test)] mod tests { … }` (the workspace test-module layout rule: bodies live under `crates/<crate>/tests/unit/<module>/tests.rs`); the integrator's own file — no lane involvement. E3b 3e2488b2 moved the tests, E3c 2ff82b4d registered the moved file as an H2.5g shadow input (the dry preflight `WALK_DRY=1` caught it); the walk relaunched 13:00 from 2ff82b4d. Criterion 1 counts run directories, so this refusal counts as a second launch. Rule for the kit: run `WALK_DRY=1 bash scripts/chain-walk.sh` before every real launch.
- E1e 66abd797 (10:41): `contracts.rs` module order restored to rustfmt's — the E1b registration had put `h2_7b_w4i_controls` before `w4a`/`w4b`, so `cargo fmt --check` was red on the launch tree and in every lane battery; the two w2 TS5055-hint boundary controls made exact. E1f 17b34e75 (11:36): two checker node-builder unit controls expected an inline `export` on a synthesized import-equals property alias — stale after W4-A's T7 port (upstream `canHaveExportModifier` excludes ImportEqualsDeclaration). E3 (12:31, before the walk): `crates/binder/src/bind.rs` and `crates/program/src/symlinks.rs` registered as H2.5g runtime inputs (identity 243 → 245; schema minItems/maxItems), the three w4 controls files as non-runtime shadow inputs; the fuzz manifests' pins re-recorded (checker `lib.rs` → fuzz-preflight, `check.rs` → fuzz-oracle-deviations → fuzz-preflight's chained pin); 100 curated h2-7a owner-inventory anchors re-pointed to the moved `tsc-port` headers (the lanes' and A0's emitter edits shifted lines) and the artifact regenerated (`--write`: audit 308/0/0, anchors 7/7); walk-preflight clean, static checks leave only the walk's own re-mints (5g profile, 7a-close).
- 09:39 on the user's request the lane trees moved from the background band (E-cores; a 7b sweep took 20-25 min instead of 2.5) to the FOREGROUND band at `nice 20` (one P-core per process, yielding to the user at once); the caps stay jobs 1 / test threads 1; the demoter loop re-applies `taskpolicy -B` + `renice 20` every 30 s.
- 08:22 hosted `gates` on the E1 heads is RED by design (run 33996594489, 40m43s): `cargo xtask acceptance`'s H2.7b suite check reports the 21 closed rows as stale manifest entries ("is exact now (shrink the manifest)") plus the inherited `jsDeclarationsUniqueSymbolUsage` facet reduction; it turns green after the E2 re-mint, as in w3 (hosted passed on E4/E5/E6 there). No code defect: the compiler suite 458/10 exact/known and the project suite 196/0.
- 07:39 the user reported high CPU load (the E1 train battery's compiler-contracts test at ~200% + the two lanes' cargo work): the integrator stopped the E1 train battery after its bands had all matched w3 (7b 1523/34, 6c 321/318/4, 5h 830/58/44, 6a 171/4/2, 6b 6/0/0, 5g 8511 exact) — its unit-test / clippy / fmt / ledger tail runs on the merged train — and demoted both lane process trees to `nice 20` (priority only; the foreground band stays — the E-core lesson). The kill of the canonical `xtask h2-7b-acceptance` may have caught a lane's in-flight scratch sweep (a relative `target/debug/xtask` command line is indistinguishable from the canonical one by path): if a lane STATUS reports one aborted sweep around 07:39, that is the integrator's interference, not a lane fault.
