# H2.6c / W5 — post-W4 residue closure wave

Status: revision 1 (2026-08-31), operator-authored, pending sol
cross-review. Parent: `h2-6c-m-2.md` §3 wave table (the W1–W4 named
bands are closed or absorbed; W5 is the first census-driven residue
wave) and `h2-6c-w1.md` §§2–4 (probe fidelity, class discipline,
manifest mechanics — all carried forward unchanged). Baseline:
`ratchets/h2-6c-known-divergences.v1.json` at 481 rows (279 refusal
rows untouched; the 202 non-refused rows are this wave's frozen pool).

## 1. Pool identity and the census instrument

The W5 pool is the FROZEN 202-row non-refused manifest set: 193 rows
with `writes_diverging > 0` (1,191 positional write divergences) and 9
diagnostics-bearing rows without write divergence.

The ratchet counter (`count_diverging_writes`,
h2_2c_acceptance.rs:2382) is POSITIONAL: it zips expected and actual
write sequences in order and adds the length difference, and the
`_with_data` wrapper (:2779) adds up to one more per pair on callback
metadata deltas. A single missing `.d.ts` write mid-sequence therefore
shifts every later pair and inflates the row's count. This semantic is
FROZEN — it is the ratchet's exact-join basis and W5 does not change
it. For truth-finding, W5 adds a PATH-JOINED census mode to the
`#[ignore]`-gated band probe
(`crates/compiler/tests/integration/source_map_band_probe.rs`,
`TSRS_H2_6C_PROBE_LIST` + `census.jsonl`): every rust write is matched
to the frozen observation write at the same path, yielding per-case
exact/diverging/missing/rust-only sets and reported-diagnostic deltas.
The probe now mirrors ALL three frozen execution routes exactly as the
acceptance prepares them (recorded-compiler-plan and qualified-vfs on
the complete MapFamily floor; project-mount through the hermetic
project mount with the acceptance's structural-key filtering,
h2_2c_acceptance.rs:3660). Census evidence for the full pool was
minted 2026-08-31 (202/202 rows; session artifact
`w5-census/census.jsonl` + `w5-analysis.txt`).

## 2. Census truth: pool decomposition

Path-joined comparison of all 202 rows:

- **166 writes truly byte-diverge** (7 javascript + 159 source-map);
  everything else in the 1,191 positional count is sequence-shift
  artifact, declaration absence, or path relocation.
- **312 declaration writes are absent** (`.d.ts` → H2.7b owns exact
  non-bundle declaration output; `.d.ts.map` → H2.7e). Parent §6.4
  anticipates exactly these count-only deferrals.
- **59 missing + 118 rust-only writes** are path relocations owned by
  H2.7d (`outFile` bundles), H2.8a (`outDir`), H2.8b
  (`noEmitHelpers`), consistent with the SPECIAL-5 rulings
  (h2-6c-w1.md §3) which this wave extends to the full `outFile` band.
- **66 rows are fully exact except declaration absence** (project
  SingleFile/Simple/Subfolder/Multifolder bodies across all 8
  root-option prefixes, compiler `properties` + `out-flag#es2015`):
  the entire root-option surface (sourceRoot/mapRoot
  absolute/relative/URL forms) is PROVEN EXACT on js+map bytes.
- Project-suite javascript is byte-exact in every body; the only
  project byte defect is in module-variant source maps.

## 3. Defect classes (census-clustered; causes pinned or TO-PIN)

Class discipline is h2-6c-w1.md §3's: one representative founds the
hypothesis, membership is per-case, fixes verify on the probe before
mechanical rollout, and no hand-authored expected bytes. Numbering
continues from K12.

- **K13 — namespace/enum member-access name child unpositioned in
  declaration-export arms.** Evidence: `sourceMapValidationModule`
  generated `m4.x = 30;` / `m3.foo = foo;`, `sourceMap-Comments`
  `tools.Test = Test;`, `typeResolution` `SubSubModule1.ClassA =
  ClassA;` — the access START maps to the original name but the
  name-END boundary segment is missing (e.g. expected extra
  `(18,0,1,21)` after `tools.Test`). The K2-S reference-substitution
  path is EXACT (its name child carries the range,
  builtins.rs:861-865); the declaration-lowering arms go through
  `namespace_member_name` (builtins.rs:13165, four call sites
  :10620/:10622/:11700/:11702) which ranges only the ACCESS. Upstream
  `getNamespaceMemberName` (_tsc.js:24812) builds the access over the
  CLONED original identifier, which keeps its source positions, so the
  printer emits the name's trailing mapping. FIX SHAPE: set the name
  child's text range to the original name inside
  `namespace_member_name`, mirroring the K2-S precedent. Candidate
  membership: the compiler `sourceMapValidation*`/`sourceMap-*`
  map=1 stragglers, `typeResolution`, `contextualTyping`,
  `recursiveClassReferenceTest`, `sourcemapValidationDuplicateNames`,
  jsxFactory rows' class-name arms, `sourceMapValidationImport` line-7
  arm; per-case verification decides K13 vs K14 vs K15 attribution of
  each diverging line.
- **K14 — dotted-namespace nested close-line leading boundaries.**
  Evidence: `sourceMapSample`/`sourceMapValidationClasses`/
  `recursiveClassReferenceTest` close lines missing the leading
  `(0,0,L,0),(1,0,L,1)` segments (the `}` and `)` of each NESTED
  IIFE close in a dotted `module Foo.Bar.Baz` chain, all mapping to
  the single source `}`), while non-dotted closes are exact
  (`sourceMapValidationModule` line 5/16). CAUSE (TO-PIN exact line):
  the dotted-namespace recursion synthesizes the outer wrapper
  levels' function bodies/call parens without the module-end range
  the single-level arm applies. FIX SHAPE: range the nested levels'
  body-close/call boundaries from the source module end, exactly as
  the single-level arm does.
- **K15 — external-module export lowering mappings (the project
  module band, 64 rows / 128 maps).** Two sub-mechanisms, both
  CommonJS/AMD lowering sites, javascript bytes already exact:
  - (a) the `exports.a = exports.b = void 0;` preinitializer
    statement is emitted UNMAPPED; upstream maps its start/end to the
    exported-declarations span (expected `[(4,0,6,0),(26,0,8,1)]` on
    the amd variant). Rust site: the exportedNames chunk loop,
    builtins.rs:4265-4272, creates statement/accesses with no text
    range. Upstream site TO-PIN in transformModule's exportedNames
    chunk emission (the chunk elements are the ORIGINAL exported
    identifiers; position retention flows through the created
    accesses).
  - (b) `exports.m1_a1 = 10;` lowering of `export var m1_a1 = 10`
    misses the leading LHS piece segments (expected
    `(0→0,11),(8→0,11),(13→0,16)` = access start, name start, name
    end — the same cloned-identifier discipline as K13). Rust site:
    the direct-exported-variable assignment creation
    (`create_export_access` consumers in the CommonJS visitor,
    builtins.rs:2964 region); exact arm TO-PIN.
  Membership: all 64 project Module\*/MixedSubfolder rows across all
  8 root prefixes × amd/commonjs (the diverging map count per row
  equals the body's external-module file count), plus the compiler
  `sourceMapValidationImport` lines 9-10 arms.
- **K16 — ES5 for-of increment: one EXTRA rust trailing segment.**
  `ES5For-of8`/`ES5For-of34` (es5): rust emits a duplicate final
  segment `(21,0,3,12)` after the correct `(20,0,3,12)` on the
  for-increment line. Site TO-PIN in the es2015 for-of lowering's
  increment emission (`convert_for_of_statement_head` family,
  es2015.rs:12068 region); upstream emits a single boundary.
- **K17 — legacy-decorator ES5 residual range arms.**
  `sourceMapValidationDecorators` (es5+es2015): several segments map
  to the DECORATOR line instead of the past-decorators position
  (rust `(30,0,20,4)` vs expected `(30,0,22,4)`; rust `(4,0,41,4)`
  vs `(4,0,43,4)`), one out-of-order `__param`-adjacent segment
  `(19,0,32,21)` is missing, and the ctor-property arm start
  differs. These are the K8a/K8d mechanisms in arms the W4A landing
  did not reach (parameter-property constructors and accessor pairs
  on the es5 path). Sites TO-PIN per arm in legacy_decorators.rs
  against _tsc.js:98667/:98860.
- **K18 — ES-decorators class lowering: helper order + js bytes.**
  `esDecorators-classDeclaration-sourceMap` (es2015/es2022): rust
  emits `__classPrivateFieldIn` BEFORE `__esDecorate`-era helpers
  (expected: after, at line 51), shifting all subsequent lines, plus
  segment-level deltas inside the static-adder lines (missing
  decorator-expression segments `(22,0,4,6),(73,0,4,7)`). The esnext
  row is exact except declaration-map absence. Sites TO-PIN in
  standard_decorators.rs + the helper emission order authority
  (helpers.rs); upstream order per _tsc.js's priority-ordered helper
  list.
- **K19 — escaped-identifier spelling lost in export lowering.**
  `unicodeEscapesInNames01` (es5): rust emits `exports.x = 10;`
  where upstream keeps `exports.x = 10;` (js diverges on 2
  files, maps on 4 — columns follow spelling). The exports-access
  creation uses decoded text instead of the escaped/raw identifier
  text. FIX: carry the escaped text through `create_export_access`
  (and the K15(b) arm), matching the 6a unicode band's 8-write
  residue. Sibling of the K2-X escaped-name finding.
- **K20 — out-flag es5 comment placement.** `out-flag.ts#es5`: rust
  retains two source comment lines (`//// @outFile: bin\`,
  `// my class comments`) in the emitted js that upstream drops
  (es2015 emits them identically — the divergence is es5-only, so
  the es5 class downlevel's comment attachment differs). CAUSE
  TO-VERIFY(probe): likely the downleveled class var-statement's
  leading-comment ownership. The es2015 row of the same fixture is
  blocked-H2.7b-only.
- **K21 — emitBOM write flag.** `emitBOM.ts#default`: the js
  callback bytes are EXACT but rust reports
  `write_byte_order_mark=false` where upstream reports true (the map
  correctly stays false). FIX: propagate the `emitBOM`-driven BOM
  flag onto the javascript write's callback metadata.
- **K22 — option-relationship validation diagnostics.** Rust emits
  the 5107 deprecation set correctly but MISSES: TS5069 ("Option
  'mapRoot'/'sourceRoot' cannot be specified without … 'sourceMap'
  or … 'declarationMap'") on the six project
  `*WithNoSourceMapOption` controls (the m-2 wave table's original
  "W4" band, never closed) and both `optionsInlineSourceMap*`
  compiler rows; TS5053 ("Option 'X' cannot be specified with option
  'Y'") for sourceMap×inlineSourceMap and mapRoot×inlineSourceMap.
  FIX: add the missing relationship checks at the site that already
  emits the 5107 family, matching upstream message/order exactly.

## 4. Blocked and reassigned dispositions (no code owed in W5)

Recorded per parent §6.4/§7 — rows stay countable, never silently
promoted or dropped:

1. **blocked_by=H2.7b (declaration output):** the 312 absent `.d.ts`
   writes across the pool, including the 66 rows exact-but-for-
   declarations (all 16 SingleFile project rows, 32
   Simple/Subfolder, 16 Multifolder, compiler `properties` ×2,
   `out-flag#es2015`). W5 proves their js/map facets exact via the
   census; the rows shrink at H2.7b.
2. **blocked_by=H2.7e (declarationMap):** `classStaticBlock25`
   es2022/esnext and `esDecorators…sourceMap#esnext` (absent
   `.d.ts.map` only), plus the `.d.ts.map` facets of the K18 rows.
3. **blocked_by=H2.7d (outFile bundles):** 28 compiler rows (the
   `out-flag2/3`, `outModuleConcat*`, `outModuleTripleSlashRefs`,
   inline-family, `sourceMapWith*Files*`, `declarationMapsWithSourceMap`
   rows). Census: zero byte divergence on matched paths — pure
   bundle-path relocation (+ consequential diagnostics). Extends the
   SPECIAL-5(a) precedent to the full band.
4. **blocked_by=H2.8a (outDir):** 4 rows (SPECIAL-5(a) three + the
   newly ruled `jsFileCompilationWithMapFileAsJsWithOutDir`, whose
   extra rust TS5055 overwrite error is a consequence of unrelocated
   output paths, not an independent defect).
5. **blocked_by=H2.8b (noEmitHelpers):** 1 row (SPECIAL-5(b),
   unchanged).
6. **checker-domain:** `sourceMapValidationDestructuringForArray-
   BindingPattern#es2015` misses TS2318 "Cannot find global type
   'Iterable'" (lib=es5). This is global-type resolution, not
   map/emit; disposition = reassigned out of 6c waves to the checker
   owner (exact owner slice TO-VERIFY in review).

## 5. Wave execution

1. Train commit 1 (DONE in this branch, tests-only bytes): the probe
   route extension + census mode + diagnostics dump.
2. W5 packet cross-review (sol): verdict + the TO-PIN sites resolved
   or the class split amended; the class-membership table freezes on
   sign-off (census.jsonl is the byte evidence).
3. Implementation lanes (thick-train, 1–2 trains): sol takes
   K15+K17+K18 (module-lowering + decorator internals); luna takes
   K13/K14/K16/K19/K21 after the K13 pattern lands probe-verified;
   K22 is a bounded options-validation lane; K20 needs its
   probe-founded cause first. Every fix validates red-suite-first
   (PRE_SUITE with the probe/census on the fixed binary) before any
   walk.
4. Manifest mechanics per h2-6c-w1.md §4.4 unchanged: regenerate the
   FULL proposal in a temporary workspace, shrink-only, curated
   owners preserved; rows leave only fully-exact (the no-declaration
   compiler/conformance rows and the 8 diag-only rows can); rows with
   declaration writes shrink facet counts and stay.
5. Adjacent-band sweeps before push (w1 §4.6): h2-5h/6a/6b — the
   K13/K14/K16/K19 fixes heal most of the 6a manifest's 26 rows and
   the K19 fix the 6a unicode band; the 6b manifest's 2 rows are
   K22's.
6. Walk converge (one invocation) + full local gate at the train
   head; PR opens early per the standing directive.

## 6. Prohibitions

All of h2-6c-w1.md §5, plus: the positional ratchet-counter semantic
is untouched; the census stays `#[ignore]`-gated and non-gating; no
blocked/carry-over row is promoted, dropped, or "fixed" by extending
production floors across the H2.7d/H2.8a/H2.8b option-owner
boundaries; no diagnostic text is hand-tuned beyond the exact upstream
messages.

## 7. Cross-review record

Round 1: operator draft (this revision) → sol verdict PENDING.
