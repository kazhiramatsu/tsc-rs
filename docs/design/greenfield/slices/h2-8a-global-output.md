# H2.8a: original global output comparison

The reviewed source set is 809 original inputs: 804 historical global output
IDs, one additional H2.7c rootDir ID, and four additional D/E output IDs. It
contains 809 whole-Program inputs and no transpile entry. Prepared inputs keep
original option values, root selection, file text/order, virtual links, config
projection, and the pinned 233-file project mount. Existing parent observations
retain their distinct request conventions and are not copied as new results.

The owner audit at checkpoint `43e1107e8` yields 769 output-only candidates, 3 composite
intersections for H2.8b, 8 isolatedModules/verbatimModuleSyntax intersections for
H2.8c, and 29 noEmit/syntax/depth intersections for H2.9. These are candidate
classes, not successes. The original required_slices lists remain as provenance.
20 old importHelpers intersections have no effective outFile; the ordinary
emitter no longer rejects that option. Two ordinary case-insensitive controls
also have no outFile guard. Their remaining intersections, if any, still apply.
No runtime boundary is removed by this classification.

The next observer must run every whole-Program input twice through pinned TS
6.0.3, preserve complete diagnostics/callbacks/bytes/maps/lists/status/exit and
source/library order, and prove every added source-boundary reason is reached.
The existing D/E preparation/observation algorithms are reused with a new set
and new current-owner calculation; configFilePath survives option spread while
the non-enumerable configFile AST does not. Rust uses the same existing complete
original-corpus comparator after explicitly extending its option projection for
the actual input union. All observed differences are failures until corrected
under the corresponding owner, not automatic new later-owned rows.

Candidate producer: `crates/oracle/h2-8a-candidates.mjs`.
Complete observer: `crates/oracle/h2-8a-observations.mjs`.
Preparation check: target/h2-8a-candidates-current-probe.log.
Option union: target/h2-8a-candidate-options-probe.log.
The canonical TS observation now contains all 809 complete tuples, each
observed twice (1,618 Programs; 3,186 writes in one repetition). All 23 D/E
intersections retain identical input hashes and complete TS tuples. Rust
qualification remains pending. Final profile, architecture, canaries and old-input
compatibility migrations remain separately required.

The first comparison is `cargo test -p tsc-rs-compiler --test
h2_8a_original_corpus original_output_matrix_candidates -- --nocapture
--test-threads=1`. The shared D/E comparator gains only projections for actual
additional options and normalized typeRoots; it retains original diagnostic,
callback, output, source/library order, activity and status comparison. Original
D/E selectors and expected tuples remain unchanged. The new selector validates
all 809 identities, source hashes, generator/input hashes and owner joins before
executing all 769 eligible inputs twice. Failure capture goes under
`TSC_RS_H2_8A_FAILURE_DIR` and never rewrites the oracle.

This packet authorizes evidence preparation and comparison only. A new
production divergence requires its exact upstream owner and bounded runtime
amendment before a fix. Existing later references are not observations of Rust
success and an unexpected Rust failure cannot expand their membership.

The reference mint completed successfully in
`target/h2-8a-global-observations.log`. Its 40 later references were observed as
TS commands, without executing them as Rust admissions. The existing walk ORDER
now lists the two new producers; no historical walk is run or claimed under the
authorized lightweight workflow. The before comparison records its immutable
input/observation hashes and baseline revision beside failure tuples in the new
directory named by `target/h2-8a-global-before-location.txt`.

The complete first Rust comparison finishes in 1066.88 seconds: 769 eligible
inputs = 666 exact twice + 103 failed twice. All 188 project inputs are exact;
failures are compiler18 and conformance85. Of the failures, 87 produce complete
unequal tuples and 16 stop at typed/runtime boundaries. The 206 failure records
and original complete tuples remain saved; no failure is reclassified as a
success or hidden by changed expected data. The reference40 remain outside the
Rust selector. `target/h2-8a-global-before-summary.json` records the complete
result, and the external before directory retains the log, summary and exact
comparison-source hashes in addition to the immutable input/oracle copies.

Initial causal groups include omitted JSDoc export-assignment checks, incorrect
AMD/UMD module-factory selection for fixed extensions, missing Program path
options in declaration specifier synthesis, external helper imports, declaration
serialization/factory adaptation and comment preservation. Each repair needs a
bounded upstream-owned packet; the first full comparison is not an H2.8a close.
