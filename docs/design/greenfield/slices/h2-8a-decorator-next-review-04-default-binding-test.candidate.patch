From 6f916cd5e315aa5ef03ed8b9af780f2f460fba7a Mon Sep 17 00:00:00 2001
From: kazhiramatsu <kazutakehiramatsu@gmail.com>
Date: Fri, 11 Sep 2026 10:18:58 +0900
Subject: [PATCH] test(A6-41 review): scope anonymous default binding
 assertions

---
 crates/emitter/tests/unit/builtins/tests.rs | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/crates/emitter/tests/unit/builtins/tests.rs b/crates/emitter/tests/unit/builtins/tests.rs
index 6ecc9a3ca..030e620f5 100644
--- a/crates/emitter/tests/unit/builtins/tests.rs
+++ b/crates/emitter/tests/unit/builtins/tests.rs
@@ -4330,7 +4330,11 @@ fn using_anonymous_default_decorator_handoff_keeps_owner_and_binding_domains() {
                             "ordinary default_N must not enter the FileLevel domain ({case})",
                         );
                     }
-                    if metadata.generated_binding_is_file_level_optimistic() {
+                    // Decorator helpers also own FileLevel bindings. Inspect the
+                    // anonymous default export's binding without conflating them.
+                    if metadata.generated_binding_is_file_level_optimistic()
+                        && metadata.generated_binding_preferred_base() == Some("_default")
+                    {
                         file_level_identifier.get_or_insert(node);
                         assert_eq!(identifier.text, "_default", "{case}");
                         assert_eq!(
-- 
2.50.1 (Apple Git-155)

