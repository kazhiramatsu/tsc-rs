# h2-7b-w2 — register (values recorded as minted; `__X__` = TO-FILL at the named step)

## Design gate
- Packet rev 1 (2026-09-05, integrator-authored draft from the w1 census + the verified owners; landed at E1 1f777680): sol cross-review r1 (launched 09:07, worktree `tsc-rs-w2-r`, 8 min) = REVISE — B1 `preflight.py --verify` diffed from `origin/main` (would report E1's files as lane violations) → now diffs `<launch base>..HEAD` from the ticket's `Base SHA`; B2 `audit.js` compared only owner + vector set → now the full §6 rule (full-row equality, monotone coarse facets derived from the vector, refusal fields unchanged, fingerprint re-derived, duplicates rejected; self-tests against the canonical rows; also used by the E2 review); B3 criterion 5 had two ceilings → 8 h 33 m everywhere; non-blocking 1-3 applied (W2-E allowed paths narrowed to the two output subtrees; the battery scripts copied into each lane worktree at the ticket's relative path — lane B had already found the canonical absolute path; the re-mint record gained explicit zero fields) → rev 2 (09:19) → r2 (8 min) = REVISE — B1 RESOLVED, B3 RESOLVED, B2 NOT RESOLVED: the audit's derivation missed the runner's `write_N_data_diagnostic` / `source_maps` / `status_writes` facets, compared vectors as sets (a reorder passed) and ignored duplicate canonical ids; plus the verifier kept only a rename's destination, and `w2-remint.sh` masked every failing exit → rev 3 (09:29): `audit.js` mirrors the runner's vocabulary (deep-equal rows, sorted/unique vectors, duplicates on both sides, refusal rows; tamper self-tests), `preflight.py` disables rename detection on status and diff, `w2-remint.sh` exits non-zero on any failed step → r3 (10 min) = REVISE — items 2 and 3 RESOLVED; item 1 NOT RESOLVED on one point: any `refusal:*` element passed as a refusal (an impossible `refusal:7:not_variant` row audited OK); plus the verify base could be read from the lane-mutable worktree ticket → rev 4 (09:38): `audit.js` requires the exact runner-derived refusal element (sha256 of the compact JSON of kind/option; synthetic refusal self-tests), `preflight.py --verify` takes the launch base as the integrator's explicit argument and refuses a disagreeing worktree ticket; the non-blocking lock re-entrancy (the full battery would have deadlocked its own light stage) fixed with `W2_BATTERY_LOCK_HELD` → r4 (3 min, confirmation scope) = AGREE: B1 RESOLVED, B2 RESOLVED, lock RESOLVED — **rev 4 RATIFIED 09:42 JST** (four rounds; every blocking finding was a real trust-path defect in the operator's scripts — the audit vocabulary, the verifier's base and rename handling, the re-mint exit, the refusal element — and none in the packet's mechanisms or lane sets).

## Preflight (the w2 operating rules)
- Launch check (`preflight.py SPEC-W2-A.md SPEC-W2-B.md SPEC-W2-E.md` at E1): OK at E1 1f777680 — SPEC-W2-A 12 paths, SPEC-W2-B 9, SPEC-W2-E 1; all three intersections empty; 0 single-writer hits; 0 missing files.
- Verify before integration (`preflight.py --verify <worktree> <SPEC>`): W2-A __VERIFY_A__; W2-B __VERIFY_B__; W2-E __VERIFY_E__.

## Lanes (the staffing cell)
- W2-A (gpt-5.6-sol xhigh): launched 09:07 JST from E1 1f777680 (time box: stop 11:57, full battery 12:02, STATUS 12:42); STATUS __STATUS_A__ (rows fixed / left / out of scope: __ROWS_A__); full battery __FULL_A__.
- W2-B (gpt-5.6-sol xhigh): launched 09:07 JST from E1 1f777680 (same time box); STATUS delivered 11:05 (1 h 58 m; checkpoint 5b6a7edb, merged into the train decec8b7). Rows: **36 exact** — B1 6, B4 20, B2 1, B3 9; left 0; out of scope with proven owners: the two `jsFileCompilationSameNameDtsNotSpecifiedWithAllowJs` rows (TS5107 suffix exact, strict subsets; the remaining TS5055 chains carry a TS5068 child Rust adds in `crates/emitter/src/plan.rs::overwrite_input_diagnostic` because the harness project adapter clears `configFilePath` — upstream `verifyEmitFilePath` `_tsc.js:125029-125044` adds it only when the config path is absent; both surfaces single-writer), `declarationEmitRetainsJsdocyComments` (declaration write exact, strict subset; the JS write loses the JSDoc in `crates/emitter/src/builtins.rs::flatten_module_destructuring_declaration` — `set_original_and_range` with the enclosing VariableDeclaration erases the inner comment owner; upstream `flattenDestructuringAssignment` `_tsc.js:93251-93328`), and the shared row `strictPropertyInitialization` (a missing member, NodeBuilder — W2-A's). Full battery: light stage green; xtask 270/0; 6c 639/4/643; **5h stale entry `emitDecoratorMetadataSystemJS.json#amd` is exact now (adjacent-band closure; the integrator shrinks the manifest)**; 6a 171/4/2; 6b 6/0/0; 5g 9,027/8,511; compiler contracts green after the B4 no-emit boundary; checker; emitter contracts; ledger 3,790/0/0. Integrator review note: B4 keeps the `ConfigRootPlan` ownership for config-backed programs with `noEmit: true` (the no-emit harness adapter's route, whose contract filters TS5107 by design) and runs the merged-options removed/deprecated pass for emitting config-backed programs; the `no_emit` test is a proxy for the adapter identity — a follow-up may replace it with an explicit adapter flag on `PreparedProgram`.
- W2-E (gpt-5.6-luna max): launched 09:07 JST from E1 1f777680 (STATUS 12:42); STATUS delivered 09:52 (45 min; checkpoint b1ab9e76, merged 43c75ed3): the 34 non-declaration-write rows are single `.js` writes (module-namespace-wrapper 9, comment-emission 6, module-reexport-shape 5, system-module-wrapper 5, amd-module-wrapper 3, import-assertion/attribute 2, four singletons), none of the 34 fixtures is in the 5g/6c populations; `residual-owners.md` (41 + 23 rows) and `h2-7b-w3-seed.md` delivered.
- Integrator: E1 (packet draft, register skeleton, the three registered stubs, the 5g shadow registration) landed 2026-09-05 09:06 JST on `h2/7b-w2` from `main @709fb8fa`; checkpoints __CHECKPOINTS__.

## Timeline (JST)
- lane launch 09:07 → handoff __T_HANDOFF__ → lanes merged __T_MERGED__ → train battery green __T_BATTERY__ → re-mint __T_REMINT__ → walk __T_WALK_START__ … cert __WALK__ → gate __T_GATE_START__ … exit __T_GATE_END__ → merge __T_MERGE__ (wall lane launch → merge: __WALL__; w1 reference 8 h 33 m = 00:31 → 09:04 JST).

## First w2 sweep / re-mint — TO-FILL
- exact __EXACT__ / diverging __DIVERGING__ (rows __ROWS__; owner stays `h2-7b-m-2-divergence-closure`) — measured on the merged train before the canonical re-mint; closed rows __CLOSED__ (list, each with its §3 mechanism); byte-identical (full row) __EQUAL__; strict subsets (vector shrank, coarse facets consistent, no `false → true`, no numeric increase, fingerprint re-derived) __SUBSETS__; added or replaced elements 0; new rows 0; owner changes 0; coarse-facet growth 0; duplicate ids 0.
- adjacent bands (merged train, workers=2): 6c __6C__ (base 639 / 4 / 643), 5h __5H__ (824 / 64 / 44), 6a __6A__ (171 / 4 / 2), 6b __6B__ (6 / 0 / 0), full 5g __5G__ (9,027 / 8,511); whole suites: compiler contracts __CC__, checker __CHK__, xtask __XT__, emitter contracts __EC__; clippy; fmt; ledger __LEDGER__ (base 3,787 / 0 / 0).

## Walk / gate / PR — recorded in the PR body at the final head
- walk cert __WALK__ (launches: __WALK_LAUNCHES__); gate __GATE_LINE__ (reruns: __GATE_RERUNS__); hosted __HOSTED__; PR __PR__.

## Success criteria (packet §7) — recorded at close
| # | criterion | value | pass |
|---|---|---|---|
| 1 | one walk launch | __C1__ | __P1__ |
| 2 | zero train-battery-first regressions | __C2__ | __P2__ |
| 3 | zero allowed-path STOPs | __C3__ | __P3__ |
| 4 | zero perf-contention reruns | __C4__ | __P4__ |
| 5 | > 23 rows in ≤ 8 h 33 m | __C5__ | __P5__ |
| 6 | zero lane conflicts / multi-writer pins | __C6__ | __P6__ |

## Implementation-time amendments
- (none yet)
