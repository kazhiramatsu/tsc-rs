# h2-7a-m-3.5 — Phase-E frozen-constant register (E3/E4)

Companion to [h2-7a-m-3.5.md](h2-7a-m-3.5.md) §11. This file is outside
every generator input closure (the m-2 register-location precedent); it
records the E3/E4 evidence-derived constants. Values marked PREDICTED are
the packet's frozen predictions; FINAL values are read from the E3 mints.

## W-H2.7A-P reprint artifact (§7)

- P1 rows: PREDICTED 202 (fixed_point 197 true / 5 false: the four
  NodeBuilder `SingleLine` tuple/mapped-type writes
  F4/overload, F8/grammar-second, S2/latebound-4, S3/typeofexpr-3 and the
  outFile amd-module write S2/entityname-1). FINAL: 202 rows, fixed_point 197 — REPRODUCED.
- P2 units: PREDICTED 997 measured / 4 excluded (parse-diagnostic:
  compiler/duplicatePackage.ts + duplicatePackage_withErrors.ts
  `/node_modules/b/node_modules/x/index.d.ts`,
  compiler/expressionTypeNodeShouldError.ts `base.d.ts`,
  conformance/externalModules/umd-errors.ts `err3.d.ts`) / 993 rows.
  FINAL: 993 rows, 4 parse-diagnostic exclusions (+ 7 non-UTF-8 fixtures skipped, recorded) — REPRODUCED.
- P3 rows: PREDICTED 109 (4,372,988 input bytes, 0 parse diagnostics).
  FINAL: 109 rows — REPRODUCED.
- P4 rows: PREDICTED 46 (roster sha256 of the embedded P4_INPUTS: 1af8e9fdbae9a453…).
  FINAL: 46 rows, zero parse refusals — REPRODUCED.
- Gating rows total: PREDICTED 1,350 (typescript_oracle_runs 2,700). FINAL: 1,350 rows, 2,700 oracle runs, input 4,544,016 / expected 4,530,359 bytes.
- Kind coverage (OptionalType / NamedTupleMember / ImportType /
  TypePredicate / MappedType / TemplateLiteralType, all strata): FINAL 1 / 6 / 40 / 343 / 47 / 21 (the two P4 obligations met).
- Artifact fingerprint (E3): e837838874467f8c… ; reprint_content_roll_sha256: 364e4e3b38a0d676… ;
  typescript_oracle_runs: 2700
- Witness pins carried: raw file sha ecf304d94766f8b7…, case-manifest fingerprint
  81d5b13a…, observation roll 4d2e6f6d… (both reproduced by the E3 witness re-observation).

## Probe schema-3 (§8)

- printed_results entries: PREDICTED 647 (320 createTypeOfDeclaration +
  151 createReturnTypeOfSignatureDeclaration + 4 createTypeOfExpression +
  8 createLiteralConstValue + 3 getDeclarationStatementsForSourceFile +
  161 createLateBoundIndexSignatures). FINAL: 647 — REPRODUCED (320/151/4/8/3/161).
- null entries (undefined results): FINAL 160; vector entries: FINAL 5
  (lengths 1/1/2/15/20 — 2/15/20 and 1/1 expected: REPRODUCED).
- Schema-2 projection guard (permanent 112-case, 6b43e5a1…): PASSED at the
  E3 mint and at --check / S3 cases included: 8 (120-case corpus).
- Trace-content roll: unchanged by construction (ca24d47c…) — FINAL ca24d47c54b14df3… REPRODUCED.
- printed-results roll (EXPECTED_PRINTED_RESULTS_ROLL): FINAL
  7554535bd6245a5ec1b3f84bcfa16806ff50881ba5fb1e85f69c02356780c4e9
- Probe artifact fingerprint (E3): 044142c9c0aeb117… ; per-case public_output_roll drift:
  NONE expected — FINAL none (all 120 cases inert=true, double observation identical).

## E2 generator repairs recorded

- h2-7a-probe-traces.mjs `--selftest` was red on the trusted base
  (verified 2026-09-02 on unpatched main: "schema-2 projection guard input
  changed (validateArtifact): 3/112") — the permanent 112-case guard was
  applied to the 3-case selftest context. E2 scopes the guard to the
  canonical witness context (the fallbackSweep idiom); the canonical
  `--check`/`--write` paths are unchanged in strength.

## Witness artifact succession (parent-packet pin move)

- The E1 h2-7a.md amendment moves `inputs.parent_packet`; the witness
  re-mints by full re-observation (240 runs) reproducing the frozen
  observation identity (manifest 81d5b13a…, roll 4d2e6f6d…) — FINAL:
  fingerprint dcb46c98cbb9f13f… (was 55825c37…), reproduced: YES (manifest
  81d5b13a639f5cc8…, roll 4d2e6f6dc52cf5e4… byte-identical; 240 runs, 11:18-11:24).

## Owner-inventory succession (P7 walk)

- Trusted-base fingerprint 61f2735ddc5d53d4… → E3 re-mint 8d32f6ed890fe71b… (generator
  hash moved; audit still 118/190/0 — the P5 flips + P7 re-mint close it);
  summary.audit FINAL (P5 re-mint, fingerprint fde21e1cfae36cb2…): already_exact 308,
  foundation_needed 0, pending 0;
  anchor verification: 190 header-verified rows (mint-time `tsc-port` check) + 118
  legacy range-checked rows; partition projection byte-identical: focused test
  (h2_7a_partition_projection) re-run at the P7 head — see the walk cert.
  (The P7 walk re-mints once more when resolver.rs moves in P3; the audit
  summary is invariant under that re-mint.)

## E4 surprise-trigger assessment (§9.E4 — operator)

- P1 non-fixed-point class outside the two named: NO
- P2 exclusion count ≠ 4: NO
- P4 parse refusal: NO
- printed_results count ≠ 647: NO
- schema-2 projection drift: NO
- public_output_roll change: NO
- VERDICT: no trigger fired — Phase P proceeds on this operator
  verification record (packet §9.E4). Witness identity reproduced: YES.

## Residue re-owning record (§2d)

- UTF-16 literal faces (type_nodes.rs :754/:1152/:1174, chains.rs :1922,
  declaration_emit.rs :3882): typed fail-closed residue re-owned by H2.7b;
  corpus reach at E4: 0 synthesized lone-surrogate literals in the 120-case
  corpus (no printed_results entry and no P1 write contains a lone surrogate).
- `reportLikelyUnsafeImportRequiredError` exact specifier string: stays
  the m-3 lossy projection; owned by ca (unchanged here).
