# h2-7b-w1 — register (values recorded as minted; `__X__` = TO-FILL at the named step)

## Design gate
- Packet rev 1 (2026-09-05, integrator-authored from the W1-E draft + the lane outcomes): sol cross-review r1 = REVISE (B1: the componentwise gate must reject ANY owner change; 3 non-blocking wording items) → rev 2 applied all four → r2 = **AGREE (ratify as-is)** (non-blocking: the §3 counting overlap → the disjoint accounting sentence added; the TO-FILL execution surfaces stay TO-FILL) → RATIFIED by the operator under the m-2 §6 closure-wave authority (2026-09-05 03:5x; the user may amend).

## Lanes (the staffing cell)
- W1-A (gpt-5.6-sol xhigh): rev 1 STOP 01:05 (package-metadata handoff outside the ticket), rev 2 10 / 11 rows (checkpoint 86d93e1b), rev 3 11 / 11 + 5 closures (scratch 1,388 / 169 / 36; 4 rows changed identity), rev 4 3 of those 4 exact (+3 closures; scratch 1,394 / 163 / 36; 4 strict subsets; the mixin row = emit-diagnostic order → integrator) — checkpoint 59f0d578. STATUS revs archived under `target/session-notes/7b/lanes/w1/`.
- W1-E (gpt-5.6-luna max): census complete 01:22 — 132 per-case diffs plus summary.json, 8 first-hunk buckets, hypotheses with `_tsc.js` spans; instrument checkpoint b2fd3e99.
- Integrator: the emit-diagnostic collection order (`execute.rs`, `sort_and_dedupe_diagnostics` before `EmitOutcome::new`) + the 15th control; 5g profile shadow inputs +2; 19 anchors re-pointed.

## First w1 sweep / re-mint — TO-FILL
- exact 1,395 / diverging 162 (rows 162; owner stays `h2-7b-m-2-divergence-closure`) — measured on the merged train (lanes A rev 2-5 + the integrator's §3.4) before the canonical re-mint; closed rows (list, each with its §3 mechanism; measured on the integrated train before the re-mint, 23 rows — §3.1 package `exports` blockage: 15; §3.2 export-assignment fallback anchor: 1; §3.3 alternative-containing-module candidates: 6; §3.4 emit-diagnostic collection order: 1): `compiler/declarationEmitMixinPrivateProtected.ts#default` (§3.4), `compiler/declarationEmitObjectAssignedDefaultExport.ts#default` (§3.2), `compiler/declarationEmitUsingAlternativeContainingModules1.ts#default` (§3.3), `compiler/declarationEmitUsingAlternativeContainingModules2.ts#default` (§3.3), `compiler/declarationEmitUsingTypeAlias1.ts#default` (§3.1), `compiler/declarationEmitUsingTypeAlias2.ts#default` (§3.3), `compiler/declarationsIndirectGeneratedAliasReference.ts#default` (§3.3), `compiler/reactTransitiveImportHasValidDeclaration.ts#default` (§3.3), `conformance/declarationEmit/typesVersionsDeclarationEmit.multiFile.ts#default` (§3.1), `conformance/declarationEmit/typesVersionsDeclarationEmit.multiFileBackReferenceToUnmapped.ts#default` (§3.3), `conformance/node/legacyNodeModulesExportsSpecifierGenerationConditions.ts#default` (§3.1), `conformance/node/nodeModulesExportsBlocksSpecifierResolution.ts#module%3Dnode16` (§3.1), `conformance/node/nodeModulesExportsBlocksSpecifierResolution.ts#module%3Dnode18` (§3.1), `conformance/node/nodeModulesExportsBlocksSpecifierResolution.ts#module%3Dnode20` (§3.1), `conformance/node/nodeModulesExportsBlocksSpecifierResolution.ts#module%3Dnodenext` (§3.1), `conformance/node/nodeModulesExportsSourceTs.ts#module%3Dnode16` (§3.1), `conformance/node/nodeModulesExportsSourceTs.ts#module%3Dnode18` (§3.1), `conformance/node/nodeModulesExportsSourceTs.ts#module%3Dnode20` (§3.1), `conformance/node/nodeModulesExportsSourceTs.ts#module%3Dnodenext` (§3.1), `conformance/node/nodeModulesExportsSpecifierGenerationConditions.ts#module%3Dnode16` (§3.1), `conformance/node/nodeModulesExportsSpecifierGenerationConditions.ts#module%3Dnode18` (§3.1), `conformance/node/nodeModulesExportsSpecifierGenerationConditions.ts#module%3Dnode20` (§3.1), `conformance/node/nodeModulesExportsSpecifierGenerationConditions.ts#module%3Dnodenext` (§3.1); canonical re-mint confirmation: the 23 rows above are exactly the deleted set; strict-subset rows (5): `compiler/declarationEmitForGlobalishSpecifierSymlink.ts#default`, `compiler/jsDeclarationsGlobalFileConstFunctionNamed.ts#default`, `conformance/node/nodeModulesDeclarationEmitDynamicImportWithPackageExports.ts#module%3Dnode18` / `%3Dnode20` / `%3Dnodenext`; added elements: 0; new rows: 0; componentwise review (merged-train scratch vs the m-2 manifest): equal 157 / deleted 23 / subsets 5 / rejected 0; canonical re-mint review (2026-09-05 05:22, write mode → normal run 1,395 / 162 / 36 exit 0): equal 157 / deleted 23 / strict subsets 5 / added 0 / new rows 0 / owner changes 0 — ACCEPTED under the m-2 §6 rule with no registered exception
- bands (merged train, workers=2): 6c 639 / 4 / 643, 5h 824 / 64 / 44, 6a 171 / 4 / 2, 6b 6 / 0 / 0, full 5g 9,027 / 8,511 (h2_8a 6 / h2_9 510 deferred); whole suites: compiler contracts 198 / 0 (16 w1 controls), checker 1,700, xtask 270 / 0 (the H2.1e band green again), emitter contracts 449, clippy `-D warnings`, fmt; ledger 3,787 / 0 / 0

## Walk / gate / PR — recorded in the PR body at the final head

## Implementation-time amendments
- 2026-09-05 **battery finding (integrator)**: `cargo test -p tsc-rs-xtask` — 3 H2.1e tests red on the merged train: JSON-module imports of `./package.json` resolve to the projected package input's token (host-only) → `UnknownTargetToken`; the package projection must stay out of the authoritative resolution table (upstream: package bodies via the host for specifier generation, JSON imports resolve to program sources) → W1-A rev 5 (sol). Lesson: a ticket that changes shared compiler/checker inputs lists `cargo test -p tsc-rs-xtask` (the 1a-5f band executions) and the early-band runners. Also fixed on the train: 4 clippy lints in the census instrument (`clippy --fix`), the `tsc-hash` of `alternative_containing_module_chains` (ledger stale=1 → 0).
