# A6-41 decorator-next: visit order, super paths, name owners (isolated draft)

Purpose: implement and verify the A6-41 rows left open after the receiver-frame
draft (`h2-8a-decorator-receiver-frames.md`): decorator/heritage/member visit
order with tsc's `pendingExpressions`, the decorated-class `super` paths, the
generated-name owners, and the visit-cache / receiver-identity assumptions.
This is an isolated research candidate in the worktree
`~/dev/tsc-rs-dec-next` (branch `draft/h2-8a-decorator-next`); root production,
hosted acceptance and whole-H2.8 completion are out of scope.

Pinned source: `vendor/typescript-6.0.3/lib/_tsc.js`, whole SHA-256
`1c59e77a54b186ec43fa7f3e0d3c4bb15ca5eb5ba43e96b1d3a267139eddd3e3`
(`transformESDecorators` 98946–100620, named evaluation 93715–93960,
`isNamedEvaluation` 15917–15985).

## Start point

Commit `cb4e5f3e8` on the draft branch = `6e298cda8` + the attempt65 archive
`source-and-inputs.tar.gz` (SHA-256
`60be6b2790c600be85907ed6446168c3449a11e5d0711525cf39ae76ee005592`, the
`prelaunch.archive` of `ratchets/h2-8a-decorator-receiver-emitter-checks.v1.json`).
All 1014 inputs and 109 vendor inputs of that prelaunch manifest match byte for
byte; the two archived design documents were restored to their `6e298cda8`
versions (they are not build inputs). Production file SHA-256 at the start:
`standard_decorators.rs` `7872d2f6…dfe0`, `class_fields.rs` `c423c244…8081`,
`class_fields/downlevel.rs` `506ea986…0b9`. The existing 36 receiver-context
commands reproduce 36/36 exact twice on this start point (own target dir,
demoted; log `target/dec-next-runs/baseline-context/run.log`).

## Witnesses (fresh TypeScript, twice, diagnostic-free)

Three new complete-command groups, generated by
`scripts/generate-decorator-next-witness-inputs.py` (ES2015/ES2022/ESNext ×
set/define like the receiver-context fixture), observed by
`scripts/observe-decorator-next-witnesses.mjs <group> --write` (two
repetitions, zero exceptions, zero reported diagnostics) and compared as
complete commands (JS, maps, diagnostics, emit result, write callbacks, status,
exit, partial writes) by the contracts module
`crates/compiler/tests/integration/h2_8a_decorator_next_witnesses.rs`
(`TSC_RS_DECORATOR_NEXT_CASE_SET=transform-order|super-paths|name-owners|all`,
`TSC_RS_DECORATOR_NEXT_CASE_FILTER=<substring>`).

| group | commands | fixture SHA-256 |
| --- | --- | --- |
| decorator-transform-order | 60 (10 sources) | `c831b004c92557b383d19e364829ae960f65361a8253a24a1f53aa4248c8a4ae` |
| decorator-super-paths | 42 (7 sources) | `7ef1f767d49eb7f3c89008ba1120a4e93f9de2f1c063bd550c9988fdb0e998de` |
| decorator-name-owners | 24 (4 sources) | `9cc04b4a69e083a0660f7709b5e84b12a9e3a92acb66744fe23448149905a71e` |

`decorator-super-paths/*/super-undecorated-control` is an adjacent control for
the class-fields owner (no decorators at all); `super-member-decorated-only`
has member decorators but no class decorator (no `classThis`, so
`transformESDecorators` leaves `super` alone). ESNext/define commands keep the
decorators native (tsc emits `@dec` unchanged there; the standard-decorator
transform is not constructed for that configuration).

Baseline on the unmodified candidate (commit `c9ba0380b`, log
`target/dec-next-runs/baseline-witnesses-v3/`): 126 commands, 27 exact twice,
99 failed twice (every ESNext/define command exact, 5 of 6 configurations of
every other source failing except the two control sources with 4/6). Four
ES2015 commands fail with the typed error "transform node … has no parse-tree
identity for an emit-resolver query" (a decorated computed auto-accessor
lowered by `class_fields/downlevel.rs`).

## Upstream facts used by the implementation

- `transformClassLike` transforms class decorators first, visits the extends
  expression second (both in the enclosing frame, before `enterClass`), then
  visits non-constructor members and constructors in two passes, each member
  under its own class-element frame.
- `pendingExpressions` (saved/cleared by `enterClass` and depth-0
  `enterOther`, restored on exit) receives `_x_decorators = [...]` for every
  decorated member in source order; a decorated member with a non-literal
  computed name absorbs the queue into `[(…, _a = __propKey(expr))]`
  (`visitReferencedPropertyName`), and any later computed name whose visited
  expression is not simply inlineable absorbs it too
  (`visitComputedPropertyName`), decorated or not. Whatever is left after the
  members becomes expression statements right after the `_metadata`
  declaration of the leading static block; only those statements get lexical
  `this` replaced by `_outerThis` (`let _outerThis = this` unshifted before the
  class-definition statements, after the hoisted `var` temporaries of
  `mergeLexicalEnvironment`).
- Class decorators keep lexical `this` (the IIFE is an arrow); a static
  property's decorators are visited with `classThis` (`this.dec` →
  `_classThis.dec`).
- `safeExtendsExpression`: an anonymous class expression, anonymous function
  expression or arrow function in the extends position becomes
  `(0, expression)`.
- Hoisted temporaries restart at `_a` in every nested lexical environment
  (nested class IIFE, static property initializer IIFE, static block) and the
  temp sequence skips `_i` and `_n`.
- Named evaluation of an anonymous decorated class expression: variable,
  parameter, binding element, shorthand property, assignment (`=`, `&&=`,
  `||=`, `??=`) and property assignment names give `__setFunctionName(_classThis,
  "<name>")`; a class property with a non-literal computed name hoists the
  `__propKey` cache temp and names the class by it
  (`static [_c = __propKey(keys.a)] = (() => { … __setFunctionName(_classThis, _c) … })()`).
  A decorated computed property whose initializer is a decorated class hoists
  the same generated name twice (`var _b, _b`); not reproduced (open row).
- `_outerThis` is `Optimistic` without `FileLevel`/`ReservedInNestedScopes`:
  numbered in print order across the whole file (`_outerThis`, `_outerThis_1`,
  … across siblings and nested classes) and against source identifiers and
  global names. FileLevel names (`_classThis` unless a static private/accessor
  member exists, `_metadata`, `_classSuper`, `_classDecorators`, …) collide
  with source identifiers and with `hasGlobalName` of other files
  (`var _classThis = 1` in a script file gives `_classThis_1` everywhere).
- `super` in decorated static initializers/blocks (with `classThis` and
  `classSuper`): `Reflect.get(_classSuper, key, _classThis)`, `.call(_classThis,
  …)`, `.bind(_classThis)` for tagged templates, `Reflect.set(…)` with a
  hoisted result temp when the value is used, compound/logical assignment
  through a stabilized key temp, pre/postfix updates through
  `expandPreOrPostfixIncrementOrDecrementExpression`, destructuring targets
  through `({ set value(_a) { Reflect.set(…) } }).value`. Temporaries hoist
  into the static block or, for a property initializer, into an IIFE
  `(() => { var …; return …; })()`.

## Execution record

(filled per commit below)

### Item 1 — visit order, pending expressions, `_outerThis` (commit `65f688302`)

Causes found by the baseline (all confirmed by fresh TypeScript):

1. Member decorator array assignments were emitted as decoration-block
   statements instead of tsc's `pendingExpressions`; decorated fields with
   computed names emitted `[_a]` plus a separate static block, and no computed
   name ever absorbed the queue (`pending-into-undecorated-names`,
   `decorated-computed-fields`, `nested-class-in-computed-name`).
2. Lexical `this` in decorator expressions was rewritten to `_outerThis`
   eagerly, including class decorators and expressions that end in a computed
   name (`outer-this-computed-name`, `outer-this-nested-numbering`).
3. `let _outerThis` preceded the hoisted `var` temporaries
   (`outer-this-static-block`).
4. The extends expression was visited before the class decorators, and the
   `(0, expression)` safe-extends form was missing (`heritage-safe-extends`).
5. Named evaluation of anonymous decorated class expressions covered only
   variable declarations; parameters, property assignments, class properties
   (literal and non-literal computed names) emitted `__setFunctionName(_, "")`
   (`two-phase-constructor`, `decorated-class-expression-positions`,
   `outer-this-nested-numbering`).
6. Decorated methods with computed names lost the parsed name node (a
   synthesized name without range/original) and the decorator call binding
   lacked `setTextRange` on the cache assignment; the injected constructor
   body updated the parsed (single-line) block instead of creating a fresh
   multi-line one.

Changes (`standard_decorators.rs` only): `pending_expressions` owned by the
visitor and saved/restored by the class and depth-0 other frames;
`transform_class_member` = classElementVisitor with `partial_transform_*`
(decorators queued in the element frame, `visit_referenced_property_name`
absorbing the queue through `updateComputedPropertyName`),
`previsit_property_name` = visitPropertyName under the name frame with
`visitComputedPropertyName` absorption; two member passes (constructors
second) with balanced frames on `Ok` and `Err`; leftovers → leading block
statements through the `_outerThis` visitor; `var` temporaries → `let
_outerThis` → definitions; class decorators transformed before
`prepare_class_super`; `safeExtendsExpression`; `isNamedEvaluation` sources in
`visit()` and `prepare_property_named_evaluation` (computed names hoist the
`__propKey` cache temp and name the class by it,
`DecoratedClassRuntimeName::AssignedReference`). Every `Err` path discards the
visitor (`transform_nodes` returns the error), so frames are only restored on
`Ok`; the member loop still exits its element frame before propagating.

Result: decorator-transform-order 10/60 → 32/60 exact twice
(`target/dec-next-runs/item1-r3/`, run.log + captures + analysis.txt);
`heritage-safe-extends`, `nested-class-in-computed-name`,
`pending-into-undecorated-names`, `decorated-class-expression-positions`
6/6. Residuals at that commit: nested-scope temp numbering (item 3a),
`_outerThis` numbering (item 3b), class-fields cache-assignment handoff on
comma-list computed names (item 3c).

Open rows from item 1: a decorated computed property whose initializer is a
decorated anonymous class (tsc hoists the same generated name twice, `var _b,
_b`); literal computed names on decorated members (`@dec ["x"]`) still take
the temp path instead of `{ computed: true, name: "x" }`; object-literal
computed names inside decorated static contexts do not absorb the pending
queue (tsc's `visitor` does).

### Item 3a — temporaries are scope-owned generated bindings (commit `68f2a4f93`)

Cause: hoisted temporaries were transform-time strings drawn from one
class-scoped counter and a shared `used_names` set, so nested class IIFEs
continued the enclosing sequence (`var _f` / `_b` where tsc restarts at `_a`),
computed-name cache temps did not reserve their spelling in nested scopes
(`var _a, _c` in `class-decorator-before-heritage`), and the by-spelling
`computed_temp_bindings` map keyed bindings on printable text.

Changes (`standard_decorators.rs`): `DecoratorLexicalEnvironment` stack
(`start_lexical_environment`/`end_lexical_environment`) for the class IIFE,
class static blocks, static property initializers (`(() => { var …; return
…; })()`) and function-like bodies; `hoist_temp_variable(reserve_in_nested_scopes)`
= `createTempVariable(hoistVariableDeclaration)` as
`TargetBinding::allocate` (finalizer-named per scope in traversal order) or
`allocate_reserved_in_nested_scopes` for `getGeneratedNameForNode(name)`
cache temps; plans, contexts, access objects, setter names and the
named-evaluation reference carry `TargetBinding` handles
(`create_binding_identifier`); the by-spelling lookup in `create_identifier`
is gone. Provisional spellings follow makeTempVariableName (skipping `_i`,
`_n`) but are never load-bearing.

Result: decorator-transform-order 32/60 → 38/60 exact twice
(`target/dec-next-runs/item3a-r1/`); `class-decorator-before-heritage` 6/6;
ES2022/define `two-phase-constructor` exact. Residuals: `_outerThis` numbering
(item 3b), the class-fields cache handoff (item 3c) and, at ES2015, the
`__propKey`/`__setFunctionName` helper order (the explicitly named class's
`__setFunctionName` is a class-fields-pass helper upstream).
