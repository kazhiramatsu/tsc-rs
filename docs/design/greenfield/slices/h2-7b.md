# H2.7b — non-bundle declaration output: slice-opening packet

Status: **rev 9 RATIFIED / design only** (2026-09-03; sol rounds 1-8
REVISE adopted in full, round 9 **AGREE** — §10). This packet authorizes no production edit
and no oracle observation; it defines scope, the candidate band, the
upstream survey boundary, the Rust gap matrix, W-H2.7B, and the ladder.
It boards the `h2/7a-ca` train as a design-only passenger under its own
design gate (the h2-7a.md-on-h2/6c-close precedent); the era it opens
begins only after the H2.7a close lands (`next_slice` → H2.7b,
[h2-7a-ca.md](h2-7a-ca.md) §5).

The active route is the [greenfield execution guide](../README.md):
pinned tsc semantics, current emitter architecture, schedule, then this
packet. [H2.7a](h2-7a.md) and its five rung packets supply the dormant
foundation this era activates; [h2-6c-m-2](h2-6c-m-2.md) (the flip
rung), [h2-6a-ca-1](h2-6a-ca-1.md) (band observation), and
[h2-6a-ca-2](h2-6a-ca-2.md)/[h2-6c-ca](h2-6c-ca.md) (acceptance wiring +
landing) are the structural precedents for a band-defined runtime
activation era.

## 1. Identity, authority, and boundary

- **Slice ID / kind:** h2-7b, runtime activation of non-bundle `.d.ts`
  output — the FIRST admission-profile change since H2.6c; both
  band-defined (§2: a 1,593-row candidate band that m-1 splits into the
  ADMITTED band and a count-only DEFERRED set) and scope-defined (the H2.6c handoff rows and the frozen W-H2.7A
  replay through the activated path).
- **Authority:** the row at
  [h2-transition.mjs:693](../../../../crates/oracle/h2-transition.mjs#L693) is
  ["H2.7b", "planned", ["H2.7a"], ["dts", "declaration-only",
  "callback-metadata"], "exact non-bundle declaration output"]. The
  schedule row
  ([§4.5](../post-h1-completion-slices.md#45-maps-declarations-outputconfig-and-broad-qualification),
  :1570) reads: "Non-bundle `.d.ts` emit, callback metadata,
  declaration-only routing, output paths, and exact resolver/NodeBuilder
  results. H2.7a and stable Program/emit ownership."
- **Owner rows pinned to H2.7b** (h2-transition.mjs `ROOT_SPECS`): the
  path workers `getDeclarationEmitOutputFilePath` (:143, _tsc.js:16577)
  and `getOutputDeclarationFileName` (:178, _tsc.js:116400). The
  orchestration owners `forEachEmittedFile` (:173, _tsc.js:116312),
  `getOutputPathsFor` (:175, :116373), `emitFiles` (:180, :116530) are
  inventoried to H2.8a, `getOutputPathsForBundle` (:174, :116365) to
  H2.7d, `Program.emit` (:182, :123568) to H2.8d, and
  `emitFilesAndReportErrors` (:185, :129412) to H2.8e.
  **The declaration-arm split is a MACHINE amendment of the owner graph,
  made at the m-1 design gate under the schedule's SCC split rule
  (post-h1-completion-slices.md :1581-1584 — "split it with letter
  suffixes before runtime work"; never a prose-only disposition, F1).**
  Row model (rounds 2-3): the h2-transition owner inventory represents
  WHOLE `FunctionDeclaration` roots selected by (name, line) and keyed by
  declaration identity (`selectDeclaration` / `selectedByDeclaration`,
  h2-transition.mjs:271-298), asserts `owner_roots === 50` (:382), and
  renders the `declaration` shape of
  `.github/ci/contracts/h2-owner-inventory.schema.json`; an arm is NOT a
  root and must never be fed through `selectedByDeclaration` (it would
  collapse onto its enclosing function's node). Therefore m-1 adds a
  machine-distinct ARM table `ARM_SPECS` rendered as a new `owner_arms`
  block with its own `$defs.owner_arm` shape and `summary.owner_arms ===
  4`, while `owner_roots` stays 50 and the whole-function `declaration`
  shape is untouched. An arm row carries: `key`; `enclosure` — the
  function that ACTUALLY encloses the arm (name, start line, whole-
  function range + sha256), whether or not it is a frozen root;
  `ranges[]` — one or more EXACT line segments covering ONLY the
  H2.7b-owned statements, each with start/end positions and its own
  `sha256`, so a continuous span never swallows a later-owned branch;
  `owner_slice: "H2.7b"`, `disposition`, `role`, `activation`;
  `consumers[]` — the frozen root keys that reach the arm; and
  `dependencies[]` — the shared or later-owned helpers the segments call,
  Segment rule: a `ranges[]` segment contains ONLY lines whose every
  statement is H2.7b-owned; a line that co-locates an H2.7b predicate
  with another owner's arm is recorded under `shared_lines[]` (both
  owners named, no hash claim of exclusivity), and every excluded line
  or callee is a `dependencies[]` entry with its owner. The four arms,
  measured line-by-line against the vendored bytes on 2026-09-03 (m-1
  pins and hashes them):
  - `declaration-source-output-paths` — enclosure `getOutputPathsFor`
    :116373-116387 (frozen root `source-output-paths` :175). Segment:
    [:116383] alone — the `declarationFilePath` derivation with its
    `getDeclarationEmitOutputFilePath` call. Shared lines: :116381 (the
    `emitDeclarationOnly` JS-path suppression — H2.7b routing semantics —
    co-located with the JSON same-location arm, H2.8a); :116385 (the
    shared return). Dependencies: `getDeclarationEmitOutputFilePath`
    (H2.7b root :143), `getEmitDeclarations` :18255 (computed option),
    `isJsonSourceFile` :116379 (shared), :116384 `declarationMapPath`
    (H2.7e), :116375-116376 the bundle arm (H2.7d). Consumers:
    `source-output-paths`, `output-enumeration`.
  - `declaration-emit` — enclosure `emitFiles` :116530-116858 (frozen
    root `emit-files` :180; the frozen inventory records that endpoint);
    `emitDeclarationFileOrBundle` is its nested function :116640-116715,
    reached from `emitSourceFileOrBundle` :116555-116566. Exclusive
    segments (every statement H2.7b-owned): [:116640], [:116642],
    [:116644-116645], [:116649], [:116651-116668], [:116670],
    [:116672-116679], [:116681-116695], [:116697-116699],
    [:116703-116708], [:116712-116715], and the nested
    `collectLinkedAliases` [:116716-116735]. Shared lines (H2.7b + the
    named co-owner): :116641 (the `emitOnly === Js` guard — H2.8d);
    :116643 (`emitOnly` H2.8d ‖ `emitDeclarationOnly` H2.7b →
    `emitSkipped`); :116646 (the bundle `sourceFiles` selection — H2.7d);
    :116647 (`forceDtsEmit` — H2.7c semantics / BLD1 caller ‖ the JSON
    filter H2.7b); :116648 (the `outFile` bundle arm H2.7d ‖ the
    `filesForEmit` fallback H2.7b); :116650 (the pre-pass predicate:
    `emitOnly` H2.8d ‖ `noCheck` H2.8c ‖ `emitResolverSkipsTypeChecking`
    BLD1 ‖ `canIncludeBindAndCheckDiagnostics` shared); :116669
    (`declBlocked`: diagnostics + `isEmitBlocked` H2.7b ‖ `noEmit` H2.9);
    :116671 (the `forceDtsEmit` bypass — H2.7c/BLD1); :116696 (the
    `declarationMapPath` argument — H2.7e); :116700-116702 (the
    `sourceMap` / `sourceRoot` / `mapRoot` map options — H2.7e).
    Excluded → dependencies: :116680 (the `sourceMap:` printer option =
    `declarationMap`, H2.7e); :116709-116711 (the `.d.ts.map`
    `emittedFiles` listing, H2.7e); :116736-116743 (`markLinkedReferences`,
    the JS lane — active); `transformNodes` (shared transform driver);
    `getDeclarationTransformers` (H2.7a root :171); `createPrinter` (the
    shared printer); `emitResolverSkipsTypeChecking` :116527-116529
    (H2.8d/BLD1 arm); `canIncludeBindAndCheckDiagnostics` :18898
    (shared); `printSourceFileOrBundle` (the `declaration-write` arm).
    Consumers: `emit-files`, `program-emit`.
  - `declaration-write` — enclosure `emitFiles` :116530-116858 (frozen
    root `emit-files` :180): `printSourceFileOrBundle` :116744-116804 is
    NESTED inside `emitFiles` (not a top-level sibling). Exclusive
    segment: [:116803] — `return !data.skippedDtsWrite`, the dts-specific
    write result. Shared lines (the print/write mechanics the active JS
    lane executes on every write): :116744-116749 (entry +
    source-file/bundle split), :116761-116763 (the `writeFile` print
    dispatch), :116764 (`let sourceMapUrlPos;` — declared outside the map
    branch and consumed by the shared metadata line :116800),
    :116796-116798 (the no-source-map `writeLine` branch), :116799-116802
    (`getText`, the `data` contract, the `writeFile` call, `clear`).
    Excluded → dependencies: :116750-116758 and :116765-116795 (the
    source-map generator and map-write branches: H2.6a-H2.6c for JS,
    H2.7e for declarations); :116759-116760 (`writeBundle`, H2.7d);
    `writeFile` :16644-16655 (H2.8a root :147); `shouldEmitSourceMaps`
    :116805-116807 (a sibling nested function of `emitFiles`). Consumers:
    `emit-files` (via the declaration-emit call :116694-116706).
  - `declaration-collision-preflight` — enclosure `verifyCompilerOptions`
    :124750-… (NOT a frozen root; the frozen `program-emit` root
    :123568-123591 is a CONSUMER). Exclusive segment: [:125025] — the
    always-on declaration-path `verifyEmitFilePath` invocation. Shared
    lines: :125022-125024 (the `emitDeclarationOnly` wrapper — H2.7b
    routing ‖ the JS-path verification :125023 — H2.8a). Excluded →
    dependencies: the outer gate and enumeration :125018-125021 +
    :125026-125027 (`!noEmit && !suppressOutputPathCheck`,
    `forEachEmittedFile` — H2.8a); the generic verifier
    `verifyEmitFilePath` :125028-125050 (input-overwrite chain, canonical
    key, duplicate-output diagnostic — H2.8a); `blockEmittingOfFile`
    :125418-125420 (H2.8a; feeds `isEmitBlocked` :123592-123594 →
    `declBlocked` :116669). Consumers: `program-emit`, `emit-files`.
  The parent rows keep their inventoried owners for the general
  output-path matrix (outDir/rootDir relocation, `listEmittedFiles`, the
  js/map arms → H2.8a; every bundle arm → H2.7d). Placement: `ARM_SPECS`
  is defined BELOW the frozen option-owner span (h2-transition.mjs
  :448-467, whose exact text and line numbers the H2.7a inventory
  generator pins — h2-7a-owner-inventory.mjs:37/:1639-1647; verified
  hash `25632b51…`), so that span does not shift; `transition_rows === 39`
  (:775) is untouched. Regeneration and stale cone (pin-only, ZERO
  oracle observation): `h2-transition.mjs --write` regenerates all
  THREE outputs because each records the generator hash (:798-812) —
  `ratchets/h2-owner-inventory.v1.json` (content: + the `owner_arms`
  block), `ratchets/h2-candidate-dispositions.v1.json` (content
  UNCHANGED — dispositions derive from blockers, not owner rows; the
  `cases` roll `ed0036eb…` the H2.7a close artifact asserts stays valid;
  only the generator/pin fields move), `ratchets/h2-profile-transition.v1.json`
  (pins); then the walk re-pins the whole h2 ladder, the H2.7a owner
  inventory (its option-owner span pin intact), and the H2.7a close
  artifact (h2-7a-ca.md §4.4 close-time surface) — every step a receipt
  hit / pin re-mint.
- **The two "declaration-only" axes are distinct owners:** the compiler
  OPTION `emitDeclarationOnly` (:116381/:116367 js path → undefined,
  :16604 selection in the outFile arm, :116643 emitSkipped) is H2.7b's
  "declaration-only routing"; the API `emitOnly` axis (`emitOnlyDtsFiles`
  → `EmitMode::DeclarationOnly` in Rust, plan.rs:44-49/:190-194) is
  H2.8d's "ordinary emit-only/declaration-only axes" and stays refused.
- **Activation state — before:** the planned declaration member is
  refused at [plan.rs:208-212](../../../../crates/emitter/src/plan.rs#L208)
  and tolerated for js/map members at
  [execute.rs:627-635](../../../../crates/emitter/src/execute.rs#L627)
  (the H2.6c allowance — THE flip seam); `emitDeclarationOnly` is
  bootstrap-refused (execute.rs:112-115); `PrintRequest::Declaration` is
  refused ([printer.rs:1051-1053](../../../../crates/emitter/src/printer.rs#L1051));
  the printer's `declaration_syntax` gate is constructed only at the
  dormant allowlist; `H2RuntimeSlice::H2_7b` exists (activity.rs:38)
  with no `h2_7b_profile()`; production runs `h2_6c_profile()`
  ([compiler/src/lib.rs:834](../../../../crates/compiler/src/lib.rs#L834));
  the H2.7a close artifact hash-gates plan.rs / execute.rs / activity.rs
  and the printer refusal span (h2-7a-ca.md §4.1 block 5). **After the
  ladder:** every ADMITTED case writes its non-bundle `.d.ts` byte-exact
  with exact callback metadata, `emittedFiles` order and membership,
  `emitSkipped`/`declBlocked` composition, and `emitDeclarationOnly`
  routing; `H2.7b` admitted; the H2.7c/7d/7e/8a/8c/8d arms stay typed
  refusals with their owners.
- **Non-goals (owned elsewhere, retained as typed controls; F13 owner
  map):** bundles/`outFile` (H2.7d — :116648, :116365); `declarationMap`
  / `.d.ts.map` (H2.7e — :116680, :116696/:116700-116702, :116709-116711);
  `declarationDir`, `stripInternal`, `isolatedDeclarations`, the
  `getDeclarationDiagnostics` Program API (exported :122935, wrapped :123674-123676), the `noEmitOnError`
  escalation (:125641-125655), the `emitDeclarationOnly`-without-
  `declaration` option diagnostic (:124946-124950), and the
  forced-declaration SEMANTICS (`forceDtsEmit`'s effect on selection
  :16600-16638 and on the declBlocked bypass :116671) (H2.7c);
  `noCheck` (:116650) and the transpile APIs incl. `transpileDeclaration`
  (H2.8c); the API `emitOnly` axis and targeted-source emit (H2.8d); the
  BuilderSignature CALLER of `forceDtsEmit` — `computeDtsSignature`
  :126236-126258, the only `forceDtsEmit=true` caller in this build —
  and the `skippedDtsWrite` PRODUCERS (:127523/:127532) (BLD1); `noEmit`
  (`route:noEmit=true` → H2.9, h2-transition.mjs:446); the
  `outDir`/`rootDir` matrix, `listEmittedFiles`,
  `suppressOutputPathCheck` (:125018) and filesystem-fault activation
  (H2.8a); custom `afterDeclarations` transformers (API1).

## 2. Candidate-band census — ratchet is authoritative

The frozen input is
[h2-candidate-dispositions.v1.json](../../../../ratchets/h2-candidate-dispositions.v1.json)
(15,642 rows; `cases` content roll `ed0036eb…`, byte-identical since the
H2.6c close; `inventory_fingerprint_sha256` `5e3255e3…`). A read-only
python scan on 2026-09-03 (no oracle ran). `next_slice` is the
lowest-ranked required slice at FREEZE time (h2-transition.mjs:583 /
:512), so the live first blocker is recomputed against the 26 active
slices of `h2-5g-profile.mjs:637-643`.

| ratchet predicate | compiler | conformance | project | transpile | total |
| --- | ---: | ---: | ---: | ---: | ---: |
| required_slices contains H2.7b (the chain) | 1,034 | 861 | 528 | 33 | 2,456 |
| candidate band: required_slices ⊆ ACTIVE ∪ {H2.7b} (the SELECTOR — not yet an executable closure, see below) | 921 | 476 | 196 | 0 | 1,593 |
| next_slice == H2.7b (frozen) | 1 | 0 | 0 | 0 | 1 (`compiler/modulePreserve4.ts#default`) |

Machine constants for the m-1 qualification machine:

    GLOBAL_H2_7B_ROWS = 2456
    CANDIDATE_BAND_H2_7B_ROWS = 1593      (compiler 921 / conformance 476 / project 196 / transpile 0)
    FROZEN_NEXT_SLICE_H2_7B_ROWS = 1
    ADMITTED_H2_7B_ROWS / DEFERRED_H2_7B_ROWS = TO-VERIFY(m-1)   (the effective-option split below)

**The candidate band is a frozen-blocker SELECTOR, not an executable
dependency closure (F2).** `required_slices` derives only from the
H2.0a profile blockers and the source kind (h2-transition.mjs:423-512),
so a band row's EFFECTIVE options may still name a later owner. A
read-only census over the 921 compiler rows (fixture `settings` +
`configurations` of `vendor/typescript-6.0.3/test-suite-expansion.v1.json`,
2026-09-03) finds 25 such rows: `isolatedDeclarations: true` 21 (e.g.
`computedPropertiesNarrowed.ts`), `noEmitOnError: true` 2 (e.g.
`DeclarationErrorsNoEmitOnError.ts`), `stripInternal: true` 1
(`stripInternal1.ts`), and 1 `emitDeclarationOnly` WITHOUT `declaration`
/ `composite` (`declFileEmitDeclarationOnlyError1.ts` — upstream emits
the option diagnostic :124946-124950, plans neither path :116381-116385,
and takes the no-declaration-path early return :116641-116645: NO write,
`emitSkipped = true`; H2.7c's option-diagnostic lane, F3); 10 rows carry
an explicit `declarationMap: false` (negative controls that stay in the
band as observations). The 476 conformance rows and the 196 project
routes are TO-VERIFY(m-1) under the same census. Therefore m-1 (the
h2-6a-ca-1 §2/§4 precedent) parses effective options across compiler,
conformance, and every project route, freezes a distinct ADMITTED band
(every row with `getEmitDeclarations(options)` true and no later-owned
option) and a count-only DEFERRED set with first owner / required slices
/ facets / reason (every H2.7c-owned row deferred), and adds fail-closed
controls for those options. The H2.7b declaration-only contract covers
ONLY rows where `getEmitDeclarations(options)` is true.

Live first blocker: `H2.7b` for all 2,456 chain rows — every frozen
`next_slice` value (H2.1a 989 / H2.1b 797 / H2.1c 356 / H2.1e 302 /
H2.1d 9 / H2.5g 1 / H2.5a 1 / H2.7b 1) is already active; no earlier
unlanded slice gates the chain. Band facts the machine asserts:

- H2.7b blockers inside the band (h2-transition.mjs:448/:454/:457):
  `rejected-option:declaration` only 1,481; `declaration` +
  `emitDeclarationOnly` 111; `emitDeclarationOnly` only 1. Zero
  `product:declaration` rows (those 21 are transpile-API rows, all
  behind H2.8c). Zero `composite`, `declarationDir`, `declarationMap`,
  `outFile`, `outDir` rows by construction of the closure.
- 134 band rows also carry `rejected-option:sourceMap` — they exercise
  the H2.6a/6b/6c map lanes together with the declaration member.
- Transpile rows in the band: 0 (every transpile chain row also requires
  H2.8c) — the transpile-API execution route is dormant and excised with
  a fail-closed census guard (`transpile_rows === 0`, the
  h2-6a-ca-1.md §2 CA-3 shape); a re-census that adds one FAILS the
  build.
- The 863 non-closed chain rows and their later gates (a row may count
  under several): H2.8a 574, H2.7d 238, H2.8c 38, H2.8b 33, H2.7c 12,
  H2.7e 12, H2.9 4 — count-only context for the ca handoff.

**The H2.6c handoff** ([h2-6c-ca.md](h2-6c-ca.md) §4; manifest
`ratchets/h2-6c-known-divergences.v1.json`, 451 rows, ONE owner value
`h2-6c-m-2-divergence-closure`, no per-row later-owner field): 433
manifest rows sit in the H2.7b chain (276 refused + 157 non-refused
declaration-bearing — exactly the §4 set, by the join non-refused ∧
H2.7b ∈ required_slices); **133 of the 157 are inside the candidate
band** (all `emit_refused=false`) and shrink under the shrink-only
`run_h2_6c` re-sweep after the flip (minus any row the m-1 census
defers); the other 24 decompose EXACTLY as H2.7d-only 18 / H2.7e-only 5
/ H2.7d + H2.7e 1 (the ratchet join, 2026-09-03; F4) and carry unchanged
into the ca handoff. The 67-row exact-but-for-declarations set is a
HISTORICAL W5 subset established by path-joining the separate 202-row
W5 census (h2-6c-w5.md §2 :47-65; disposition :307-315) — it is NOT a
ratchet-join result (F5); if H2.7b needs its identities as a live gate,
m-1 re-derives them from the same path-joined observation relation and
freezes that derivation explicitly. The 6c qualification artifact
already carries every upstream declaration write of its 643 cases (726
`kind: "declaration"` writes with callback/materialized bytes, BOM,
`sourceFiles`; `data.diagnostics` only as a COUNT — §4 G9), so the
overlap has frozen expected bytes twice over once m-1 lands.

## 3. Static upstream owner survey (frozen at m-1)

Measured 2026-09-03 against `vendor/typescript-6.0.3/lib/_tsc.js`
(134,465 lines) by line-anchored static scan; no oracle ran. The m-1
rung pins spans/hashes on the H2.7a inventory rows; the H2.7a-era
dormant pieces are already hashed there.

| surface | anchor span | content |
| --- | --- | --- |
| declaration output path | `getDeclarationEmitOutputFilePath` :16577-16579 → `…Worker` :16580-16585 (`declarationDir \|\| outDir` → `getSourceFilePathInNewDirWorker` :16639-16643 → `removeFileExtension + getDeclarationEmitExtensionForPath` :16586-16591: `.mjs/.mts → .d.mts`, `.cjs/.cts → .d.cts`, `.json → .d.json.ts`, else `.d.ts`); `getOutputDeclarationFileName(…Worker)` :116400-116408 (config side — uses the INPUT extension) | H2.7b owner rows :143/:178; the `declarationDir` read is `proven-unreachable` in-band (§9.2) |
| output-path composition (declaration arm) | `getOutputPathsFor` :116373-116387 — `jsFilePath = emitDeclarationOnly \|\| isJsonEmittedToSameLocation ? undefined : ownOutputFilePath`; `declarationFilePath = forceDtsPaths \|\| getEmitDeclarations(options) && !isJsonFile ? getDeclarationEmitOutputFilePath(...) : undefined`; `declarationMapPath` only with `getAreDeclarationMapsEnabled` (H2.7e) | the declaration arm; the js arm and `forEachEmittedFile` :116312-116341 stay H2.8a; `getOutputPathsForBundle` :116365-116372 stays H2.7d |
| selection | `getSourceFilesToEmit` :16600-16616 (`emitDeclarationOnly` changes `moduleEmitEnabled` ONLY in the `outFile` arm :16604) and `sourceFileMayBeEmitted` :16617-16638 | in-band: no outFile → selection unchanged by `emitDeclarationOnly` |
| **output-path collision preflight (Program side)** | `if (!options.noEmit && !options.suppressOutputPathCheck)` :125018 → `forEachEmittedFile` → `verifyEmitFilePath(jsFilePath)` SKIPPED under `emitDeclarationOnly` :125022-125024, `verifyEmitFilePath(declarationFilePath)` ALWAYS :125025; `verifyEmitFilePath` :125028-125050 (input-overwrite chain `Cannot_write_file_0_because_it_would_overwrite_input_file` with the tsconfig hint when no `configFilePath`; the canonical key `toFileNameLowerCase` unless `useCaseSensitiveFileNames`; `Cannot_write_file_0_because_it_would_be_overwritten_by_multiple_input_files`); `blockEmittingOfFile` :125418-125420 (`hasEmitBlockingDiagnostics` + a config diagnostic) → `isEmitBlocked(declarationFilePath)` :123592 → `declBlocked` :116669 | the declaration arm becomes REACHABLE at activation (G12, F7); the base matrix stays H2.8a |
| computed options | `declaration.computeValue = !!(declaration \|\| composite)` :18151-18156, `getEmitDeclarations` :18255; `declarationMap.computeValue` :18169-18173 (H2.7e) | Rust already mirrors `declaration \|\| composite` at plan.rs:333 (composite stays refused at execute.rs:122 — no in-band row) |
| emit driver | `emitFiles` :116530-116858 (`emitSkipped` :116538; `emittedFilesList` iff `listEmittedFiles` :116533; the return :116549-116554 `{ emitSkipped, diagnostics, emittedFiles, sourceMaps }`); `emitSourceFileOrBundle` :116555-116566 (strict order js → declaration → buildInfo); `emitJsFileOrBundle` :116587-116639 | H2.8a orchestration; H2.7b owns the declaration call and its result composition |
| **declaration emit** | `emitDeclarationFileOrBundle` :116640-116715 (76 lines): guards :116641-116645 (`emitOnly === Js` return; no `declarationFilePath` → `emitSkipped = true` iff `emitOnly \|\| emitDeclarationOnly`); `filesForEmit = forceDtsEmit ? sourceFiles : filter(isSourceFileNotJson)` :116647; the outFile bundle arm :116648 (H2.7d); the `collectLinkedAliases` pre-pass :116649-116653 (per file when `emitOnly && !getEmitDeclarations \|\| noCheck \|\| emitResolverSkipsTypeChecking \|\| !canIncludeBindAndCheckDiagnostics(sf)` — in-band only the last disjunct can fire; `canIncludeBindAndCheckDiagnostics` :18898, the nested `collectLinkedAliases` walker :116716-116735; :116736-116743 is `markLinkedReferences`, the JS lane's); `transformNodes(..., declarationTransformers, allowDtsFiles=false)` :116654-116662; transform diagnostics → `emitterDiagnostics` :116663-116667; `declBlocked = diagnostics.length \|\| host.isEmitBlocked(declarationFilePath) \|\| noEmit` :116669; `emitSkipped \|\|= declBlocked` :116670; `if (!declBlocked \|\| forceDtsEmit)` :116671 → `Debug.assert(transformed.length === 1)` :116672; printerOptions :116673-116685 (`removeComments, newLine, noEmitHelpers: true, module, moduleResolution, target, sourceMap: emitOnly !== BuilderSignature && declarationMap` (false in-band), `inlineSourceMap, extendedDiagnostics, onlyPrintJsDocStyle: true, omitBraceSourceMapPositions: true`); `createPrinter(printerOptions, { hasGlobalName: resolver.hasGlobalName, onEmitNode, isEmitNotificationEnabled, substituteNode })` :116686-116693; `dtsWritten = printSourceFileOrBundle(...)` :116694-116706; `emittedFilesList.push(declarationFilePath)` iff `dtsWritten` :116708, `.d.ts.map` unconditionally when `declarationMapPath` :116709-116711 (H2.7e asymmetry, recorded); `declarationTransform.dispose()` :116714 | the H2.7b core; the dormant Rust seam `transform_declaration_unit_for_harness` (orchestration.rs:59-127) already implements :116654-116669 |
| the write | `printSourceFileOrBundle` :116744-116804 on the declaration path: no source map (`shouldEmitSourceMaps` false) → `writer.writeLine()` :116796-116798; `text = writer.getText()`, `data = { sourceMapUrlPos: undefined, diagnostics: transform.diagnostics }`, `writeFile(host, emitterDiagnostics, path, text, !!emitBOM, sourceFiles, data)` :116799-116801 (→ `writeFile` :16644-16655 with the `Could_not_write_file_0_Colon_1` onError); `return !data.skippedDtsWrite` :116803 | callback metadata contract: `writeByteOrderMark = emitBOM`, `sourceFiles`, `data.diagnostics` = the transform diagnostics, `data.sourceMapUrlPos` undefined; `skippedDtsWrite` is a builder-host field (§9.5) |
| resolver hooks at emit time | `hasGlobalName(name)` :88396-88398 (`globals.has(escapeLeadingUnderscores(name))`) has TWO printer use sites — the shared JS printer :116624 (the active js lane) and the DECLARATION printer :116688 (H2.7b activates only this one; the frozen H2.7a inventory records both use sites); `isEmitBlocked` :123592-123594 (`hasEmitBlockingDiagnostics.has(toPath(emitFileName))`) | G5 (F16) / already-ported preflight `is_emit_blocked` |
| `emittedFiles` order | per unit js :116633-116635, js.map :116636-116638, dts :116708, dts.map :116710; build-info last :116585 | Rust records the js/map order at execute.rs:772-775; the dts entry joins after the unit's map |
| Program boundary (context) | `emit` :123568-123591, `emitWorker` :123595-123624 (`handleNoEmitOptions` :125636-125663 first; the `getEmitResolver` call :123601-123605; `getTransformers(options, customTransformers, emitOnly)` :123613); CLI `emitFilesAndReportErrors` :129412-129466 (:129432-129437 the noEmit+declaration diagnostics escalation; :129452-129457 the `TSFILE:` loop); the option diagnostic `emitDeclarationOnly` without `declaration`/`composite` :124946-124950 | H2.8d / H2.9 / H2.7c / H2.8e owners; admitted rows never take these branches |
| H2.7a-dormant plug points | `transformDeclarations` :114265-115802 → `DeclarationTransformer` (declarations/mod.rs:90-109); `getDeclarationTransformers` :115950-115955 → `get_declaration_transformers` (selection.rs:9-29); the orchestration seam → orchestration.rs:59-127 + `mount_declaration_program_sources` :16-37; `PrintRequest::Declaration` printer.rs:856 | activated, not re-ported |

## 4. Current-Rust gap matrix (validated 2026-09-03)

| gap | current evidence | disposition |
| --- | --- | --- |
| G1 — the flip seam | execute.rs:627-635 tolerates `Unsupported(Declaration)` for `declaration == Some(true)` units; the unit loop :682-861 requires a JS path (:690-692), executes js/map only, and never constructs `EmitArtifact::declaration` (artifact.rs:110-125 exists, unused) | m-2 converts "tolerate the refusal" into "execute the member" after the unit's js/map artifacts (upstream order) |
| G2 — plan-level refusals | plan.rs:208-212 refuses the declaration member; :213-217 `declarationMap` (H2.7e, stays); :221-225 `ScriptOutputMissingJavaScriptPath` fires for `emitDeclarationOnly` units (js path `None` by the already-ported arm :315-320); `EmitMode::DeclarationOnly` refusal :190-194 (H2.8d, stays) | m-2 lifts :208-212; lifts the execute.rs:112-115 refusal ONLY for `emitDeclarationOnly && getEmitDeclarations(options)` (`declaration \|\| composite`) and RETAINS a typed refusal for the standalone option until H2.7c lands its option diagnostic (:124946-124950; F3); relaxes :221-225 exactly for ADMITTED `emit_declaration_only` units; the h2-6c-m-2 §2-A masking lesson: name every predicate the lift unmasks (execute.rs:112-115, plan.rs:221-225, execute.rs:690-692) with its frozen evidence = the ADMITTED combined-option rows of the m-1 census (111 candidates today, minus any the census defers; never the nominal 111 + 1) |
| G3 — printer | `PrintRequest::Declaration` refused at printer.rs:1051-1053; `declaration_syntax` (:724, :767-772) constructed only at the dormant allowlist (`crates/checker/src/declaration_emit.rs`, h2_7a_m35_controls.rs:31); the m-4 L2 lane prints via `PrintRequest::SourceFile` with the exact upstream printerOptions block minus maps (declaration_transformer_replay_decision_equal.rs:882-894) | DECIDED HERE (F8): m-2 ACTIVATES `PrintRequest::Declaration` at the ONE production declaration site (added to the m-3.5 allowlist) with `declaration_syntax = true`, the transform hooks (`onEmitNode` = `emitNodeWithNotification`, `isEmitNotificationEnabled`, `substituteNode`) mapped through the `TransformationResult`, and the resolver-backed `hasGlobalName` handler — upstream installs all four unconditionally (:116686-116693); `PrintRequest::SourceFile` keeps its default declaration-syntax refusal (declaration_printer_reprint_contract.rs:344-358 retained) |
| G4 — orchestration completion | orchestration.rs:59-127 implements :116654-116670 (transform → diagnostics → `declBlocked` three-way short-circuit → `emitSkipped`) for the harness; missing for production: the `collectLinkedAliases` pre-pass (:116649-116653, :116716-116735), the `transformed.length === 1` assert, the print, the artifact, the write with metadata, the `emittedFiles`/`emitSkipped` composition, `dispose` | m-2 ports the remainder function-complete; the harness seam stays as the L1/L2 bridge |
| G5 — two resolver members | `hasGlobalName` (:88396-88398; the declaration printer hook :116688 — the shared JS-printer use site :116624 is the active js lane's and is NOT touched, F16) AND `collectLinkedAliases` (:55675-55727; the emit-time pre-pass :116649-116653 / the nested walker :116716-116735) are both absent from the 43-member `EmitResolver` trait and its method enum (resolver.rs:417-462, :675-1322); the checker worker `collect_linked_aliases` exists crate-private and "production-dormant until H2.7b" (declaration_emit.rs:1486-1497); the H2.7a inventory rows for both use sites carry `target_rung: H2.7b` (F6) | m-2 adds BOTH as typed members (unavailable-by-default; `UnavailableEmitResolver` refuses) with CheckerSession impls forwarding the checker workers; the pre-pass predicate is ported exactly — `emitOnly && !getEmitDeclarations \|\| noCheck \|\| emitResolverSkipsTypeChecking(emitOnly, forceDtsEmit) \|\| !canIncludeBindAndCheckDiagnostics(sf, options)` (:116650-116652, :18898) — with focused true/false-branch tests; the recursive export-assignment / export-specifier walker (:116716-116735) is specified function-complete |
| G6 — `skippedDtsWrite` ↔ the sink disposition | upstream :116803 returns `!data.skippedDtsWrite` and pushes the dts `emittedFiles` entry only when true; the only producers are builder-signature hosts (:127523/:127532). Rust ALREADY carries the observable: `EmitWriteDisposition::SkippedUnchanged` (sink.rs:5-16, "reserved for a future builder that suppresses an unchanged write") is excluded from `emittedFiles` at execute.rs:868-871 (F9) | m-2 maps the upstream callback mutation onto the existing sink disposition — the ordered declaration listing respects `SkippedUnchanged`; NO second field on the immutable text metadata; the producers stay BLD1 controls |
| G7 — path worker ledger | plan.rs:512-533 `declaration_output_path` mirrors :16580-16591 but carries no `tsc-port`/`tsc-hash`/`tsc-span` triple | m-2 adds the triple (ledger check counts it) |
| G8 — admission | `H2_7b` variant (activity.rs:38) without `h2_7b_profile()`; production constructor at compiler/src/lib.rs:834 | m-2: `h2_7b_profile()` ORs the bit onto `h2_6c_profile()`; the single call site moves; `observe_runtime_slice(H2_7b)` at the declaration member |
| G9 — acceptance runner, option floor, and the write comparator | `run_h2_6c` (h2_2c_acceptance.rs:4012-4059) is the clone base; `EmitOptionFloor::{Established, SourceMap, MapFamily}` (harness upstream_suites/execution.rs:89-93) — `apply_compiler_setting` projects `declaration`/`composite`/`isolateddeclarations` on every floor (:855-857) but DROPS `emitdeclarationonly` and `declarationdir` (:908-912). The 6c serializer stores `data.diagnostics` only as `data_diagnostics_count` (h2-6c-qualification.mjs:1408-1428) and the clone-base comparator checks path / bytes / BOM (`count_diverging_writes` :2382-2399) plus data presence / URL / count (`count_diverging_writes_with_data` :2775-2804) — it ignores artifact kind, source provenance, on-error presence, and diagnostic CONTENTS (F11) | m-2 opening commit: `run_h2_7b` + `EmitOptionFloor::DeclarationFamily` = EXACTLY `MapFamily` + `emitDeclarationOnly` (nothing silently dropped; `declarationDir`/`stripInternal`/`isolatedDeclarations` rows are DEFERRED by the m-1 census, not dropped by the floor); the m-1 machine stores the normalized FULL `data.diagnostics` (code, category, messageText, file, start, length) per write; ONE shared exact-write comparator for H2.7b compares path, callback bytes, BOM, artifact kind, ordered `sourceFiles`, on-error presence, data presence, `sourceMapUrlPos`, and every diagnostic field (the stronger helper at h2_2c_acceptance.rs:359-424 is the model) — never the count-only 6c facet |
| G10 — production path resolver | the harness `PlanDeclarationPaths` (declaration_transformer_replay_decision_equal.rs:1919-1966) is the exact production shape (`EmitOutputPaths::declaration_path`; reference target declaration → javascript → source) | m-2 lifts it into the emitter (plan-derived `DeclarationPathResolver`) |
| G11 — controls that pin dormancy: the assertion-level retained/lifted table (F10, round-2 precision) | LIFTED at m-2 (exactly these assertions, current line boundaries): the planned declaration member refusal — output_plan_contract.rs:149-152 (the `with_declaration` tuple ONLY, inside the :148-166 loop whose other tuples stay) and h2_7a_m4_controls.rs:179-189 (the `Unsupported(Declaration)` plan assertion); the printer-declaration literal h2_7a_m4_controls.rs:191-197 and `PrintRequest::Declaration` (printer_foundation_contract.rs:697-699; declaration_printer_reprint_contract.rs "Declaration stays unsupported"); the compiler-option `emitDeclarationOnly` bootstrap refusal for the COMBINED shape (`emitDeclarationOnly` with `declaration`/`composite`) — the ca control 3 gains the combined-shape admission while its standalone-shape refusal assertion STAYS (F3); ONE production `with_declaration_syntax(true)` site added to the m-3.5 allowlist (h2_7a_m35_controls.rs:31/:90-127); the H2.7a close artifact's `refusal_surfaces` for plan.rs / execute.rs / activity.rs / the printer span → `lifted-at-H2.7b` with re-frozen semantic baselines. RETAINED: h2_7a_m4_controls.rs:169-177 (the bundle-root plan refusal) and BOTH execute literal assertions :198-208 (the `include_str!` + the bundle-root refusal text :198-204; the absence of `transform_declaration_unit_for_harness` in execute :205-208 — production activates through a production entry, never the harness seam); output_plan_contract.rs:123-146 (the API `EmitMode::DeclarationOnly` / `BuilderSignature` / `BuildInfoOnly` mode loop) and :153-165 (the `declarationMap` and `buildInfo` tuples with the shared assertion); the printer `Bundle` request refusal (printer_foundation_contract.rs:690-696); `PrintRequest::SourceFile`'s default declaration-syntax refusal (declaration_printer_reprint_contract.rs:344-358); the ca controls 1-2 (H2_7a never admitted) and the standalone `emitDeclarationOnly` refusal; every non-goal option refusal (`declarationMap`, `declarationDir`, `stripInternal`, `outFile`, `noEmit`, `noCheck`, …) | m-2 edits exactly the LIFTED assertions and re-pins them; a retained assertion that turns red is a defect, never an amendment |
| G12 — the declaration-output collision preflight (F7) | the Rust shared preflight (plan.rs:399-466) already scans `[javascript_path, declaration_path]` for input overwrite and duplicate outputs (:439-443) — but the declaration arm has been unreachable (the member was refused), the JS check is not suppressed under `emitDeclarationOnly` (:125022-125024), the canonical-case key (`toFileNameLowerCase` unless `useCaseSensitiveFileNames`, :125043), the tsconfig-hint chain (:125033-125038) and the overwrite chain (:125040), the diagnostic ORDER, and the propagation into `isEmitBlocked(declarationFilePath)` → `declBlocked` (:125418-125420 → :123592 → :116669) are unqualified for the declaration artifact | `modified-requalify` at m-2 for the declaration arm ONLY (H2.7b), covering `!noEmit && !suppressOutputPathCheck` gating, JS suppression under the option, the always-on declaration check, canonical-case collisions, diagnostic ordering, and `declBlocked` propagation; the base collision matrix (js/map, outDir/rootDir) stays H2.8a |

## 5. Lane semantics — W-H2.7B study contract

Two evidence classes, both upstream-only, no hand-authored bytes:

1. **The band qualification artifact** `ratchets/h2-7b-qualification.v1.json`
   (m-1): the h2-6c machine cloned with named deltas — band predicate
   (required_slices ⊆ ACTIVE ∪ {H2.7b}), the declaration option floor
   (every selected row carries `declaration: true` or
   `emitDeclarationOnly: true`; none carries `outFile`/`outDir`/
   `declarationDir`/`declarationMap`/`composite`), the transpile census
   guard, declaration writes captured as today (callback + materialized
   bytes, BOM, `sourceFiles`, `data_present` / `data_source_map_url_pos`
   / `data_diagnostics_count`), fresh-process double observation, owner
   closure to the H2.7a inventory's option-owner rows; the effective-
   option census with the ADMITTED / DEFERRED split (§2, F2/F3); the
   normalized FULL `data.diagnostics` per write (F11). Resource contract
   (F15, schedule :169-172): the m-1 child packet freezes the worker
   count, CPU/memory/wall ceilings, the shard/checkpoint layout, the
   resume command, and the stop thresholds BEFORE observation begins.
   TO-VERIFY(m-1): admitted/deferred counts, option census over the
   conformance + project routes, in-band declaration write count, run
   cost (≤ 1,593 × 2 fresh runs).
2. **The frozen W-H2.7A evidence replayed through the ACTIVATED path**:
   the m-4 L2 byte lane (199 unblocked + 3 blocked windows, 116 eligible
   cases) re-run against production `emit_files_with_activity` instead of
   the harness bridge — "the frozen evidence replays through the dormant
   foundation at the ca rung and again at the H2.7b activation"
   (h2-7a.md §5). No H2.7a artifact re-mints.

| lane | W-H2.7B must confirm | must falsify |
| --- | --- | --- |
| `.d.ts` bytes | callback and materialized bytes exact incl. trailing newline (:116796-116798), BOM (`emitBOM`), newline kind, comment retention under `onlyPrintJsDocStyle`, no helpers | any byte divergence, BOM/newline drift |
| callback metadata | `writeByteOrderMark = !!emitBOM`, artifact kind, ordered `sourceFiles` exact, `data.diagnostics` = the transform diagnostics compared on EVERY field (code, category, messageText, file, start, length — F11), `data.sourceMapUrlPos` absent, `onError` present | any metadata field divergence |
| `emittedFiles` | order js → js.map → d.ts per unit; dts present iff written; no `.d.ts.map` in-band | order/membership drift |
| `emitSkipped` / `declBlocked` | `emitSkipped = js-side \|\| declBlocked`, `declBlocked = transform diagnostics \|\| isEmitBlocked(dts path) \|\| noEmit`, the diagnostics reach the emit result exactly (codes/messages/spans incl. TS2883 specifier strings) | any skip-state or diagnostic difference |
| declaration-only routing | `emitDeclarationOnly` WITH `getEmitDeclarations(options)` true: js/map members absent (:116381-116382), dts written, `emitSkipped` composition per :116643; the option-without-`declaration` case is H2.7c's (deferred by the m-1 census, F3) | a js write, or a wrong skip state |
| output paths + collisions | non-bundle path = source directory + `.d.ts`/`.d.mts`/`.d.cts`; `.json` sources excluded (`!isJsonFile`); the declaration-artifact collision preflight (G12: input overwrite, duplicate outputs incl. canonical-case, JS check suppressed under `emitDeclarationOnly`, `declBlocked` propagation) | wrong extension/path, JSON leakage, a missed or spurious block |
| resolver/NodeBuilder results | the traced decisions (m-2/m-3/m-4 lanes) reproduce through production: `hasGlobalName` collisions, accessibility, serialization | any decision drift vs the frozen traces |
| adjacent controls | `declaration: false`/JS-only cases unchanged; the H2.6c js/map bytes byte-identical for every declaration-bearing unit (the corpus ratchet); every non-goal option still refused | any adjacent behaviour change |

## 6. Ladder plan — thick trains

1. **h2-7b-m-1 — band census + qualification machine (evidence,
   mjs-only).** `crates/oracle/h2-7b-qualification.mjs` (the
   h2-6c-qualification clone, §5.1 deltas and nothing else structural),
   `.github/ci/contracts/h2-7b-qualification.schema.json`, registration
   (ORDER 70 → 71 after `h2-7a-close`, LADDER_ORDER, qualification.mjs +
   qualification.test.mjs 21 → 22 — restated at the m-1 Phase E from
   19 → 20: the H2.6c acceptance-wiring repair (PR #502) registered the
   two 6c pairs 19 → 21 FIRST, h2-7b-m-1.md §1 F13 — pin-index), the frozen artifact
   (the admitted rows double-observed; the deferred rows count-only with
   first owner / required slices / facets / reason) with gate-tax-3
   receipts and the frozen resource contract (F15). The owner-graph
   amendment of §1 (F1): the `ARM_SPECS` table + `owner_arms` block in
   h2-transition.mjs (per-segment exact ranges + hashes, enclosure,
   consumers, dependencies) placed below the frozen :448-467 span, the
   three outputs regenerated (owner inventory + arms; dispositions
   content unchanged; profile pins), the pin-only re-mints of the h2
   ladder, the H2.7a inventory, and the H2.7a close artifact, zero
   observation. Zero production
   crate bytes. Design gate names the exact deltas.
2. **h2-7b-m-2 — the activation flip (runtime; ONE design gate, one
   flip, first post-flip sweep, manifest freeze — the h2-6c-m-2
   shape).** Opening commit: `run_h2_7b` (the `run_h2_6c` clone:
   `validate_h2_7b_qualification`, `H2_7bExecutionInputs`, the
   `EmitOptionFloor::DeclarationFamily` floor, a 27-slice guard,
   facets incl. the positional `.d.ts` write comparison,
   `h2-7b-known-divergences.v1.json` created only by the first full
   sweep, shrink-only, owner `h2-7b-m-2-divergence-closure`). Then the
   flip commit: G1-G12 — `has_global_name` AND `collect_linked_aliases`
   on the trait (G5); the production `DeclarationPathResolver` (G10);
   the declaration member executed function-complete (the
   `collectLinkedAliases` pre-pass under its exact predicate, transform,
   diagnostics, `declBlocked`, the `transformed.length === 1` assert,
   `PrintRequest::Declaration` with `declaration_syntax` at the one
   allowlisted site + the `hasGlobalName` / notification / substitution
   hooks (G3), the write with full metadata, `emittedFiles` /
   `emitSkipped` composition respecting `SkippedUnchanged` (G6),
   `EmitArtifact::declaration`, `observe_runtime_slice(H2_7b)`); the
   collision-preflight requalification for the declaration arm (G12);
   the refusal lifts (plan.rs:208-212; execute.rs:112-115; plan.rs:221-225
   for `emit_declaration_only`); `h2_7b_profile()` + the call site (G8);
   exactly the G11 LIFTED assertions re-pinned incl. the H2.7a close
   artifact's `refusal_surfaces` → `lifted-at-H2.7b`; the ledger triple
   on the path worker (G7). First full sweep → the manifest baseline; the
   `run_h2_6c` re-sweep shrinks the 6c manifest by the in-band exact rows
   (≤133); the 5h/6a/6b queues re-run shrink-only. Corpus ratchet
   byte-identical (js/map bytes unchanged by activation).
3. **h2-7b-w* — sol-signed divergence-closure waves** (the h2-6c
   W1-W5 discipline: probe fidelity, defect-class ledger, shrink-only
   manifests, each wave a train).
4. **h2-7b-ca — acceptance wiring + close/transition.** `run_h2_7b`
   appended to `fn acceptance` (the fixed unsplit hosted boundary grows
   by exactly one call — from `run_h2_6c`, on the boundary since the
   repair PR #502; restated at the m-1 Phase E), the `h2-7b-oracle` local-CI resume phase,
   policy pins; the close record (exact/diverging/deferred totals, the
   later-dependency handoff: the 863 residual chain rows by owner, the
   24 declaration-bearing 6c rows as H2.7d 18 / H2.7e 5 / both 1, the
   m-1 deferred set); the admitted-row manifest EMPTY (F12, §7); the
   ONE-commit landing:
   `active_runtime_slices` += `H2.7b`, `inactive_runtime_slice_count`
   11 → 10, `completed_runtime_slices` 25 → 26, `runtime_admissions` /
   `executed_candidates` += the band's admitted rows (TO-VERIFY(ca)), an
   `h2_7b_*` adoption block, `next_slice` → `H2.7c` (scope
   `declaration-diagnostics-and-options`), `next_runtime_activation_slice`
   → `H2.7c`; the h2-5h-a-foundation parent assertions + its schema pins
   (25 → 26; 9,196/9,715 → the new values); the H2.7a close artifact's
   live `transition_landing` mirrors (its `runtime_contract` stays
   frozen history); no STAGE change.

Every rung passes its own design gate before implementation; rungs
bundle onto thick trains; the m-1 census may split m-2 (the SCC rule:
a split amends this ladder at the affected rung's design gate, never
mid-implementation).

## 7. Acceptance

The H2.7b close record must show:

1. The frozen band artifact: the 1,593-row candidate selector (the
   machine constants of §2 asserted at every build) split by the
   effective-option census into the ADMITTED band (double-observed,
   deterministic, every declaration write captured with full callback
   metadata) and the DEFERRED set (count-only, first owner / required
   slices / facets / reason — every H2.7c-owned row among them).
2. **Every ADMITTED row exact on every facet** (bytes, full callback
   metadata, `emittedFiles`, `emitSkipped`, diagnostics) at close; the
   shrink-only H2.7b manifest is permitted DURING the closure waves only
   and is EMPTY at ca (F12 — "exact non-bundle declaration output",
   schedule :60-66/:1570/:1692-1705). A row that cannot close is
   re-dispositioned as later-owned/deferred BEFORE execution with its
   first owner, required slices, facets, and reason, never left under the
   generic H2.7b closure owner. The first-sweep baseline is never copied
   from pre-flip data.
3. The W-H2.7A L2 lane exact through the production path; the four
   H2.7a artifacts unchanged (pins only).
4. The corpus ratchet byte-identical for js/map outputs (T0 100% FP=0);
   the 6c manifest shrunk by the in-band exact rows; the 5h/6a/6b
   queues shrink-only.
5. `H2.7b` admitted with activity observed at the declaration member;
   every H2.7c/7d/7e/8a/8c/8d/BLD1/API1 arm still a typed refusal
   (controls re-pinned, the H2.7a close artifact amended, never
   silently green).
6. Local and hosted gates green at every train head; the ca landing
   walk-converged.

## 8. Prohibitions

- No bundle/`outFile` arm, no `.d.ts.map`, no `declarationDir` /
  `stripInternal` / `isolatedDeclarations` lift, no
  `getDeclarationDiagnostics` API, no `noCheck`/transpile route, no
  `emitOnly`/`forceDtsEmit`/BuilderSignature axis, no custom
  `afterDeclarations` transformer, no `outDir`/`rootDir` lift.
- No refusal lift without a frozen-evidence band covering its
  condition, option shape, suite, and fixture family (the h2-6c-m-2
  rule); every predicate a lift unmasks is named with its band before
  the flip.
- No hand-authored `.d.ts` expectations; the band artifact and the
  frozen W-H2.7A evidence are the only expected-byte sources. No
  fixture/case-specific branches, no output substitution, no
  unknown-as-success fallback.
- No H2.7a artifact re-observation; no NodeBuilder substitution by the
  checker's display path; no partial port presented as the owner.
- No manifest baseline from pre-flip data; manifests shrink only and the
  admitted-row manifest is empty at ca.
- No option floor that silently drops a declaration-family setting; the
  deferral of later-owned effective options is a census decision, never
  a projection accident (the h2-6a-ca-2 §4-A.1 trap).
- No re-owning of the H2.8a path matrix or the H2.7d bundle paths; the
  §1 declaration-arm split is a machine amendment of the owner graph
  (the `owner_arms` block, machine-distinct from the whole-function
  roots) at m-1, never a prose disposition and never a silent merge of
  rows.
- The hosted boundary grows by exactly one call at ca; no other
  acceptance change.

## 9. Unresolved items and trace

1. Owner-row arm split (schedule "output paths" vs the H2.8a/H2.7d
   inventory rows): RESOLVED as a MACHINE amendment — the `ARM_SPECS`
   table / `owner_arms` block (enclosure, exact per-segment `ranges[]`
   each hashed, consumers, dependencies with owners; `owner_roots` stays
   50, `owner_arms` 4; the collision arm's enclosure is
   `verifyCompilerOptions`, `program-emit` a consumer) at the m-1 design
   gate (§1, F1), all three h2-transition outputs regenerated, pin-only
   re-mint cascade, zero observation.
2. `declarationDir` read inside the H2.7b path worker (:16580-16585;
   plan.rs:512-517 already mirrors it): `proven-unreachable` for the
   ADMITTED band (the m-1 census defers every `declarationDir` row; 0 in
   the candidate band today); the arm is ported as-is behind the retained
   execute.rs:141 refusal; H2.7c lifts it.
3. `emitDeclarationOnly` (plan.rs:221-225, execute.rs:112-115/:690-692;
   the outFile-arm selection :16604 is not in-band): lifted ONLY for
   `emitDeclarationOnly && getEmitDeclarations(options)`; the standalone
   option keeps a typed refusal until H2.7c (F3); the m-2 design gate
   names the unmasked predicates; the ADMITTED combined-option rows of
   the m-1 census are its frozen evidence.
4. Resolver members: `hasGlobalName` AND `collectLinkedAliases` at m-2
   (G5, F6).
5. `skippedDtsWrite`: mapped onto `EmitWriteDisposition::SkippedUnchanged`
   (G6, F9); producers stay BLD1 controls.
6. The 6c manifest's single owner value: the 157 / 133 sets are
   re-derived by the ratchet join (machine constants); the 24 outside
   rows are H2.7d 18 / H2.7e 5 / both 1 (F4); the 67-row set is the W5
   path-joined subset, re-derived from that relation only if a live gate
   needs it (F5); the shrink happens through the shrink-only `run_h2_6c`
   re-sweep.
7. `EmitOptionFloor::DeclarationFamily` = exactly `MapFamily` +
   `emitDeclarationOnly` (G9, F2/F11); the exact-write comparator with
   full diagnostics: m-2 opening commit.
8. `h2_7b_profile()`: m-2 (G8).
9. The declaration-output collision preflight (G12, F7): m-2.
10. TO-VERIFY(m-1): the effective-option census over conformance (476)
    and project (196) routes, admitted/deferred counts, declaration write
    count, the resource contract values. TO-VERIFY(ca): the exact
    transition values.

Evidence read: the H2.7b fact sheet (target/session-notes/7b/
h2-7b-factsheet.md, 2026-09-03) and the sol round-1 review, the schedule
§4.5 rows and house rules, h2-transition.mjs rows :143/:173-:185/
:412-:512/:693-:696, the dispositions, 6c-manifest, and
test-suite-expansion scans (band 2,456 / 1,593 / 863; the 24-row
18/5/1 decomposition; the compiler-row effective-option census 21/2/1/1
+ 10 explicit-false), _tsc.js anchors listed in §3, the Rust seams
listed in §4 (incl. resolver.rs:417-462, declaration_emit.rs:1486-1497,
sink.rs:5-16, execute.rs:862-884, h2-6c-qualification.mjs:1408-1428,
h2_2c_acceptance.rs:2382-2399/:2775-2804), the precedent packets
h2-6c-m-2 / h2-6a-ca-1 / h2-6a-ca-2 / h2-6c-ca / h2-6c-w5 and their merge
stats; no oracle or qualification machine ran.

## 10. Cross-review record

Round 1 (2026-09-03): operator draft rev 1 → sol **REVISE** (13
blocking + 2 advisory). Verified without finding: the band arithmetic
(2,456 / 1,593 / 863 / 134 / 1,481+111+1 / 433 / 157 / 133), the
option-vs-API declaration-only distinction, the two-class evidence
concept, the passenger timing. Dispositions, every finding INCORPORATED
in rev 2: F1 the arm split as a machine `ROOT_SPECS` amendment at m-1
(§1, §6, §8, §9); F2 the candidate band is a selector — m-1 effective-
option census + admitted/deferred split (operator-reproduced over the
921 compiler rows: isolatedDeclarations 21, noEmitOnError 2,
stripInternal 1, standalone emitDeclarationOnly 1, declarationMap:false
10) (§2, §5, §7); F3 the standalone `emitDeclarationOnly` row → H2.7c
and the declaration-only contract gated on `getEmitDeclarations` (§1,
§2, §5); F4 the 24 rows = H2.7d 18 / H2.7e 5 / both 1 (operator-
reproduced) (§2, §6); F5 the 67-row set as the W5 path-joined subset
(§2, §9); F6 `collectLinkedAliases` on the trait with the exact pre-pass
predicate (G5); F7 the collision preflight as G12 + a §3 row; F8
`PrintRequest::Declaration` + hooks decided (G3); F9 `skippedDtsWrite`
↔ `SkippedUnchanged` (G6); F10 the assertion-level retained/lifted
table (G11); F11 full-diagnostics capture + one exact-write comparator
(G9, §5); F12 admitted rows exact and the manifest empty at ca (§7,
§8); F13 the non-goal owner map (§1); F14 anchors refreshed
(h2-transition rows :173/:174/:175/:180/:182/:185, blockers
:446/:448/:454/:457, sort :512, `getSourceFilesToEmit` :16600/:16604);
F15 the m-1 resource contract (§5, §6).

Round 2 (2026-09-03): rev 2 → sol **REVISE** — F2, F4-F9, F11-F13, F15
RESOLVED; F1 PARTIAL (the whole-function root model, `owner_roots ===
50`, `selectedByDeclaration`, and the three regenerated outputs
required a machine-distinct arm row model), F3 PARTIAL (the
`emitDeclarationOnly` lift had to be conditional on
`getEmitDeclarations`, with the standalone refusal retained and G2's
evidence the admitted count), F10 PARTIAL (the execute literal
assertions and the plan-contract span had to stay retained /
narrowed), F14 ADVISORY (three anchors + the selector wording).
Rev 3 incorporates all four: §1 mechanics (the `ARM_SPECS` /
`owner_arms` model, `owner_roots` 50 + `owner_arms` 4, the three
outputs regenerated, the pin-only cone), G2 / G11 / §9.3 (the
conditional lift and the assertion-level table with exact spans:
LIFTED h2_7a_m4_controls.rs:181-192 + :194-201, output_plan_contract.rs
:148-151; RETAINED :171-179 + :202-210, :123-147 + :152-157), and the
anchors (`:16604`, blockers :448/:454/:457, sort :512, "candidate
band" / "ADMITTED" wording).

Round 3 (2026-09-03): rev 3 → sol **REVISE** — F3 and F14 RESOLVED; F1
PARTIAL (a continuous arm range still swallowed later-owned lines —
`declarationMap` :116384, `outFile` :116648 — and the collision arm's
structural parent was wrong: `program-emit` ends at :123591 while the
check lives inside `verifyCompilerOptions` :124750 with
`blockEmittingOfFile` :125418-125420 a separate sibling), F10 PARTIAL
(the lifted printer span :194-201 overlapped the execute literal at
:201; the retained execute span omitted its setup :198-201; the plan
contract's build-info tuple runs through :160). Rev 4 incorporates
both: per-segment `ranges[]` each hashed with later-owned lines
excluded and modeled as dependencies, `enclosure` decoupled from the
frozen roots (`verifyCompilerOptions` for the collision arm,
`program-emit` a consumer), and the exact current boundaries —
LIFTED h2_7a_m4_controls.rs:179-189 + :191-197, output_plan_contract.rs
:149-152; RETAINED :169-177 + :198-208, :123-146 + :153-165.

Round 4 (2026-09-03): rev 4 → sol **REVISE** — F10 RESOLVED (the spans
exactly partition the current assertions); F1 PARTIAL (four authoring
ranges still mis-anchored or over-inclusive: the path call is :116383
alone, `collectLinkedAliases` ends at :116735, the `writeBundle` branch
:116759 is H2.7d, the collision arm's H2.7b-specific statements are
:125022-125025 only). Rev 5 pins every segment line-by-line from the
vendored bytes, adds the `shared_lines[]` rule for co-located lines
(:116381, :116385, the print/write mechanics of
`printSourceFileOrBundle`), and records every excluded line and callee
as a dependency with its owner.

Round 5 (2026-09-03): rev 5 → sol **REVISE** — F1 PARTIAL: the four
round-4 boundaries confirmed correct, but `emitFiles` spans
:116530-116858 (so `printSourceFileOrBundle` is nested, not a sibling),
several declaration-emit lines co-locate later-owned predicates
(`emitOnly` :116641/:116643/:116650, the bundle selection
:116646/:116648, `forceDtsEmit` :116647/:116671, `noEmit` :116669, the
declaration-map handoff :116696/:116700-116702), `:116764` is shared
(the map dependency starts at :116765), and the collision wrapper
:125022-125024 is shared with :125025 exclusive; §3/G4/G5 still cited
:116716-116743. Rev 6 incorporates all of it: exclusive `ranges[]`
reduced to purely H2.7b lines, every mixed line moved to
`shared_lines[]` with each co-owner named, the `emitFiles` extent and
nesting corrected, and every `collectLinkedAliases` citation set to
:116716-116735 (the remaining :116736-116743 mentions name
`markLinkedReferences` as the JS lane's).

Round 6 (2026-09-03): rev 6 → sol **REVISE** — F1 PARTIAL only on
downstream prose anchors: the §1 `ranges[]` / `shared_lines[]` partition
itself checked out (:116764 vs :116765, :125022-125024 shared /
:125025 exclusive, `collectLinkedAliases` :116716-116735), but §3, G4,
G12, and the non-goal map still carried pre-verification anchors. Rev 7
refreshes them from the vendored bytes: `emitFiles` :116530-116858
(nested `printSourceFileOrBundle`), the collision call :125025 and the
verifier :125028-125050 (the wrapper :125022-125024, the canonical key
:125043, the hint chain :125033-125038, the overwrite chain :125040),
`declBlocked` :116669, `emitSkipped` :116670, the bypass :116671, the
assertion :116672, the printer options :116673-116685, the `dtsWritten`
call :116694-116706, the `.d.ts` push :116708, and the declaration-map
handoff :116696 / :116700-116702 (replacing the stray :116705).

Round 7 (2026-09-03): rev 7 → sol **REVISE** — F1 PARTIAL on three
repeated anchors (the non-bundle JS suppression is :116381, the no-map
branch :116796-116798, the `.d.ts` push :116708) and F16 BLOCKING (the
survey said `hasGlobalName` is wired into the declaration printer only;
upstream also installs it in the shared JS printer :116624 — the frozen
H2.7a inventory records both use sites). Rev 8 incorporates both: the
anchors refreshed at every repetition (§1, §3, §5) and the two
`hasGlobalName` use sites named with only :116688 activated by H2.7b
(§3, G5).

Round 8 (2026-09-03): rev 8 → sol **REVISE** — F1 and F16 RESOLVED;
F17 BLOCKING: five Program/API anchors in the §1 non-goals and the §3
Program-boundary row were stale (`getDeclarationDiagnostics` is the
Program API exported at :122935 and wrapped at :123674-123676;
`emitWorker` :123595-123624 with the `getEmitResolver` call
:123601-123605 and `getTransformers` :123613; the `TSFILE:` loop
:129452-129457). Rev 9 replaces exactly those five anchors.

Round 9 (2026-09-03): rev 9 → sol **AGREE** — F17 RESOLVED (all five
anchors match upstream; the rev 8 → 9 diff changed exactly those), no
new findings. This revision is the H2.7b-era design authority; it
boards the `h2/7a-ca` train as a design-only passenger.
