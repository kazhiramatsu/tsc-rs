# H2.7a / m-3 — NodeBuilder foundation (dormant): rung packet

Status: revision 4 **RATIFIED — design authority** (2026-09-01),
operator gate closure after sol rounds 1-3 (§16: round 3 returned
exactly ONE supplied-value mechanical consistency fix + one
advisory count, zero design objections — both applied verbatim;
every substantive element verified across the three rounds; closure
under the 2026-08-31 user calibration directive, the m-2 round-4
precedent). Third rung of the [h2-7a.md](h2-7a.md) §6 ladder, authorized
by the m-2 partition amendment ([h2-7a-m-2.md](h2-7a-m-2.md) §2,
RATIFIED 2026-08-31). **Two-phase structure** (the m-2 template):
Phase E (docs + h2-7a.md S3-authorization amendment + S3 evidence
supplement + mint + register) precedes any `crates/**/*.rs`
production edit; Phase P proceeds under the §8.E4 verification rule.
Corpus-inert both phases. SELF-CONTAINED: no content is delegated to
earlier revisions.

## 1. Identity, boundary, and authority

- **Slice ID / kind:** h2-7a-m-3, kind `foundation` (dormant).
- **Trusted base:** `main` @ d592fcad (PR #497, the m-2 landing).
- **Purpose:** land the seven NodeBuilder-backed resolver members
  (trait + typed results + implementations), the error-name
  entity-building machinery of the m-2 §2c obligation, and the
  createNodeBuilder + syntactic-type-node-builder ports (with their
  module-specifier synthesis closure) as dormant, function-complete
  checker foundation behind a consumer seam — contract-proven
  decision-equal against the frozen W-H2.7A probe traces.
- **Non-goals:** printer/factory type-specific constructor FACES and
  type-node parenthesizer RULES, `symbolToString` and error-name
  BYTE exactness (all m-3.5 — the m-2 §2c byte gate), declaration
  transformer / orchestration / selection seam (m-4), output
  activation (H2.7b+), display-path behavior change, `hasGlobalName`
  (H2.7b).
- **Activation state:** before = after = dormant/unreachable
  (plan.rs:208, execute.rs:629, printer.rs:925 byte-identical; no
  production caller of any new member exists until m-4/H2.7b).
  Next owner: h2-7a-m-3.5.
- **Authority artifacts (trusted-base values, verified 2026-09-01;
  round-1 F2 + round-2 F9 — the COMPLETE tuple):** the STABLE
  observation identity is the case-manifest fingerprint
  `89bb0627cee58b5d12aeb6fd5e95a92d26e1bbb54fd592750b49a34b64a89efb`
  + witness observation-content roll
  `091cea9c5dd7a2c60a551d9292cd19832f9b71c77a28508eeae7252ccf556312`
  + probe trace-content roll `dcf1243f5b8f3187…` + the
  probe→witness file-sha pair (probe pins raw witness file sha
  `ec2823c890d772e082ff753fdb61a73079000546e3a52f585008b28001f8598d`;
  probe raw file sha
  `d459898a9d467efbcc2211d29df97b35e658337f514c69bcddc9faef8fd84265`)
  — the m-2 register disposition-7 rule. Whole-file embedded
  fingerprints at the trusted base — witnesses `84e478a1d31d743f…`,
  probe `abbb443aa5271a60…` (walk-re-minted at the m-2 P6 head; the
  E4 register's `5f669ada…`/`34a0e69d…` are earlier E-phase
  history) — are register-recorded succession values, not harness
  assertions; they become historical again at the S3 mint (§7.8).
  - `ratchets/h2-7a-owner-inventory.v1.json` fingerprint
    `ab284d5a0a69d3f8115f4f8cf8aefd3ebf5e55f73fc22ae3503ab01c3b5ed3bc`
    (m-2 P6-head re-mint; partition projection byte-identical to the
    7bd5… v1 authority under the m-2 §2d guard). It re-mints again
    at THIS rung's P-head walk (`inputs.rust_evidence` hashes
    resolver.rs and factory.rs, both edited here); the
    partition-projection focused test bridges the succession.
  - Vendored `_tsc.js` SHA-256
    `1c59e77a54b186ec43fa7f3e0d3c4bb15ca5eb5ba43e96b1d3a267139eddd3e3`.

## 2. Scope ratification (design-gate body)

**(a) Rung-shape decision — recorded, not amended.** h2-7a.md §6.3
delegates the m-3 internal split and the syntactic-builder rung
decision to the m-1 inventory. The frozen inventory answers: ALL 149
node-builder rows and ALL 54 syntactic-builder rows carry
`target_rung = h2-7a-m-3` (with the 7 member + 7 use-site
resolver-subset rows); no m-3a/m-3b rung exists. This packet RECORDS
that machine decision; the rung shape needs no amendment. The
syntactic builder is structurally inseparable anyway: the three
serialize* API members delegate to it as the front door
(:50980/:50991/:51002).

**(b) S3 authorization — a parent-packet amendment element (round-1
F1).** S3 is a second post-observation supplement — an exception to
the m-1 freeze that the parent packet must authorize, exactly as
h2-7a.md's m-2 amendment element (4) authorized S2. Phase E1
therefore lands ONE narrow h2-7a.md amendment carrying exactly: (1)
the S3 output-informed supplement authorization (targets, restricted
use, this packet as the mechanics authority), and (2) the §9.4
closure note recording the inventory-decided rung shape. No ladder,
scope, or partition text changes. The witness/probe artifacts pin
h2-7a.md (`inputs.parent_packet`) and re-mint at E3 regardless (the
S3 generator edit already misses every receipt key), so the
amendment adds no extra mint cost.

**(c) The seven members (m-3 HEAD; the m-2 §2a deferred partition).**
createTypeOfDeclaration (:88359-88366),
createReturnTypeOfSignatureDeclaration (:88382-88388),
createTypeOfExpression (:88389-88395), createLiteralConstValue
(:88506-88509, helper literalTypeToNode :88491-88505),
getDeclarationStatementsForSourceFile (:88612-88621),
createLateBoundIndexSignatures (:88624-88691), symbolToDeclarations
(:88692-88694, delegating to the NodeBuilder member :51136-51164).
Transformer use sites (inventory rows): :114437, :114676, :114701,
:114703, :115425, :115610, :115626. **Flag words (round-1 F5,
exact):** FIVE transformer-driven members receive the pinned
constants `declarationEmitNodeBuilderFlags` (:114263 =
1024|2048|4096|8|524288|4|1) and
`declarationEmitInternalNodeBuilderFlags` (:114264 = 8) —
createTypeOfDeclaration, createReturnTypeOfSignatureDeclaration,
createTypeOfExpression, getDeclarationStatementsForSourceFile,
createLateBoundIndexSignatures — with exactly one site (:115425,
the fakespace path) adding `| 2 /* NoSyntacticPrinter */` to the
internal word; the THREE serialization member wrappers additionally
compose `flags | 1024 /* MultilineObjectLiterals */`
(:88365/:88387/:88394); `createLiteralConstValue` receives NO flag
words — its upstream signature is `(node, tracker)` (:88506,
call :114676).

**(d) Error-name entity machinery (m-2 §2c obligation).**
m-3 lands symbolToNode (:51122-51135), symbolToExpression
(:53337-53387), symbolToEntityNameNode (:53110-53113) and their
chain/lookup cluster as NodeBuilder members — the entity-building
half of upstream `symbolToString` (:50649-50681). `symbolToString`
itself is printer-backed (:50671-50679) and stays m-3.5 with the
BYTE gate; the m-2 SHADOW wiring of errorSymbolName/errorModuleName
(display-slice source, merge.rs:391-399) is byte-UNTOUCHED this
rung. No display-slice substitution is presented as the owner.

**(e) S3 coverage supplement — output-informed, restricted use.**
This gate (with the §2b parent authorization) ratifies ONE
supplement stratum for the two members with ZERO schema-2 trace
volume: createTypeOfExpression and createLiteralConstValue (the
corpus recorded 612 isLiteralConstDeclaration calls without one
true-with-initializer site, :114667-114679). Its only
observation-derived input is the frozen schema-2 per-site volume
table (§7.1); candidate bytes/decisions are never consulted.
Restricted use: S3 cases feed only the m-3 focused contracts and
later replays; they join no m-1 lane-coverage, quota, family, or
determinism claim; every m-1 and S2 claim stands as signed (§7.7
projection guards). Complete frozen contract in §7.

**(f) Evidence usage constraint honored.** The m-2 E4 register's
use constraint binds this gate: the v1→schema-2 migration-proof
equality is NOT cited for any output-node claim. m-3's gating
evidence is the §6 decision-lane contract; output-node BYTE shape
gates at m-3.5 (printer faces, parenthesizer) and at the ca
byte-equal witness replay, never here.

## 3. Pinned upstream map

Member rows and every ported function: spans + sha256 are the frozen
inventory rows (fingerprint §1; machine authority — the m-1 §1.4
rule). Cluster anchors for the port plan (roster-verified against
the inventory rows; full per-lane rosters ride the implementation
SPECs):

| # | cluster | anchor span | content |
| - | --- | --- | --- |
| 1 | syntacticBuilderResolver seam | :50778-50956 | the **26-member** checker-supplied callback object (round-1 F7): evaluateEntityNameExpression :50779, isExpandoFunctionDeclaration :50780, hasLateBindableName :50781, shouldRemoveDeclaration :50782, createRecoveryBoundary :50785, isDefinitelyReferenceToGlobalSymbolObject :50788, getAllAccessorDeclarations :50789, requiresAddingImplicitUndefined :50790, isOptionalParameter :50806, isUndefinedIdentifierExpression :50807, isEntityNameVisible :50810, serializeExistingTypeNode :50813, serializeReturnTypeForSignature :50816, serializeTypeOfExpression :50823, serializeTypeOfDeclaration :50828, serializeNameOfParameter :50842, serializeEntityName :50845, serializeTypeName :50856, getJsDocPropertyOverride :50859, enterNewScope :50866, markNodeReuse :50881, trackExistingEntityName :50884, trackComputedName :50887, getModuleSpecifierOverride :50890, canReuseTypeNode :50929, canReuseTypeNodeAnnotation :50932. Five members re-expose m-2 checker-native emit workers (isExpandoFunctionDeclaration, isDefinitelyReferenceToGlobalSymbolObject, isOptionalParameter, requiresAddingImplicitUndefined, isEntityNameVisible) — the Rust seam routes them to the SAME declaration_emit.rs workers |
| 2 | NodeBuilder API surface | :50957-51204 | typeToTypeNode, typePredicateToTypePredicateNode, serializeTypeForDeclaration/ReturnTypeForSignature/TypeForExpression (withContext + syntactic front door :50980/:50991/:51002), indexInfoToIndexSignatureDeclaration, symbolToNode :51122, symbolToDeclarations :51136-51164, simplifyClassDeclaration/Modifiers/InterfaceDeclaration :51165-51194, symbolToDeclarationsWorker :51195-51204 |
| 3 | withContext / context nucleus | :51205-51256 (inventory row) | context construction (flags/internalFlags/maxTruncationLength/maxExpansionDepth/approximateLength/typeStack/out), SymbolTrackerImpl installation, truncation-vs-NoTruncation reportTruncationError arm, out copy, encounteredError→undefined |
| 4 | context/infra helpers | :51257-51310 | addSymbolTypeToContext/restore, saveRestoreFlags, checkTruncationLength(IfExpanding), canPossiblyExpandType, shouldExpandType (9 rows, 118 lines) |
| 5 | type-node dispatch | :51311-52503 | typeToTypeNodeHelper :51311-51318 + typeToTypeNodeWorker :51319-52211 (893 lines) + reference/mapped/anonymous/resolved-type workers (27 rows, 1,853 lines) |
| 6 | signatures/scopes | :52504-52938 | signatureToSignatureDeclarationHelper :52504+, enterNewScope :52692+, createRecoveryBoundary :52612+, typeParameter/parameter workers (roster in SPEC) |
| 7 | chains/entity | :52939-53486 | lookupSymbolChain(Worker) :52939-53017, symbolToEntityNameNode :53110, symbolToTypeNode :53114-53252, symbolToExpression :53337-53387, getSpecifierForModuleSymbol :53060-53109 (§3a below) |
| 8 | declaration serialization | :53487-53721 | serializeTypeForDeclaration :53487-53508 (accessor arm :53495, syntactic consult :53497), serializeReturnTypeForSignature :53524-53546 (consult :53534), existing-type-node reuse tail :53715 |
| 9 | symbolTableToDeclarationStatements | :53722-55438 (inventory row) + tail helpers :55439-55450 (isExpanding, isHashPrivate, getClonedHashPrivateName) | the JS-synthesis owner (1,717 lines): symbol-table walk, per-class declaration synthesis, merge/alias handling, serializeAsAlias :54707+, serializeSymbolWorker :53992+, makeSerializePropertySymbol :55099+ |
| 10 | SymbolTrackerImpl | `var SymbolTrackerImpl = class` :90969-91068 (100 lines), sha256 e5de2244865bcd5f14f7d0e16b3f308b63a98cfa2c624b917288ec7d5aaf8b86 | OUTSIDE the NodeBuilder span and OUTSIDE the inventory FunctionDeclaration grammar (reached via `new`, round-1 F8) — this packet pins it directly: inner-unwrap loop, canTrackSymbol, disableTrackSymbol gate, trackSymbol true→onDiagnosticReported / false+non-TypeParameter→context.trackedSymbols push, per-report onDiagnosticReported, suppressReportInferenceFallback gate, push/popErrorFallbackNode passthrough |
| 11 | syntactic builder module | :133276-134447 | createSyntacticTypeNodeBuilder: 54 rows; API serializeTypeOfDeclaration :133753-133785, serializeReturnTypeForSignature :133807-133829, serializeTypeOfExpression, serializeTypeOfAccessor, tryReuseExistingTypeNode; fallback entries inferTypeOfDeclaration :133943, inferReturnTypeOfSignatureSignature :133962 (reportFallback flag); annotation-reuse visitor with recovery boundaries; `strictNullChecks` captured at creation |
| 12 | member wrappers + literalTypeToNode | :88359-88395, :88491-88509, :88612-88694 | §2c spans; getParseTreeNode filters (hasInferredType / isFunctionLike / isExpression), AnyKeyword-token fallbacks, locals-vs-exports arm + resolveExternalModuleSymbol, index-info composition + static-modifier recomposition + trackComputedName |
| 13 | transformer symbolTracker (CONTEXT — m-4 consumer, protocol pinned here) | :114280-114304 (object) + :114317-114331 reportExpandoFunctionErrors + :114360-114369 trackSymbol worker | the callback protocol §5.3 replays: trackSymbol calls `resolver.isSymbolAccessible(sym, enclosing, meaning, true)` and returns the issued-diagnostic boolean; reportInferenceFallback re-enters isExpandoFunctionDeclaration + getPropertiesOfContainerFunction; reportLikelyUnsafeImportRequiredError carries `(specifier, symbolName)` (round-1 F8) |
| 14 | reached closure (non-surface) | inventory reached rows | node-builder reaches 185 helpers (162 shared/core-utility, 12 shared/core, 3 shared/path, 8 shared/emit-factory: setParent/setOriginalNode/setEmitFlags/setCommentRange/setSyntheticLeadingComments/addSyntheticLeading+TrailingComment/setTextRange); syntactic-builder reaches 85. The closure is ONE-HOP by construction — §3a records the one transitive interior it hides |

**(3a) Module-specifier synthesis closure (round-1 F9 — measured,
in scope).** `getSpecifierForModuleSymbol` :53060-53109 (inventory
row, m-3) consults, in order: the no-host/ambient arm (escapedName
quote-strip or getNonAugmentationDeclaration fileName), the
enclosing-declaration specifier reuse
(`canHaveModuleSpecifier` :15203 + `tryGetModuleSpecifierFromDeclaration`
:15220), the per-symbol mode-aware specifier cache, and the FULL
computation arm `getModuleSpecifiers` :45447-45459 →
`getModuleSpecifiersWithCacheInfo` :45460 →
`computeModuleSpecifiers` :45493, inside the moduleSpecifiers
module :45368-46289 (round-2 F3 span correction: the module opens
at the `// src/compiler/moduleSpecifiers.ts` banner :45368 and the
checker banner resumes :46290; the closure includes the pre-entry
helpers the machinery calls directly — `getModuleSpecifierPreferences`
and `tryGetModuleSpecifiersFromCacheWorker` with its optional
host-cache consult :45443). The reached closure's one-hop rule
left that interior un-rowed; it is IN m-3 scope because the frozen
corpus REACHES it: four witness cases carry import-type syntax in
their `.d.ts` bytes (h2-7a/F1/alias-from-indirect-file ×3,
h2-7a/F1/alias-inlining ×5, h2-7a/F5/js-functions ×2,
h2-7a/F8/grammar-second ×1 — operator-measured from the frozen
observation payloads 2026-09-01, sol round-2 re-verified).
**Computation-arm reachability proof (round-2 F3):** for those
serializations the enclosing declaration fails
`canHaveModuleSpecifier` (no originalModuleSpecifier), the pinned
CLI emit host (:123482-123521) supplies `getSymlinkCache` and
`getModuleResolutionCache` but NOT `getModuleSpecifierCache` — so
`tryGetModuleSpecifiersFromCacheWorker` finds no host cache — and
the per-symbol `links.specifierCache` starts empty, so the FIRST
import-type serialization per (symbol, mode) necessarily ran
`computeModuleSpecifiers`. The host protocol is the **14-member**
`createBasicNodeBuilderModuleSpecifierResolutionHost` shape
:90948-90968 (getCommonSourceDirectory, getCurrentDirectory,
getSymlinkCache, getPackageJsonInfoCache,
useCaseSensitiveFileNames, redirectTargetsMap,
getRedirectFromSourceFile, isSourceOfProjectReferenceRedirect,
fileExists, getFileIncludeReasons, readFile,
getDefaultResolutionModeForFile, getModeForResolutionAtIndex,
getGlobalTypingsCacheLocation), PLUS the optional tracker-host
capabilities the machinery consults when present:
`getModuleSpecifierCache` (:45443), `getModuleResolutionCache`,
`getNearestAncestorDirectoryWithPackageJson` — each modeled as a
typed optional member. Port discipline: function-complete over the
reachable closure; capabilities the pinned CLI host PROVIDES
(symlink cache, module-resolution cache — empty for the VFS corpus
is a legitimate FACT) get program-fact-backed implementations;
capabilities it does NOT provide (getModuleSpecifierCache) stay
absent in Rust exactly as upstream; genuinely unbacked members
(package-json info cache, global typings cache,
nearest-package-json ancestor) answer their TYPED absent/empty
forms — each a named host-fact row in the ledger — and any arm
whose behavior would silently depend on an absent capability fails
closed instead of guessing. The display slice's
`specifier_for_module_symbol_slice` (check.rs:8379-area) is the
no-host arm's already-exact reuse anchor; every other arm is new
emit-channel work.

## 4. Rust semantic map

| upstream object/state | Rust design | notes |
| --- | --- | --- |
| NodeBuilderFlags word | `EmitNodeBuilderFlags` (u32 newtype, resolver.rs) — upstream numerics verbatim; pinned constants incl. NO_TRUNCATION=1, GENERATE_NAMES_FOR_SHADOWED_TYPE_PARAMS=4, USE_STRUCTURAL_FALLBACK=8, MULTILINE_OBJECT_LITERALS=1024, WRITE_CLASS_EXPRESSION_AS_TYPE_LITERAL=2048, USE_TYPE_OF_FUNCTION=4096, ALLOW_EMPTY_TUPLE=524288, DECLARATION_EMIT (:114263 composition) | full constant table from the vendored inline numerics |
| InternalNodeBuilderFlags word | `EmitInternalNodeBuilderFlags` (u32 newtype): WRITE_COMPUTED_PROPS=1, NO_SYNTACTIC_PRINTER=2, ALLOW_UNRESOLVED_NAMES=8, DECLARATION_EMIT=8 (:114264) | |
| caller-supplied tracker | `trait EmitSymbolTracker` — §5.3 exact protocol with the re-entrant access handle (round-1 F11) | replaces rev-1's lossy sketch |
| module-specifier host | `trait EmitModuleSpecifierHost` — the §3a 14-member basic protocol PLUS the three typed optional capabilities (getModuleSpecifierCache :45443, getModuleResolutionCache :45719, getNearestAncestorDirectoryWithPackageJson :45655); `Option<&dyn …>` on the tracker; absent → the :90948 basic-host fallback port over checker/program facts | corpus-REACHED (§3a); host-fact rows per the §3a residue split |
| NodeBuilderContext | `struct NodeBuilderContext<'…>` (checker node_builder module): the COMPLETE :51209-51245 field set (round-2 F6) — flags, internal_flags, enclosing_declaration, enclosing_file, tracker slot, max_truncation_length, max_expansion_depth, encountered_error, suppress_report_inference_fallback, reported_diagnostic, visited_types, symbol_depth, infer_type_parameters, approximate_length, tracked_symbols, **bundled** (outFile ∧ external-module enclosing — exercised by the S3 outFile fixture), truncating, used_symbol_names, remapped_symbol_names, **remapped_symbol_references**, reverse_mapped_stack, **must_create_type_parameter_symbol_list / type_parameter_symbol_list / must_create_type_parameters_names_lookups / type_parameter_names / type_parameter_names_by_text / type_parameter_names_by_text_next_name_count** (the copy-on-write name-table sextet), enclosing_symbol_types, mapper, depth, type_stack, out{can_increase_expansion_depth, truncated} — PLUS the SYNTACTIC builder's own dynamic context state: `no_inference_fallback` with its save/restore discipline (:133947/:134126) and the recovery-boundary state. Focused state-restoration tests are P2 deliverables | an EXPLICIT per-call struct. REJECTED alternative (recorded): reusing the display slice's CheckerState-flattened `slice_*` context fields (state.rs:364-433) — that would couple the emit and display contracts h2-7a.md G2 keeps distinct; the display fields stay untouched |
| synthesized results | `TransformNode` allocated into a caller-supplied `TransformArena` source slot; **trait members take `arena: &mut TransformArena` + `target: TransformSourceId`** (round-2 F4 resolution) — checker code creates TRANSIENT `NodeFactory` views via seam (1) for creation/parenthesizer-bearing operations, and calls the arena's EXISTING pub provenance/metadata surface directly between views: `set_original_node` :529 (pub), `metadata_mut` :437 (pub — emit flags, comment ranges, synthetic comments), `NodeFactory::set_text_range` :2255 (pub), `clone_node` :1361 (pub) | provenance discipline §5.6; no metadata façade is needed — the arena surface is already public |
| emitter seam (round-1 F10 + round-2 F4/F5) | THREE enumerated ADDITIVE factory.rs edits, no existing-face change: (1) pub NodeFactory construction over a caller-owned `TransformArena` (NodeFactory::new is pub(crate) :1178; TransformationContext::new private), (2) a validated pub parse-tree PROJECTION `EmitResolverNode → TransformNode` on TransformArena (inverse of parse_tree_resolver_node :275; TransformNode::new is pub(crate) :105) so reuse clones and different-kind provenance (fresh AnyKeyword/string-literal nodes with originals — created via create_node/create_token then arena.set_original_node) can anchor originals, (3) admission of kind-only TYPE tokens in `create_token` — `SyntaxKind::ThisType` (198) exceeds LastToken and `create_node` rejects `NodeData::Token`, so ThisType is today unconstructible. PLUS `crates/emitter/src/lib.rs` re-export-list additions for the new public types (round-2 F5 — the resolver module is private with an explicit re-export list :16/:23/:73-78). **setParent discipline (round-2 F4):** the Rust arena models no synthetic parent pointers; upstream `setParent` (:19057, reached row) dependence is THREADED EXPLICITLY through the Rust workers (house rule), with a named escape — a checker-side synthetic-parent map — if a P3 lane finds a dynamic-lookup consumer; each upstream setParent use site's downstream reads are owned by the consuming worker's SPEC | factory.rs + lib.rs join the fence for EXACTLY these additive seams; type-specific constructor faces + parenthesizer rules stay m-3.5. factory.rs is an inventory rust_evidence pin — the P-head re-mint absorbs it (§1) |
| trait result types | three serialize members `Option<TransformNode>`; getDeclarationStatements/createLateBoundIndexSignatures per upstream nullability (`Option<Vec<TransformNode>>` distinguishing undefined vs arrays; empty-locals `[]` is `Some(vec![])`); createLiteralConstValue `TransformNode`; symbolToDeclarations `Vec<TransformNode>` + `EmitSymbolExpansionOut { can_increase_expansion_depth: bool, truncated: bool }` | encounteredError→None (:51253) |
| error contract (round-1 F6 + round-2 F1) | NEW `EmitResolverError` variants: `UnavailableForSymbol { method, symbol }` (symbol-scoped members have no node argument) and `Factory { method, error: Box<TransformError> }` — BOXED because `TransformError::Resolver` already wraps `EmitResolverError` (transform.rs:1015/:1103) and the unboxed form is an infinite-size cycle; every factory/arena operation inside the seven members maps its TransformError losslessly; no panic path | existing node-scoped variants unchanged |
| syntactic-builder seam | checker-internal boundary mirroring the 26-member §3.1 object; the five m-2 re-exposures route to the existing declaration_emit.rs workers; the front-door boundary observable per §6.2 | Rust structure free (house rule); boundary events pinned |
| replay observation | the EXISTING checker-internal harness seam `CheckerState::with_declaration_emit_replay_observer_for_harness` (declaration_emit.rs:1657) EXTENDS to the serialization domain: request/report grow; the replay module constructs the TransformArena/factory itself (checker depends on tsc-emitter) and records NodeBuilder outcomes via crate-internal access | round-1 F14 adopted — NO second public observer seam |

## 5. Trait and implementation design (self-contained)

1. **Signatures** (all `Result<_, EmitResolverError>`; fail-closed
   defaults — node-scoped members default to `Unavailable`,
   symbol-scoped to `UnavailableForSymbol`; `EmitResolverMethod`
   33→40 + name()):
   - `create_type_of_declaration(arena: &mut TransformArena,
     target: TransformSourceId, declaration: EmitResolverNode,
     enclosing_declaration: EmitResolverNode, flags:
     EmitNodeBuilderFlags, internal_flags:
     EmitInternalNodeBuilderFlags, tracker: &mut dyn
     EmitSymbolTracker) -> Option<TransformNode>` — the
     getParseTreeNode(hasInferredType) filter and AnyKeyword-token
     fallback are checker-side, upstream-exact (round-2 F4: members
     receive the ARENA; NodeFactory views are transient
     checker-side constructions via seam 1)
   - `create_return_type_of_signature_declaration(…same shape…)`
     (isFunctionLike filter)
   - `create_type_of_expression(…same shape…)` (isExpression filter)
   - `create_literal_const_value(arena, target, node:
     EmitResolverNode, tracker) -> TransformNode` — NO flag words
     (§2c)
   - `get_declaration_statements_for_source_file(arena, target,
     node, flags, internal_flags, tracker) ->
     Option<Vec<TransformNode>>`
   - `create_late_bound_index_signatures(arena, target, cls,
     enclosing_declaration, flags, internal_flags, tracker) ->
     Option<Vec<TransformNode>>` (static-modifier recomposition is
     factory-side, never node mutation)
   - `symbol_to_declarations(arena, target, symbol:
     EmitResolverSymbol, meaning: EmitSymbolMeaning, flags,
     maximum_length: Option<u32>, verbosity_level: Option<i32>,
     out: Option<&mut EmitSymbolExpansionOut>) ->
     Vec<TransformNode>` (API surface; simplify* post-passes)
2. **Reuse/fork discipline (corpus-inertness invariant).** The
   checker already carries a COMPLETE STRING-emitting NodeBuilder
   port — the `*_slice` graph (check.rs ~5100-13100: typeToTypeNode
   workers, symbolToTypeNode, getSymbolChain, parameter/
   type-parameter declarations, mapped types, type predicates,
   annotation-reuse gate, bounded expression printer
   display_clone.rs) with its context flattened onto CheckerState
   (state.rs:364-433). The emit channel is a NEW node-channel
   implementation (upstream function-complete) that REUSES
   decision-only workers where contract-identical — symbol chains
   (symbol_chain_slice check.rs:7508, the m-2 emit twins
   declaration_emit_accessible_symbol_chain/containers
   check.rs:13412/:13421), the no-host specifier arm
   (specifier_for_module_symbol_slice), name-as-written
   (entity_symbol_name_as_written_slice :5731), the annotation-reuse
   GATE logic (annotation_reuse_text_slice :10041 decision core),
   projection/index/signature machinery per the reuse-anchor survey
   — while every STRING-rendering layer stays display-owned and
   untouched. Where recorded deltas exist the EMIT worker follows
   upstream and the display path keeps its behavior. Small missing
   utilities land as new ports: isPrimitiveLiteralValue,
   isStringANonContextualKeyword (over tsc_syntax
   identifier_to_keyword_kind), plus visibility widenings
   (get_first_identifier functions.rs:2436, the index-info accessors
   annotate.rs:7193/:7218).
3. **Tracker protocol (round-1 F8/F11 — re-entrancy-safe).**
   `EmitSymbolTracker` mirrors the :114280-114304 transformer object
   + SymbolTrackerImpl forwarding:
   - `track_symbol(&mut self, access: &mut dyn EmitTrackerAccess,
     symbol: EmitTrackerSymbol, enclosing:
     Option<EmitTrackerNode>, meaning: EmitSymbolMeaning) ->
     Result<bool, EmitResolverError>` — Ok(upstream's
     issued-diagnostic boolean); the checker records the (symbol,
     enclosing, meaning) triple into context.tracked_symbols on
     `Ok(false)` for non-type-parameters (SymbolTrackerImpl
     :90983-90993)
   - `report_inference_fallback(&mut self, access: &mut dyn
     EmitTrackerAccess, node: EmitTrackerNode) ->
     Result<(), EmitResolverError>`
   - `report_private_in_base_of_class_expression(&mut self,
     property_name: &str)`; `report_inaccessible_unique_symbol_
     error()`; `report_cyclic_structure_error()`;
     `report_inaccessible_this_error()`;
     `report_likely_unsafe_import_required_error(&mut self,
     specifier: &str, symbol_name: Option<&str>)` (both upstream
     parameters — F8); `report_truncation_error()`;
     `report_nonlocal_augmentation(containing_file:
     EmitTrackerNode, parent_symbol: EmitTrackerSymbol,
     augmenting_symbol: EmitTrackerSymbol)`;
     `report_non_serializable_property(property_name: &str)`
   - `push_error_fallback_node(node: Option<EmitTrackerNode>)` /
     `pop_error_fallback_node()`
   - `module_specifier_host(&self) -> Option<&dyn
     EmitModuleSpecifierHost>`
   **Re-entrancy:** `EmitTrackerAccess` is a checker-provided
   handle, valid only for the callback's duration, exposing exactly
   the FOUR resolver queries the upstream transformer tracker
   re-enters (round-2 F2) — `is_symbol_accessible(symbol,
   enclosing, meaning, should_compute_aliases=true) ->
   Result<EmitSymbolAccessibilityResult, EmitResolverError>`
   (:114362), `is_expando_function_declaration(node) ->
   Result<bool, _>` and `get_properties_of_container_function(node)
   -> Result<Vec<EmitFunctionProperty>, _>` (:114317-114331), and
   `requires_adding_implicit_undefined(parameter, enclosing) ->
   Result<bool, _>` — reached via reportInferenceFallback →
   getIsolatedDeclarationError's parameter branch
   (:114193-114201), witnessed in the frozen corpus at
   h2-7a/F12/static-block-substitution event_seq 71-73 — all
   backed by the ALREADY-BORROWED CheckerState, so no second
   `RefCell` borrow ever occurs. **Fallibility (round-2 F2):**
   every access method returns `Result` (CheckAbort →
   CheckerAborted exactly as the m-2 emit wrappers); the callbacks
   that receive the handle (`track_symbol`,
   `report_inference_fallback`) are themselves fallible —
   `Result<bool, EmitResolverError>` / `Result<(), _>` — and the
   checker propagates a callback error out of the serialization
   member fail-closed (never fabricated success); the pure report
   methods without a handle stay infallible.
   `EmitTrackerSymbol`/`EmitTrackerNode` are opaque checker-minted
   tokens (parse-tree or synthesized identity) accepted back
   verbatim by the handle; payload projections for recording are
   exposed on the handle (`describe_symbol`, `describe_node` — the
   probe symbolRef/nodeRef shapes). The SymbolTrackerImpl port
   (context wrapper: onDiagnosticReported, disableTrackSymbol,
   suppress gate) is checker-internal.
4. **Dormancy by construction.** The syntactic builder and
   NodeBuilder are consulted ONLY through the seven members (m-4's
   future call sites) and the harness seam; the check phase never
   consults them (upstream consultation sites are all inside the
   NodeBuilder owner; instantiation :46483 is instantiation only).
   Zero admission/profile/manifest surface changes.
5. **Function-complete, not function-shaped.** Every inventory row
   with `target_rung=h2-7a-m-3` (149 + 54 + 14) PLUS the
   packet-pinned non-row surfaces (SymbolTrackerImpl §3.10, the §3a
   specifier interior) is owned: ported with
   tsc-port/tsc-hash/tsc-span headers at authoring time, or
   recorded reuse-of-proven-exact with the Rust anchor, or a
   fail-closed typed seam with a ledger residue row (the
   verbosity/maximumLength expansion arms; absent host
   capabilities §3a). Rust module boundaries stay free (schedule §1
   house rule).
6. **Provenance discipline.** Reuse clones and derived nodes:
   `set_original_node` FIRST (it merges the original's EmitMetadata
   — factory.rs:529-554), then `set_text_range`, then any
   emit-flag/comment attachment (the W5 parse-identity panic class,
   extended by the metadata-merge order). Fresh synthesis carries
   neither. `factory.updateX` forms become
   create-from-modified-data + set_original_node + set_text_range,
   kind-preserving.
7. **No new public surface** beyond the trait members,
   result/flag/tracker/host/out types, the three factory.rs seams,
   and the existing harness seam's request/report growth; the 33
   existing members and their tests untouched.

## 6. Evidence design — decision-equal replay (gating contract)

Extends the m-2 harness (the declaration_emit.rs:1657 seam + the
compiler integration test) to the UNION domain; the m-2 12-member
gate stays green unchanged.

1. **Replay domain growth.** Domain adds the six traced
   serialization members (root pairs: createTypeOfDeclaration 314,
   createReturnTypeOfSignatureDeclaration 148,
   createLateBoundIndexSignatures 152,
   getDeclarationStatementsForSourceFile 3, createTypeOfExpression
   0→S3, createLiteralConstValue 0→S3). The maximal-root rule is
   unchanged. **Domain-growth consequence (explicit):** m-2 member
   events nested inside serialization roots stop being invoked
   directly and are reproduced (and compared) as nested effects of
   the serialization replay; m-2 member events that remain maximal
   keep their exact m-2 gating. Invocation flags per §2c (member-
   specific; the :115425 internal-word variant recovered per root
   from the traced withContext flag words and asserted consistent).
2. **Gating comparisons per serialization root** (order-sensitive
   within the root's event span, by event_seq):
   - root result: presence/nullness, vector length, and the result
     reference under the §6.3 matching classes;
   - `nodebuilder.withContext.result` sequence: the STATUS
     DISCRIMINATOR (`"node"` / `"error"` / `"fallback-undefined"`,
     round-1 F12) + (flags, internalFlags, approximateLength,
     typeStack.length, truncating, out.truncated,
     encounteredError, result reference class) — EXACT;
   - `tracker.*` callback sequence: site + the RECORDED payload
     shapes — EXACT over what the trace holds (round-2 F8): for
     `reportLikelyUnsafeImportRequiredError` the frozen projection
     is `[specifier is-string, specifier slash-component count,
     symbol_name]` (probe encoder :437-440; e.g.
     h2-7a/F6/references-second records `[true, 3, "NestedProps"]`)
     — the gate compares exactly that lossy projection, and the
     EXACT specifier string is a NAMED RESIDUE carried to the
     m-3.5/ca byte gates where `.d.ts` bytes pin it; other tracker
     sites' symbolRef/nodeRef/string payloads compare in full;
   - syntactic front door: `syntactic.serializeTypeOfDeclaration`
     (:133753) / `serializeReturnTypeForSignature` (:133807)
     entry/result pairing, per-frame fallback verdict from the
     inferTypeOfDeclaration :133943 / inferReturnTypeOfSignature-
     Signature :133962 markers (reportFallback flag), result
     reference class — EXACT. `serializeTypeOfExpression` has NO
     probe site: S3 typeofexpr roots expect ZERO syntactic.* events
     (site-coverage asymmetry, recorded);
   - nested m-2-domain resolver events inside the root: root-result
     equality under the m-2 rules;
   - paint effects: the m-2 §7.4 rule extended to serialization
     roots (order-insensitive (declaration, value) write-set
     equality within the root span).
3. **Node-reference matching classes (round-1 F12 + round-2 F7,
   formal).** The classes apply ONLY to NODE-VALUED results. A
   traced reference compares against a Rust-side node by class:
   (i) PARSE-OWN — trace `[fileTag,kind,pos,end,-1,-1,-1,-1]`:
   the Rust node must be the parse-tree node with EXACTLY those
   file/kind/pos/end coordinates; (ii) ORIGINAL-PROJECTED — trace
   `[-1,-1,-1,-1,oFileTag,oKind,oPos,oEnd]`: the Rust node must be
   synthesized with an original chain resolving to exactly those
   coordinates; (iii) SYNTHETIC-WITHOUT-ORIGINAL — all-sentinel:
   the Rust node must be synthesized with NO parse-tree original;
   (iv) ABSENT — the recorded null/absent forms map only to Rust
   `None`. A `"fallback-undefined"` withContext status never
   matches a produced value (status compares before reference).
   **Non-node-container rule (round-2 F7):** VECTOR-VALUED results
   (getDeclarationStatementsForSourceFile — three frozen vectors of
   lengths 2/15/20; createLateBoundIndexSignatures — two frozen
   vectors of length 1) record the probe's non-node sentinel ref
   plus `Array.length`; their gate is presence + EXACT length +
   the nested event stream within the root span — the node classes
   are NOT applied to the container (per-element references were
   never recorded). The same frame-aware rule governs a
   `withContext.result` whose producing member is vector-valued:
   status `"node"` + sentinel reference there means
   PRESENT-CONTAINER, and the member-level length gate carries the
   structure.
4. **Expected-zero constants (frozen reds).**
   `nodebuilder.withContext.decision` = 0,
   `nodebuilder.moduleSpecifierOverride.*` = 0,
   `tracker.reportTruncationError` = 0,
   `tracker.reportPrivateInBaseOfClassExpression` = 0,
   `tracker.reportInaccessibleThisError` = 0,
   `tracker.reportCyclicStructureError` = 0,
   `tracker.reportNonlocalAugmentation` = 0,
   `tracker.reportNonSerializableProperty` = 0 over the m-1+S2
   corpus (S3 values freeze at E4). The harness asserts BOTH sides:
   the traces hold exactly the frozen counts and the Rust replay
   produces zero events of the zero classes. A nonzero Rust-side
   event on a zero lane is a red, never a skip. Nonzero singletons
   (reportInaccessibleUniqueSymbolError 1,
   reportLikelyUnsafeImportRequiredError 1, trackSymbol 519,
   reportInferenceFallback 352) gate by the §6.2 sequences.
5. **Non-gating recorded diagnostics.** Nested traced call
   topology, per-root event counts by site, and context bookkeeping
   differences SymbolTrackerImpl legally absorbs — recorded with
   counts (the m-2 §7.4 rule).
6. **symbolToDeclarations (no traced events).** Focused structural
   contracts only (fresh-session determinism, meaning/flag routing,
   simplify* behavior on witness-corpus programs); named residue to
   its API1/H2.8-era consumer together with the
   verbosity/maximumLength arms. No replay claim at this rung.
7. **Exclusion accounting.** The m-2 frozen classes carry over with
   per-member counts for the six members frozen at E4; every class
   carries its owning-rung disposition; no silent exclusion.
8. **Determinism + inertness.** Full union-domain pass twice in
   fresh sessions; identical results and counts. Corpus-inertness:
   declaration-band conformance projection as PRE_SUITE and the
   full conformance gate at the head (T0 100%/FP=0 unchanged).

## 7. S3 supplement — complete frozen contract

1. **Frozen schema-2 volume table (entry/result pairs; m-2 E4
   register FINAL):** createTypeOfDeclaration 314;
   createLateBoundIndexSignatures 152;
   createReturnTypeOfSignatureDeclaration 148;
   getDeclarationStatementsForSourceFile 3; createTypeOfExpression
   0; createLiteralConstValue 0. Targets = the two zero rows.
2. **Scope/universe/order:** the m-2 S2 machinery verbatim
   (compiler+conformance `@declaration:true` raw-byte universe —
   1,415 candidates re-verified 2026-09-01; suite rank then
   bytewise relative path; project/transpile out of scope,
   recorded).
3. **Member predicates (inline, exact; bytewise over the raw
   fixture including all @filename sections):**
   1. `typeofexpr` — basename contains `mixin` (ASCII
      case-insensitive) OR some match of
      `extends\s+[A-Za-z_$][\w$.]*\s*\(` (heritage call
      expression — the :115626 non-entity-name extends path).
   2. `literalconst` — some multiline match of
      `^\s*export\s+const\s+[A-Za-z_$][\w$]*\s*=\s*(-?\d|["'\x60]|true\b|false\b)`
      (const with primitive-literal initializer — the
      :114667-114679 shouldPrintWithInitializer path; \x60 = the
      backtick byte).
4. **Selection algorithm.** For each member in §7.3 order: scan
   candidates in §7.2 order, skipping any fixture already selected
   by an earlier member, any fixture among the m-1 94-case
   manifest's 57 distinct fixtures, and any of the 16 S2 fixtures —
   the member ADVANCES; select the first 4 matches. **Frozen result
   (operator-computed 2026-09-01; raw predicate populations 31/50,
   post-exclusion eligible yields 31/49 — round-1 F13):**
   typeofexpr = amdDeclarationEmitNoExtraDeclare,
   anonClassDeclarationEmitIsAnon,
   declarationEmitClassMixinLocalClassDeclaration,
   declarationEmitExpressionInExtends2; literalconst =
   constDeclarations2, declarationEmitAmdModuleNameDirective,
   declarationEmitConstantNoWidening,
   declarationEmitPartialNodeReuseTypeOf (all
   `typescript-6.0.3/compiler/…`). The generator re-derives; drift
   = red. Recorded covariate: amdDeclarationEmitNoExtraDeclare
   carries `@outFile` — its NodeBuilder contexts run `bundled=true`
   and upstream takes the H2.7d bundle orchestration arm; the
   restricted-use rule keeps every H2.7d claim out of m-3 evidence
   while the resolver-boundary events exercise the target member
   identically.
5. **Case construction.** The S2 §8.6 machinery verbatim:
   declaration-effective cases per fixture in configuration_index
   order, cap 2, trim rows recorded; IDs `h2-7a/S3/<member>-<k>`
   (+`-c<index>` only for multi-configuration fixtures — none of
   the eight carries a comma-matrix directive, so the predicted
   case list is exactly typeofexpr-1..4 + literalconst-1..4: 8
   cases / 16 observations; the mint must reproduce). Roles
   `["supplement"]`; lanes: both members → "type serialization".
   Predicted denominators: cases 112→120, oracle runs 224→240. S3
   cases are constructed AFTER the m-1 projection and S2 rows are
   assembled and are EXCLUDED before buildCoverageMatrix/quota
   validation (the S2 collection-time filter, extended to
   family_id `S3`).
6. **Schema/generator updates (Phase E; round-1 F3 — witness
   schema ONLY).** The witness contract schema: family_id union
   gains `S3`; the 112/224 constants become 120/240; the ordered
   `m3_supplement` object mirrors `m2_supplement`
   (selector_version "m3-s3-v1", volume_table (6 rows §7.1),
   members (2 rows), case_ids (8), trim_rows, counts {fixtures:8,
   cases:8, observations:16}); `m2_supplement` is byte-preserved.
   The PROBE schema carries none of family_id/supplement/count
   constants and does NOT change; its registry pin moves only if
   its bytes move. The witness registry pin test updates in the
   same slice. The generators' `inputs.packet` pin moves to THIS
   packet file (parent_packet stays h2-7a.md and embeds the §2b
   amendment's hash after E1).
7. **Protection (freeze guards; round-1 F4).**
   (i) `H2_7A_M1_PROJECTION_SHA256` (witness side) unchanged,
   still enforced.
   (ii) NEW witness-side `H2_7A_M2_S2_PROJECTION_SHA256` over the
   canonical projection of the 18 S2 cases + observations + the
   m2_supplement object, computed from the CURRENT artifact at E2
   authoring time, enforced on every `--write`/`--check`.
   (iii) NEW probe-side PERMANENT guard
   `H2_7A_M2_SCHEMA2_PROJECTION_SHA256` over the canonical
   projection of ALL 112 current probe cases — case_id, fileTable,
   and VERBATIM trace_events (no field elisions) in artifact
   order — computed from the CURRENT artifact at E2 authoring
   time, enforced on every `--write`/`--check` thereafter: the S3
   re-observation cannot move a byte of the frozen schema-2
   traces.
   (iv) The one-time v1 MIGRATION projection (v1FieldProjection)
   updates its S2 case-id-prefix exclusion to exclude `h2-7a/S3/`
   as well, keeping its frozen 94-id denominator intact.
8. **Succession (round-1 F2).** New witness/probe/case-manifest
   fingerprints and content rolls freeze into the §10 register at
   E3/E4; the harness continues to pin the OBSERVATION identity
   (case-manifest fingerprint + both content rolls + the
   probe→witness file-sha pair — m-2 disposition 7), taking new
   values from the register; the §1 trusted-base whole-file values
   (84e478a1…, abbb443a…) become recorded history.
9. **Floors.** Each S3 target member records ≥1 replayed-eligible
   root event pair (zero = E4 surprise trigger); ≥1 typeofexpr case
   records a non-entity-name heritage serialization; ≥1
   literalconst case records a literal-const initializer synthesis.

## 8. Implementation sequence — two phases

Allowed files — Phase E: docs/design/greenfield/slices/
{h2-7a.md (the §2b amendment ONLY), h2-7a-m-3.md,
h2-7a-m-3-register.md, README row};
crates/oracle/{h2-7a-witnesses.mjs, h2-7a-probe-traces.mjs};
.github/ci/contracts/h2-7a-witnesses.schema.json (probe schema only
if its bytes actually move — §7.6); the schema-registry pin test
file; ratchets/h2-7a-{witnesses,probe-traces}.v1.json (mint
outputs); scripts/{walk-preflight.py, pin-index.py} lists only if
surfaces move. Phase P adds: crates/emitter/src/{resolver.rs,
factory.rs (the §4 three additive seams ONLY), lib.rs (re-export
-list additions for the new public types ONLY — round-2 F5)};
crates/checker/src/{lib.rs, emit.rs, declaration_emit.rs,
node_builder/** (new), the syntactic-builder module (new), and
existing checker modules where reuse wiring requires visibility or
emit workers (check.rs, state.rs, links.rs, modules.rs, expr.rs,
annotate.rs, engine.rs, structural.rs, functions.rs, program.rs)};
checker/emitter/compiler test files;
ratchets/h2-7a-owner-inventory.v1.json (P-head re-mint); ledger.
Forbidden: plan.rs, execute.rs, printer.rs, writer.rs,
transform.rs, manifests/profiles, vendor/**,
docs/design/greenfield/slices/h2-7a-m-2.md (signed authority), any
m-3.5+/m-4 surface, any factory.rs change beyond the three §4
seams. Single write owner: the operator in the canonical checkout
(mints canonical-path-only; delegated lanes work in worktrees and
the operator verifies + commits — lanes cannot commit).

**Phase E (evidence; zero production bytes):**
E1. Docs: the h2-7a.md §2b amendment element + this packet lands as
    docs/design/greenfield/slices/h2-7a-m-3.md + the register
    skeleton. Check: rendered diff + `git diff --check`.
E2. Witness S3 selector + witness-schema updates + the three §7.7
    guards + v1-projection S3 exclusion + registry pin test.
    Check: both generators `--check` refuse cleanly pre-mint
    (stale-pin refusal expected), post-mint exit 0 twice each.
E3. Mint (canonical path, demoted): full re-observation (the
    generator-hash receipt keys miss by design); 120 cases / 240
    witness runs + probe runs; fresh-process double observation;
    the §7.7(iii) guard proves the 112 frozen probe cases
    byte-identical.
E4. Register verification (operator): every §10 row against frozen
    predictions. **Surprise triggers → sol re-review before Phase
    P:** selector drift; any S3 target member still at zero
    volume; a §7.7 guard failure; nonzero
    moduleSpecifierOverride/withContext.decision anywhere; an
    unpredicted exclusion class; a §7.9 floor miss. Absent a
    trigger, Phase P proceeds on the operator verification record
    (the m-2 §9.E4 rule).
**Phase P (production):**
P1. resolver.rs: flags newtypes + constants, tracker/host/access
    traits + token types, error variants (Boxed Factory form), 7
    members (fail-closed defaults), EmitResolverMethod 40 +
    name(); factory.rs three seams; emitter lib.rs re-exports.
    Check: fail-closed default tests + existing 33-member tests
    green + seam unit tests (projection validation, ThisType
    token, cross-crate type usability from the checker).
P2. Checker scaffolding: node_builder module skeleton
    (NodeBuilderContext, withContext port, SymbolTrackerImpl port,
    name-generation tables, truncation machinery, tracker-access
    shim) + the syntactic builder module with its 26-member seam
    (five routes to m-2 workers), both dormant. Check: focused
    unit tests per cluster.
P3. Serialization clusters (§3.5-3.9 + §3a): type-node dispatch;
    signatures/scopes; chains/entity (the §2d machinery) + the
    module-specifier closure (lane H); declaration serialization;
    symbolTableToDeclarationStatements + symbolToDeclarations +
    simplify*; literalTypeToNode; late-bound index-signature
    composition. Delegated lanes in worktrees per cluster; each
    lane lands with focused tests; the operator sequences
    conflicting-file lanes within the train. Check: per-cluster
    focused tests.
P4. declaration_emit.rs emit workers + emit.rs wrappers for the 7
    members. Check: member-level focused tests.
P5. Harness extension (§6; constants transcribed from the §10
    register) — union domain, decision-lane gating, matching
    classes, expected-zero assertions, S3 replay, paint extension.
    Check: harness green twice (fresh sessions).
P6. Ledger rows for every ported cluster + host-fact residue rows.
    Check: `cargo xtask ledger check` green.
P7. Head: ONE walk invocation —
    `PRE_SUITE="cargo test -p tsc-rs-compiler --test contracts declaration_resolver_replay_decision_equal" bash scripts/chain-walk.sh`
    (bare, per the m-2 operator-disposition-5 precedent);
    owner-inventory re-mints here under the §2d partition-
    projection guard. Then
    `taskpolicy -b nice -n 15 cargo xtask ci --baseline d592fcad > /tmp/m3-ci.log 2>&1; echo "ci exit: $?"`
    (perf-only red → same command at normal priority). PR opens
    after E4; body carries the gate summary; merge commit on
    green.

## 9. Acceptance

1. Design gate: this packet sol-converged; the §2a record, §2b
   parent amendment, §2e S3 ratification, and §2f usage constraint
   explicit.
2. Trait: exactly 7 new members + flag/tracker/access/host/out
   types + the two error variants + the three factory seams;
   fail-closed defaults; the 33 existing members
   behavior-identical; fmt/clippy/tests green.
3. Foundation: every `target_rung=h2-7a-m-3` inventory row PLUS the
   packet-pinned SymbolTrackerImpl and §3a specifier closure owned
   (ported with headers / proven-reuse / fail-closed typed seam
   with ledger residue); dormant and production-unreachable; the
   syntactic front door and infer-fallback boundaries observable.
4. Evidence: S3 minted deterministic + inert; the four §7.7 guards
   enforced; harness union-domain pass with zero gating mismatches
   (root results incl. matching classes and status discriminators,
   decision-lane sequences, expected-zero lanes, paint sets, seed
   contracts) twice in fresh sessions; frozen constants matched;
   residues named in the register.
5. Partition-projection focused test green against the P7 re-minted
   inventory; ledger check green.
6. Corpus-inert: full conformance byte-identical (T0 100% / FP=0
   unchanged); ratchet/scope/families/escapes green vs d592fcad.
7. Dormancy machine controls: zero admission delta, zero candidate
   execution, zero activity change; declaration-family refusals and
   the dormant plan member byte-identical (plan.rs:208 /
   execute.rs:111-141/:629 / printer.rs:925); no
   observe_runtime_slice call, profile constructor, or mask bit.
8. Walk one-invocation converge; full local gate green; hosted
   pass; merge commit.

## 10. Phase-E frozen-constant register (recorded at E3/E4)

Evidence-derived only: successor witness/probe/case-manifest
fingerprints + content rolls; the 8 S3 case IDs + trim rows
(predicted §7.5; mint must reproduce); S3 per-member volumes (§7.9
floors); expected-zero confirmations (§6.4 list) across the
120-case corpus; the three new guard hashes (§7.7 ii/iii values);
exclusion-class counts per serialization member; predicted harness
per-member replayed/excluded constants (inserted into code only at
P5). Register location: the separate
[h2-7a-m-3-register.md](h2-7a-m-3-register.md) OUTSIDE every
generator input closure; this packet is FINAL as of the
pre-final-mint commit; `inputs.packet` pins THIS packet,
`inputs.parent_packet` pins the amended h2-7a.md.

## 11. Traceability and resources

1. **Per-member traceability (7 rows):**

| member | worker (declaration_emit.rs → node_builder) | wrapper (emit.rs) | focused test | step | evidence |
| --- | --- | --- | --- | --- | --- |
| createTypeOfDeclaration | emit_create_type_of_declaration | create_type_of_declaration | dm_serialize_type_of_declaration | P4 | 314 root pairs; §6.2 lanes |
| createReturnTypeOfSignatureDeclaration | emit_create_return_type_of_signature_declaration | create_return_type_of_signature_declaration | dm_serialize_return_type | P4 | 148 root pairs; §6.2 |
| createTypeOfExpression | emit_create_type_of_expression | create_type_of_expression | dm_serialize_type_of_expression | P4 | S3 typeofexpr; §7.9 floor |
| createLiteralConstValue | emit_create_literal_const_value | create_literal_const_value | dm_literal_const_value | P4 | S3 literalconst; §7.9 floor |
| getDeclarationStatementsForSourceFile | emit_get_declaration_statements_for_source_file | get_declaration_statements_for_source_file | dm_declaration_statements_for_source_file | P4 | 3 root pairs (JS synthesis) |
| createLateBoundIndexSignatures | emit_create_late_bound_index_signatures | create_late_bound_index_signatures | dm_late_bound_index_signatures | P4 | 152 root pairs |
| symbolToDeclarations | emit_symbol_to_declarations | symbol_to_declarations | dm_symbol_to_declarations | P4 | §6.6 focused contracts; named residue |

2. **Commands (literal):** generators —
   `node crates/oracle/h2-7a-witnesses.mjs --check`,
   `node crates/oracle/h2-7a-probe-traces.mjs --check`; harness —
   `cargo test -p tsc-rs-compiler --test contracts declaration_resolver_replay_decision_equal`;
   partition test —
   `cargo test -p tsc-rs-compiler --test contracts h2_7a_partition_projection`;
   inventory —
   `node crates/oracle/h2-7a-owner-inventory.mjs --check`; walk +
   gate — the §8.P7 lines; ledger — `cargo xtask ledger check`.
3. **Pin movement:** parent-packet hash (E1 → embedded at E3);
   witness/probe fingerprints + manifest fingerprint + content
   rolls (E3); owner-inventory fingerprint (P7 — resolver.rs AND
   factory.rs rust_evidence move); witness contract-schema pin +
   registry test (E2); the three §7.7 guard constants (E2);
   gate-tax receipts re-mint by `--check`; harness-pins manifest at
   walk tail; h1 profile ladder re-mints in-round (crate bytes —
   the standard §3 recovery phase).
4. **Ceilings:** demoted mints/gates; default oracle parallelism;
   normal-priority perf rerun path only; harness inside the
   compiler integration tests.
5. **Checkpoints:** walk resume journal; per-step focused tests;
   the §10 register; PR body gate summary.

## 12. Required-reference and architecture-impact table

Fresh audit of emitter-architecture.md rows :179-219 + §6
constraints; applicable rows (all others premise-unchanged):

| row | recorded state | m-3 impact and lifecycle |
| --- | --- | --- |
| `E-PROTOCOL` (:180) | active-qualified (m-2 close ref) | **modified-requalify**: member set 33→40 + tracker/access/host/flags/out types + two error variants; ownership boundaries unchanged; active-unqualified(candidate) during P, requalified at the train close with a fresh validation ref |
| `E-RESOLVER-BASE` (:198) | active-qualified (m-2 close ref) | **modified-requalify**: same transition rule; fail-closed preserved (incl. the new symbol-scoped variant) |
| `E-RESOLVER-IDENTITY-G` (:184) | active-qualified | **modified-requalify**: the factory seam adds the validated resolver-node→transform-node projection (the inverse direction); parse-interval anchoring unchanged |
| `E-CHECKER-FACTS-BASE` (:202) | active-qualified (m-2 close ref) | **modified-requalify**: serialization facts join; producers checker-side |
| `E-PRINTER-BASE`/`E-PRINTER-G` (:210/:211) | active-qualified | premise-unchanged (printer untouched; §2d defers bytes to m-3.5) |
| factory rows (:210-area constructor/parenthesizer constraints) | active-qualified | **modified-requalify (narrow)**: three additive seams (§4); no constructor face, no parenthesizer rule — those stay the m-3.5 premise |
| `E-PLAN-FUTURE` (:182) / `E-OUTPUT-FUTURE` (:219) | dormant | premise-unchanged (refusals byte-identical, §9.7) |
| `EA-GAP-MAPS-DECLS` (:639) | open | advanced-not-closed: the NodeBuilder/syntactic/specifier foundation lands; closure stays with H2.7b-era audits |

## 13. Local-gap matrix (cluster level; classes per the §1.1 vocabulary)

| semantic cluster | current Rust symbol(s) | class | evidence |
| --- | --- | --- | --- |
| NodeBuilder context/withContext | display context flattened on CheckerState (state.rs:364-433) | partial-or-stale, non-substitutable (display-coupled; no node channel, no tracker, no out contract) | G2; reuse survey |
| SymbolTrackerImpl composition | — | missing | §3.10 |
| type-node dispatch (node channel) | type_to_string_slice_node check.rs:5910 + worker graph (STRING channel) | partial-or-stale, non-substitutable (renders text; SliceTypeNodeKind surrogate instead of nodes) | reuse survey |
| symbol chains | symbol_chain_slice check.rs:7508; declaration_emit_accessible_symbol_chain/containers check.rs:13412/:13421 (m-2 emit twins) | already-exact (decision-only reuse) | m-2 §14; survey |
| entity-name building (symbolToNode/Expression/EntityNameNode) | symbol_expression_face_slice check.rs:7411 (STRING); merge.rs:391-399 shadow source | partial-or-stale (the §2d machinery lands the node channel) | m-2 §2c |
| serializeTypeForDeclaration / annotation reuse | annotation_reuse_text_slice check.rs:10041 + reuse workers :10177-:11808 | partial-or-stale, decision core reusable; emit-side node channel lands here | G2; survey |
| syntactic type-node builder | — | missing | G7; inventory 54 rows |
| symbolTableToDeclarationStatements | — | missing | G2; inventory rows |
| literalTypeToNode | is_fresh_literal_type types/tables.rs:662; is_declaration_readonly expr.rs:3740; literal payloads types/ty.rs:121/:270 | missing (composition) | m-2 §14; survey |
| late-bound index signatures | get_index_infos_of_type structural.rs:8092; private index-symbol accessors annotate.rs:7193/:7218 (widening) | missing (composition) | survey |
| module-specifier synthesis (§3a) | specifier_for_module_symbol_slice (no-host arm, already-exact reuse); NO Rust getModuleSpecifiers/symlink/package-json machinery | missing (function-complete port + typed host protocol + host-fact residues) | §3a measurement |
| factory consumption seam | NodeFactory::new pub(crate) :1178; TransformNode::new pub(crate) :105; ThisType unconstructible; parse_tree_resolver_node one-directional :275 | missing seam (the three §4 additive edits) | survey; F10 |
| small utilities | isPrimitiveLiteralValue —; isStringANonContextualKeyword — (identifier_to_keyword_kind base); get_first_identifier private functions.rs:2436 | missing / widening | survey |

## 14. Prohibitions

All h2-7a.md §8 rows, the m-2 §6 discipline rows, plus:

- No printer.rs/writer.rs/transform.rs byte; factory.rs limited to
  the three §4 additive seams and emitter lib.rs to re-export
  additions (no constructor face, no parenthesizer rule, no
  existing-face behavior change); no
  `PrintRequest::Declaration` lift; no plan/execute/refusal/
  manifest/profile byte; no observe_runtime_slice/mask/constructor.
- No display-path behavior change; no NodeBuilder substitution by
  the display slice; no string-render-then-parse TypeNode
  construction; no synthetic node discarding identity, provenance,
  or transform flags; set_original_node precedes set_text_range AND
  all metadata writes on every reuse clone.
- No hand-authored expected bytes; no witness family selected or
  adjusted after observation output exists beyond the
  parent-authorized S3 stratum; no inventory disposition left
  unknown; no receipt minted by `--write`; no artifact carrying its
  own receipt.
- No h2-7a-m-2.md edit; the h2-7a.md edit is EXACTLY the §2b
  amendment element.
- No m-2 gating weakened: the 12-member replay, seed contracts,
  paint rules, and all four §7.7 guards run unchanged in the union
  harness.
- No unknown-as-success in the specifier closure: absent host
  capabilities answer typed absent forms or fail closed (§3a);
  never a fabricated specifier.
- The migration-proof equality is never cited for output-node
  claims (§2f).

## 15. Readiness summary

Authority: §1 (stable observation identity + trusted-base
fingerprints + inventory fingerprint + vendor sha). Upstream: 7
member spans + 14 cluster rows + the §3a closure with measured
corpus reachability (4 import-type cases).
Trait: 7 signatures + tracker/access/host protocols (§5). Evidence:
§6.1-6.8 with volumes 314/148/152/3/S3, formal matching classes,
status discriminators, 8 expected-zero lanes. S3: 2 predicates, 8
fixtures/8 cases predicted, yields 31/50 raw / 31/49 eligible,
four freeze guards (§7.7). Rust map: 11 rows (§4). Local-gap rows:
13 (§13). Architecture rows: 8 (§12). Traceability: 7 member rows +
literal commands (§11). Undispositioned 0; every §10 row is a
Phase-E-produced
constant under frozen machinery. Phase gating: production bytes
begin only after E4 verification with the §8.E4 surprise triggers
escalating to sol.

## 16. Cross-review record

Round 1: operator draft (rev 1) → sol **REVISE**, 12 blocking + 2
advisory (2026-09-01), ALL adopted in this revision alongside the
operator self-audit fixes recorded the same day:
S3 re-authorized by a narrow h2-7a.md amendment element while the
rung-shape no-amendment record stands (F1 — §2b); trusted-base
fingerprint succession corrected to the walk-re-minted values with
the disposition-7 stable identity (F2 — §1/§7.8); S3 schema
changes scoped to the witness contract only (F3 — §7.6); the
probe-side permanent 112-case schema-2 projection guard added and
the v1 migration projection taught the S3 exclusion (F4 — §7.7);
the flag-word table corrected — five members carry the constants,
three wrappers add 1024, createLiteralConstValue carries none
(F5 — §2c); the symbol-scoped unavailable variant and the
TransformError mapping variant added (F6 — §4/§5.1); the
syntactic-builder seam corrected to 26 members with the five m-2
re-exposures routed to existing workers (F7 — §3.1, operator
self-audit convergent); SymbolTrackerImpl span corrected and
hash-pinned to :90969-91068 with the (specifier, symbol_name)
signature and exact boolean contracts (F8 — §3.10/§5.3, operator
self-audit convergent); the module-specifier closure measured —
four frozen cases emit import types, the computation arm is
corpus-reached, lane H + the 15-member host protocol + host-fact
residues added (F9 — §3a, extended beyond the finding by the
operator's witness-byte measurement); the factory seam corrected to
factory.rs (TransformArena lives there) and extended with the
validated resolver-node→transform-node projection and the ThisType
token admission (F10 — §4, operator survey convergent); the
tracker protocol made re-entrancy-safe via the checker-provided
EmitTrackerAccess handle carrying exactly the three re-entered
queries (F11 — §5.3); the withContext status discriminator and the
four formal node-reference matching classes added to the gating
tuples (F12 — §6.2/§6.3); raw vs post-exclusion S3 yields labeled
(F13 — §7.4); the second public observer seam dropped in favor of
extending the existing declaration_emit.rs:1657 harness seam
(F14 — §4, operator self-audit convergent).

Round 2 (verification): sol **REVISE**, 8 blocking + 1 advisory
(2026-09-01) — the round-1 incorporation and the operator additions
verified (S3 selection re-derived, decoded declaration counts
3/5/2/1 confirmed, spans/hashes reproduced); ALL findings adopted
in this revision: the Factory error variant boxed to break the
TransformError↔EmitResolverError cycle (r2-F1 — §4); the FOURTH
tracker re-entry (requiresAddingImplicitUndefined via
getIsolatedDeclarationError :114193-114201, witnessed at
h2-7a/F12/static-block-substitution seq 71-73) added to
EmitTrackerAccess and the whole access/callback protocol made
fallible with fail-closed abort propagation (r2-F2 — §5.3); the
lane-H closure re-pinned to the true module span :45368-46289 with
getModuleSpecifierPreferences + tryGetModuleSpecifiersFromCache-
Worker included, the basic host corrected to 14 members plus three
typed optional capabilities, and the computation-arm reachability
PROVEN from the CLI emit host's capability roster
(:123482-123521 — getModuleSpecifierCache absent) + empty
links-cache start (r2-F3 — §3a); the trait boundary redesigned to
pass `&mut TransformArena` (transient NodeFactory views via seam
1; provenance/metadata through the arena's existing pub surface;
setParent explicit-threading discipline with a named escape;
different-kind provenance via create+set_original_node)
(r2-F4 — §4/§5.1); emitter lib.rs re-export additions fenced in
(r2-F5 — §8); the NodeBuilderContext field map completed —
bundled, remapped_symbol_references, the copy-on-write name-table
sextet, and the syntactic builder's dynamic no_inference_fallback
save/restore state with P2 restoration tests (r2-F6 — §4); the
non-node-container rule added — node classes apply only to
node-valued results; the five frozen vectors (2/15/20 and 1/1)
gate by presence + exact length + nested events, with the
frame-aware present-container reading of withContext results
(r2-F7 — §6.3); the reportLikelyUnsafeImportRequiredError gate
scoped to the recorded lossy projection with the exact specifier
string carried as a named m-3.5/ca residue (r2-F8 — §6.2); the
complete six-value trusted-base identity tuple recorded
(r2-F9 — §1).

Round 3 (targeted verification): sol **REVISE** (2026-09-01) with
exactly ONE blocking mechanical fix and ONE advisory — every
round-2 incorporation verified faithful (the boxed error cycle,
the four-query fallible access protocol with a confirmed absence
of any fifth re-entry, the :45368-46289 closure and the
host-capability roster re-read from :123482-123521, the
arena-based trait boundary against the factory.rs pub surface, the
lib.rs fence, the completed context map, the vector rule
re-measured from the artifact, the lossy tracker projection, the
re-computed raw file SHAs): F1 — §4's host row still said
"15-member protocol", contradicting the corrected §3a; fixed to
"14-member basic protocol plus the three typed optional
capabilities" (the §16 round-1 wording stays as history). F2
(advisory) — §15 said 10 Rust-map rows where §4 holds 11; fixed.

**GATE CLOSURE (operator, 2026-09-01).** With round 3 returning
only supplied-value mechanical fixes — each applied verbatim — and
the ratification body (§2) verified across all three rounds, this
packet is CONVERGED and stands as the h2-7a-m-3 design authority
under the user calibration directive: the §2a rung-shape record and
§2e S3 ratification hold subject to the §2b h2-7a.md amendment
element landing at E1; Phase E is authorized; Phase P proceeds
under the §8.E4 verification rule with its enumerated
sol-escalation triggers.
