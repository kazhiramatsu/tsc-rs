# h2-7b-w3 — the third closure wave after the H2.7b activation flip (rev 1 — 2026-09-05: sol cross-review r1 REVISE (D1 owner set, the verify base in the register, criterion 4 weakened, criterion 5 inconsistent in the register) → all four applied; sol r2 AGREE — RATIFIED 20:31 JST)

Parent: `docs/design/greenfield/slices/h2-7b-m-2.md` (§6: the closure-wave authority). Previous waves: `h2-7b-w1.md` (23 rows, 185 → 162), `h2-7b-w2.md` (56 rows, 162 → 106; the w2 operating rules and the six success criteria). Trusted base: `main @c0e21f32` (PR #506). Train: `h2/7b-w3`.

## 1. Scope

The canonical H2.7b manifest at the base has 106 rows (owner `h2-7b-m-2-divergence-closure`). w3 closes rows by upstream-faithful ports, split across two implementer lanes with disjoint file sets, from VERIFIED owners produced by the W3-X analyses (gpt-6-astra and gpt-5.6-sol on the same ticket, mutually cross-reviewed: `target/session-notes/7b/w3/`):

| lane | classes (rows) | file set |
|---|---|---|
| W3-B (emitter) | B1 ES2015 namespace-export lowering (7); B2 string imported names → element access (8); B3 AMD/System namespace naming and setters (8); B4 constructor rebuild / shorthand key / instantiation-expression printing (3); B5 nested statement-comment suppression (3 verified + 3 optional); B6 declaration printing and statement normalization — the printer parenthesizer for the first generic type argument, expando future-reserved names, `using` → `const` (4); B7 the late-paint replacement handoff (the A2 rows the trace proves; ≥1); B8 the destructuring leaf range for `declarationEmitRetainsJsdocyComments`'s JS write (1) — 35 + | `crates/emitter/src/{builtins.rs, builtins/system.rs, builtins/es2015.rs, factory.rs, printer.rs, comment_cursor.rs, declarations/statements.rs, declarations/tracker.rs}`, `h2_7b_w3b_controls.rs` |
| W3-A (checker) | A1 leftovers (5); A2 missing-or-extra-members not owned by the late-paint handoff (≤ 28); A3 leftovers (1 unverified to trace; the 2 union-order rows and the unique-symbol row are checker-core: recorded, not ported); A4 JSDoc type reuse (5); R1/R2 the reverse-direction rows (3); T2/D3/D4 diagnostic display and relation rows (3, checker-core files under a conformance guard; D1 `bigintWithLib` is WITHDRAWN from this wave — its cross-reviewed owner set includes `elaboration.rs`, which the lane's fixed allowed set does not name) — up to 45 | `crates/checker/src/node_builder/*.rs`, `syntactic_type_node_builder.rs`, `declaration_emit.rs`; `check.rs` / `narrow.rs` / `structural.rs` for the named rows only (`calls.rs` / `engine.rs` stay listed for the lane's fixed set but D1 is withdrawn); `h2_7b_w3a_controls.rs` |

Not targeted: the rows both analyses left UNVERIFIED without a two-path proof (the comment rows `accessorInAmbientContextES5`, `commentsClassMembers`, `declarationEmitClassMemberNameConflict`; `declarationEmitAmdModuleNameDirective`'s resolver file set; the self-import removal row; D2 / D5), the checker-core identity/order rows (T5, T6), the TS5055 pair whose owner is the harness project adapter + `plan.rs` (single-writer; a separate integrator item).

## 2. Staffing cell and the w3 operating rules (the w2 rules plus three additions)

| role | who | ownership | deliverable |
|---|---|---|---|
| integrator + core (single writer) | operator | the w2 single-writer set (xtask, oracle, harness, ratchets, scripts, .github, docs, `execute.rs` / `plan.rs`, `contracts.rs`, pins, walk / gate / mint) | packet, E1 stubs + shadow registration, preflight, merged-train battery, re-mint, walk, gate, PR; the TS5055 pair if time allows |
| implementer W3-B | Codex gpt-5.6-sol xhigh | the emitter set above | rows, `h2_7b_w3b_controls.rs`, STATUS |
| implementer W3-A | Codex gpt-5.6-sol xhigh | the checker set above | rows, `h2_7b_w3a_controls.rs`, STATUS |
| analysis / review | Codex gpt-6-astra xhigh (docs only, bounded) | no production code | the packet cross-review; residual-owner analysis for w4 |

Rules: the w2 rules 1-5 unchanged (single writer; census-fixed allowed paths + launch preflight + `--verify` from the operator's launch base; two-stage battery; no lane batteries while the gate runs; OUT-OF-SCOPE instead of STOP). Additions from w2's findings: (6) every lane, the evidence lane included, runs the light stage (fmt + clippy) before handoff — w2's only train-battery-first finding was a lint in the evidence instrument; (7) single-worker or single-thread steps run in the foreground band with `nice` only — the background QoS band (`taskpolicy -b`) pins work to efficiency cores and cost w2 ~2 h across the train battery and the walk pre-suite; multi-process walks and gates stay demoted; (8) an edit to a checker-core file (the D class, R2) is followed by the 2xxx and syntactic conformance bands in the lane; a T0 regression reverts the edit.

## 3. Mechanisms ported in this wave (recorded at close; the lane STATUS files under `target/session-notes/7b/lanes/w3/deliverables-{a,b}/` carry the per-function mechanism notes and command tails)

- **W3-B (45 rows, `lane/w3-b` 926e2aaf):** B1 the ES2015 module transform's `NamespaceExport` arm with the original attributes (7); B2 string imported names keep the literal `propertyName` and select element access (8); B3 the AMD dependency parameter keeps the identifier namespace binding, System `createSettersArray`'s namespace-export setter branch, publication split from generated-name allocation (8); B4 the multiline constructor is rebuilt even with empty parameter-property assignments, an ES5 shorthand's key is not a value reference, the instantiation expression's erased type arguments keep the factory's own precedence (3); B5 the statement-comment container-end suppression (3); B6 expando properties named with future reserved words, `using` / `await using` declarations become `const` (3); B7 the iterative late-paint replacement handoff (`handleSymbolAccessibilityError` → `transformAndReplaceLatePaintedStatements`, 12 A2 rows); B8 the destructuring leaf keeps its own comment range and `original` (1).
- **W3-A (6 rows, `lane/w3-a` 2edfe832):** T2 the inferred predicate parameter name unescaped at construction; R1 `remappedSymbolReferences` install/restore and the function-expression-with-members worker branch (2); R2 `getVariableDeclarationOfObjectLiteral`'s owning-variable alternative container; D3 the `nameType.symbol` fallback for declarationless late-bound property display; D4 the generic-mapped-source / string-index relation arm. Checker-core edits ran the 2xxx / syntactic conformance bands green.
- **Integrator:** E1b — the node builder's synthesized type references go through the factory's `create_type_reference_node` (the parenthesizer applies); the printer never parenthesizes (the h2-7a reprint contract holds); W3-B's printer-level T1 change reverted and the row reopened with its corrected owner (the reuse walk's clone-and-update; W4-A0 later traced it to `crates/emitter/src/declarations/subtree.rs`). E5 — the emitter lib unit test `typescript_transform_preserves_jsdoc_recovery_type_arguments` re-pinned to tsc 6.0.3's `const ValidFoo = (foo);` (the batteries had never run the lib unit tests; the session scripts now do).
- **Not ported (recorded):** the three optional comment rows, T5/T6 (checker-core identity / union order), T7, J10, D1 (its owner set includes `elaboration.rs`), D2/D5, the TS5055 pair (harness), the exit-code symlink rows — all analysed for w4.

## 4. The manifest re-mint and its review — the m-2 §6 componentwise rule via `audit.js`. Measured (E2, on the merged train): 106 → 55 rows — 51 closed, 55 byte-identical, 0 strict subsets, 0 added or replaced elements, 0 new rows, 0 owner changes; `cargo xtask h2-7b-acceptance` normal: 1,502 / 55 / 36. (the w2 form: full-row equality, sorted/unique vectors, coarse facets re-derived from the runner's vocabulary, the exact refusal element, no added/replaced elements, no new ids, no owner change). Adjacent-band manifests may only shrink.

## 5. Verification — as w2: the lanes' two-stage batteries; `preflight.py --verify <worktree> <SPEC> <E1 sha>`; the merged-train battery (`train-battery.sh`, foreground band, 1 worker, 2 test threads); anchors re-point after emitter edits; E2 re-mint (`w2-remint.sh` form); walk-preflight; ONE walk; `gate.sh <main sha>` (marker; run with `nice` in the foreground band when the machine is idle, else demoted with the automatic normal rerun, which criterion 4 counts); PR opened early so hosted overlaps.

## 6. Fences — the w2 fences unchanged.

## 7. Success criteria (fixed before launch)

| # | criterion | measured how |
|---|---|---|
| 1 | exactly one walk launch | `target/chain-walk/runs/` between E1 and the merge |
| 2 | zero train-battery-first regressions (lints included) | every red in the merged-train battery the lanes' full batteries did not show |
| 3 | zero STOPs caused by an allowed-path gap | STATUS out-of-scope entries whose owner file the tickets had named for that lane |
| 4 | zero perf-contention gate reruns | `gate.exit` lines; a normal-priority rerun counts (the inherited w2 measurement) — so the w3 gate runs in the foreground band with `nice` only when the machine is not in interactive use (no E-core penalty, no perf-only red, no rerun); a demoted gate's automatic rerun would count as a rerun |
| 5 | more than 40 rows closed in wall time ≤ 8 h 33 m (lane launch → merge) with the fixed tail kept | the re-mint audit; the register timeline |
| 6 | zero lane file conflicts and zero multi-writer pin surfaces | `preflight.py --verify` per lane; clean merges |

## 8. Remaining rows after this wave

55 rows remain in the canonical manifest (owner `h2-7b-m-2-divergence-closure`). The w4 packet (`h2-7b-w4.md` §10) partitions them one row / one disposition: 11 closed by the ahead lane W4-A0 (`lane/w4-a0` 9f8f6ff5: A4 JSDoc type reuse 5, A1 `import()` leftovers 5, `exportDefaultNamespace` incidentally); 10 integrator harness/program items (S2 directory symlinks 8 — the suite VFS builder, the symlink cache and the runtime-dependency prelude; P1 the TS5055 pair — the project runner's config identity); 14 W4-A checker rows (S1 the JS declaration resolver's own export table 5, T7, J10, C1/S3/S4, E2/E3/E4, D1); 8 W4-B emitter rows (computed literal names 3, nested callback fields, decorators, T1, the empty expando namespace, J5); 12 OPEN with recorded boundaries (`jsFileMethodOverloads{4,5}`, `privateFieldsInClassExpressionDeclaration`, `jsDeclarationsUniqueSymbolUsage`, the three comment rows, `declarationEmitClassInherritsAny`, `arrayFlatNoCrashInferenceDeclarations`, `variadicTuples1`, D2 `checkingObjectWithThisInNamePositionNoCrash`, D5 `recursiveConditionalTypes`).
